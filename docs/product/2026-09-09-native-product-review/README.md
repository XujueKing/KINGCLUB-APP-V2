# KING CLUB 原生 App：产品与迁移评审包

- 初版：2026-09-09；最新更新：2026-09-10；状态：**In Review — 等待详细规格检阅，不是已批准生产开发/迁移清单**。
- 主项目：`D:\WEB3_AI\KINGCLUB-APP-V2`。本机不存在用户书写的 `D:\WEB3\_AI\KINGCLUB-APP-V2`，不另建第二份项目。
- 目标：以正在使用的小程序为业务和视觉原型，在 Flutter Android/iOS App 中补齐原生体验，由 CCSOP 的 KingClub 独立业务实例承接 API 与数据。
- 当前阶段交付：重新盘点、产品需求、架构建议、迁移设计和可检阅 ROADMAP；先检阅，再冻结范围并逐项开发。

## 最新需求与已定变更

[碎片需求台账](REQUIREMENT_INBOX.md)持续记录用户原意与变更；目前 22 条，覆盖 8 个业务板块和全局媒体要求，[产品主表](PRODUCT_REQUIREMENTS.md)已扩为 41 项。[技术调研](MEDIA_AND_FACE_RESEARCH.md)区分媒体压缩、颜值评分、自研美颜与腾讯核身。

用户最新明确：**实名核身第一版不做，还是用旧照片上传接口的方法**。首版按旧上传/检测/评分/会员审核流程整理，腾讯核身只留未来参考。管理员功能纳入 App 的授权角色范围；中央内容扩展创作/美颜/游戏大厅，储物箱扩展购券/赠券/道具。已明确功能不再被旧“暂缓”永久排除，具体页面/规则和交付批次继续评审。

## 先看这四份

1. [产品需求与功能范围](PRODUCT_REQUIREMENTS.md)：是否有遗漏，哪些能力必须首发。
2. [UI 保真与流畅度验收](UI_AND_PERFORMANCE.md)：把“99% 类似”和“流畅”变成可检查的标准。
3. [迁移方案](DATA_AND_API_MIGRATION.md)：只处理 KING CLUB 数据，旧系统继续可用。
4. [ROADMAP 与待决策项](ROADMAP.md)：按验收结果推进，不以页面数量冒充产品完成度。

技术审阅：[架构方案](ARCHITECTURE.md)、[资产事实与缺口](SOURCE_BASELINE.md)。

查漏附件：[72 路由](inventory/LEGACY_ROUTES.md)、[106 接口](inventory/LEGACY_INTERFACES.md)、[94 表 / 1484 字段](inventory/LEGACY_TABLES.md)。这些是结构盘点，不是全部新接口/字段映射已经设计完毕。

## 已确认事实

- 用户确认：旧小程序学习流程和 UI；V2 为主开发项目；新 API 和数据库落在 `ccsop-service`；UI 目标保持 99% 类似。
- 用户追加确认：以前手工代码和存储过程不必照搬，允许现代化重构；以有效业务、数据一致性、可维护性和可验证的性能为验收依据。
- 用户进一步确认：SQL 是多网站混包，KING CLUB 业务表按 `k_` / `K_` 前缀识别；通过 `s_interface` 分类及真实依赖交叉核对。
- 当前快照的分类链为 `s_interface.interfaceType -> s_interface_type.typeId -> parentId`，根分类为“酒吧”，下分 App 和服务器端使用，共 103 个接口；后续群聊脚本另有 3 个接口。
- 本机 V2 已有 48 页 UI/Mock 和首批跨页交易修复；这不等于 72 个旧路由对应的全部产品能力已经完成，更不等于生产可用。
- CCSOP 当前 KingClub 分支有登录/统一身份和通用基础设施，不是完整的聊天、短视频、交易、会员业务服务器。
- 本轮不执行旧 SQL、不导入用户数据、不发送短信/支付、不修改旧服务、不部署、不提交或推送 Git。

## 本轮建议与旧批准的关系

原 48 页和 33 个功能的 UI/Mock 基线仍有效；此前延后的完整群管理、作品发布和真实资产交易重新进入**完整产品需求评审**，不能自动删除，也不能自动标为已批准首发。

新建议以本评审包标注的“当前建议 / 待用户决策”为准；经用户确认后，逐项回写 `docs/features/` 独立功能/页面契约和项目唯一交付账本。未获批准前，不改变现有 `UI Flow Approved` 门禁，不在 `lib/` 临时新增未评审功能。

历史架构里曾讨论 `personId`、多 schema 和 REST；后续已有更明确的 `U... userAccount`、独立 KingClub 实例/库、CCSOP 超级接口决策。本轮沿用后者，不根据旧讨论再次造一套身份体系或替换服务器框架。

## 阅读完成的含义与边界

本轮完成五份资料的目录/结构盘点，并审读注册、身份、超级接口、会员准入、聊天、媒体、订单支付、资料与数据治理等关键链路；流式扫描整个 9.09 GB 导出文件，仅提取结构、分类和非敏感目录。

不声称逐行人工审查数百万行真实数据或全部历史代码。动态菜单、Java 专用接口对应的最新部署源、线上当前 schema、所有数值枚举/业务规则、全量字段转换和真机视觉仍需后续证据。这些缺口已登记，不能用“全面阅读”替代生产验收。

## 检阅时请确认

在 [ROADMAP 的 D01～D11](ROADMAP.md#待用户决策) 中逐步确认首发批次、规则、媒体质量、游戏深度、历史迁移及发布输入；“首版不做实名核身”已确认，不再列为待选。批准文档不自动批准生产切换；每个高风险阶段仍有独立验收。

# 2026-09-10 会话恢复与开发接续基线

本次先恢复上下文并核对进度，不重复开发短信登录、恢复短信正文、部署或办理备案。状态依据为用户本次确认、两份聊天导出、本地 Git 提交及当前源码；没有重新检查远程服务器运行状态或发送短信。

## 来源与优先级

1. 本次用户确认：真实短信登录已部署，短信正文已恢复旧版，域名备案正在处理。
2. `C:/Users/xiaoshafa/Documents/Codex/Exports/KINGCLUB-APP-V2-聊天记录-2026-09-09至2026-09-10.md`（42 条记录）。
3. 同目录 `KINGCLUB-APP-V2-聊天记录-2026-09-10-下午及晚间.md`（120 条记录，截止 20:40:45）。两份是历史原文，后续用户纠正优先于早期助手解释；图片附件未包含在导出内。
4. 本地项目文档、Git 历史与源码。聊天中的临时提交号可能经 amend 更新，以当前历史中最终提交为准，不恢复临时版本。

导出记录保留在项目外，不复制聊天全文、个人信息或图片到 Git。根交付账本仍是批准状态的唯一来源，本文件保存接续证据。

## 核实的仓库状态

| 仓库 | 分支 / HEAD | 本次检查 |
|---|---|---|
| KINGCLUB-APP-V2 | main / b35c33e | 修改本次交接文档前工作区干净；与本地 origin/main 引用一致 |
| ccsop-service | business/kingclub-v2 / 390632b | 与本地远端跟踪引用一致；保留用户既有 `.gitignore` 修改 |
| KingClub-git（只读原型） | master / 9299208（1.1.38） | 工作区干净；不回退到历史 505d222 |

以上未执行 fetch/ls-remote，不把本地远端跟踪引用称为本次 GitHub 实时核验。此前推送成功由聊天及部署日志记录支持。

## 已完成，不重复

| 内容 | 当前证据 |
|---|---|
| 完整产品需求、跨页 Mock 修复 | c37c60d；8 个产品板块、41 项 PR；48 页只是既有 UI/Mock 基线 |
| 注册准入共享 Mock、低分待审/重传、刷新进入、多尺寸验证 | 2526f44、d232095 |
| 逐功能数据库审查、保留旧事务和业务语义 | 25669f4；不能再声称旧注册无事务或会留下半套资产 |
| 登录 UI、旧协议正文、输入渐变/占位/收键盘、页面转场 | 916244f、ef0f0d3；聊天中的 a4151fe/819d98a/cf9a7f3/469be04 为中间状态 |
| 欢迎页细节、统一 KingBackButton、页脚 | 190b917、abe56f1 |
| NEXT / 获取验证码最终恢复旧色 | 443e848；底色 #24180A、文字 #AAC9B69E，不再自行提亮金色 |
| Flutter 真实短信登录和加密客户端 | e3a8174、b35c33e；K107/K101/K102、系统安全存储、Android 兼容 P-256 |
| 创蓝真实短信、新会员表登录 | 后端 bb2271e；迁移 023；既有登录过程写 kingclubMember |
| IDC 部署、可用 HTTPS 入口和安卓安装 | 后端 901aadf、2cda3d0；后端开发日志与 20:14 聊天记录 |
| 旧版短信正文恢复 | 后端 390632b；部署脚本及更新脚本均已修正；用户本次再次确认已完成 |

短信正文：`您正在登录KingClub克洛泽酒吧手机APP，验证码（5分钟内有效）：{code}`；`【湖南领美】`由短信平台添加。恢复已在服务端生效，不需要为此重装 APK。

## 有效授权与开发习惯

- 18:54 用户要求“现在正式接接口进行短信验证登录，用新的会员表”；18:56 允许部署 IDC；19:06 明确整台是测试服务器，直接使用，不额外搭隔离环境。不得再以早期“短信必须等待 Module UI Accepted”阻断已授权工作。
- 首版保留姓名＋身份证号＋自拍上传后的腾讯照片实名认证，只不接新增 App 活体核身 SDK。旧的“不接权威库”解释已撤销。
- 注册/照片链路是首个功能，逐功能核对旧表、接口、事务、副作用后继续；短信授权不代表腾讯照片已实现或整 App 验收通过。
- UI 以旧 CSS/布局及用户最新反馈为准，原生转场按最终实现保留。Profile/AOT 曾用于流畅度检阅；后续真实短信构建脚本产出的是 Debug 包，不混称同一包。
- 小 UI/文字改动仅做必要定向检查与可用的热更新；业务状态改动做对应模块验证。不要为小改动反复跑全量测试、安装 JDK、重打 APK，构建异常先定位，不盲目重试。
- 旧小程序、旧 Java 与混库参考只读。新模型继续 MySQL 8.4、U...统一身份与 KingClub 业务表；保留用户后端工作区改动。

## 域名与测试入口

当前已部署版本使用 `https://test.wuyexin.cn/kingclub-v2`，代码证据为 `tool/build_real_auth_apk.ps1`。目标入口为 `https://api.sh-kingclub.cn`，此前 DNS 已配置，备案/接入处理中，以用户本次说明为准。

不重复配置 DNS、申请证书或指导备案；待用户告知完成后再核验目标入口、签证书及切换构建参数。历史聊天中备案类型解释曾有纠正，本文件不将其转换为新的办理结论。

## 当前未完成与下一开发顺序

1. **真实会话恢复与会员状态衔接**：`SecureSessionStore` 目前仅保存会话；源码未见读取恢复逻辑。`mobile_login_page.dart` 在新会员分支仍调用 `mockRuntimeProvider.startOnboarding`。先补真实登录后的状态边界，不能把本地 Mock 放行当作服务器已批准。
2. **真实注册申请与受控上传**：核身自拍、正面照、着装照三个槽位；对应页面仍依赖 MockRuntime。完成数据库审查、申请/文件归属、状态契约后逐项替换。
3. **腾讯照片实名与两图评分**：后端现有 `MockIdentityAdapter`，本次 src 搜索未发现 ImageRecognition/DetectFaceAttributes 正式实现。继续正式 adapter、任务去重和结果恢复；权限、计费与规则按既有功能文档核实，不自动重复收费或伪造 verified。
4. **审核结果刷新与真实准入**：查询权威申请/会员状态，覆盖低分待审、重传、审核通过刷新进入；不能把刷新实现为本地改 approved。
5. 其他产品工作包继续按 ROADMAP 排序。历史 Golden 差异和整 App 验收仍单列，不把过去测试数量作为本次结果。

本次完成文档/Git/源码交叉核对；没有修改业务代码、执行数据库迁移、重新部署、发短信、调用腾讯或重建 APK。下一阶段从上述第 1 项继续。

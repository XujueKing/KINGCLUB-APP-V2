# 网络与超级接口传输验收

## 文档准入

- [x] 超级接口语义端口、SecureEnvelope、握手与 HTTP adapter 分层明确
- [x] nonce/requestId、结果未知、幂等键与禁止重放边界明确
- [x] K101/K102/K103/只读请求分别定义重试策略
- [x] 超时、取消、断网、TLS、解密、契约和业务错误映射目标明确
- [x] 页面/domain 不依赖 Dio 或 interfaceId
- [x] UI 阶段只使用 Fake，真实密文传输在 `UI Flow Approved` 后接入
- [x] 日志禁采字段、环境隔离、失败关闭与回滚边界明确
- [x] 用户于 2026-08-26批准本模块，状态更新为 `Approved for Development`

## 实现与真实接入验收

- [ ] 握手、加解密、签名和测试向量与服务端契约一致
- [ ] nonce/requestId 每次传输唯一，重放包不能被客户端复用
- [ ] Fake 覆盖成功、断网、超时、结果未知、业务拒绝与会话失效
- [ ] 生产构建不能关闭 TLS 校验或指向非生产环境
- [ ] 请求日志与崩溃附件通过敏感字段扫描
- [ ] Android/iOS 弱网、取消、前后台和证书异常完成真机验证

实现项在相应阶段执行；当前批准只允许 UI/Mock，不代表真实超级接口已经接通。

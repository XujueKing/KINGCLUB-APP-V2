# 内页返回按钮统一

## 基准纠正（最新）

用户明确要沿用之前聊天会话的位置。恢复旧聊天箭头视觉坐标：距安全区左侧 18dp，顶部 20dp，8×16dp 图标；不再按注册页内容边缘计算。将 48dp 点击区放在 x=0，再把内部图标左移 2dp，复现旧聊天 x=-2 的按钮视觉位置，同时保留完整点击范围。注册、扫一扫、点单等所有使用公共返回组件的内页一起跟随该基准。测试新增固定坐标断言，避免仅验证各页面彼此一致却基准错误。

用户要求全部内页，包括扫一扫。仅修改独立 commerce worktree 中的 UI，不操作聊天主工作目录。

标准沿注册流程：安全区下方 4dp，48×48dp 点击区，8×16dp 原有返回图片，横向位置通过 KingBackButton.leftOffset 按内容边缘计算。新增 kingAppBar 统一标准 AppBar 的自动返回与显式返回回调。既有符合标准的注册/登录标题栏保留。

覆盖扫一扫真实入口及演示入口、扫码点单及无效桌台入口、聊天/群聊共用标题栏、聊天附件与详情页、设置/资料/二维码/法律/注销/支付安全、好友资料/黑名单/申请、储物取件等存在差异的标题栏。聊天会话此前靠左的例外已取消，右侧更多按钮同步对称。弹窗关闭、删除、清空输入等非页面返回按钮不改。

返回行为保持原回调；自动路由返回使用 maybePop，保留 PopScope 未保存确认机制。通话页原返回结束通话行为未改变。

同时按用户要求：点单搜索框右边距缩至 20rpx，返回位置跟随统一规范；购物栏内容高度由固定 80dp 改为 120rpx，购物袋 80×64rpx、结算按钮 180×68rpx，底部安全区独立保留。商品区可见高度与末尾避让使用同一底栏高度。

验证：60 项通过、1 项原测试跳过；包含 320/393/430 宽度下注册、聊天、扫一扫、点单返回按钮矩形完全一致及点击回调，自动路由返回、扫码退出、资料未保存确认、购物袋与分类置顶等。全 lib/src 静态检查及最终安装状态随后记录。未进行 A 手机或聊天业务运行测试。

最终验证：全 lib/src 与新增测试静态分析无问题；三尺寸点单渲染检查通过。构建 91.7 秒成功；B 手机独立商务包安装 Success、启动 Status: ok，提交 24b1fe38 已推送。待用户实机视觉确认。

# 会话与安全持久化验收

## 文档准入

- [x] SessionBundle schema、generation、环境绑定和原子提交协议明确
- [x] K102 登录、K103 轮换和启动恢复使用同一提交边界
- [x] 页面只能消费 SessionView，不能读取或持久化秘密字段
- [x] SingleFlight、撤销、过期、受限、注销和环境切换规则明确
- [x] 平台插件通过自有端口包装，可由 Fake 替换
- [x] UI 阶段不落真实凭据，真实安全存储后置到 `UI Flow Approved`
- [x] 失败清理、隐私、测试与回滚目标明确
- [x] 用户于 2026-08-26批准本模块，状态更新为 `Approved for Development`

## 实现与真实接入验收

- [ ] 任意写入、回读或活动 generation 切换失败不留下半会话
- [ ] 并发请求只触发一个刷新任务，旧 Token 不被再次使用
- [ ] 注销、撤销、过期、账号合并和环境切换清除正确
- [ ] Fake 覆盖匿名、候选、认证、刷新、撤销、过期、受限和失败
- [ ] Android 备份排除与 iOS Keychain 生命周期完成真机验证
- [ ] 日志、崩溃附件和普通缓存无法获得秘密字段

实现项在相应阶段执行；当前批准只允许 UI/Mock，不代表平台安全存储已经完成。

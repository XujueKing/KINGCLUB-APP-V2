# 钱包与资产流水页验收

- [x] 各资产摘要、单位、年度筛选和流水层级明确
- [x] 零资产、空流水、加载、分页、离线、未知状态明确
- [x] pending、frozen、reversed 和 asOf 语义明确
- [x] 关联订单只传 OrderRef，普通流水不新增页面
- [x] 只读边界、隐私遮盖、会话切换和无障碍明确
- [x] 用户于 2026-08-25 批准 `Asset Ledger Wireframe v1`

UI Mock 阶段验证 ASSET-M01～M20；全局文档门禁已满足，可以实现页面 Fake 流程。

## Legacy UI Mock 准入补充

- [x] 旧版视觉来源冻结为 `mybalance` 的黑褐背景、资产 tab、年度摘要和流水列表结构
- [x] 2026-08-28 复验：恢复旧版订单、余额、金币、钻石四页签，移除旧版不存在的资产摘要卡
- [x] 2026-08-28 复验：默认态不显示醒目的“加载更多”按钮，滚动到底触发本地 Fake 分页
- [x] 页面首发完全只读，不出现充值、提现、转赠、兑换或客户端资产分摊
- [x] ASSET-M01～M20 均定义为本地 Fake 状态，不调用真实资产或订单服务
- [x] 关联订单只传 Fake `OrderRef`；普通流水留在当前页展开
- [x] 本页可进入 UI/Fake 实现，真实接口和生产资产数据继续保持阻断

## 2026-08-28 Android UI Mock 验收记录

- [x] 复刻旧版黑褐背景、居中标题、资产 tab、金色选中线、年度摘要和紧凑流水结构
- [x] 订单、余额、金币、钻石四页签全部在 `1080 × 2400` Android 视口内完整可见，不横向裁切
- [x] 余额使用人民币两位小数，金币和钻石使用整数枚数，页面不计算跨资产总额
- [x] 未启用资产隐藏；零资产、摘要失败和空流水不会由客户端伪造或反推余额
- [x] pending、reversed、frozen、offlineCached 和 unknown 状态均有明确只读表达
- [x] 普通流水留在当前页展开；关联订单只产生 Fake `OrderRef` 导航意图
- [x] 年度切换、原子刷新、稳定加载更多和失败重试均可离线操作
- [x] 会话失效先清除资产、流水与 cursor，再触发登录 reset
- [x] 页面没有充值、提现、兑换、红包、转赠或资产抵扣入口
- [x] 钱包专项 13 项测试通过，包含顶部返回键可见性和点击回调
- [x] Android 实机尺寸截图无溢出，返回键和右侧 Mock 标记均位于可见范围
- [x] `flutter analyze` 无问题；Flutter 全量 121 项测试通过

本记录只批准 UI 与 Fake 流程；真实资产、订单、支付、超级接口和 WebSocket 接入继续保持阻断。

2026-08-28 新证据：[android_asset_ledger_four_tabs_v3.png](screenshots/android_asset_ledger_four_tabs_v3.png)。钱包专项测试 13/13 通过，`flutter analyze` 0 问题。

## 2026-08-29 正式文案与关联订单 v3

- [x] 默认余额页保持旧版年度汇总、两条流水和紧凑间距
- [x] 展开普通流水不显示 `Fake`、`Mock`、测试或非真实账户说明
- [x] 退款已入账使用正式产品文案，处理中状态不提前承诺到账
- [x] 星光香槟套餐的“查看订单”打开 V2 AA 已完成订单
- [x] 余额、金币、钻石仍独立显示单位，不做跨资产换算
- [x] Android 1080 × 2400 真机无溢出、遮挡或底部碰撞
- [x] 订单页签仅展示带受控 `OrderRef` 的流水，关联内容与订单详情一致
- [x] 金币抵扣 `-30 枚` 打开888号桌已支付订单
- [x] 金币和钻石的汇总、收入与支出均使用“枚”，不显示人民币符号
- [x] 订单页签的年度摘要分别显示余额与金币单位，不把 `30 枚` 错算为 `¥0.30`

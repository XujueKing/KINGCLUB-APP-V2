# 语音草稿预览生命周期修复

预览原先允许播放按钮连续发起异步操作；退出面板时等待中的本机草稿检查完成后仍可能调用已释放播放器，切到后台也没有主动停止。

现在串行防重点击，播放操作跨异步检查页面/账号/前台代次；退出、账号事件和切后台使待处理操作失效并停止播放。播放开始未完成时禁用发送/删除，取消完成事件订阅并释放生命周期监听。已确认UI布局不变。

定向analyze通过。既有会话语音播放与草稿隔离8项测试通过，覆盖的是播放控制器及存储回归，不是本面板的设备音频验收。本次未重新打包；将随下次安装交付。用户此前无声语音的实际录音/播放结果仍待实机确认，不把此修复当作无声根因已解决。

页面级补充：预览复用NativeChatVoiceOutput，通过可注入输出与草稿加载器验证真实Widget异步生命周期。三项测试通过：等待草稿检查时关闭页面不播放、切后台不播放且停止输出、连续点击只检查并播放一次；销毁恰好释放一次且无页面异常。音频设备在测试中受控，不代表扬声器实测。定向analyze通过。

安装记录：b184676代码已构建profile/preview ARM64包，62.5秒、149.0MB，SHA256 10e83c7e525fd281fff6ff002e9eb45430a0cc00bc7f3b71415e0e123871b8db。adb保留数据覆盖安装Success，启动Status ok。原有onboarding三文件工作区改动随包包含但未提交。此前“未重新打包”状态由本记录更新；声音实机验收仍待确认。

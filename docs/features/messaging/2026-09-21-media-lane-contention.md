# 缩略图与原图同时下载时的 peer 通道争用

d3ff0aab 双机图片消息 `f666c180-7e1e-4a3c-ab47-bb4bc38c4ea0` 正常显示，但完整图及缩略图均出现 HTTP 206。发送端有 3618 字节完整图片缓存，SHA-256 `f87605a764437b327d2bcc0619e5241a1d8b576fcd285500eea600ef4a4d0bc8` 与规范化源一致；只观察到一次 PeerServe source 阶段 StateError、接收入口零帧。旧日志未区分媒体种类，不能断言该错误是原图源损坏。

代码检查发现每个 peer 通道仅允许一个 receive，缩略图和原图同时请求时，第二个立即抛 busy，调用方继而回退 HTTP。新增测试复现该确定的并发行为。本次允许一个候选下载等待当前接收释放，最多两秒；额外请求仍立即回退，长文件不会形成无界等待队列。释放后重新检查通道、取消状态并重新授权。关闭通道立即唤醒等待者，迟到完成不会覆盖新的接收对象。

发送端非 release 诊断仅增加已验证的 media 枚举，不记录内容、身份或密钥，用于区分正常缩略图缓存缺失与原图异常。无需修改 SuperVM wire、服务端或 UI。

验证：新增释放后继续、两秒超时、取消、关闭四种争用测试，含单等待者上限；与缓存准备、取消续传、媒体源读取组合共 22 项通过，静态分析和 diff 检查通过。手机仍为 d3ff0aab，尚未用新包证明这一修正解决本轮实机 HTTP 回退；不将图片 UDP 计为通过。

## 修正版手机验证

随后 A/B 均原位安装 882e422b，APK SHA-256 `c9d7758260a2f677c0cd633482ca05de5c4245a49dcd381ebd4985469d919714`，两端 Success，登录及记录保留。

同一 3618 字节小图新消息 `71900bec-9031-438e-8517-133ad32318ae`：08:06:24 image source-ready，接收计数 relayFrames=4、relayBytes=3618、UDP=0；随后明确为 image-thumbnail 的 source 阶段失败。服务器该消息仅 thumbnail GET，无完整 image GET。证据支持原图 P2P 中继成功、缩略图正常回退，不将冷启动小图的中继选择视作 UDP 故障。

再使用合成 1920×1080 测试图（FFmpeg testsrc2 加噪声，原 PNG 4527282 字节），通过实际照片入口发送。压缩后的原图 1057476 字节；新消息 `2464fabe-a37b-4304-8939-b74767237b98`，08:09:26 image source-ready，08:09:35 接收统计 UDP 1074 帧/1047716 字节、relay 268 帧/261568 字节。计数包含重传，不能相加当作唯一有效文件长度。

A 两份持久化原图与 B 发送缓存均为 1057476 字节，SHA-256 `569a973af0cf6d0dab1dd160689348f7fdc7c0dbcb2461a03722910d54059b5d`。服务器仅该消息的 3032 字节 thumbnail GET，无完整 image GET；A 会话正常显示合成图。缩略图另记录约 3 秒连接超时后 HTTP 回退，不能误记为完整原图失败。

本轮接受实际图片 LAN UDP + 加密中继混合传输与哈希一致性，不称全程纯 UDP；原图中途断点、跨运营商及公网多入口仍未实机验收。

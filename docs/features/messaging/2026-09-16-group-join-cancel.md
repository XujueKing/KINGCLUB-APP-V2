# 撤回入群申请

在已有 pending 申请结果下增加“撤回申请”，确认后调用 685 本人撤回接口。以 groupId/applicationId/status/changed 的服务端确认更新界面；不删除本机申请编号，以便退出重进恢复终态。失败不显示成功，可重试。已处理冲突刷新真实成员及申请状态，不把撤回当退群。

确认、请求期间禁止重复点击。账号变化、退后台及离开页面后不使用旧响应。实时状态变化在操作结束后统一刷新。后端 117 migration 未上线，完整功能待真实接口联调，不计作交付。

验证：撤回、申请重进恢复及原申请流程共 14 项测试通过，3 文件静态分析通过。覆盖取消确认不发请求、服务失败不报成功、管理员先通过后刷新成员权限、登录变化丢弃迟到撤回响应、错误回执拒绝。未安装手机；后端代码为 ccsop-service 165a02b，117/685 仍未部署，真实 MySQL 并发及 A/B 验收待完成。

后端后续 59abc21 已完成隔离真实 MySQL/加密 HTTP 验证：两次各 12 轮撤回和通过同时提交，每轮仅一个成功，申请终态与是否入群一致；重复撤回、非本人拒绝及审计字段通过，测试会话全部撤销。该结果不代表在线服务已发布或手机验收，117/685 在线部署及 A/B 验收仍待完成。

## 2026-09-16 撤回接口在线状态更新

后端测试 API 已最小发布 117/685（镜像 group-join-cancel-117），真实 MySQL / 加密 HTTP 和并发审核撤回检查通过；在线健康、接口目录、执行器及环境/端口/挂载一致性核验通过。替代此前“117/685 未部署”状态。客户端撤回代码及测试已完成，但新包尚未安装，A/B 手机撤回验收未完成，不能计作整项交付。

## 2026-09-16 06:55 A 安装最新包

基于 0b4f973 工作区构建 Profile/preview ARM64，保留用户 onboarding 三文件变更但不提交它们。真实测试 API，原有 LAN relay 公钥及证书构建配置保留；未以本次安装宣称 relay 在线或自适应联调通过。Gradle 65.7 秒，150.6MB，原生 ARM64 ELF 检查通过。

APK SHA256 D0623C329169C4A549B4F1FA7FB6A63A990249A31CD29A573355C60F6D6A1F79。A adb install -r 返回 Success，lastUpdateTime 2026-09-16 06:55:30。安装后的第一次启动仍显示既有系统通话错误 Activity，未进行坐标点击；再次显式启动并核对前台为 KINGCLUB 和首页后才进入消息。现有群/单聊列表正常，未要求重新登录。

本包包含头像并发修复 fb70359、撤回入群申请 86901c4。撤回功能已安装 A，但 pending 申请的实际撤回及重进验证仍待双机验收；B 暂停状态保持，已询问是否可恢复，尚未回复。没有把安装成功当成功能验收。

## 入群状态与撤回请求的前后台竞态（实现前约束）

切回前台时，若撤回尚在请求中，状态刷新应排到写入请求结束后，避免先读到 pending 而丢掉迟到的 canceled 响应。查询仍只信服务端，登录变更仍作废旧响应；提交/重新申请结束也需排空已排队的状态读取。增加受控慢撤回、后台再前台的回归，不能把测试替身当作双机验收。

验证结果：受控撤回请求在切后台再前台后才完成，修复前复现停留 pending；修复后自动查询 canceled，无重复撤回。撤回/恢复/申请共 15 项通过，2 文件静态分析通过。同绑定自动续期不会发布 SecureSessionStore.changes，本次未发现该路径误报登录变化，不改动登录逻辑。本修复尚未安装手机。

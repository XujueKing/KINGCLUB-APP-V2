# 历史搜索入口与发送人显示

A 对 B 单聊，从聊天详情进入查找聊天内容，搜索已有离线测试消息，真实接口返回目标；点击后进入独立上下文页，截图确认目标高亮。

发现并修复搜索及其上下文显示内部 userAccount：单聊传入当前账号和对方称呼映射，显示“我”或对方名称；群详情的搜索传入当前成员名映射，无法识别的旧成员使用“群成员”，不伪造昵称。消息身份与历史查询参数不变。

6 项搜索、详情头像和群设置组件测试通过，4 文件静态分析通过。Profile/LAN 包已安装 A，实机结果显示“我 · 昨天 15:44”，上下文显示“我／徐珏”，目标仍能定位。本次 APK 同时包含 7c63e93 的列表错误提示。B 尚未更新。

剩余：群搜索新名称尚未手机验收；聊天内点击引用消息的另一条上下文入口尚未接姓名映射；上下文里的媒体目前仅显示文字摘要，不能把这项验收当作图片/视频/语音完整历史浏览交付。

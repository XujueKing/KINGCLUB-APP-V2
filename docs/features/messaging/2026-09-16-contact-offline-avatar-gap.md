# A 通讯录断网重启验收及头像缺口

2026-09-16 06:12，A 使用包含 3324ae0 的 Profile/LAN 包。联网进入通讯录，名单包含两个已有联系人，均显示真人头像。关闭网络、强制结束进程、冷启动后进入通讯录；Android `Active default network: none`。

结果：两个联系人、性别及字母分组仍显示，并有网络不可用提示；但两个真人头像均退回默认头像。因此名单离线恢复通过，头像离线恢复不通过。未编辑或发送消息给联系人，B 未操作。

定位：`ChatMemberAvatar` 仅在 profile Future 成功后取得 avatar.fileId，再交给 `CachedMediaImage`。`cachedChatAvatarProfile` 只有进程内缓存；断网重启后资料请求失败，无法取得本机图片的内容键。联系人加密快照只保存 peer/nickname/remark/bio/gender。因此不能仅凭已有图片磁盘缓存宣称头像可以离线显示。

修复要求：按当前登录账号持久保存最小头像文件标识；仅明确网络失败时读取已缓存图片，不缓存鉴权 headers、令牌或签名 URL，不将旧快照当成在线媒体授权。明确权限拒绝、头像移除、账号切换应清除或禁止旧图；迟到请求不得污染新账号。完成后重做本次真机步骤。

网络已恢复，Wi-Fi、移动数据设置均为 1，默认网络 123。私有截图及 XML 位于 build/phone-a-contacts-online/offline.*，不提交。本节点记录真实失败证据，尚未修复该缺口。

## 06:19 修复及复测

新增 ChatAvatarSnapshot，按登录账号及目标会员用安全存储保存 fileId，始终先请求实时资料，只有明确 NETWORK_ERROR 才返回 cacheOnly 描述。明确拒绝或头像移除清除标识；会话身份及请求版本检查阻止迟到写入。存储中不含路径、鉴权头或完整资料。

MediaCache.cachedImage 只查当前账号已有图片，不使用 URL、不请求网络。ChatMemberAvatar 支持该本地描述，通讯录及聊天列表接入同一仓库方法。已有磁盘预算/清理仍适用；缓存被清理时显示默认头像，首次未加载过的头像仍需联网。

19 项头像描述、组件、内存请求共享及媒体缓存测试通过；9 文件静态分析无问题。Profile/LAN ARM64 构建 67.7 秒，已安装 A，APK SHA256 `064C19C25F61F04A0E68807603E9976FE2BAE94D17577BAA880182E09BA5624C`。安装包与最终源码只有三处 if 大括号格式修正差异，无行为差异；工作区原有 onboarding 改动参与构建但未提交。

A 联网打开通讯录后断网、强制结束进程、冷启动重新进入，系统确认默认网络 none，两名联系人真人头像均保持显示，修复前同流程均为默认头像。私有截图 build/phone-a-avatar-fixed-online.png / offline.png；B 未操作。测试后已重新启用 Wi-Fi 和移动数据。此项只确认 A 通讯录离线头像，不代表所有资料页、权限变更或 B 端已完成真机验收。

## 会话相关入口扩展

单聊消息双方头像、单聊详情、建群选人、转发选人和群详情成员头像统一通过 avatarProfile。本人使用 501 本人接口，其他会员使用 612；本人签名路径只用于当前在线图片加载，持久快照仍只有 fileId，离线模式不下载。资料主页仍请求完整实时资料，不用头像快照替代内容权限。

18 项快照、单聊详情头像、转发及群设置测试通过，8 文件静态分析通过。该扩展尚未打包安装；此前 A 通讯录实测不自动代表这些入口也验收完成。

## 06:26 扩展包安装，验收暂停

包含 86e27d4 的 Profile/LAN 包构建 64.6 秒，APK SHA256 `4926674E7DE981367AC780C23217C5B48DE03DB85E913DD5F4D1C769DEAFC177`，A 覆盖安装成功，package lastUpdateTime 为 2026-09-16 06:26:08。B 未操作。

启动命令虽返回 Status ok，但 LaunchState 为 UNKNOWN；未先确认前台页面即继续坐标点击，后续截图显示系统拨号失败弹窗，前台为 com.android.server.telecom/.OplusErrorDialogActivity。已立即停止触屏操作，并请用户将 A 恢复至 KINGCLUB 首页。无法把此轮算成启动/聊天头像真机验收，也不推断第三方通话成功。后续每次启动后必须先检查前台 Activity 及当前页面，再决定点击；不可将启动、未读出的 UI dump 和后续坐标点击放在同一批次。

## 并发头像请求修复

测试复现两个页面同时请求同一头像时，第二个请求仅开始就使首个有效响应抛出 SESSION_CHANGED，导致默认头像。改为只有更新请求已完成授权判断才取代旧响应；未完成的新请求不作废有效结果。新拒绝先完成、旧成功迟到时仍拒绝旧结果，磁盘标识不恢复。

复现测试修复前失败，修复后头像快照、内存共享及组件共 13 项通过，两文件静态分析通过。该并发修复未安装；A/B 触屏验收仍暂停，未重复请求用户操作。

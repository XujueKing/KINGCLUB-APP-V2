# A Flutter帧记录只读观察

在83bef3a预览运行期间，通过现有Profile VM服务只读获取时间线，不修改业务数据或开启额外时间线流。只保存本机忽略目录，VM地址与完整时间线不提交。

按线程配对B/E事件，完整Frame样本327个：P95 7.309ms、最大15.479ms；GPURasterizer::Draw样本328个：P95 7.156ms、最大12.939ms；LAYOUT根样本327个P95 3.471ms，BUILD样本893个P95 3.004ms。统计只覆盖当前环形记录中完整事件，不能代表整个会话、未采集尖峰或用户感觉。此结果支持下一步优先核对位移轨迹与系统IME同步，不证明绘制已全场景达标。

随后读取UI时为徐珏会话，输入区在键盘展开位置。准备清空时间线、收起再展开键盘进行定向采样前，A已从ADB消失；命令未执行，二次devices清单为空（A/B均不在）。因此没有键盘定向结果，不能引用旧XML冒充操作后的页面。也未关闭手机网络或发送新消息。

下一步在设备重新可用后重新获取当前VM地址及UI，采样明确的IME展开/收起区间，比较输入区和最后一条消息位移；不以本表替代用户视觉验收。

# Call route teardown

Fix outgoing presentation ownership when the navigator is destroyed instead of a normal pop. Navigator teardown can dispose the call page without completing the awaiting push future. Bind the exclusive presentation lease to the call route subtree as well as the existing finally block. A late release must not unlock a newer owner. The guard changes no UI and does not release merely on backgrounding or dispose the underlying chat while the call route remains alive. Cover group picker teardown and the guard lifecycle; retain existing direct/group call tests. Incoming flow lifecycle remains a separate follow-up.

Validation: 11 call history, presentation ownership, direct call page and group call page tests passed; static analysis of four changed Dart files passed. Group audio/video tests now destroy the navigator instead of popping the picker and assert a new presentation lease can be acquired. Route scope test verifies ownership is held while mounted and an old delayed release cannot unlock the next call. No phone installation; this fix postdates the 72902aa APK.

Incoming follow-up: app-owned presentation leases replace unscoped busy booleans. Session reset/application disposal releases the owned lease; identity checks prevent a late previous flow from unlocking a new one. Incoming call routes release on subtree teardown. Invitation dialogs report only unexpected disposal: a normal accept/decline retains ownership across the asynchronous state read and transition into group calling. Backgrounding alone does not reset ownership. Validation: 48 ownership/scope/dialog/foreground-inbox/call-page/app-shell tests passed, and seven-file static analysis passed. Navigator destruction completes the waiting presenter as well as releasing its lease. Normal invitation accept/decline retains ownership until handoff; unexpected teardown releases it.

Expanded app_smoke_test failed 15 cases. Clean unchanged 72902aa reproduced the identical 15 failures (28 passed / 15 failed for that file alone), compared by test-name sets. These include obsolete storage-tab and fake-contact expectations; they remain unresolved, not counted as passing. The prior 179-file chat batch excluded app_smoke_test and does not establish an all-app pass. Logs: build/incoming-call-route-release.log, build/incoming-call-route-release-final.log, build/incoming-call-route-release-analyze.log; baseline build/app-smoke-baseline-sept18.log in preview worktree. No APK rebuilt or phone installed for this change.

# 好友资料旧版截图对齐

用户确认封面、重叠圆头像、白色顶部圆角内容区、统计、签名、标签及作品／动态／相册的旧版结构。

真实入口 PublicMemberPage 保留上述布局，本次封面区域增加 20 个逻辑像素（随屏宽缩放）；动态卡片最小高度按 146 逻辑像素缩放，恢复截图中较宽松的时间轴节奏。资料和内容仍来自真实接口，不填入截图示例数据。仅聊天的内容权限保持服务端控制。

验证：public_profile_content_test 两项通过，覆盖切换栏目迟到响应、隐私变更、动态分页和相册空状态；页面静态分析通过。尚未重新打包安装或在手机上做截图对比。视频缩略图目前仍是播放占位，不能声称已完全复刻截图中的视频预览。

## 视频首帧预览

好友作品／动态／相册视频接入缓存源文件生成静态 JPEG 首帧，320px 宽、质量 80，列表不播放视频。预览图使用当前查看账号隔离、文件版本键、图片缓存预算和退出清理，生成期间清理缓存会丢弃迟到结果。仅在当前资料／内容接口授权后创建控件；隐私变更移除内容。

首次预览仍需要下载原视频，服务端小缩略图仍待补充，不能当作首屏流量优化已经完成。缓存和权限回归 7 项通过，实际首帧原生解码与手机截图待验证。依赖接口依据：https://pub.dev/packages/get_thumbnail_video 。最初 video_thumbnail 0.5.6 因过时 jcenter 配置无法在当前 Gradle 构建，已替换，不修改全局依赖缓存。


构建结果：preview profile arm64 APK 构建通过（120.2 秒，118.6 MB），SHA256 `87f27ee84d255ef512b043fcc6891ae3bd4d90f44e6742b79ac630e4f7ccb71c`。包含当前工作树既有 onboarding 三个文件的改动，本次未编辑或提交它们。手机 462606d8 为 offline，未安装。当前包还包含已提交的定位选择界面，但后端 065 尚未部署，不能认为定位消息已上线。


## 真实会话头像

真实单聊和群聊消息行不再固定使用 LegacyFakeAvatar。本人使用 501 资料接口，其他成员使用 612 授权资料接口；每个会话页面按发送人复用请求，42dp 圆头像接入账号隔离图片磁盘缓存。重连、关系及群状态变更清空请求缓存，登录变更清空页面。缺失、拒绝或失败显示中性人像，无加载转圈。演示路径仍保持原素材。

头像/资料针对性测试 4 项通过，变更静态分析通过。额外运行旧 direct_chat_extensions_visual_test 的 3 项失败：返回箭头预期 11x22、实际 8x16；旧附件金图不一致；旧礼物按钮键不存在。没有更新金图掩盖差异，旧视觉用例需要按已确认 UI 重新验收。本次真实头像尚未构建安装或真机验证。


## 头像接口契约与新版安装包

已对照服务端：501 本人头像为 attachments 带 token 路径，612 访客头像为 profile-media 路径及 x-profile-media-token 请求头。补充有效描述进入私有图片缓存、刷新等待及拒绝不保留旧头像的测试；6 项头像/资料测试在默认测试环境通过，静态分析通过。组件允许显式传入 API 基址供测试使用，运行仍默认构建配置。

preview profile arm64 构建完成，59.8 秒、131.8 MB，SHA256 `9be0102634c880667930b06f3e3971627fcc7aad2cd51b0d76d55acfdfd8ada5`。包含群名称加密缓存、群资料拒绝权限清理与真实消息头像；也包含工作树原有 onboarding 三文件改动，本次未修改或提交。手机最近检查为 offline，未安装，真实头像网络下载与真机布局尚未验收。


## 最新截图与旧版操作区复核

按用户最新截图及旧版 pages/userInfo 的布局：顶部封面、叠放头像、白色圆角资料面板，统计、签名、资料标签，再接作品/动态/相册。保留原生返回按钮，不复制小程序胶囊。

本次缩短封面上方留白 20dp（按屏宽缩放），栏目宽度从 76 改为 56dp 并增加左侧 16dp 留白。关注和私信移回资料区随页面滚动，采用旧版浅灰/未关注粉色小圆角按钮；本人资料不显示这两个动作。真实会员信息、内容加载和仅聊天隐私校验继续沿用现有接口。

验证：资料/内容 4 项测试通过，包含隐私变化移除内容、过期响应拒绝和仅聊天仍可私信。手机视觉效果尚未验收。

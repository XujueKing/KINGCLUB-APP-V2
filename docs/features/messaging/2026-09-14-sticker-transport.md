# 收藏表情真实发送（进行中）

当前面板收藏/表情包只在设备本地保存，真实会话 onSticker 被 _requiresRealMedia 拦截。先接静态收藏表情到既有图片上传、确认、单/群可靠队列和私有读取链路，点击后预览，明确点发送才发送。确认页标题为“发送表情”。选择器保存路径读取前限制 20MiB，离开会话或控制器变化不继续打开页面。

服务端 chat-image-processing 已检查 pages==1，动态 GIF/WebP 不会静默取首帧，而是拒绝；动态表情专用存储/发送/渲染协议仍待实现。当前消息类型仍为 image，不能声称表情专用消息类型已交付。设备表情库的账号隔离与同步也仍待完成。

静态表情入口已连接发送确认页，保留演示会话原本的本地展示；真实会话不再插入伪造本地已发送表情。两个 UI 文件 analyze 通过，图片上传/单群队列共 10 项回归通过。该测试验证复用的传输流程，尚未手机点击收藏表情并在另一设备收到，不能标完整表情交付。

## 设备收藏账号隔离
收藏数组改为面板实例状态，磁盘目录按账号摘要隔离。账号变化清空旧视图并重新加载；迟到加载/导入不得覆盖新账号的状态，路径记录仅接受本账号目录的直接文件。原无归属 chat_stickers/library.json 保留不删除，也不自动分配给任一账号；旧收藏归属确认/迁移另行处理。

账号隔离验证：真实目录 fixture + Flutter 组件测试中，A 读取延迟、切换 B 后仅显示 B，A 的迟到结果不覆盖，切回 A 恢复 A。输入面板原有 6 项布局/交互回归通过，三个修改/测试文件 analyze 通过。测试 I/O 已置于 tester.runAsync，避免模拟时钟阻塞真实文件读取。原无归属库保留未迁，已向用户询问是否全部属于当前账号，未收到确认前不分配归属。尚未手机跨账号验收。

# 2026-09-20 中继断线自动恢复

## 已完成

中继断线不再固定等待 30 秒。首次约 1 秒重试，连续失败按 2、4、8、16 秒退避，最长 30 秒。加入最多 20% 的随机错峰，最终等待时间仍不超过 30 秒。连续在线 30 秒才重置失败计数，避免节点反复接受连接又断开时高频重连。

切后台取消重试与稳定计时器，切回前台立即连接。旧握手迟到不能替换新连接，退出账号后不会继续重连。节点身份认证、会员权限、加密和服务端回退路径保持原规则。

## 验证

新增 5 项自动化检查通过：连续失败退避上限、短暂成功不重置/稳定后重置、前后台切换、迟到连接隔离、账号切换取消。相关自适应文字、消息控制器、离线待发队列的 49 项检查本轮亦通过。修改文件静态分析通过。

本轮使用可控连接替身验证调度及生命周期，不代表真实公网 SuperVM 或双机网络验收。没有安装手机或变更线上服务；公网节点部署、跨运营商打洞尚未交付。

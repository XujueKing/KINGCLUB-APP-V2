# 群二维码（进行中）
用户已授权完整原生群聊。群设置进入独立群二维码页，沿用全局右入页面动画、黑底香槟文字、白底二维码。只调用当前账号656，群名由已授权619读取。短码不落盘；仅前台有效时展示，切后台/登录变化/群变更立即隐藏，恢复重新鉴权。10分钟有效码临近到期续签，网络失败时有效旧码可继续显示，但权限失效立即隐藏。二维码不能作为已完成扫码入群的证明；657预览及申请/审批客户端仍待完成。共享服务未部署656时明确错误，不回退假码。

## 实现与验证
群设置入口、独立GroupQrPage和656客户端调用完成。619群名后再次检查生命周期才签码，响应群ID/格式/TTL校验；单调时钟自请求起扣除有效期，避免网络耗时或系统时钟回拨延长有效期。前后台、登录变化、群事件清码并丢弃迟到响应，不持久化二维码。
Flutter页面测试覆盖响应校验与重试、后台隐藏/恢复、会话失效后的迟到响应拒绝；与群公告和成员管理共6项测试通过，4个变更文件analyze无问题。日志build/group-qr-client-tests.log、build/group-qr-client-analyze.log。未进行手机安装或共享环境656/657联调；入群审批、扫码路由和预览客户端仍待完成。

## 扫描预览接入
扫一扫按KC:G:前缀路由到群预览，严格格式检查后调用657；个人码仍507。预览仅显示群名、人数和本人是否已加入，不显示成员列表或历史。已加入者可进入已有群会话；未加入者提示联系群成员邀请（现有邀请流程），不伪造已加入。会话变化、离开前台和群事件清除预览，迟到响应丢弃。

群扫码预览实现：新增GroupQrPreviewPage及657调用，扫一扫KC:G:分支已连接。前后台/登录/群事件清除结果；到期清除；严格检查群UUID/人数/布尔成员标记及期限。群内成员进入既有DirectChatPage，非成员无自动加入写操作。扫一扫补充会话与迟到响应保护、finally中恢复相机。群预览4项、群二维码2项、原好友扫码2项合计8测试通过；4文件analyze无问题（build/group-qr-preview-tests.log、build/group-qr-preview-analyze.log）。相机硬件扫描、路由全链及共享接口联调仍未实机验收；现有测试不能证明扫码入群可用，申请/审批接口仍待实现。

## 预览生命周期补验
新增可注入事件流（默认仍使用真实KingclubRealtime），用于模拟真实群变更/重连顺序。新增到期清除、后台恢复重新授权、群变更立即隐藏、两次请求乱序及最终权限拒绝测试。group_qr_preview_test共6项通过，2文件analyze无问题；日志build/group-qr-preview-lifecycle-tests.log和build/group-qr-preview-lifecycle-analyze.log。此为客户端状态机测试，未替代手机相机与服务端联调。

## 申请入群表单
预览非成员显示申请入群，进入原生独立表单，群名+最多200字备注。658持久幂等标识由账号安全存储保存，键为原短码和规范备注的SHA256摘要，不落盘原短码。首次发送后冻结备注，失败继续同请求重试；服务端明确pending才显示等待审核，accepted/already_member返回预览重新核实成员身份，拒绝/过期不进群。会话变化立即清空，后台不显示私人备注，禁止自动发送。共享服务未部署时报错，不模拟成功。

申请表单及658接入完成：首次提交冻结备注，失败复用账号安全存储里的请求标识，回执群ID/状态/申请UUID严格校验后才清理幂等标识；跨Repository重建仍能恢复同一请求。安全存储不含原短码或备注。退出登录立即清表单和回执，后台隐藏内容，不自动重新发送。申请/预览/幂等存储共11测试通过，4文件analyze无问题；build/group-join-client-tests.log、build/group-join-client-analyze.log。审核管理员客户端仍待实现；未进行共享服务部署或手机端申请实测。

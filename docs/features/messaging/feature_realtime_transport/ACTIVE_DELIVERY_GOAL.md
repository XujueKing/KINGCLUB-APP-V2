# 自适应聊天传输交付目标

2026-09-20 用户明确启用目标模式，以下任务全部处理完成前保持目标 active。
不得用阶段提交、测试跳过或单纯辅助类实现代替实际接入。

## 已有基础

- MemberRelayRuntime / NovoRudpRelayFrameLink 已有认证设备通道。
- NovoRudpLanRoute 已有局域网候选、加密探测、心跳和直连失效回退。
- NovoRudpNatSocket 已有签名探测及保留端口交接，未接实际公网聊天路由。
- 文字有实际发送预算、代次隔离及重连退避恢复。
- HTTP 附件有加密分块缓存、断点续传、最终摘要和权限复核。
- UDP 文件接收器以临时文件保存碎片，只在整文件校验后交付。

## 必须完成

- [ ] 将公网端点发现、端点交换、同时打洞接到实际帧通道；授权来源不改变。
- [ ] 统一局域网、公网直连、中继和服务端的选择、失效恢复与稳定切换。
- [ ] 确定附件跨通道分块身份/完整性契约，禁止把未验证碎片冒充已验证缓存。
- [ ] 完成 UDP 与 HTTP 的实际续传接入、取消清理及消息删除联动。
- [ ] 核对图片、视频、语音、文件实际入口覆盖，不能仅以文件测试代表所有媒体。
- [ ] 统一自动检查及可执行联调；未能执行的双机/跨网络项目明确保留未验收。
- [ ] 每个完成节点只提交本任务文件并推送 GitHub。

## 约束

保留当前 UI、业务权限及并行工作。SuperVM 是通用公网基础设施，不改无关共识。
隐私能力按证据表述，不承诺绝对不可追踪。已有 novorudp_file_sender.dart 未提交
改动不属于本轮所有，不覆盖或顺带提交。不存在真实节点配置时先完成接入代码，
查证部署与设备状态后处理外部条件，不提前把目标标为完成。

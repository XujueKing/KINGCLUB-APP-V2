# WebSocket 实时传输验收

- [ ] 未登录、过期会话、错误签名和重放连接被拒绝
- [ ] 所有生产帧均加密并校验签名
- [ ] 入站 seq 重放和过期 timestamp 被拒绝
- [ ] 客户端始终只有一个活动 socket 和一个心跳计时器
- [ ] 前后台、网络切换、服务器重启和连续失败重连可恢复
- [ ] 重连使用新 nonce/requestId/连接密钥，不复用旧帧序号
- [ ] 未授权频道订阅被拒绝并写审计
- [ ] 断线期间事件可通过 HTTP 游标补偿
- [ ] 重复 eventId/messageId 不产生重复业务结果
- [x] 会话撤销后 HTTP 与 WebSocket 均失效（服务端隔离 E2E；Flutter 行为待实现）
- [ ] 代理、应用和审计日志不记录签名、密钥或完整敏感查询串

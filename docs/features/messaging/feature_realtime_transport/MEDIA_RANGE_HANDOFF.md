# 媒体分块与通道交接

2026-09-20：ChatFileDownloader 可接收明确 media/fileId 的媒体对象引用，复用
已实现的加密分块缓存、UDP 检查点、HTTP 重试和反向恢复。
HTTP 请求使用 1MiB 单段 Range；要求 206、精确 Content-Range、长度、媒体类型、
整对象 SHA256 和相同 fileId。原文件接口和缓存身份保持兼容。

单聊/群聊授权按实际媒体 API 请求；缩略图与 HEVC 使用各自路径。
对端完成后的媒体类型/fileId 再次向服务器复核，失败不交付。

验证：40 项下载器/交接测试通过，包括图片、语音、视频分别 UDP→HTTP、HTTP→UDP，
不齐边界分片及原有坏摘要拒绝。测试使用真实磁盘/加密缓存、替身已认证帧和 HTTP。
尚未连接页面入口、部署新服务器或完成设备验收；媒体分块缓存的删除持久化映射
也仍待入口接入时完成，不得直接称全媒体交付完成。

# 附件重试优先利用已有分块

HTTP 下载按顺序保存加密完整分块。重新下载时，在服务端重新授权后验证第一个
缓存分块；存在有效分块就继续现有分块续传，不再先启动全量设备下载。
首块认证失败或过期时仍走正常设备优先路径。首块只解密一次并复用于重建文件。

这优化已缓存 HTTP 数据的路由选择，不把尚未整体校验的 UDP 临时碎片混入缓存，
也不宣称已经实现 UDP/HTTP 任意分块互通。删除、权限和最终摘要校验保持有效。

验证：下载及续传测试共 39 项通过，修改文件静态检查通过。
新增案例验证存在有效首块时不调用设备下载，以及首块损坏时恢复设备优先尝试。
本批未安装手机验收。

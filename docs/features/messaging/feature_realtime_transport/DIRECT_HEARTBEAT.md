# 直连心跳时效

2026-09-20：直连探测增加每个候选端点独立的随机挑战、单次消费和三秒响应期限。
仅有加密认证不足以证明路径当前可用：迟到的旧 pong 不能刷新六秒在线窗口。
候选端点变化时清除未完成挑战；没有匹配挑战的回复保持走原有回退路径。
旧版无挑战 pong 不建立新直连，仍可使用原有中继。

同机真实 UDP/原生加密的 LAN 回归独立于公网中继配置运行；公网测试仍需要真实配置。
验证：两项挑战时效测试和一项真实原生加密 LAN 回归通过。LAN 回归包含无效首候选、
断开六秒后停止直连、端口重新绑定后恢复收发；四项真实中继测试因配置缺失跳过。
三个变更源/测试文件的 Dart analyze 通过。
本节点不代表图片、语音、视频的双通道入口已接完，也不代表公网双机验收完成。

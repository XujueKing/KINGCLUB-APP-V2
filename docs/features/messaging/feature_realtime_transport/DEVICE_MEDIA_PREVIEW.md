# 2026-09-20 双机媒体预览包

已构建 Profile/ARM64 预览 APK，包内 NovoRUDP 原生库校验通过。A、B 均覆盖安装 Success；没有卸载或清除数据。构建携带对端文件、群文件、LAN 和 STUN 配置。本机已启动独立测试中继（有运行时限），不是公网常驻节点。

原生 DLL + 实际 WSS 中继 + 实际 UDP 测试 5 项全部通过：无效首候选跳过、端口重绑定恢复、中继丢失最终回执、LAN 丢失回执、LAN 中断及应用下载器路径。传输内容最终完整性校验通过；该证据不替代手机媒体播放验收。

A 保持登录且中继报告出现活动连接；B 安装后停在登录页，已请求登录原测试会员。B 的 Android 使用 topResumedActivity 字段，原启动脚本只识别 mResumedActivity 导致误报；现优先新字段并回退旧字段，B 实机前台检查通过。

本机到现有 TURN 的 UDP STUN 请求超时；云端探测时未捕获相应请求，尚不能判定具体上游阻断位置。公网寻址/跨网络直连仍未验收，不能将局域网测试通过称作公网成功。继续处理 B 登录后的媒体双机联调和公网可达性。

# 媒体对象的对端通道

2026-09-20：接入服务端 K260916000686 的 media 参数，六种媒体对象贯穿
BindingRuntime → MemberRelayFiles → PeerFileChannel → 权限首次读取/定时复核。
media 与 fileId 加入 sameFile 比较，拒绝对象版本替换。

文件保留控制版本 1/2；媒体使用版本 3/4，request/ready/reject 必须匹配媒体类型、
消息、传输 objectId 和群范围。老客户端不接受新媒体请求时走原有失败回退。

发送侧直接读取已有 MediaCache 持久化文件，不创建额外的长期副本。
NovoRudpFileSender 在发送数据前验证长度和完整摘要，缺缓存/编码不符即失败，
不为了提供 P2P 转发而先从服务器下载。既有权限和五秒续权仍有效。

当前状态：授权、控制包和缓存来源已接；实际图片/语音/视频下载入口、HTTP 分块
恢复和群媒体目录/握手能力仍待接入。服务端 128 migration 尚未部署。
不得据此声称已完成媒体双通道交付或手机验收。

验证：33 项客户端测试通过（媒体对象身份、六类请求、群权限及原有双向续传），
7 个相关源/测试文件静态检查通过。控制包测试使用替身链路，尚非双机媒体发送测试。

# 头像刷新与新消息高度稳定

用户实机反馈头像每次进会话闪烁、新消息从零高度撑开并挤动历史。移除消息 Align.heightFactor 动画，消息直接占最终高度，仅对本次新发消息保留淡入；这不等于历史位移帧率已经验收。

CachedMediaImage 增加显式同内容保持选项，仅头像开启。相同 contentKey、存储、私有状态下凭据刷新保留现有图片；内容变化重置，安全会话变化强制重载。ChatAvatarSnapshot 维护当前会话内的文件 ID 快照，进入会话初帧可以复用，磁盘读取仍为账号隔离；会话变更清内存映射。

15项相关测试和静态分析通过，包含同头像更新URL保留图像、换图清旧图、现有权限/账号隔离及发送定位。提交ecd7954已合入预览e27b977，A覆盖安装成功；启动脚本一度因ADB重连失败，随后am start确认当前实例运行，UI为徐珏会话并显示14:41新消息。用户正在实际操作，未再注入测试消息。完整顺滑度待用户实机反馈，视频离线测试未执行，网络未关闭。

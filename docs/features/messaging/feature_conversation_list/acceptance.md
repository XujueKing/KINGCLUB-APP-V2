# 会话列表功能验收

## 文档准入

- [x] 单聊、系统通知和通讯录的信息架构明确
- [x] 排序、搜索、未读、隐藏和事件合并规则明确
- [x] ConversationRepository 与 CONV-M01～M11 明确
- [x] 群聊、正文搜索和列表删除历史已排除
- [ ] 用户确认列表范围、滑动动作和搜索边界

## UI Mock 验收

- [ ] 完整、空、搜索、分页、离线、实时更新和会话失效可演示
- [ ] 摘要/未读版本不因重复或乱序事件倒退
- [ ] 多尺寸、读屏、滑动替代动作和 200% 字体通过
- [ ] 不访问真实会话、WebSocket 或推送服务

当前只具备评审条件，不允许编码。

## 2026-09-13 旧版列表比例与原生顶部
用户指定：左起聊天、通讯录，右侧旧版圈加号，默认聊天。两个分支复用统一头部；再次点击消息回到聊天。add.png 与旧版 images/add.png SHA256 相同，包内加载。
列表按750rpx宽度适配：头像96rpx圆形，行高140rpx，名称30rpx/#FFFFFFBB，摘要26rpx/#FFFFFF66，时间24rpx，头像与文字25rpx间距。未读34rpx、字号22rpx，压头像右上角（left70rpx、top-3rpx），无描边；置顶条84rpx。系统与好友共用列表内容组件。保留现有示例数据和点击流程，本轮不宣称真实聊天消息接口已接通。

验证：flutter analyze 通过；17项壳层/通讯录测试通过，新增默认聊天和顶部顺序断言后壳层10项通过，列表未读/置顶/删除及切换2项回归通过。Android preview APK构建、覆盖安装成功，真机截图检查顶部顺序、旧版加号、头像右上角气泡与列表字号（本地 build/chat-layout.png，不提交会员截图）。

2026-09-13 分隔线微调：按用户要求居中，宽度为屏幕90%（底栏宽度）减4dp，两端各内收2dp；颜色透明度由0x30降至0x1C，线高保持0.5dp。头像与文字位置不变。静态检查通过。

2026-09-13 免打扰：使用用户提供SVG（内置 muted.svg、原色#999FB3、26rpx），只在免打扰时显示于日期下方。未读>0且免打扰时头像角标改16rpx红点，不显示数字；非免打扰数字上限99，不显示99+；未读为0不显示角标。当前示例会话详情开关通过回调同步壳层与列表，重进详情保留本次壳层会话状态，未接服务端持久化。静态检查和2项静音/数字上限/零未读/详情回调测试通过，APK已构建并覆盖安装成功。

2026-09-13 顶部与静音对齐：聊天/通讯录选中标题与发现共用白色22dp/w600样式（聊天放大、发现缩小），未选标签18dp；顶部文字左边缘与加号右边缘均距屏幕22dp，保留44dp点击区。静音SVG使用与时间一致的#FFFFFF66，和消息预览放在同一个Row中垂直居中，关闭免打扰不占图标位置。静态检查通过；17项壳层/通讯录测试及2项静音状态/预览对齐测试通过。

2026-09-13 顶部位置再次校准：聊天按发现的SafeArea内left18/top10定位，替换居中按钮导致的额外偏移；通讯录未选字号16dp，与聊天使用alphabetic基线对齐。加号可见右边缘改18dp（再右移4dp）。静态检查及17项壳层/通讯录测试通过。

2026-09-13 顶部搜索：聊天与通讯录共用黑金搜索框，固定在标签下方，左右18dp；聊天按显示名称本地筛选（忽略首尾空格及大小写），搜索时显示命中的折叠置顶会话，无结果有提示；清空恢复原有列表/置顶/未读状态。通讯录复用备注昵称筛选，移除原列表内重复搜索框。支持清空、键盘搜索收起、点击空白收起。当前筛选仅针对现有示例列表，不接聊天正文或服务器搜索。静态检查、19项相关测试及新增筛选/清空断言通过。

2026-09-13 搜索与置顶条样式：搜索上移6dp，使用用户提供的内置search.svg并着色为原搜索提示色；顶部标题位置不变。折叠置顶条去圆角，背景#C9B69E/10%，上下0.5dp细线通屏，文案/图标统一消息预览色#FFFFFF66。普通会话头像位置及窄分隔线宽度不变。静态检查通过。

2026-09-13 可选状态行：搜索下方显示无圆角灰底提示行（设备图标/网络图标+文案），无状态整行不占位；网络不可用优先于设备提示，设备数量与手机通知状态由参数提供，不伪造截图中的2台设备。当前聊天仍为UI示例，设备会话真实接口未接入。网络提示保留既有本地失败/重试示例。置顶上下线统一下方分隔线#C9B69E/0x1C、0.5dp，背景仍5%。

2026-09-13 搜索随列表与置顶分组：聊天搜索移入ListView首项，通讯录搜索移入Sliver首项；上滚自然离屏，顶部标签不动。提示仅“搜索”，聚焦隐藏；左右10dp、圆角4dp、高84rpx与折叠条同高。置顶会话与底部折叠按钮同属5%背景分组，避免背景叠加；搜索仍可找到折叠的置顶会话。静态检查及9项搜索/免打扰/通讯录测试通过。

2026-09-13 置顶边界与官方消息：长线移至整个置顶分组的上下外边界，折叠按钮不再单独画长线，内部会话保留短线。KING CLUB固定属于普通列表、无置顶操作，不计入置顶数，折叠置顶不隐藏官方会话；无置顶好友时隐藏空分组。

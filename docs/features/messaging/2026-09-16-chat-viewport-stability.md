# 键盘和消息状态变化的列表稳定

用户仍报告键盘收放及收发消息抖动，不能把此前组件测试当作顺滑度验收。

本次取消每次回执/同步刷新触发的自动滚动；反向列表本来位于最新端时无需再次滚动，仅本人在阅读历史时新发送才定位最新。普通发送状态和已发送暂无回执状态保留相同文字行占位，避免状态变更改变消息行高度。SafeArea保持底部viewPadding，避免键盘显示隐藏时安全区高度切换；视口高度变更只在非滚动且精确贴底时纠正到底部，不在用户拖动的24px范围强行吸附。

7项面板、键盘尺寸跟随、历史位置发送测试通过；静态分析通过。真实IME连续帧、收发滚动顺滑度尚未验收，不能声称彻底修复。

安装：预览0399edb已覆盖A，调试连接短暂离线后通过指定设备reconnect恢复；安装Success、稳定前台启动通过，UI为徐珏会话并显示14:49消息。保留账号数据，未关闭网络、未操作B。

同时完成视频权限拒绝后封面和完整视频两个消息本地入口一起失效；真实临时文件测试确认两者删除，旧播放器权限/解码回退测试此前已保留。此不代表所有媒体全局撤回清理已完成。

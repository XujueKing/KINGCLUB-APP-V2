# 原生视频消息接入

状态：数据层与持久化队列完成；页面入口、上传到处理的衔接、视频播放器尚未接入，不能计为手机视频聊天已交付。

沿用已确认聊天 UI。MessagingRepository 接 669 视频处理（独立 125 秒接收超时，其余请求保持原超时）、665 单聊发送、667/668 私有媒体凭据；GroupChatRepository 接 666 群聊发送。处理回执须 ready 且编号、时长、尺寸、音轨标记合法，构造 ChatVideo 后进入发送队列。

视频元数据写入现有按账号加密的 outbox，先落盘再发送。同一条消息重试保留 clientMessageId 和视频资产编号；群消息沿用群成员代次。回执逐项匹配视频编号、尺寸、时长及音轨标记，失败保留队列；隐藏或撤回回执按现有墓碑处理。历史磁盘白名单增加五个视频字段，不保存访问令牌。

验证：单聊/群聊视频队列测试覆盖网络失败后重建控制器恢复、错误回执不移除队列、写盘失败不发送、群权限失效不入队。语音队列原测试同时通过。尚未安装此版本或部署共享后端的视频接口，真实手机录制、播放及两个账号联调仍待完成。

## 选择和发送确认页

照片/拍摄入口通过两项菜单选择照片或视频，保留原加号网格。视频保存到独立会话草稿，限制 64MB，相机提示两分钟；服务器再校验真实格式和时长。确认页使用本地播放器预览，沿用文件分块上传，调用 669 处理后写视频 outbox。草稿编号作为 clientMessageId，处理或网络失败保留源文件及编号；已入队后才清理草稿，避免重启后重复消息。尚未安装实机验证；接收视频卡片与私有播放器仍未完成。

## 接收视频卡片及播放器

聊天列表的视频消息显示服务端封面、时长和播放按钮，点击进入黑底播放器，支持暂停和拖动进度。缓存使用会员范围及文件编号/摘要，显示前先取得当前消息凭据，读取后校验大小与 SHA256，再次授权后才显示或播放。后台清空播放状态，前台重新授权；登录变化永久失效，关系/群状态变更重新校验。下载完成后本地播放，当前不做边下边播；已缓存内容也不能绕过授权。静态检查、凭据边界测试和无权限播放器测试通过。共享接口尚未部署、APK 尚未更新，真实解码和双机验收仍未完成。

## 构建与安装记录

后端共享测试服务已部署 chat-097，编解码及健康检查通过。App 代码 1fe854f 构建 preview/profile/android-arm64。首次命令遗漏 KINGCLUB_API_BASE_URL，安装后审查发现会进入 MockAuthRepository，因此该包不是有效联调包。已重新显式指定真实 HTTPS API 地址构建，耗时 81.3 秒，APK SHA256 为 31B3D0D8C3891384C308DBC182C8FD8DF36C80230A4C8282E1F994D1444B9142。覆盖安装时 ADB 报 device not found，真实接口包尚未安装，等待 USB 重连；不得将之前安装成功记为真实视频验收。新增 scripts/build-chat-preview.ps1 强制传入 HTTPS API 地址，防止后续漏参。

## 相册返回误判登录变化修复

上述真实接口包在 USB 重连后于 2026-09-15 01:47:56 覆盖安装成功，保留应用数据。用户实测选择视频后提示“登录状态已变化”，未发送成功，因此视频实机验收仍未通过。

原因：应用 resumed 后检查会员状态，refreshMembership 保存同一套登录凭据。原 saveSession 每次都递增 MemberQrMemory.generation 并广播登录变化，使相册打开前绑定的 ChatFileDraftStore 失效，也会重建聊天控制器及中断上传。

修复：保存时比较 sessionId、apiKeyId、apiKey、会员账号及访问状态；同一登录仅刷新资料和二维码展示缓存，不递增登录代次、不广播账号变化。二维码使用独立 presentationGeneration，迟到的旧缓存响应仍被拦截。真正换账号、轮换凭据、退出登录或会员访问状态改变仍使旧任务失效。

验证：相册返回对应的草稿绑定检查、凭据/账号/权限改变失效，以及现有聊天重绑定、注册恢复合计 25 项测试通过；视频缓存二次授权、迟到凭据不读取缓存、单聊/群聊视频队列共 9 项通过。相关静态检查通过。缓存测试使用确定性文件流避免 Flutter 测试时钟与磁盘异步读取互相等待；生产读取和摘要校验保持不变。修复 APK 正在构建，待覆盖安装后重新由用户实测选择、发送、封面和有声播放。

修复包已通过真实 API 构建脚本完成并成功 adb install -r 覆盖安装，保留账号数据；SHA256：F37845883B73AC389F8CD955B323CA4B8BB66FD9DFECE6C2E92649E9478363D0。构建脚本同时修正 Windows PowerShell 把 Flutter 插件标准错误警告误判为终止错误的问题，仍按真实退出码判定构建成功。等待用户重新选择短视频验证，本记录不代表双机收发通过。

## 迟到登录响应与并发保存

继续检查发现，refreshMembership 和令牌轮换的网络请求返回后无条件保存；如果用户此时已经退出或换号，会恢复旧账号。旧会话返回撤销错误时，也可能无条件清除新登录。

SecureSessionStore 增加按当前凭据比较后保存/清除，跨实例的修改按顺序执行。会员刷新和轮换保存必须匹配发起时的 sessionId、apiKeyId、apiKey、userAccount；旧响应不触发 onAuthenticated。失效错误只清除其对应会话，旧恢复检查不强制将新登录重定向到短信页。正常同一登录的资料更新仍可连续成功，不重新引入相册返回误判。

验证：31 项测试通过，包含真实安全存储 mock 配合延迟接口的退出/切号后返回、退出与旧保存并发、旧撤销错误不影响新登录、同登录更新，以及原注册恢复/聊天重绑定。四个相关文件静态检查无问题。本节点只更新代码并推送，尚未替换手机上 02:02:24 安装的相册修复包；避免打断用户正在进行的视频发送验收。

## 视频转发接入设计

修复视频消息走文字分支、只转发“[视频]”的问题。沿用好友/群聊选择与确认页，确认后读取原消息当前视频凭据，校验缓存文件大小及 SHA256，再上传为当前会员的独立文件资产并调用已有 669 处理，得到新视频资产后写入目标会话 outbox。发送由现有 665/666 队列处理，失败保持 clientMessageId；不得在目标队列保存原消息凭据或直接发送别人拥有的资产。退出登录中断准备；准备重试复用已上传源资产和处理结果。此路径会重新规范化视频，尚未实现服务端无损资产复制，实际手机转发待验收。

视频转发代码和 17 项相关测试完成：来源单聊/群聊、摘要损坏不上传、退出后迟到凭据不上传、处理失败复用源资产、确认页写盘失败保留 clientMessageId，并验证进入真实视频发送接口而非文字接口。当前未打包安装，未计为手机转发验收通过；H.265 保留与回退尚未实现。


## HEVC 优先播放及原编码转发（2026-09-15）

667/668 请求可选 preferHevc；完整视频优先请求HEVC，缩略图不改变。ChatVideoGrant将codec与私有槽位绑定，兼容旧响应缺省H.264；不接受未知编码或错配路径。播放前仍校验文件长度、SHA256及重新授权；解码HEVC失败仅回退一次H.264，授权/缓存错误不触发格式回退。转发优先取得有权访问的HEVC源版本，重新上传为发送者自己的独立资产；没有HEVC时自然使用H.264。

10项授权、回退、转发及播放器测试通过，相关文件analyze通过。覆盖HEVC/H.264都解码失败时只尝试两次、不无限循环，以及撤权/会话变化阻止播放。后端video-v3及迁移098已部署，真实HTTP+FFmpeg验证通过。真机HEVC播放、音轨和异机兼容仍待验收；测试替身不算真机交付。当前手机ADB offline，尚未安装此次客户端更新。

本次真实API profile preview ARM64包构建成功，Gradle耗时69.5秒；APK SHA256：`b9239caab6e93629fca5d07cff29caaf99f1be764a52aa86b0e364f05514c018`。定向ADB重连后设备仍未就绪，安装待连接恢复。


## 上传前 Android 视频处理（实施中）

复用播放器所用Media3 1.9.2，接Transformer和effect。发送前在应用私有目录处理；原文件用于预览和重试，不覆盖。低于4MiB或源平均码率不超过2.2Mbps且边长不超过1280直接上传；HDR和无法安全识别的媒体交给现有服务端处理。其他SDR视频尝试H.264/AAC、最长边1280和30fps，只有结果更小、时长/音轨检查通过才替换上传输入；失败回原文件。处理在Media3后台完成，关闭页面取消，账号变化禁止后续上传。缓存按原文件内容摘要复用，成功入队后清理，失败保留以复用现有分块重试。真机速度/画质/音画同步须另行验收。

参考Android官方Transformer入门及自定义编码设置：
https://developer.android.com/media/media3/transformer/getting-started
https://developer.android.com/media/media3/transformer/customization


### 上传前处理实现进展

已接入ChatVideoSendPage及原生ChatVideoUpload：Media3异步处理、两分钟取消超时、SDR判断、4MiB/低码率跳过、1280边长和30fps、H.264 1.5Mbps/AAC 64kbps目标设置；实际码率可能由设备编码器调整。输出必须比原文件小、时长差不超过300ms、音轨存在状态一致。缓存账号+内容摘要隔离，原文件不覆盖，关闭页面取消，成功入队后清理派生缓存。源视频仍受既有64MiB与两分钟限制。未在手机上测得压缩比或耗时，不承诺画质无损。

13项Dart视频模型/优化分支/单聊群聊队列检查及相关analyze通过；Android真实API profile preview ARM64编译成功（本次新增依赖构建169.6秒）。设备恢复后adb install -r成功，保留账号数据，启动成功。已请求用户发送大于4MiB短视频核对实际压缩、画质和音画同步，尚未收到验收结果。APK SHA256：`b33d091b3de45f12c85195c3fcf09c7642bbafb89b4acac5c28a5c2fe4eed658`。


### 原生压缩进度

按当前处理任务ID每300ms读取Transformer.getProgress；只有原生返回有效0–100数值时显示百分比，不生成模拟进度。请求不重叠、百分比不倒退；处理完成/取消后停止轮询并忽略迟到响应，进入上传阶段清空压缩进度。8项优化器测试通过，新增覆盖无效/回退进度和结束后迟到回调；真实设备进度展示仍待验收。


真实用户反馈发送18MB视频后，服务端元数据确认已发送；上传源约18.27MiB，服务端版本约8.75MiB（约减少52%），音轨存在。这只证明服务端处理，不证明手机端压缩成功。源为符合尝试条件的SDR H.264；手机当时USB断开，无法核对原生日志，已询问是否更新手机发送及是否出现压缩提示。增加固定诊断标签KingclubVideoUpload，仅记录跳过/缓存/处理成败、大小/时长、错误码和异常类名，不记录账号、视频内容、路径或凭据；真机故障原因仍待确认。

进度及诊断版本：相关analyze与8项测试通过；真实API profile ARM64 APK编译成功（最后原生诊断增量11.8秒），SHA256 `22c20ac926394de968d88f0d2080ed7b86ba68bd2f0e718e903faa3ca43e2c4b`。用户反馈没有看到压缩提示；当前手机ADB offline，尚未安装此进度/诊断版本，不能认定故障已修复。


### 当前手机原生编码实测

重连后安装进度/诊断APK成功，安装时间核对先前手机确为上传前优化版本。新增 scripts/native-video-probe 独立无启动图标的instrumentation验证程序，直接反射调用已安装App的ChatVideoUpload；只使用合成视频，不调用网络或发送聊天消息。PCLM50实测合成29fps、720×1280、8秒有声视频8468630→2938329字节，耗时3651ms，返回COMPRESSED。原生输出检查包含时长及音轨存在；测试程序已卸载，App重新打开。此证据证明手机可以执行原生压缩，不证明普通发送页面已经成功压缩用户18MB样本；其故障仍待真实诊断日志。不得将有音轨等同于实际听到声音或音画同步验收。


### Android 12 码率修正与发送任务取消

第二次真实发送日志确认 PLAN skip=false、OUTPUT accepted=false：源19159706字节，原生结果19589133字节，保护规则回退原文件。该手机API31。Android MediaCodec官方文档说明Android12起VBR模式可因最低画质策略提高实际码率，CBR不受该规则影响；Media3 1.9.2源码确认默认VBR且显式设置会要求重编码。因此改为设备支持时优先CBR 1.5Mbps，不支持时保留VBR及实际大小检查。缓存标识加版本避免复用旧策略的派生文件。以合成8秒视频不超过2000000字节为设备预算验证条件，不能只看比8MB源文件小就算目标码率通过。

发送页增加会话事件取消：立即停止优化器和上传器；等待预览暂停、上传回执或处理回执后检查页面和登录是否仍有效；禁止迟到结果继续处理/入队。两项Widget真实异步边界测试及八项优化器测试通过，相关analyze通过。CBR版本真实API profile ARM64构建成功（69.6秒）并已覆盖安装，保留数据。独立原生验证程序安装仍等待手机确认，尚未得到CBR实测结果；不得标记上传流量优化已通过真机验收。随后补充发送结束时的会话有效性检查，避免迟到完成关闭已失效页面，该小改动尚未重新安装。

参考：https://developer.android.com/reference/android/media/MediaCodec
源码：https://github.com/androidx/media/blob/1.9.2/libraries/transformer/src/main/java/androidx/media3/transformer/DefaultEncoderFactory.java

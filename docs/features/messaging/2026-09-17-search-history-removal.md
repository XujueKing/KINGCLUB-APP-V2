# 搜索结果跟随本地历史清空

搜索页此前监听单条 ChatMediaDeletion 和实时网络通知，但未监听同账号历史库的 clearedConversations。远端搜索结果未下载为本地消息时，清空历史不一定产生这些消息的逐条删除事件，导致当前结果或正在查询的旧响应可能继续显示。

现绑定同账号历史库，按 localConversation 匹配删除事件。整段清空立即清除结果、输入及类型筛选，并递增查询代次拒绝迟到响应；用户可以重新发起查询。部分删除保留序号屏蔽集合和 hiddenThrough 边界，后续查询也不再展示对应结果。无关会话不受影响，页面退出取消订阅；等待历史监听建立后才开始查询。

16 项搜索页、本地 SQLite 搜索、取消扫描和历史删除事件测试通过，两个文件静态检查通过。新增单聊/群聊组件测试覆盖未落盘结果、无实时通知的整段清空、迟到响应、再次查询、部分删除和退出后的事件；不是手机实测。未改变待发队列策略。本节点尚未构建或安装 A/B。

## A 安装及搜索回归

预览分支合入 f7e0edb 为 378bd17，再由 b7ca1d5 移除该分支不存在的媒体类型筛选重置语句，保持尚未上线的媒体分类入口不进入预览。该分支 6 项搜索测试和两文件静态检查通过。Profile/LAN 构建 65.1 秒、159.4 MB，覆盖安装 A（462606d8）成功，启动助手第二次确认稳定前台。

安装前 A 在群邀请空列表，没有输入内容。安装后从消息列表进入授权 A/B 单聊、详情和查找聊天内容，搜索 KINGCLUB-PERF，实际出现既有 KINGCLUB-PERF-20260917-01 测试消息且无搜索失败提示。UI 证据私存 build/kc-search-result.xml。未发送/删除消息，未操作 B；本次只证明安装及真实搜索回归，不将自动化清空边界算作手机清空验收。

## 历史上下文同步清除

继续检查发现上下文页同样缺少本地整段历史删除监听。现订阅同账号历史库并匹配会话：整段清空或目标序号被删除后，清空页面内容、停止语音、禁用旧目标重试；迟到读取不能恢复目标。部分删除屏蔽对应序号和隐藏边界。对已展示但尚未落盘的消息发出媒体失效通知，使从该页面打开的媒体预览也能收到删除事件。

19 项上下文刷新、离线及媒体组件测试通过，三个文件静态分析通过。新增覆盖单聊/群聊清空正在播放的本地语音、无逐条消息事件时取消未完成上下文读取、无关会话隔离及页面退出；原单条删除测试继续保留。未把受控音轨输出或组件测试称为手机实测。此追加上下文修复尚未安装，A 仍为 b7ca1d5。

### 上下文修复安装回归

预览 dc6b5ec 已覆盖安装 A：Profile 构建 65.7 秒、159.4 MB，启动助手第二次验证前台。安装前仅保留上一轮测试搜索词，未发现新输入。安装后重新打开 A/B 单聊的搜索页面，查询 KINGCLUB-PERF 并点击既有测试结果，真实进入消息上下文、显示目标 KINGCLUB-PERF-20260917-01，没有读取失败提示。UI 证据私存 build/kc-context-installed.xml。

本轮未发送或删除历史、未操作 B。证明追加修复已安装及正常读取路径可用，清空边界仍以本节点自动化测试为证据，不声称本轮完成手机清空验收。

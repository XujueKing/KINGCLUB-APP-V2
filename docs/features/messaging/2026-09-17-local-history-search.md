# 本地聊天记录检索

搜索页网络失败时回退到当前账号、当前单聊或群聊的加密本地记录；权限和登录错误不回退。界面明确提示仅搜索本机已保存记录，不能表示服务端完整历史。回退后继续向前分页使用同一本地来源。

正文及文件名支持不区分大小写的子串检索；附件类型筛选复用本地检索。每批解密最多 50 行，按序号游标扫描，以匹配结果分页，不建立明文索引。事务保证删除期间读取一致性，忽略 stale、hidden、recalled 内容。文件结果显示文件名。

验证：单聊、群聊分别在真实 SQLite 文件中写入 125 条记录，验证跨扫描批次分页、大小写、文件名、类型过滤、其他会话隔离，以及删除后关闭重开数据库和 stale 排除。搜索页既有交互回归另行运行。

限制：尚未安装到手机做断网搜索验收。联网搜索仍以服务端契约为准，本次不改变服务端文件名搜索。未缓存到本机的历史不会被本地搜索找到。

## 离线上下文

单聊、群聊搜索结果及会话中的引用消息入口均接入本地上下文：服务端读取发生 NETWORK_ERROR 时，整段回退本地事务读取，不拼接两个来源。读取目标前后各最多 20 条，加目标最多 41 条；校验序号和消息 ID 同时匹配，已删除、stale、已撤回目标不可打开。界面显示本机记录提示。权限或会话错误不允许回退。

15 项上下文相关测试通过：真实 SQLite 有界窗口和删除后重启不可打开，组件网络错误回退、权限错误不回退、退出登录屏蔽迟到本地结果，以及既有媒体和删除刷新回归。尚未完成手机断网端到端验收，不能将组件测试等同于实机交付。

## A 机实测补充

2026-09-17：预览分支 713d760 已以 profile 模式打包并覆盖安装到 A，159.4 MB，构建 78.3 秒。启动后三次前台采样通过。预览分支只移植本次离线功能，未引入依赖未部署服务端的媒体分类搜索。

- 单聊：开启飞行模式并关闭 Wi-Fi，`dumpsys connectivity` 确认 `Active default network: none`。搜索 KINGCLUB，出现本机搜索提示和已有项目测试消息 KINGCLUB-PERF-20260917-01；点击该结果，上下文显示目标消息及本机记录提示。操作结束再次确认网络 none。
- 群聊：相同断网条件，搜索 OFFLINE-GROUP，找到已有测试消息 OFFLINE-GROUP-RESTART-0916-0600，点击进入本地上下文成功，结束时网络仍为 none。
- 两次操作均在 finally 恢复网络，最终 airplane_mode_on=0、wifi_on=1。B 未操作，没有新发消息或删除真实记录。
- UI 证据保存在本机忽略目录 build/kc-offline-search.xml、kc-offline-context.xml、kc-offline-group-search.xml、kc-offline-group-context.xml，不提交私人历史内容。

本次证明已保存文本在单聊和群聊均能离线搜索并打开上下文；文件名检索、删除后重启和分页已有自动化覆盖，但尚未做对应手机操作。大规模历史检索耗时、离线引用跳转及本次上下文媒体播放仍待单独验收。

## 取消无效扫描

搜索页更换关键词、离开前台、退出页面或登录状态失效时，本地搜索通过 generation / mounted / foreground / invalid 检查停止后续扫描，抛出取消并释放数据库事务。原先仅屏蔽 UI 结果但仍扫描全部记录，可能延后同库的新消息写入。

真实 SQLite 测试写入 2000 条加密记录，扫描开始后排入新消息写入并取消旧搜索，验证旧搜索在不足 30 次活跃检查内退出、新消息成功落库并可被下一次搜索找到。含分页、删除和搜索页回归共 10 项测试通过，静态检查通过。本次取消优化尚未安装到 A，不将桌面 SQLite 测试称为手机性能测量。有效搜索仍需要扫描匹配记录，大数据量的索引与性能优化尚未完成。

## 上下文语音本地资源修复

A 机预览 713d760 实测复现：群聊上下文中点击已缓存的 3 秒语音，在系统网络 none 时提示“网络连接失败，请联网后点击语音重试”。检查发现上下文入口未传 voiceAssetId，导致播放器跳过本地缓存分支。现将资源编号和 conversationId 传给现有播放器，保持删除与会话事件约束。

31 项相关测试及静态检查通过，新增组件覆盖单聊/群聊离线上下文点击本地语音不请求服务器，并在删除目标后停止播放。修复实机复测待补充。

### A 机修复复测

预览 26f1e46 已安装 A，profile 构建 64.7 秒、159.4 MB；包含搜索取消优化。通过群聊离线搜索进入上下文，在网络 none 下点击同一条已保存的 3 秒语音，UI 出现“点击停止”，无网络连接失败提示。前后 media.metrics 差异出现本次 AAC decoder（24 kHz、单声道）、AudioTrack 音频输出区间约 2.68 秒、underrun=0。证明本机离线读取并启动音频输出，不代表用户主观听感验收。测试结束再次确认网络 none 后恢复 airplane_mode_on=0、wifi_on=1，B 未操作。

私有证据：build/kc-cv-playing.xml、kc-cv-finished.xml、kc-cv-metrics-before.txt、kc-cv-metrics-finished.txt。单聊上下文同一路径已组件测试，但本次实机仅群聊；视频、图片上下文的离线验收继续待办。

## 上下文图片/视频发送源缓存

上下文的图片缩略图、视频封面和两者全屏入口现在将本人发送消息的 clientMessageId 传给既有媒体组件，与主会话一致；收到的消息不会带入发送源编号。此前上下文只传服务器 messageId，只有发送源缓存、尚无消息别名缓存时可能离线不可用。

25 项上下文媒体/图片/视频测试及静态检查通过。新增测试仅验证本人/对方及缩略图/全屏四种接线，真实本地存储读取由既有媒体组件测试覆盖；未把接线测试当成真机播放。此修复尚未装机，A 仍为 26f1e46。

### A 图片与视频上下文实测

预览 480e543 已覆盖安装 A，profile 构建 65.2 秒、159.4 MB，前台启动验证通过。B 未操作。

- 群聊上下文的既有 8 秒项目测试视频：断网后点击全屏，截图显示测试色条及运动图案，内嵌时间约 7.93 秒、进度条到末尾；media.metrics 出现对应应用的视频/音频解码及音轨记录。打开前和结束后均确认 Active default network: none。
- 单聊上下文的既有 wine_flip 测试图片：断网打开全屏，截图显示原图双箭头，未出现加载失败；打开前后网络均为 none。
- 两次 finally 恢复后 airplane_mode_on=0、wifi_on=1；未发送或删除消息。
- 证据在忽略目录 build/kc-context-video.png、kc-context-video-before.txt、kc-context-video-after.txt、kc-context-image-full.png 及对应 UI XML。

本次证明上下文入口离线可打开已有图片和视频；不能仅凭界面区分读取了发送源缓存还是已存在的消息缓存别名，也不代表全部媒体格式、超大文件、主观音质和 B 端验收完成。

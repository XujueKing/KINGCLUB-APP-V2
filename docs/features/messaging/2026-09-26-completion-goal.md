# 聊天完整交付目标（进行中）

用户于本轮明确要求开启目标模式，包含此前后置的系统离线通知与锁屏来电。以下是交付门槛，不以某次测试通过代替整个模块完成。2026-09-26 当前工作以 main 294a5e71 为已提交基线。

| 范围 | 当前证据与不足 | 完成门槛 |
| --- | --- | --- |
| 文字、好友、资料、设置、群管理 | 已有实现和分批测试，需对现代码核对完整功能 | A/B 申请接受、关系权限、消息收发、群角色及设置与重启持久化集中验收 |
| 双人音视频 | 用户已确认双向画面和声音；故障恢复仍需覆盖 | 后台返回、切网、对端挂断、权限撤销正确收尾 |
| 群通话 | 双向语音、A 锁屏持续语音及新版双端近同时主动退出已实测；双机视频实际画面待确认；5 秒停 Wi-Fi 后提前结束已复现，恢复窗口修正正在构建 | 双机群通话及后台保持、离开和错误清理验证 |
| 文件与本地媒体 | 8MB 离线读取及 32MB HTTP 续传已验；peer 完整块转 HTTP 仅本地测试 | 媒体发送播放、跨通道续传、删除与共享引用保留集中验证 |
| 直连和自适应 | 当前 A/B 跨网 ICE 请求无响应，中继/HTTP 已有证据 | 当前网络定位结论、可直连网络成功证据及不可直连时透明回退、重连、去重 |
| 系统通知及来电 | OPPO SDK 已接入；A 正式包注册绑定及进程重开后再绑定通过；普通通知服务端已部署但发送开关关闭，待服务端凭据 | 实际送达、点击后鉴权、去重、锁屏及进程回收后验证 |
| UI/稳定性 | 历史分页和动画已有修正，未完成整体最终验收 | A/B 字号适配、进入底部稳定、键盘/面板/消息动画、长记录及离线恢复验收 |

## 最初推送接入调查（历史，后续进展见下方）

- A、B USB 当前在线；第三台设备不在授权测试范围。
- App 搜索未发现 Firebase/HMS/Heytap/token 注册实现，仅 CallForegroundService 通知通道。
- 服务端聊天已有 durable outbox / websocket 路径；尚不能据此宣称厂商推送已具备。
- 用户授权操作 OPPO 官方开发者中心查文档与配置；已打开官方登录页，由用户输入登录凭据。尚未读取到应用配置或取得推送权限。
- Android 全屏来电存在平台权限限制，必须实现无全屏权限时的通知退化，不能承诺所有锁屏状态强制弹窗。参考 https://developer.android.com/about/versions/14/behavior-changes-14 和 https://source.android.com/docs/core/permissions/fsi-limits 。
- OPPO 历史官方 SDK 文档说明需要平台开通并获取应用配置；后续以登录后当前文档及实际应用状态确认 SDK 和权限，不直接照搬旧版 2.1.0：https://open.oppomobile.com/bbs/forum.php?mod=viewthread&tid=1918 。

不在代码、日志或 Git 中保存推送密钥。通知只做失效提示和路由线索；内容/来电状态在当前有效会话下重新鉴权，过期邀请不能启动录音。服务器发送密钥不下发 App。不得为测试修改真实注册评分、账号或其他开发会话文件。

## 2026-09-27 双机复测增量

- B 已完成正式包登录，不能继续按旧记录视为待登录。群语音和锁屏用户确认、双端主动退出服务端证据见 `2026-09-27-group-call-ab-acceptance.md`。
- 新增 B Wi-Fi 暂停 5 秒再恢复的实机观察：通话提前结束、显示网络连接失败；已恢复 B Wi-Fi，退出测试通话，服务端群占用数为 0。
- 媒体列表读取曾在 8 秒结束通话、中继凭据读取仅重试约 2 秒，与 ICE 的 25 秒恢复上限不一致。现把媒体列表失败容忍统一为 25 秒，已建立通话的中继读取每 2 秒最多重试 11 次。仅重试 NETWORK_ERROR 的只读请求，不重试鉴权失败或发布/消费写操作；关闭通话会取消等待。43 项针对性测试及修改文件 analyze 通过，真实短断网恢复仍待新包实测。
- OPPO 控制台当前已失去登录，已打开登录页等待用户登录；私有目录仍只有客户端配置，服务端 MasterSecret 未配置。未把普通通知送达或锁屏新来电记为完成。

## 2026-09-27 状态更新

- 用户指定既有普通通知通道，不申请 IM 分类。客户端配置已私有保存，MasterSecret 仍待安全转存，不能把设备注册当作离线通知送达。
- A 正式包已登录，通知设置关闭/开启与回前台刷新验证通过。B 正式包仍待用户完成首次协议和登录，旧预览版的登录不自动迁移。
- 群媒体编码修正、缺库 APK 重建与启动恢复见 `2026-09-27-group-encoding-compatibility.md`；失败席位释放见 `2026-09-27-group-failure-seat-release.md`。群通话连接、后台保持均未标验收通过。
- 直连、媒体、UI 的原有验收范围不因本轮推送注册或构建通过而扩大。目标保持进行中。

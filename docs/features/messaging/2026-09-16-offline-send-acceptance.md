# A 离线发送、重启与自动补交

使用包含 6f6ba95 的 A Profile 包，在已授权 A/B 会话发送一条测试文字 `OFFLINE-RESTART-A-0916-0600`。本次未操作 B。

1. 关闭 A 网络后点击发送，界面出现该文字并标记“等待网络恢复…”，没有成功送达标记。
2. 保持断网，force-stop 后重新启动 App，打开同一会话，该消息仍存在并保持等待状态，证明真实本机队列跨进程保留。
3. 恢复 Wi-Fi 和移动数据，Android 默认网络恢复。未点重试，消息自动从等待状态变成普通历史行，时间从本地 05:57 更新为服务端确认的 05:58。
4. 从聊天详情进入服务端历史搜索，完整搜索测试文字，返回一条“我 · 今天 05:58”记录，没有重复结果。
5. 测试结束 Wi-Fi 和移动数据设置均为 1，与测试前一致。

本次证明 A 离线排队、断网冷重启恢复、联网自动提交及当前服务端搜索无重复。未确认 B 实际接收/声音通知，未覆盖提交已成功但回执丢失的另一种幂等场景，也不代表群聊离线发送验收。

私有界面记录位于 build/phone-a-outbox-*.xml，不提交仓库。本节点无源代码变动。

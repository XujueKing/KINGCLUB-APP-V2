# Chat avatar navigation

Verify group sender identity is used for profile navigation rather than the group ID or a direct-peer fallback. Received message avatars already navigate correctly. A widget check exposed missing tap wiring for outgoing avatars. Connect the same sender-specific callback to the outgoing avatar without changing layout. Verify received group messages and locally restored queued outgoing messages with injected authenticated repository responses; this is UI/controller evidence, not live member-profile permission acceptance.

Implemented outgoing avatar GestureDetector using the existing callback. Ten tests pass across group-chat avatar identity/navigation, group applicants and direct-detail avatar reuse. Changed-file analysis passes. The group fixture covers a received member message and an outgoing queue restored while history requests fail offline; both navigate to the sender account using the same authenticated repository, never to the group ID. Self-profile hides relationship actions through the existing PublicMemberPage account check. No device installation in this node. Logs: build/group-avatar-navigation.log and build/group-avatar-navigation-analyze.log.

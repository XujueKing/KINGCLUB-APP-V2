# A 群设置与历史文件真机验收

在已有 A/B 测试群 `KINGCLUB-AB-0915-OK` 操作，使用包含 bfea129 的 Profile 包。A 为普通成员，资料页显示两位成员，其中 B 标为群主；本次没有操作群成员增删或群主权限。

- 初始免打扰和置顶均关闭。依次开启后退出资料页并重新进入，Android Switch 均为 checked=true。
- 返回聊天列表，测试群进入置顶区域，行内显示“消息免打扰”图标。未触发新收件，因此不把通知抑制本身算作验收。
- 返回设置关闭两个开关，重新读取界面均为 checked=false，恢复原状态。
- 群历史搜索 `OFFLINE-GROUP` 返回实际历史 `OFFLINE-GROUP-A-1640`，发送人显示“我”。点击结果进入上下文，目标文字及前后记录可见。
- 在上下文打开既有 68.0 KB 群测试文件，点击下载后显示“下载完成，文件校验通过”。没有重复导出到公共目录，也没有重发文件。

本次为 A 实际界面与真实服务验收，无源代码变动；私有 UI 证据在 build/phone-a-group-*，不提交。B 未操作，群接收端下载、管理权限变化和群视频实际声音仍待验收。

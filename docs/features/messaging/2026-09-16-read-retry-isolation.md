# 已读补交异常确认隔离

现有补交按单聊、群聊顺序处理。服务端确认字段不合法时抛出 FormatException，被通用异常分支捕获后直接结束整轮，可能使后面的正常会话长期无法清除未读。

对格式错误的确认保留原已读意图，对该会话及当前序号采用现有五分钟冷却，继续处理后续会话。新序号或用户再次明确阅读仍绕过冷却。网络及登录状态错误仍停止本轮，不把异常确认当成成功，不发成功刷新事件。

验证覆盖单聊和群聊的异常首条、正常后续条目、冷却期间不重复请求及服务恢复后的补交。此为受控接口测试，不等于双机真实未读验收。

结果：已读确认、群已读队列、单聊已读队列及并发阅读共 29 项测试通过，两文件静态分析通过。

2026-09-16 06:10：包含 3324ae0 的 Profile/LAN ARM64 包构建成功，Gradle 74.4 秒、150.6 MB，SHA256 `0A9890F73A5DA97A74947D711B57F5ECC9445860DCB231EA0B1CF66CFF87A3D5`。覆盖安装 A 成功，启动 Status ok；聊天列表实际保留单聊 05:58 和群聊 06:01 的离线补发记录。B 未操作。本次仅验证安装及正常入口，不将其计为异常服务端回执的真机验收。构建包含工作区既有三处 onboarding 改动，未将它们纳入本节点提交。

# A/B 正式包群通话验收

两端使用正式包名 com.lingmei.kingclub 的 profile APK，SHA256 `0CA6848DC52BB55E3E005BECD89DC59AC3703735217BCF558BF4FEFC7223B9BC`。B 用户完成登录后，实际 UI 为正式包首页。

在既有 A/B 测试群发起群语音，B 收到邀请并接听，首次麦克风权限选择使用时允许。A/B 均显示双方“已加入”并持续计时，服务端两个成员 phase=joined、占用数=2、通话未结束。两端 CallForegroundService 均为前台服务。用户明确确认双方都能听到声音。

随后 A 退桌面并锁屏，dumpsys power 为 Dozing，前台通话服务仍有效。锁屏后的实际声音和恢复、双机群视频、结束清理尚待本轮后续确认，不能由前台语音通过推定。

后续用户确认 A 锁屏后双方仍能听到。唤醒 A 返回通话，计时为 02:25，双方仍已加入。两端近同时返回退出后短暂观察到一个群席位占用，随后归零；最终 participant 为一个 left、一个 expired，说明没有证明双方都及时主动退出。

代码核查发现 hangUp 遇到版本冲突时直接关闭控制器，无法执行 controller 已刷新状态后的下一次 leave。现仅针对明确 CHAT_GROUP_CALL_VERSION_CONFLICT 最多尝试三次退出，其他网络或权限错误不重试，不重试加入或采集。新增覆盖一次冲突后使用新 requestId 成功退出、持续冲突有限终止；会话及控制器共 15 项测试通过，变更文件 analyze 通过。此修正尚未打包安装，不将上述实机残留直接归因为已证实的版本冲突。

同版双机群视频邀请和接听成功，B 首次摄像头授权选择使用时允许；两端已持续计时超过两分钟、双方均已加入。实际双向画面与声音待用户确认，当前没有替用户认定。

等待确认后结束该视频测试，避免持续占用摄像头。两端按顺序返回退出后，前台通话服务均消失；服务端两个成员都是 left，群占用数为 0。用户尚未确认视频实际画面和声音，因此只登记连接、存活及顺序退出证据。

并发退出增量首次构建遇到 Java PipeImpl / UnixDomainSockets 的 Invalid argument: connect，发生于 Gradle 客户端连接守护进程。停止守护进程后仍复现；仅对本次构建进程设置短临时目录和 jdk.net.unixdomain.tmpdir，Gradle help 成功。未修改系统网络、全局环境或其他会话设置，APK 随后重新构建。

增量 profile 包构建成功（Gradle 233.0 秒），核心库检查通过，SHA256 `5D0FD13B313E257641B8DAC46FC0259AB2C62D20FBCB41C92D72E4F45237D127`。A/B 均覆盖安装成功并重新进入已登录首页，无清数据或卸载。新版并发退出尚待双机复测，不能将此前顺序退出证据记到这一修正上。

本记录不代表系统离线推送、锁屏新来电唤醒或跨网纯直连已通过；本次是已建立群媒体通话的验收。

新版并发退出复测：前一轮邀请已超时，先正常退出 A 留存的通话，再重新发起群语音并由 B 接听。B 显示 00:01、两位成员均已加入。通过两条并行 ADB 命令让 A/B 近同时点击返回；随后第一次服务端查询即得到 mediaKind=audio、version=8、ended=1、participant left=2、occupied=0。两端 dumpsys activity services 均未发现 CallForegroundService。本次验证了新版的一次近同时主动退出，不是等待 expired 超时清理；没有采集到冲突错误，因此不能声称本轮一定触发了重试分支，重试分支由此前针对性测试覆盖。

短断网复测：先前包在 B Wi-Fi 禁用 5 秒、随后重新启用后显示“通话已结束 / 网络连接失败，请稍后重试”。恢复 Wi-Fi 并退出两端，服务端占用归零。针对读取恢复窗口不一致的修正 e0f6463b 已构建 profile 包（91.2 秒，SHA256 AA6C0241443FBF4FD7B5EE80E84C834445D4F8D59BDCE98BC121AE5A9E86634F），A/B 覆盖安装成功，登录保持。

新版重建群语音后重复上述 B Wi-Fi 暂停 5 秒再恢复：B 显示 00:38、双方已加入；随后 A 显示 01:22、双方仍已加入。服务端最新通话 audio、version=16、ended=0、joined=2、occupied=2。未重新发出邀请。已请求用户确认实际双向声音是否恢复，不能把计时或 joined 状态等同于音频已验收；当前暂保留测试通话等待确认。

后续未收到声音确认，转做有限断网退出验证。B Wi-Fi 禁用 35 秒时，页面显示通话结束及网络失败，CallForegroundService 已不存在；finally 恢复 Wi-Fi，系统值 wifi_on=1。服务端随后 joined=1、expired=1、occupied=1，A 页面正常显示自己已加入、B 已超时。正常退出 A 并返回 B 后，服务端 left=1、expired=1、occupied=0。此次是服务端租期回收离线 B，不是 B 离线主动 leave 成功。原短断网声音确认仍未完成；测试通话已经结束。

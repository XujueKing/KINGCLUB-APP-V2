# 扫码好友与聊天头像修正（2026-09-14）

用户确认扫码申请已通过，未主动取消关注。共享测试环境核实：申请为 accepted，随后出现 relationship.changed，当前关系为单向。不能据此认定用户主动取消，也未证明事件的操作来源。

已对唯一匹配的已通过、未拉黑会话留存服务器私有备份，事务恢复互相关注并写双方 outbox 通知。恢复后直接调用双方通讯录服务，各返回 1 位好友。未批量修改其他关系、消息或余额。真实账号及备份内容不进仓库。

客户端聊天列表原先仍固定显示 touxiang.png。改为按真实 peer 读取授权资料并复用 ChatMemberAvatar，头像请求按当前登录会话复用；登录更新时清空旧请求。群聊使用群图标，不冒用个人头像。头像圆形尺寸与未读标记位置保持既有布局。

资料页“互相关注 / 已关注”原先直接调用 unfollow。现在先显示明确确认，取消或关闭弹窗不修改关注关系；明确确认后才提交。

录音排查：最近两条真实语音文件可授权读取且可解码，但 ffmpeg 测得最大与平均音量均为 -91 dB，属于静音内容。播放无声尚未修复，需继续检查采集端。当前 USB 设备断开，尚不能声称真机验收通过。

验证结果以本次定向 Flutter 测试及 analyze 日志为准；安装与用户端头像显示仍需真机验收。

客户端定向测试：public_member_page_test、chat_list_session_rebind_test、group_conversation_list_test 共 6 项通过；两处页面 analyze 通过。尚未安装本批改动。

安装更新：2026-09-14 21:51 覆盖安装 ARM64 Profile 成功并启动，包含 d2ae1ee 头像/关注确认、45d93d3 会话续期恢复、0a7c6d3 录音权限检测。未清除应用数据。设备权限已为 granted，AppOps 为 foreground/allow；新录音及头像显示等待用户验收，不用旧静音文件判定播放修复。

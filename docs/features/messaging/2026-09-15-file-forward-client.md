# 文件转发客户端

状态：客户端代码与自动测试完成，已安装此变更，真实 HTTP 转发及双机验收未完成。

复用已授权的文件下载接口与分块加密上传。源单聊/群聊文件下载后比对大小与 SHA-256，上传为转发者独立拥有的附件；通过现有好友/群聊选择页面确认后写入持久发送队列。群目标先读取成员版本，目的会话继续负责发送与重试。临时明文在上传读取结束后清理；会话失效立即取消传输。存储重试复用已准备附件及同一消息编号。

验证：原文件/上传文件摘要不一致拒绝；会话失效取消与临时文件清理；确认前不上传；队列落盘失败后重试不创建新编号；真实单聊控制器消费 fileAssetId 并调用文件发送接口。文件下载、上传、单聊/群聊队列相关 35 项测试通过；新增页面文件场景与转发辅助测试通过；5 个相关文件静态分析无问题。

仍需：真实服务端跨单聊/群聊转发，原消息撤回后的副本独立性，手机操作与播放/保存验收。不得据此声称完整聊天交付。

## 安装记录

代码 ad21d54，Profile / preview / android-arm64，构建 61.5 秒、162.0 MB。2026-09-15 00:10:56 覆盖安装到已授权连接设备，ADB 返回 Success，启动 Activity 成功。APK SHA-256：`59EB04DFF4708C0EDF07FF72A10A989763FCFBF2430DCC8024FF18B80997916B`。保留应用数据。手机仍休眠，安装不代表真实操作验收。

# 聊天记录搜索

复用 604/621 历史接口的可选 query；独立页面查询服务端可见文字记录，每页 30 条，结果按最新优先展示，加载更早结果使用最小 sequence。结果不写入普通消息历史或补收游标，不标记已读。清空、关系变化、群成员变更、重连时重新查询；退后台或登录变化立即清空显示，迟到请求不得恢复旧结果。保持黑底、现有返回箭头及右入左出的原生页面切换。

本批实现关键词查询和结果翻页；点击结果定位到原会话上下文仍待补齐。不提供 OCR、语音转写搜索。测试服务当前 087，088 上线前不计可用交付。

验证更新：单聊与群详情均接独立搜索页，2 项组件测试通过，覆盖服务端分页、权限变化清空、关键词变化拒绝迟到响应、会话失效。6 文件 analyze 无问题。后端真实隔离 MySQL/HTTP 搜索验证通过；088 尚未共享部署，客户端未打包安装。结果定位原消息仍未完成。

定位实现中：通过独立历史读取器按 sequence 获取原消息前 20 条及后 20 条，不修改普通会话游标。第二次读取的 hiddenThrough/joinedSequence 过滤第一次结果；成员代次变化、目标 ID 不匹配或撤权拒绝定位。窗口展示与原会话定位入口仍待接入。

窗口接入更新：点击搜索结果进入原生上下文页面，自动滚动至原消息并高亮，展示前后各约 20 条，文字可复制。保持搜索/普通会话游标分离，退后台或账号变化清空，权限事件重新读取。当前为独立只读窗口，尚未支持窗口内媒体播放、继续翻页及直接回复。5 项搜索与上下文测试通过，6 文件 analyze 无问题；测试服务 088 已部署，APK 构建中。

装机更新：preview debug ARM64 编译成功（237.1 秒），adb install -r 返回 Success；保留原应用数据。测试服务为 chat-088。搜索/上下文手机交互尚待实际账号验收，未以安装成功代替验收。

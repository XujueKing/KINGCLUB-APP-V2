# 群成员禁言接入

沿用群成员管理菜单，群主和管理员可对普通成员禁言/解除禁言；通过627提交群资料版本和成员代次，确认后刷新。只有619返回sendMuted布尔值的新服务才显示新增动作，旧服务不出现不可用入口。

621返回发送权限，禁言后保留历史、禁用输入与附件，并显示原因；发送控制器同时阻止新消息进入队列，暂停已排队消息重试。服务端迟到的禁言拒绝也更新本地限制。同步解除后恢复发送。此功能需要后端122，目前未上线，不能称手机已可用。

控制器12项、成员管理5项、发送滚动1项测试通过，定向analyze无问题。新增覆盖：禁言保留历史/不入发送队列、解除恢复、群主和管理员的禁言/解除确认、普通成员无管理入口。音视频通话麦克风静音不属于本项。后端122尚未上线，客户端尚未安装；完整双机通知和禁言期间离线队列恢复仍待实测。

离线队列补充：群变更通知刷新权限后重试queued消息，禁言期间暂停，解除后沿用原clientMessageId发送；重复群变更刷新不重复发送。已经被服务端拒绝并标为failed的消息仍由用户明确重试，不自动改成queued。群控制器与前台恢复合计19项测试通过；上述行为未以手机通知实测。

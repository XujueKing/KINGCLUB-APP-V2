# 群邀请状态变化保护

群邀请确认对话框期间，列表可能因实时事件或重连刷新。旧确认现在绑定打开时的列表代次；发生刷新后不再提交旧邀请操作。后台不请求列表或响应操作，返回前台重新读取，迟到的旧请求不会打开群聊或覆盖当前结果。后台清空当前邀请展示，账号变化仍沿用原有失效逻辑。

保持现有邀请、加入/拒绝确认、查看群聊 UI。已发出的操作无法由前台取消，返回前台以服务端列表为准，不将客户端中断视作服务器撤销。

4 项组件测试通过：刷新导致旧确认失效、后台事件不发请求/恢复刷新、正常确认和真实回执契约、退出登录屏蔽迟到结果。静态检查通过。这里的组件服务返回由测试控制，不计为真实两机入群验收；本次未安装，A 仍为 480e543，B 未操作。

## 当前群事件隔离

群详情和入群审核页仅对当前 groupId 的明确变更刷新。明确属于其他群的 chat.group.changed 不刷新成员/头像、不清空审核状态。connection.ready 和没有明确群 ID 的变更仍刷新以重新校验权限，邀请收件箱是跨群列表，不应用此过滤。

11 项群详情与审核组件测试通过，静态检查通过。覆盖无关群事件不读取、当前群事件/重连/旧格式无 ID 通知仍读取，保留既有通知合并、权限撤销、成员头像及设置回执回归。本次尚未装机；大群成员加载完整性尚未据此验收。

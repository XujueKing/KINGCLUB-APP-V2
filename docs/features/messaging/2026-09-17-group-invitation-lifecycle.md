# 群邀请状态变化保护

群邀请确认对话框期间，列表可能因实时事件或重连刷新。旧确认现在绑定打开时的列表代次；发生刷新后不再提交旧邀请操作。后台不请求列表或响应操作，返回前台重新读取，迟到的旧请求不会打开群聊或覆盖当前结果。后台清空当前邀请展示，账号变化仍沿用原有失效逻辑。

保持现有邀请、加入/拒绝确认、查看群聊 UI。已发出的操作无法由前台取消，返回前台以服务端列表为准，不将客户端中断视作服务器撤销。

4 项组件测试通过：刷新导致旧确认失效、后台事件不发请求/恢复刷新、正常确认和真实回执契约、退出登录屏蔽迟到结果。静态检查通过。这里的组件服务返回由测试控制，不计为真实两机入群验收；本次未安装，A 仍为 480e543，B 未操作。

## 当前群事件隔离

群详情和入群审核页仅对当前 groupId 的明确变更刷新。明确属于其他群的 chat.group.changed 不刷新成员/头像、不清空审核状态。connection.ready 和没有明确群 ID 的变更仍刷新以重新校验权限，邀请收件箱是跨群列表，不应用此过滤。

11 项群详情与审核组件测试通过，静态检查通过。覆盖无关群事件不读取、当前群事件/重连/旧格式无 ID 通知仍读取，保留既有通知合并、权限撤销、成员头像及设置回执回归。本次尚未装机；大群成员加载完整性尚未据此验收。

## 200 人成员列表核验

核对服务端当前源码：chat-groups.ts details 查询全部 active 成员，不设截断 LIMIT；group-admission.ts 在 active.length>=200 时拒绝新成员。因此当前产品上限为 200，客户端无需分页补取成员才能搜索完整名单。此结论基于源码，不替代当前远端服务版本与真实容量验证。

新增 group_members_capacity_test.dart：注入 200 个成员，首次显示及定位末尾成员期间头像请求均少于 30；第 200 位可按账号 member-199 及大小写昵称 MEMBER 199 找到；当前群变更将其移除后，旧结果消失并显示未找到群成员。测试与静态检查通过，未新增实际会员/群，不将测试夹具称为真实 200 人群验收。

## 审核页通知合并

入群审核读取采用单请求加后续刷新队列。请求未结束时，同群通知只让旧代次失效并排一次刷新，不为每条通知重复请求；清除旧审核条目、停止审核按钮操作仍立即执行。分页检测到成员版本变化时排首页刷新，避免嵌套等待自己的在途请求。

新增 20 条通知突发测试：初始读取一次，在途刷新一次，通知合并后追加一次，共三次；旧页内容未显示，最终显示新页。连同现有同意/拒绝/权限变化和头像回归共 7 项通过，静态检查通过。未进行真实群批量入群压力测试，尚未装机。

## A 机安装与普通成员实测

预览 6fef362 已覆盖安装 A，profile 构建 65.6 秒、159.4 MB；启动前台验证通过。包含群邀请生命周期、群事件范围过滤和审核通知合并，未启用未部署的群禁言新增服务契约。

真实既有 A/B 测试群：资料页显示两位成员及群主标识；按 B 账号前缀检索只剩一位群主，搜索不存在的测试词显示“未找到群成员”，清除搜索后恢复两位。进入通讯录的群邀请，当前返回“暂无群邀请”；Home 切后台后再打开仍正常显示该空状态。未更改成员或权限，B 未操作。

本次只验证普通成员读取、搜索和邀请空列表恢复，不代表真实邀请加入/拒绝、群主转让、审核批量通知或 200 人真机验收。UI 证据保存在忽略目录 build/kc-member-filter.xml、kc-member-empty.xml、kc-member-restored.xml、kc-group-invitations-current.xml、kc-group-invitations-resumed.xml。

# 图片、录音上传凭证重取

文件/视频上传已有分块凭证重取，并通过 MessagingRepository 获得登录续期。本次补上图片与录音上传返回明确的 CHAT_IMAGE_UPLOAD_DENIED / CHAT_VOICE_UPLOAD_DENIED 后的单次重取：沿用原内容指纹和 clientUploadId，重新通过会员接口取得凭证，用新密钥/AAD 和新随机 nonce 加密。持续拒绝、其他错误或账号变化不会无限重试；只有真实 ready 回执才算上传完成。这里不改变发送消息时机或服务端权限。

整个操作保持 busy，输入字节在开始时复制一次，避免重试期间调用方改变内容，内部不重复复制。原有上传编号在消息持久排队前保留。

验证：四个上传测试文件共 22 项通过；最后的内存复制调整后，图片/录音/重取相关 12 项再通过。3 文件静态分析无问题。新增 4 项测试验证续签成功、持续拒绝有界退出、上传编号一致、新 nonce，以及两个尝试均可用各自凭证解密出原字节。测试使用传输替身，未宣称真机实际过期上传验收。

Profile/LAN 包已构建并安装 A，B 未更新。同次真机验收 A 对 B 的消息免打扰设置：开启、退出再进入后保持开启，随后恢复关闭；仅证明设置持久化，未验证 B 新消息通知抑制。

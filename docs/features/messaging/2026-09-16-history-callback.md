# 历史通话记录回拨

当前会话已有服务端通话元数据卡片及回拨，历史上下文仍只显示 text。为单聊历史上下文增加按真实 call 元数据展示的语音/视频入口，从搜索和引用入口传递当前会话的既有回拨动作。保留原呼叫互斥、权限、信令和媒体处理，不凭“语音通话”文字猜测类型；群上下文不接单人回拨。

点击先停止上下文语音，待呼叫返回前拒绝重复点击。账号已失效或页面不在前台不发起。组件测试验证媒体类型和重复点击，实际呼叫不因本次测试自动触发。

11 项历史回拨、现有会话回拨、上下文媒体及刷新测试通过，4 文件静态分析通过。语音和视频元数据分别传入对应回拨类型，重复点击只触发一次；群上下文不显示单聊回拨。未打包安装或拨打真实电话，本节点不能计作双机回拨验收。

# A 语音修复更新包

## 第五次增量更新

追加948c98f位置卡片完整点击区域和f4a925a本人新排队消息滚动修复，独立构建3afacf0；两个实现文件与主分支一致。Profile/preview ARM64构建82.4秒，159.2MB，SHA-256：47F7EA72C18C6E8A830881D004147C9092A4ED1640CC6ED393FDF1A98DC01915。

A保留数据覆盖安装Success，启动助手第二次确认稳定前台。原位置消息在重启后仍可见；实际点击卡片右下方空白处打开位置详情。回到A/B会话滚到旧记录，输入并发送SCROLL-CHECK-A-0916，未手动滚动就显示新消息，输入清空。未操作B，不把发送端可见当作接收端验证。详见location-handset.md和outgoing-scroll.md。

## 第四次增量更新

追加0dcb31b群媒体前台服务租约、9987ccc可选通话通知权限、3c87370语音草稿发送状态修复，独立构建提交fa40a24；对应实现与主分支逐文件对比无差异。合并时仅处理前台服务文档冲突，代码自动合并。

Profile/preview ARM64构建82.8秒，159.2MB，SHA-256：AC8E3CD00EBD2E27B71CB0B6EDDAED61A8D5B12217F949D7BECD6093B5B8D0FA。A覆盖安装Success，启动助手第二次确认稳定前台；实际UI读取确认原登录首页和原单聊/群会话列表。未操作B，未发送消息。A为Android12，因此不能用此次安装认定Android13以上通知授权已经实测；群SFU未上线，后台通话仍待双端验证。

## 第三次增量更新

追加e3fa3c1/e4a476d视频预览生命周期、47c6374通话前台服务及cac9f3e启动取消修复，独立构建提交ba8d880。对应原生服务、主Activity、Manifest和Dart实现与主分支无差异。

Profile/preview ARM64构建75.9秒，159.2MB，SHA-256：308ABD1046D6CCD900B82FDEF549E6B90843EEADE6B9F58838B5B38C5E71A7EF。A覆盖安装Success，启动助手第二次确认稳定前台，实际UI显示原登录首页。空闲时dumpsys仅有既有定位插件绑定服务，没有CallForegroundService；未操作B，未以空闲检查代替真实后台通话验收。

## 第二次增量更新

在上述独立构建工作区追加82cb323（转文字后台校验暂停）和2c39b55（通话实时通知刷新），构建提交c8271f6；与主分支对应三个实现文件无差异。Profile/preview ARM64构建87.3秒，APK159.2MB，原生库检查通过。

SHA-256：371E09821AA3633F87AC79DE620D96A5DD8E19269495E43E8B652001F955F3F8。

A覆盖安装Success，启动助手第一次确认稳定前台；实际UI读取确认原登录主页及已有单聊/群会话列表正常。B未操作。本次尚未验证两端关系变化期间的通话终止，也未把启动冒烟等同通话验收。

## 第一次增量更新

基于 A 原安装对应代码09cc535，在独立工作区加入 ce1073a、48f7dae、0dcfd26 三项语音修复（独立构建提交ef7283a）。保留当前三个注册偏好页面工作区文件，未修改主工作区或 B。未带入需要服务端121的媒体筛选，也未带入其他较新表情同步变更。

Profile/preview ARM64构建成功，Gradle148.9秒，APK142.5MB。真实API及原有NovoRUDP设备绑定、WSS固定身份/证书、文件传输及LAN参数沿用前包；原生库经过构建脚本ARM64检查。

APK SHA-256：85A427557AE03C8A8DF690904A63A70B951C83D3B8C1281875F3AEF3FBAA72CA。

A adb覆盖安装返回Success，启动助手第二次尝试确认稳定前台。实际UI读取确认已有登录主页，点击消息后正常显示现有单聊和群聊。未新增消息或修改关系。此记录只证明安装、启动及会话列表冒烟通过；不证明异步硬件错误、蓝牙、后台或语音听感已实测。

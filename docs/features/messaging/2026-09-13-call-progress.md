# 原生通话接入进度

完整目标仍包括真实音视频、来电/忙线/挂断/重连、后台与双机验收，本文件记录增量实现，不代表交付完成。

## Flutter 接口层

CallRepository 复用账号绑定的 MessagingRepository，接入 643 发起、644 操作、645 当前/指定通话、646 信令发送、647 游标补收。CallSnapshot 校验当前账号参与、媒体/阶段、版本和结束状态一致性；指定通话响应不允许串号。请求编号由通话操作持有，重试必须复用，不在每次网络调用时生成新编号。打开接口层不会自动拨号、接听或启动麦克风。

CallSignal 保留不可变请求字段，补收拒绝逆序/重复序号、跨通话/代次和无效游标；SDP/ICE 长度基本检查，完整信令语义仍交由服务端及原生 WebRTC。没有将服务器回执当作已建立媒体连接。

5 项客户端接口契约测试通过：失败后相同拨号请求重试、当前无通话及串账号拒绝、操作携带版本及回退拒绝、终态字段验证、信令不可变及分页游标检查。使用注入返回验证客户端逻辑，不能替代双机或真实 WebRTC；后端加密 HTTP/MySQL 验证见服务端 069 记录。

尚未完成：App 通话控制器和页面、WebRTC 插件、TURN 配置与直连回退、心跳/后台来电、共享测试部署及双机验收。原视频通话按钮仍未开放，未伪造成功状态。


## NativeCallMedia 原生媒体实现

固定 flutter_webrtc 1.6.2+hotfix.1（Android 底层 io.github.webrtc-sdk:android:150.7871.01），新增 NativeCallMedia。对象构造不采集；明确 open 后 getUserMedia，请求音频回声消除/降噪/自动增益，视频目标前摄 720p/24fps、最高 30fps；音频通话不请求视频。Unified Plan/addTrack、offer/answer、远端 SDP、最多 256 个等待 SDP 的 ICE 候选、静音、远端流和连接事件已封装。ICE 服务器由调用方注入，未硬编码公共 STUN 或长期 TURN 密钥。

close 等待正在打开的资源并停止全部媒体轨道、释放 stream、close/dispose PeerConnection；关闭后迟到采集结果释放而不建立连接。7 项媒体生命周期和接口测试通过，变更静态分析通过。测试注入模拟原生接口验证竞态与释放，不能代表麦克风/摄像头采集或双机连通已实测。Android 补 CAMERA/MODIFY_AUDIO_SETTINGS，摄像头硬件非必需；蓝牙路由和后台服务尚待专项接入。

官方参考：https://pub.dev/packages/flutter_webrtc 和 https://flutter-webrtc.org/docs/flutter-webrtc/api-docs/rtc-peerconnection/ 。目前未接通页面和控制器，TURN 未配置，实际降噪效果及前后台资源行为还需真机验证。


## 原生构建与信令协商衔接

WebRTC 首次 Android preview/profile arm64 构建通过：443.8 秒、147.2 MB，SHA256 `6fdccf96b7323ef61ca739c8b16998290188c8edd9f76a9d9b35fe79e6504440`。这是 e270d92 阶段的包，包含工作树原有 onboarding 三文件改动；不包含之后的好友资料布局和本节协商层。未安装，不能据此声称双机音视频连通。

新增 CallMediaSession，限定已接听 connecting 通话及当前参与账号；使用真实 CallRepository 和可注入的 NativeCallMedia。发送 SDP 前产生的 ICE 排队，确保 offer/answer 在前；出站信令使用固定 UUID，发送失败保留队首原内容供重试。入站按服务端序号应用，远端 offer 仅应用一次，answer 回执丢失不重复创建 answer。关闭时立即禁止后续信令并释放媒体，原生部分应用异常关闭本地采集。不同协商代次明确拒绝，尚未实现 ICE restart。

10 项接口/原生生命周期/协商测试通过，静态分析通过。新增三项覆盖同步产生 ICE 的发送顺序、信令失败编号复用、answer 回执丢失和采集未打开就关闭；这些为注入式竞态测试，不是真机音视频测试。Android 主清单增加 INTERNET，避免网络权限仅在 debug/profile 存在。

仍需将协商层接到通话页面与会话生命周期：服务端状态刷新、来电接听、连接状态上报、定时补收/重试、会话退出强制 close、TURN 短期凭据、ICE 重启、后台与双机实测。当前没有开放假通话入口，也没有把服务端信令回执当作媒体已接通。


## 2026-09-14 单次通话生命周期控制器

新增 CallStateController，接收刚创建的 ringing 通话及账号绑定接口；来电只有明确 accept 成功才授权本机媒体采集。主叫在服务端进入 connecting 后创建协商会话。watch 提供 2 秒串行刷新/信令补收，同步调用合并，原生 connected 事件触发版本化上报；不会将 signaling 成功当作 connected。版本冲突重新读取状态，网络回执不确定时保留原操作编号及 expectedVersion。

end 首先立即 close 本机媒体，再等待串行网络操作，避免网络阻塞占用麦克风。挂断前重新读阶段以应对接听/取消竞态；服务端失败保留结束意图供后续刷新重试。登录变更立即关闭，迟到刷新不创建媒体。原生协商已关闭时下轮刷新结束服务端通话；未支持的代次停止本机媒体而非继续使用旧连接。

新增注入式测试覆盖：来电刷新不采集、接听回执丢失重试编号/版本不变、主叫等待接听、挂断请求未完成先释放、退出后迟到状态不重启采集。与既有协商/接口/原生生命周期合计 13 项通过。控制器尚未接入 UI/来电发现器，后台来电、TURN、ICE restart 和双机真实连通继续待完成。


## 通话页面与真实控制器绑定

新增 CallPage，原生工厂接入 CallStateController/CallMediaSession/NativeCallMedia；来电提供接听/拒绝，主叫和已接听提供挂断/静音，使用服务端阶段与原生连接状态展示等待、连接中、通话中或恢复连接。没有伪造通话时长。视频使用本地/远端 RTCVideoRenderer，页面结束关闭控制器并释放渲染资源；返回先结束通话，失败留在页面允许重试。ICE 配置是调用方必须显式提供的参数，未硬编码中继凭据。

接听回执丢失后保留接听重试入口，即使刷新已看到 connecting；未确认本机接听不启动采集。登录变更立即通知页面结束并清除视频绑定，不等待异步资源释放才更新 UI。

14 项页面/控制器/信令/媒体测试通过，页面及修改代码静态分析通过。页面测试以注入式音频会话验证来电无自动采集、接听重试和退出状态，不代表真实设备音视频、GPU 渲染或视觉验收。尚未从聊天附件入口开放；需完成真实 ICE/TURN 配置接口、前台来电发现、测试服务部署后一起接入口，后台系统来电仍待实施。


## App 获取并验证短期中继配置

CallRepository 接入 K260914000648；CallRelayConfiguration 校验当前请求通话、TURN/TURNS 地址、端口、临时用户名时间戳、凭据格式与 15 秒最小剩余有效期（最长接受 61 分钟，留一分钟时钟容差）。深复制不可变服务器列表，凭据仅内存持有，不写媒体/消息缓存，不打印到日志。CallPage.native 改为要求此类型，并在页面构造和媒体启动前再次检查通话归属/有效期。配置失效导致工厂失败时，控制器停止尝试采集并在后续刷新结束服务端通话。

客户端新增深复制/不可变、跨通话和临期拒绝、恶意地址/时间戳拒绝、真实接口编号及当前通话参数测试；与页面/生命周期/协商测试合计 18 项通过。静态分析通过。尚需来电/拨号入口编排和 coturn 实际部署，长通话的凭据续期与 ICE restart 未实现，不能以格式验证代替公网直连/中继可用性测试。


## 拨号/来电页面准备编排

新增账号范围 CallLaunchCoordinator，出站拨号同时点击合并，接口或中继返回失败继续复用原 requestId；另一目标/媒体类型不覆盖未完成请求。读取短期中继后再刷新通话，拒绝已经结束的旧页面。主叫拥有这次拨号时允许对方在配置返回前已接听（connecting），通过显式 outgoingAttempt 交给控制器；不会因此自动接听被叫。来电只准备当前账号的 ringing 通话，配置返回后的取消/接听变化会丢弃页面。close 拒绝迟到准备结果和后续请求，准备过程不创建媒体。

finishOutgoing 仅允许当前记录的同一通话且服务端已结束后清理拨号编号，避免其他旧通话误清当前重试。编号当前仅内存保存；进程重启的未完成拨号恢复仍待实现。组件尚未挂到聊天入口/前台通知监听，不声称入口可用了。

新增重复点击、回执失败稳定编号、提前接听竞态、取消来电迟到结果和退出丢弃验证；与通话测试合计 21 项通过，静态分析通过。


## 前台来电接入应用层

KingClubApp 登录且允许进入应用后创建账号会话范围 ForegroundCallInbox；chat.call.changed/connection.ready 提醒与五秒前台检查使用同一准备请求。真实 645/648 和 612 资料返回后再次检查通话 ringing、前台和会话代次，使用原生 CallPage 路由呈现。来电页面存在期间同一处理保持占用，重复通知不叠加路由；定时通知不积累等待页面关闭的回调。后台暂停发现，恢复前台重新检查；登录变化关闭启动器并丢弃旧结果。后台系统来电/推送未实现。

缺少或禁用通话接口时停止本轮轮询，等恢复前台或实时重连事件再检查，不把共享测试服务仍未部署接口的状态当作功能可用。没有拨打真实用户或自动接听。共享测试后端仍停留 065，故当前真实来电端到端仍待发布与双机验证。

通话组合 24 项通过；随后新增未部署接口暂停检查并重跑 inbox 四项通过，静态分析通过。另运行 app_smoke_test：30 项通过、13 项失败；已观察到旧文案/入口断言，包括“私人储物柜，标签”“添加好友”“缓存更新于 今天 21:08”“好友关系已结束 · 仅可查看历史摘要”等与已调整 UI 不符。未改旧断言或金图来掩盖失败；完整应用冒烟尚未通过，不以专项通过代替整体验收。


## 会话视频拨号入口接入

真实单聊附件面板的视频通话入口接入 CallLaunchCoordinator 和 CallPage.native，使用当前账号绑定的聊天仓库。拨号、读取临时中继配置、再次核对通话状态后进入原生通话页。准备中及通话页面存续期间抑制重复点击；网络失败保留原拨号 requestId，只有服务端确认结束才清除尝试。群视频和演示会话明确提示尚未接通，不调用单聊通话接口。

准备完成时若聊天页面已被其他路由覆盖或应用已退后台，结束该次通话，不开启摄像头。退出聊天或登录改变关闭 launcher，迟到返回不打开页面。若 start 已在服务端成功而客户端离页尚未获得响应，目前仍由服务端来电期限回收，主动取消／进程恢复尚待完善。

验证：direct_chat_call_entry、call_launch_coordinator、call_state_controller、call_page 共 9 项通过，入口新增测试覆盖真实按钮触发参数、重复点击抑制、失败重试同一编号以及离页迟到响应不读中继、不打开通话页；direct_chat_page 静态分析通过。没有发起真实用户通话，未打包安装或双机媒体验证。共享测试 API 尚未部署通话 066–071，因此不计为已可用的真机视频通话。


## 离开会话后的拨号回收

新增 CallLaunchCoordinator.abandonOutgoing：立即禁止继续准备页面，等待在途 start 的已验证响应得到确切 callId，读取同一通话并取消 ringing 或挂断 connecting/active。对方同时接听导致明确版本冲突时，最多三次重新读取并提交对应终止动作；不为未知结果盲目更换请求号重试。多次回收共享同一 Future。聊天页 dispose 调用回收，登录变化仍关闭 launcher，由账号绑定仓库阻止旧会话写入。

仅针对该 launcher 已知的发起通话，不读取并终止任意当前通话。若 start 的确认完全丢失而没有 callId，或者网络/会话权限阻止取消，仍需服务端期限兜底；没有声称断网、进程杀死及 active 通话心跳回收已完成。无原生媒体采集发生在该回收方法中。

验证：coordinator、拨号入口、前台来电 inbox 共 10 项通过，新增迟到 start 响应回收、同时接听冲突后挂断、重复回收合并、无 callId 不重拨不误终止的测试。三文件静态分析通过。未发起真实用户通话，未部署或安装。

# A 真实位置消息检查

A在当前Profile包进入A/B会话，通过“更多→位置→使用当前位置”获得手机定位和地点名称，确认后产生新位置消息，打开详情后调用高德地图，浏览器显示对应地点。测试地点、经纬度和原始UI只保留在本机build目录，不写入Git。未操作B，不能据此证明接收端展示和反向发送。

实机发现：卡片文字可打开，文字之间的空白处点击没有响应。修复范围为ChatLocationMessage的完整矩形点击区域，不改变卡片尺寸、颜色和文字。沿用现有页面测试，将点击目标改为空白区域，验证打开详情和会话变化后的隐私隐藏。4项页面测试及定向analyze通过，点击区域修复尚未安装。

后续实测：3afacf0包已装A；重新启动后原新位置消息仍在，点击卡片右下角空白处成功打开详情，修复已在手机验证。B未操作。

## 2026-09-17 定位拒绝实机补验

A 使用 bc0f644 Profile。系统设置中将定位从“使用时允许”改为“不允许”，返回 A/B 会话，更多→位置→使用当前位置，在实际系统授权弹窗点击“拒绝”。页面显示“未获得定位权限，请在系统设置中允许定位”；确认发送按钮 disabled，搜索地点按钮和输入框 enabled，无候选被自动发送。仅验证搜索入口保留，不将其计为拒绝权限后实际地点搜索成功。

通过系统设置恢复“使用时允许”；dumpsys 验证 ACCESS_FINE_LOCATION、ACCESS_COARSE_LOCATION 均 granted=true，与测试前一致。已返回 App 会话，未发送位置，B 未操作。权限切换最初使用 pm revoke 被厂商系统拒绝，随后通过正常系统设置完成，未修改系统调试安全选项。

## 2026-09-17 实际地址搜索

同一 A/bc0f644，在定位权限已恢复状态下，从会话“更多→位置”输入公开地点 Beijing 并点搜索，系统地理编码返回一个带经纬度的候选。候选未选中时发送禁用，点击候选后确认发送变为 enabled/clickable。随后返回会话，未点击发送。此为真实设备系统地址搜索证据，不是组件替身；不替代拒绝定位权限下的搜索、其他地区覆盖或 B 端验证。原始 UI 留在本机 build/kc-search-result.xml、kc-search-selected.xml。

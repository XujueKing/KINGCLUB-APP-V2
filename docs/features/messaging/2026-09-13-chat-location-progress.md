# 聊天位置消息进度

后端065/641/642已完成隔离HTTP验证，未部署共享服务。App新增不可变ChatLocation，明确整数微度和wgs84/gcj02，验证坐标/文本范围，不接受外部URL。单聊与群聊持久队列发送locationData，重建控制器复用clientMessageId；服务器回执按完整地点、会话与发送者核对，异内容不能确认删除待发送记录。

验证：build/location-queue-test.log 20项通过，覆盖坐标系/非法坐标/链接拒绝、不可变序列化、单群断网重试重建、错误回执/存储失败/群权限撤销及图片语音队列回归。数据层与3项测试静态分析通过。

App位置选择、地图展示和入口尚未接入，当前加号仍显示暂未开放；不是完成交付。已有Geolocator与Geocoding可复用原生定位，当前未配置地图SDK。未重打包或双手机验证。


## 原生定位、搜索与确认

聊天加号的位置打开ChatLocationPickerPage，用户主动选择当前位置才请求定位权限；Android沿用已采用的LocationManager路径，高精度单次获取，精度超过1km拒绝。系统Geocoding反查地址失败时保留真实坐标，名称显示当前位置；地点搜索使用系统原生Geocoder，不自动申请定位。候选地点选择后需明确确认，发送队列写入失败保留当前选择供重试。会话变化禁用页面并清除结果，迟到搜索不能恢复。

验证：build/location-picker-test.log 10项通过（显式定位/选择不发送/失败保留选择/会话变化丢弃迟到响应及模型队列回归），4项相关文件静态分析通过。本轮未修改注册定位流程。未重打包，原生Geocoder在具体手机上的可用性未实测。地图SDK尚未配置，已向用户询问已有高德/腾讯应用配置路径；当前只是地点选择列表，不能声称完整地图选点完成。位置消息气泡详细展示仍待接入，后端065也未发布共享服务。

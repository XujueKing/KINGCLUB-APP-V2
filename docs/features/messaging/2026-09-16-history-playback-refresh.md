# 历史上下文播放与已读通知

上下文页面目前把群已读事件当作强制清空重载，覆盖播放器已有的无中断权限复核逻辑。调整为已读事件后台读取最新历史，保留当前滚动与播放；若消息不再可见、撤回或读取失败，停止语音并替换/清空展示。会话变化、前后台切换及明确设置/关系变化继续清空并停止。带明确其他群标识的事件不影响当前页面。

验证重点：播放中的普通已读不调用原生停止；隐藏结果停止；迟到的后台读取不能覆盖账号失效状态。测试结果在执行后补充。

18 项上下文媒体、播放与新增页面通知测试通过，2 文件静态分析无问题。页面测试实际点击语音并发送已读通知：其他群不读历史、本群普通已读保持播放、隐藏后停止、读取未完成时注销以及注销后的通知均保留登录失效提示。这里使用受控接口和音频输出验证事件时序，不代表双机真机验收；本次未重新打包安装。

# 跨网络语音与离线播放

a6cd895d，A 蜂窝网络、B Wi-Fi，实际聊天按住说话录制两条约 3 秒测试语音。以下只验证传输、保存及播放器执行，不冒充人工听感验收。

## 双方在线

B 15:17:54.150 源准备完成，A 15:17:55.200 完成接收：UDP=0、中继 18 帧/17,096 字节。A 两份持久化文件与 B 源文件 SHA-256 均为 `cddc6d56b623aad778e0e60a518c78e195d76fdf6059395a8a9f04e9b77f3609`。该时间段服务端无 chat-voice GET，证明本轮使用 peer 加密中继，不是 HTTP 下载，也不是 UDP 直连。

## 发送方离线，接收方网络恢复

关闭 A 移动数据并核实 Active default network: none；B 发第二条新语音。确认 B 会话显示该条后停止 B 应用，再恢复 A 移动数据。A 默认网络重新建立后，未手动刷新便出现第二条语音。

服务端 15:20:01 对 `01205488-4de0-4e89-9567-7243a37f0d96` 返回一次 HTTP 206、19,457 字节。A 两份持久化语音与 B 源文件 SHA-256 均为 `439f8e64c664b92446eb1651df96883a6a3c52c6f65c3ccd41f65c048ee2894a`。该结果证明发送方离线时服务端回退可用，语音小于一个块，不构成多块续传证据。

## A 离线本地播放

第二条保存后，再次关闭 A 移动数据，脚本核实无默认网络后点击该条。系统播放器 15:21:49.350 setDataSource file，49.543 prepare 成功，49.566 start；AudioTrack 输出 64,512 帧，15:21:52.538 stop。没有可用网络仍能从本地启动并完成播放。不据此声称录音听感、扬声器音量或降噪效果经过人工确认。

结束后已恢复 A 移动数据并启动 B 返回测试会话。

网络补充：当前 A 移动接口有全局 IPv6，B Wi-Fi 只有 link-local IPv6，因此本次拓扑也不具备双方全球 IPv6 直连条件。IPv4 STUN 成功但双方没有收到 peer 探测的既有结论不变，不能由此确定具体 NAT 类型或运营商封锁原因。

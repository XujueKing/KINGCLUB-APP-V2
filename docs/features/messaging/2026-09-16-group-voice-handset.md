# A 端群语音实机检查

2026-09-16，用户确认 A 已准备好后，仅操作既有 A/B 测试群。B 端未操作。

- A 使用已安装的 3afacf0 预览包。按住录音约 3.5 秒后松开，聊天中新增一条 13:23 的 3 秒语音，自动滚到最新消息。
- 会话列表摘要更新为 `[语音]`，时间更新为 13:23。退出会话重新进入，3 秒语音仍在。
- 点击语音后，Android 创建本应用 UID 的 MediaPlayer；约 3 秒后停止并释放。再次进入会话后仍可触发播放器。
- 录音日志显示 VOICE_COMMUNICATION、not silenced；这只证明录音链路没有被系统静音，不代表实际人声清晰度已经验收。

本轮未验证 B 端收听、群语音内容清晰度、冷启动后回放及离线重试。不把播放器创建/结束等同于听感验收。私有 UI XML 和截图保留在预览工作树 build 目录，不提交。

# 原生聊天交付矩阵（2026-09-15当前核查）

## 2026-09-16 撤回入群申请（代码完成，未上线）

申请人 pending 状态增加撤回确认，严格验证服务端终态；失败保留申请，管理员抢先通过时刷新成员状态，不撤销已入群关系。客户端 14 项测试及 3 文件分析通过。后端 59abc21 已通过隔离真实 MySQL/加密 HTTP、两次各 12 轮并发审核/撤回，终态与成员记录一致。在线服务未部署、手机未安装、未双机验收；详见 `2026-09-16-group-join-cancel.md`。

## 2026-09-16 并发头像请求

修复后发未完成请求使先发有效响应误报登录变化的问题；保留更新拒绝优先于迟到成功的处理。先复现失败，再验证 13 项测试及两文件静态分析通过；尚未安装，详见头像缺口记录。

## 2026-09-16 会话相关头像缓存接入

单聊双方及详情、群成员、建群和转发选人已接入同一头像标识缓存，本人走本人资料接口且不保存签名 URL。18 项相关测试及 8 文件分析通过，06:26 扩展包已安装 A。启动后误落系统拨号界面，已停止操作等待恢复首页，本轮会话头像真机验收未完成。此前通讯录离线真机验收仍只覆盖通讯录。

## 2026-09-16 通讯录离线头像修复通过 A 复测

A 首测断网冷重启后名单保留但真人头像变默认，已修复按账号保存最小头像标识及只读本机图片缓存。19 项相关测试及 9 文件分析通过，更新安装 A 后同样断网冷重启，两张真人头像保留。B 未更新、权限变更真机验收仍待完成。详见 `2026-09-16-contact-offline-avatar-gap.md`。

## 2026-09-16 已读补交异常确认隔离

修复首条格式错误的确认阻断后续会话补交：异常意图保留并冷却，正常单聊/群聊继续处理。29 项相关测试、两文件静态分析通过。06:10 Profile/LAN 更新已安装 A，启动和聊天列表正常；异常回执真机验收未完成，B 未更新。详见 `2026-09-16-read-retry-isolation.md`。

## 2026-09-16 A 离线发件重启补交验收

A 向 B 断网发送测试文字后显示等待网络，保持断网强制结束并冷启动后仍保留；恢复网络后无需手动重试自动提交，服务端完整文字搜索返回一条记录。随后在双机测试群同样验证离线发送、冷重启保留、自动补交及搜索单条结果，A 网络恢复原状态。B 实际接收显示、服务端成功但回执丢失仍待验收。详见 `2026-09-16-offline-send-acceptance.md` 和 `2026-09-16-group-offline-send-acceptance.md`。

## 2026-09-16 断网冷启动真机修复

A 实测发现断网冷启动停在欢迎页，修复启动恢复仅在明确网络错误时使用仍有效、已审核、同一账号的本机快照。36 项相关测试及 3 文件静态分析通过。Profile/LAN 更新安装 A；实际无默认网络下冷启动可进入缓存会话及原消息，恢复网络后断网提示消失。已读确认和历史回拨也已随包安装，但这两项异常补交/回拨真机验收不因此算完成。详见 `2026-09-16-offline-bootstrap.md`。

## 2026-09-16 历史通话记录回拨

单聊搜索/引用进入的历史上下文使用服务端通话元数据显示对应语音或视频入口，并复用当前会话回拨链路。原聊天页面的回拨原已实现，本次补齐历史入口。11 项测试、4 文件静态分析通过；已随 6f6ba95 及后续包安装 A，真实历史回拨尚未验收。详见 `2026-09-16-history-callback.md`。

## 2026-09-16 已读回执确认

单聊/群聊即时及后台补交均校验服务端 readSequence，群回执同时匹配 groupId；异常结果不删除待补交记录、不发布成功通知。31 项相关测试通过，已随 6f6ba95 及后续包安装 A。此前群聊已读持久队列已接入，本次补强确认语义，不将受控测试计作真实断网重启验收。详见 `2026-09-16-read-ack-validation.md`。

## 2026-09-16 A 群设置、搜索及历史文件验收

现有 A/B 群中，A 开启免打扰和置顶后重进保留，列表显示置顶区域及免打扰图标，随后恢复两个开关为关闭。群历史文字搜索、进入上下文、从上下文打开群文件并下载校验通过。B 未操作；实际新收件抑制、群接收端文件及管理权限变化仍待验收。详见 `2026-09-16-group-settings-history-acceptance.md`。

## 2026-09-16 A 关系分组验收

A 新建仅含 B 的测试分组，选择关系图标并保存，通讯录 B 名字后实际显示图标；退出重进后名称、图标和成员选择保留。测试分组已删除，B 仍在通讯录，原空分组状态恢复。未操作其他会员或 B 手机。多设备编辑和中断保存仍待验收，详见 `2026-09-16-contact-group-acceptance.md`。

## 2026-09-16 视频播放连续性

群已读通知改为后台复核媒体权限，授权内容不变时保留视频播放器、进度和已读文件；拒绝或内容变化后清空。7 项视频测试、2 文件静态分析通过，Profile/LAN 更新已安装 A 并冷启动，包含前一节点历史语音刷新修复。B 未更新，双机已读期间播放验收尚待完成。详见 `2026-09-16-video-read-refresh.md`。

## 2026-09-16 历史播放连续性

修复上下文页面因普通群已读通知强制停止语音的问题，改为后台复核历史，隐藏/失权后停止；其他群通知不重载当前历史。账号失效后的通知不再清掉失效提示。18 项相关测试、2 文件静态分析通过，本节点未重新安装手机。详见 `2026-09-16-history-playback-refresh.md`。

## 2026-09-16 历史上下文媒体

单聊搜索、群搜索和引用入口已接入上下文图片、视频、语音、文件及位置组件，引用入口补齐发送人称呼。18 项测试、5 文件静态分析通过；Profile 更新安装 A。A 从真实历史打开文件并下载校验通过，对方语音进入播放状态。其他媒体与群历史真机验收、B 更新仍待完成。详见 `2026-09-16-history-media.md`。

## 2026-09-16 A 历史搜索定位验收

A 单聊搜索真实历史并进入上下文高亮目标已确认。修复该流程显示内部会员编号，搜索与上下文现显示“我／对方称呼”；6 项相关测试、4 文件静态分析通过，已安装 A 并复验。上个节点列表错误提示也已随包安装。群搜索、引用入口姓名映射及上下文媒体打开仍待完成，见 `2026-09-16-history-search-acceptance.md`。

## 2026-09-16 列表错误提示

聊天列表与通讯录区分登录失效、网络失败及其他刷新错误，不再统一显示断网或直接暴露内部异常。18 项相关测试和 5 文件静态分析通过。尚未安装此 UI 提示节点，详见 `2026-09-16-refresh-error-copy.md`。

## 2026-09-16 图片/录音凭证重取与免打扰设置

图片与录音上传被明确拒绝后，使用原上传编号自动重取凭证一次并重新加密；22 项上传测试通过，末次复制优化后相关 12 项通过，3 文件静态分析通过。更新 Profile/LAN 包已安装 A；实际过期上传仍待手机验收。A 对 B 的免打扰开关保存/重进读取及恢复关闭已真机确认，通知抑制本身未验收。详情见 `2026-09-16-upload-grant-renewal.md`。

## 2026-09-16 聊天请求续期与 A 备注验收

发现 A 使用期间凭证到期被列表误呈现为网络错误。MessagingRepository 接入明确 SESSION_EXPIRED 后共享续期、原编号单次重试；登录切换仍拒绝旧请求。33 项相关自动化及 4 文件静态分析通过。Profile/LAN 包已安装 A，重启后真实好友资料恢复；持续停留再到期的手机验收尚待完成。A 对 B 的备注保存、重进读取、字母分组更新和清除恢复已真机验证。详见 `2026-09-16-chat-session-renewal.md`。

## 2026-09-16 文件传输退出边界

NovoRUDP 文件发送的取消、账号切换、截止时间现在能中断网络等待和退避暂停；不关闭共享聊天连接。真实加密 UDP 9 项及真实 SUPERVM WSS 文件测试 1 项通过，静态分析通过。聊天附件仍使用 CCSOP 上传，未把该传输层验证计作聊天媒体直传交付。详见 `2026-09-16-file-transfer-cancellation.md`。

## 2026-09-16 A 通话记录与失败消息排序验收

- A 通讯录已有好友；实际单聊显示好友照片、语音通话 00:27、视频通话 00:14，更新后记录仍保留。
- 修复恢复出的旧失败消息始终追加在最新历史之后的问题。单聊、群聊共享时间插入逻辑，保留服务器 sequence 顺序、去重、失败状态，不自动重发。
- 两个控制器共 36 项测试通过；3 个实现文件和单聊测试静态分析无问题。群聊测试的既有 5 项分析提示未在本次处理，新增测试提示已修正。
- Profile/LAN 更新已安装 A，真机确认失败消息处于 18:52，之后为 19:25、20:48；未点击重试。APK SHA256：9ECCD6EFD599B6BDE12A7191B86F8CCE420CA1DF97E0E37F08EAAACE1B1FA39C。B 尚未安装本次更新，双机中继验收仍待完成。

## 2026-09-16 A 真机局域网中继接入

用户恢复 A 后，已安装配置可信局域网节点的 Preview/Profile 包。首次实际连接暴露双方不足一秒的时钟差导致严格握手时间校验失败；SUPERVM 增加有界 5 秒签发时钟容差，保留严格过期、签名和重放校验，7 项 Rust 测试通过。重编译节点与 Android 原生库后，A 的认证连接成功，节点连续约一分钟保持 1 个连接/会话。

当前局域网 APK 哈希为 23FBA3B22945C1B575584CCB1AF18B2E4638A9BB76475E2B51FBC48C57B87229，已安装 A，B 未更新；普通服务端包另存 app-preview-profile-service.apk。尚未完成两台手机互发中继文字或公网验收。详情：2026-09-16-android-relay-clock-skew.md。

SUPERVM 修复 6939dc96 仅本地提交，远端 novovm/supervm 对 XujueKing 返回 403，已请求写权限/目标仓库；不能认定主网代码已推送。App 构建固定到该已验证本地提交。

## 2026-09-16 03:55 中继优先文字与可信节点构建

- 实际单聊页面接入“当前可信中继已连接时优先发送普通文字”，失败回 CCSOP，同一 clientMessageId 同步正式历史；引用和媒体保持原路径。控制器 25 项测试通过。
- 原生库 + 真实 SUPERVM WSS + 真实会员加密 HTTP + MySQL + 接收端 SQLite 联调通过：对端先持久化、再提交 CCSOP，重复编号返回相同消息 ID，接收方历史只有一条；设备公钥撤销后原通道拒绝发送。临时会话已核对撤销。这里不是手机或公网验收。
- 新增主动取消握手和可选中继构建参数，私有节点公开证书只用于该连接，不关闭 TLS 校验。5 项证书及真实中继测试、4 文件静态分析通过。详见同目录 2026-09-16-relay-primary.md、2026-09-16-relay-build-configuration.md。
- 测试 API 只读核对当前运行 network-resolution-113 镜像；已有本人入群状态 115、本地消息确认修复 116、设备公钥身份反查 113。未将其他 100–111 源码能力视为上线。
- 新 Preview/Profile arm64 APK 构建成功，106.5 秒，157790280 字节，SHA256 CEF40CBEA91C44E79AE0A2C7DC35465F276C6F66177FC098EBCA9131217B8D5C。日志 build/relay-primary-build.log，原生 ARM64 库检查通过。默认不配置中继地址，未安装 A/B；不能把构建成功计为双机路由验收。
- 本次构建仍包含工作区既有三处 onboarding 修改，它们没有纳入本节点 Git 提交。手机安装、实际可信局域网配置、跨网连接和媒体自动切换继续待完成。

## 2026-09-16 本人入群查询已接通测试服务

后端 5baa5ef 发布 K260916000684 / migration 115 最小更新，测试服务已运行 group-join-status-115 镜像；最终镜像隔离加密 HTTP、运行健康检查、实际接口/目录/迁移登记核查均通过。旧服务容器保留用于回滚。此前本文件“接口未上线”的历史记录已由本条更新，其他未发布功能不变。

当前手机更新包包含申请状态查询与退出重进恢复，但 A/B 尚未安装该包并完成真实扫码申请/审批/刷新验收，不计作双机功能验收完成。

## 2026-09-16 续期版本保护

saveSessionIfCurrent 与 clearSessionIfCurrent 在账号/session/API 凭证外，增加 refreshToken、refreshTokenVersion、expiresAt、refreshExpiresAt 的版本比较；同一个会话已续期后，旧请求不得覆盖新令牌，也不得因旧失败响应清除新会话。资料刷新继续只合并会员字段，不要求旧令牌版本相同，因此正常续期不会把资料刷新变成换账号。当前版本的正常注销仍能完成。

30 项登录续期、资料刷新、注册恢复回归通过；2 文件静态检查通过。新增测试同时提交旧续期回执与旧清除请求，均被拒绝，新会话保持完整，随后当前会话注销成功。测试为本地会话存储与受控接口时序，不冒充手机真实服务端轮换验收。日志 build/session-revision-test.log、build/session-revision-analyze.log。

本节点 Preview/Profile arm64 构建成功：2026-09-16 03:13:30，157790280 字节，SHA256 6C58FF2190BCFD28652FF0917E3560599E11C4A300D60552794999DE9BFEB0B3，日志 build/session-revision-build.log，构建耗时 64 秒。包含两处续期修复及前序通讯录/位置/入群修复；尚未安装手机。工作区三处既有 onboarding 修改参与构建但未纳入本节点提交。

## 2026-09-16 资料刷新不再覆盖新续期令牌

核对资料刷新路径发现：refreshMembership 使用请求前整份 session 快照回写，若同一 session 的 refreshToken/version/expiry 已轮换，迟到的资料响应可能覆盖新值。新增串行 saveMembershipIfCurrent，只在原账号及 session/API 凭证仍匹配时，将 account/membership 合并到当前存储；凭证及续期字段保留当前值，响应账号不匹配时拒绝。正常资料更新不发登录变化事件；注销、换账号、权限变化的原有失效机制保留。

29 项相关测试通过，4 文件静态检查通过。新增控制时序测试验证旧资料响应晚于令牌轮换时，新令牌、版本、期限均保留，昵称更新且无登录变化事件；原有注销/切换/上传草稿/注册恢复回归通过。注册测试的内存存储替身补齐新方法，首轮遗漏该方法导致插件错误与等待，已修正后完整通过；旧失败测试进程已停止。日志 build/session-rotation-merge-final-test.log、build/session-rotation-merge-analyze.log。

本轮修复基于代码竞态及测试证据，不声称已证明它是之前手机报错的唯一原因。未重新打包或安装，03:03 更新包不含此次修复。

## 2026-09-16 03:03 汇总更新包

基于 2c09c88 构建 Preview/Profile arm64 成功，包含通讯录加密离线快照、位置详情小屏滚动和入群申请恢复。APK：build/app/outputs/flutter-apk/app-preview-profile.apk；157790280 字节；SHA256 32D3EEE60F8370F03A766680DC281B260A5CBF3F08E5CE7ACE431D9EE6B40495。编译 78.7 秒，日志 build/contact-snapshot-build.log。包含工作区原有三处 onboarding 修改，未把这些修改纳入聊天提交。

因连续更新包大小相同，额外读取 APK 内 libapp.so 核验：17236872 字节，SHA256 76a76911b56617dec2ead73886c58041a036495f82a3f6e651961076e84e73c4，包含本轮新增 contact_snapshot 和 group-join-receipts 字符串，确认最新代码实际进入产物；原生 NovoRUDP ARM64 库存在。

只读检查两台手机均 USB 在线；A 在 KINGCLUB，B 在其他应用登录页。未执行安装或触屏，不将构建成功计为手机断网重启验收。仍需验收当前 APK 的通讯录离线恢复、位置按钮可达、入群状态及通话记录显示；新入群状态接口上线也尚未完成。

## 2026-09-16 通讯录加密离线快照

通讯录不再仅保留内存列表：复用按账号隔离的 ChatHistoryStore，SQLite 升至 15，新增 contact_snapshot。姓名、备注、简介、性别及会员标识整体 AES-256-GCM 加密；AAD 绑定账号和刷新起始时间，只保留允许字段。整批服务端列表成功后后台替换快照，失败不覆盖，晚到的旧刷新不能覆盖新版本。网络确认空列表会清除旧好友快照，不从缓存推导发消息权限。

控制器启动刷新时并行恢复本地完整列表；已有网络快照时丢弃迟到的本地读取。换账号/销毁后禁止旧响应更新 UI。存储失败不影响实时列表，写磁盘不阻塞刷新。好友头像仍走既有头像缓存，不在此节点声明头像离线链路完成。

25 项测试通过，3 文件静态检查通过。真实 SQLite 覆盖版本 14→15 升级且保留会话行、文件重开、明文不可见、跨账号认证失败、旧写入被拒绝；控制器覆盖离线恢复、好友移除落盘、登录失效后丢弃迟到缓存。接口为测试注入，尚未完成手机断网重启验收，也未重新打包安装。日志 build/contact-snapshot-test-final.log、build/contact-snapshot-analyze.log。试验中同步等待缓存导致测试平台通道等待，最终改为后台写入并通过回归。

## 2026-09-16 小屏大字位置详情修复

按 B 手机 720×1604、2.25 像素比例、1.35 倍字号，使用合法长地点名称和地址复现位置详情底部溢出 578 像素。固定 Padding/Column 改为带相同边距的滚动详情，保留原字号、内容和地图调用参数。现在完整地址可读，底部地图与复制按钮可滚动到达；会话变化仍隐藏地点信息。

4 项位置详情测试通过，2 文件静态检查通过。新测试验证小屏大字无溢出、地图按钮可点且坐标不变、复制按钮可达。地图通道使用测试替身，不算真实高德跳转验收。日志 build/location-access-test.log、build/location-access-analyze.log。本节点未重新打包安装，上一更新包不包含此修复。

## 2026-09-16 入群申请退出重进恢复（代码与回归完成，手机待验收）

申请成功的回执写入按账号隔离的 FlutterSecureStorage，只保存群编号与申请编号，不保存二维码和备注。页面重新进入先恢复本地编号，再查询当前成员身份及本人申请状态；不自动重发申请，不把历史 accepted 当成当前群权限。离线只显示已提交过、状态待确认。拒绝/取消/过期后可明确选择重新申请，再由用户提交；不会在刷新时产生新的申请。回执最多保留最近 200 个群，旧申请的清除不能覆盖新回执。

12 项相关测试通过、5 文件静态检查无问题，覆盖仓库重建、账号隔离、页面重建后只读查询、明确重试、离线恢复及原有迟到响应守卫。日志：build/group-join-restore-test.log、build/group-join-restore-analyze.log。测试注入接口与安全存储替身，不算手机持久化验收。

更新包：2026-09-16 02:47:05 Preview/Profile arm64 构建成功，157790280 字节，SHA256 F37179435C3B9CFBFED858EDF3DFB76232FD64D9374348FAE29E93544BD27BBF，日志 build/group-join-restore-build.log。尚未安装 A/B；构建包含工作区既有的三处 onboarding 改动，这些改动不纳入本节点提交。

后端验证更新：141b0bb / b13648e 已在隔离合成库实际执行 migration 115，并通过加密 HTTP 本人状态、越权、过期和失效会话验证；线上接口仍未发布。

## 2026-09-16 入群申请状态客户端接入（待真实接口验收）

入群申请页面保留提交回执的 applicationId，在当前成员查询未确认入群时读取 K260916000684。严格校验群编号、申请编号、changed=false 与状态范围；显示拒绝、过期、取消等服务端结果。当前群成员身份验证仍优先，历史 accepted 不单独显示进入按钮。旧部署未提供接口或读取失败时保持“暂未确认”提示，不推断拒绝；刷新不会再次提交申请。后台/退出/登录变化后的迟到结果被丢弃。

6 项入群申请回归、3 文件静态检查通过（build/own-join-status-test.log、build/own-join-status-analyze.log）。新增页面覆盖多种审核结果、无当前成员权限不得进入、查询迟到跨登录失效；仓库拒绝其他群/申请及非只读回执。接口为注入，后端 migration 115 尚未实际部署验证，客户端未打包安装，不算双机交付。

## 2026-09-16 本人入群申请状态接口（后端开发中）

CCSOP 后端 0dc6056 新增 K260916000684 / kingclub-group-join-status，migration 115 登记接口与分类。活动 KINGCLUB 凭证及会员/会话校验后，仅查询当前申请人自己指定群的申请状态；不重新提交、不写成员关系。pending 超期或群解散投影 expired，已接受是历史审批结果，进入群仍须核对当前成员身份。

完整 npm run verify 通过：96 文件 402 测试、类型检查、构建、迁移 dry-run 和部署静态检查。新增服务查询用数据库替身，实际 MySQL/加密 HTTP 尚未验证；115 未部署、手机尚未调用该接口。现有页面仍仅通过成员查询确认入群，不把拒绝/过期实时展示标为已交付。

## 2026-09-16 01:34 聊天时间及近期修复安装包

搜索结果与消息上下文统一复用本地时间格式，缺失时间不显示接口空值，保留搜索定位、隐私刷新与退出清理逻辑。8 项相关测试通过、4 文件静态检查通过（build/history-time-test.log、build/history-time-analyze.log）。

ad47637 工作区加本节点时间格式修改完成 Android arm64 preview/Profile 构建，Gradle 64.6 秒；包含真实聊天时间、呼叫方向文案、已读拒绝冷却及本次之前的文件中继接口/取消检查。原有三个 onboarding 文件工作区改动仍随包构建，未提交这些文件。

APK：build/app/outputs/flutter-apk/app-preview-profile.apk，157790280 字节，2026-09-16 01:34:41；SHA-256：82D9166AAAE550A6E0AD3FCA0A26A750743981259E8F4E4D3A9E1A5686D069C9。日志 build/chat-time-delivery-build.log。接口仍为 https://test.wuyexin.cn/kingclub-v2，未配置中继 endpoint，文件中继尚无手机聊天入口。未覆盖安装 A/B，线上后端未变更；构建成功不等于双机验收。

## 2026-09-16 真实聊天发送时间显示

只读 A/B 设备确认均在线且前台为 KINGCLUB；没有触屏、安装或发送操作。A 当前真实会话可见消息但无时间，代码亦证实 createdDate 只映射未渲染，原时间行仅用于演示会话。修复真实消息行：首个有效时间、跨日、相邻时间差达到 5 分钟时显示居中淡色 12dp 时间；连续短间隔不重复显示。UTC 转手机本地时间，今天/昨天/同年日期/跨年日期分别显示，缺失或非法日期不伪造时间。头像、气泡内容和现有行内样式不变。

8 项时间格式/真实页面及通话入口回归通过，3 文件静态检查通过（build/chat-timestamp-test.log、build/chat-timestamp-analyze.log）。用例覆盖 5 分钟边界、午夜、时区转换、非法字段、时间与对应消息上下顺序；页面接口为注入。真机只确认了旧版缺失问题，更新未安装，不能标为真机修复验收完成。

## 2026-09-16 文件发送取消与账号切换

发送器等待 native 完成 ACK 校验后再次检查取消、账号代际、链路失败和期限，避免异步校验期间失效的任务返回成功。原有 finally 继续释放文件、确认订阅与 native 补传规划器。

3 项实际 native UDP 用例通过（build/file-cancellation-test.log）：不可达超时/取消唤醒、源文件变更拒绝、账号切换使一分钟确认等待在两秒内失败并停止发包，随后源文件可重命名且内容不变。2 文件静态检查通过。新增用例验证整体切换行为，未单独注入 native ACK 校验内部的精确竞争时刻；尚未打包或手机验收。

## 2026-09-16 SUPERVM 实际加密文件中继

NovoRudpFileSender 从具体 UDP 链路改为已存在的 NovoRudpFrameLink 接口，复用同一分块/补传/完成确认逻辑承载实际 WSS 中继。没有放宽加密、身份或帧大小校验，也没有把中继转发确认当作文件接收完成。

新用例连接当前真实 SUPERVM 守护进程（127.0.0.1:45172，PID 19752），加载实际 native DLL、固定可信中继公钥及测试证书，发送 262161 字节随机文件，接收方按 SHA-256 验证且逐字节比较一致。故意不发送第一次完成 ACK，后续补报确认完成；用生产默认 1 秒 ACK 等待，实测 7021ms、完成确认生成 6 次。日志 build/relay-file-live-test.log 显示通过，未跳过。2 文件静态检查通过。

首次用例暴露订阅时序要求，已改为先建立接收订阅再连接；测试专用 250ms ACK 等待下出现队列处理未完成即超时，改为生产默认值后通过。原 5 项实际 UDP 文件回归亦通过（含 18MiB 丢包/丢最终确认，72401ms；build/relay-file-test.log 同轮仍记录当时新中继用例失败，不把该整轮写成全绿）。上述时长为本机测试数据，不是手机或公网性能结论。

边界：用例显式信任两端临时测试身份，未测试真实会员目录；接收产物为验证后的临时文件，未自动进入聊天附件库。未接入手机附件 UI/自动直连—中继—CCSOP 切换，未安装 A/B，未部署公网节点。完整去中心化文件聊天仍未交付。

## 2026-09-16 通话记录状态区分呼叫方向

聊天区已验证的服务端通话记录，按消息发起方与当前账号区分“已取消/对方已取消”“对方已拒绝/已拒绝”“对方未接听/未接听”。接通过的记录保持分钟/秒时长，连接失败额外显示连接中断；保留现有图标、气泡和点击重拨入口。普通文本不推断成通话记录，服务端兼容文本及数据库未改动。

6 项通话记录与拨号入口回归通过，3 文件静态检查通过（build/call-history-direction-test.log、build/call-history-direction-analyze.log）。覆盖六种结束状态的双方文案、视频断线时长、实际页面时长展示，以及点击音频/视频记录重复点击仅发起一次相同媒体呼叫。测试接口为注入；尚未打包安装，不能视为新增双机验收。

## 2026-09-16 已读补报拒绝后的重试间隔

同一 repository 内，服务端拒绝已读补报后为该单聊/群聊及序号保留 5 分钟冷却，避免前台每 15 秒重复访问。待同步水位仍持久保存；其他会话、新水位和显式阅读可立即尝试，成功清除冷却。网络/登录异常仍保留现有恢复逻辑；关闭的恢复任务不会在迟到错误后继续更新冷却。冷却仅在内存中，应用重启会重新尝试，不代表权限自动恢复或已读已送达。

18 项已读队列及前台恢复测试通过，2 文件静态检查通过（build/read-retry-cooldown-test.log、build/read-retry-cooldown-analyze.log）。新增用例使用可控时钟和注入接口检查 5 分钟边界、相同标识的单聊/群聊隔离、新水位立即重试及显式阅读恢复。此修改尚未重新打包，01:07 APK 不含本节点。

## 2026-09-16 01:07 备注与联系人搜索集成安装包

以 594ee99 工作区完成 Android arm64 preview/Profile 构建，Gradle 65.1 秒。纳入此前备注保存跨页刷新、当前聊天昵称/备注切换、中文通讯录 A–Z 分组、全拼/首字母搜索及转发群名搜索；保留已构建的加密分块下载续传和已读补报。工作区原有三个 onboarding 文件改动一并进入 APK，但未纳入本节点提交。

产物：build/app/outputs/flutter-apk/app-preview-profile.apk，157790280 字节，2026-09-16 01:07:08；SHA-256：03D669E41FE66DA1281C72DE74D868BCBFA914FFC55ABD031BA399D3AAF51A36。日志：build/contacts-delivery-build.log。API 为 https://test.wuyexin.cn/kingclub-v2；本次未配置 SUPERVM relay endpoint，不能据此宣称手机主网自适应已接通。尚未安装 A/B，线上后端未变更。

本轮核查已读补报：网络/登录失效中止本轮并保留记录，单个权限错误不挡住其他对象；权限拒绝记录仍保留并可能按前台周期重复请求，未实现退避或清理。未把这一点标为修复。

## 2026-09-16 建群及转发搜索统一

核查真实建群/邀请与转发好友入口均复用 ContactsController.search，已继承昵称、备注、账号及拼音匹配；补齐转发群聊的群名全拼、首字母及群标识匹配。保留现有选择与确认流程，不改页面布局。

13 项页面及匹配回归通过，3 文件静态检查通过（build/selector-search-test.log、build/selector-search-analyze.log）。建群用备注首字母 TX 找到同学，搜索无结果后仍保留两位已选好友；转发群聊用 ZMPY、带空格全拼及群标识找到同一群，隐藏搜索结果后确认仍向选中群入队。接口为注入测试，未安装到 A/B。

## 2026-09-16 通讯录全拼及首字母搜索

通讯录页面与 ContactsController 使用相同匹配逻辑：姓名/备注/账号原文匹配，中文昵称及备注支持不区分大小写的全拼、首字母和带空格全拼。即使设置了备注，原昵称仍可查找；全部本机计算，不发送额外搜索请求。现有搜索输入及索引布局不变。

15 项拼音分组、搜索、联系人控制器及备注刷新回归通过，4 文件静态检查通过（build/contacts-pinyin-search-test.log）。实际页面输入 XJ 找到徐珏，再输入 chen peng you 找到陈朋友；独立用例覆盖备注首字母、原昵称、账号与不匹配结果。多音字沿用本地字库读音；尚未打包安装或真机体验验收。

## 2026-09-16 中文通讯录拼音索引

修复真实中文好友全部落入 # 的问题：使用当前显示名（备注优先）生成无声调拼音排序键，按 A–Z/# 分组，同组按完整拼音排序、同音时按账号稳定排序。数字、表情等非字母开头仍归 #。保留原有头像文字、右侧索引手势和放大气泡布局。

接入并锁定 lpinyin 2.0.3，仅新增这一项依赖，离线字库支持简繁转换与常用词组多音字（[包文档](https://pub.dev/packages/lpinyin)）。特殊姓氏/自定义昵称的读音未提供用户自选配置，不能承诺所有多音姓名读音均正确。

14 项拼音索引、备注列表刷新及联系人控制器测试通过，3 文件静态检查通过（build/contacts-pinyin-test.log）。新增真实通讯录页面用例检查中文与英文的分段、备注替代昵称后的分组、顺序及 # 置后。接口为注入；尚未打包安装。

## 2026-09-16 备注保存成功后本机跨页刷新

真实 settings 接口成功且包含备注修改时，发送账号隔离、带好友标识的本机事件。聊天列表和通讯录重新读取数据，当前单聊仅在目标好友匹配时同步历史/设置；失败不发布成功事件，页面退出或账号重绑取消订阅。没有新增推送依赖，也未改变已确认 UI。

16 项备注保存、两类列表无 WebSocket 刷新、当前会话标题、登录重绑、联系人控制器及头像缓存回归通过，6 文件静态检查通过（build/remark-refresh-test.log）。列表用例同时验证失败保持原名称、成功立即更新、其他账号不触发读取、清空备注恢复昵称。API 为注入，不代表双机验收。尚未打包安装。

## 2026-09-16 会话名称跟随备注更新

DirectChatPage 标题、聊天详情及通话入口统一读取当前会话 settings.remark；备注为空时使用授权资料中的真实昵称，最后才回退入口名称。资料请求复用现有头像缓存，不阻塞消息初始化；旧仓库迟到响应不写入新会话，登录重绑清理缓存昵称。群聊仍优先使用当前群名称，已确认头部布局不变。

5 项名称同步、会话登录重绑、语音/视频入口及真实备注保存页面回归通过，2 文件静态检查通过（build/chat-display-name-test.log）。新增页面用例验证旧入口名被当前备注替代，详情入口使用新备注，清空后同步恢复真实昵称。授权资料和聊天接口为注入，尚未打包安装、未作本批双机验收。

## 2026-09-16 文件续传整合包

群文件新增重建下载器续传用例，明确断言群文件授权 654 与单聊 652 接口及各自下载路径。连同加密缓存、真实 TCP 断线、文件下载器、详情页和转发器，5 个测试文件 33 项通过，4 文件静态检查通过（build/integrated-download-resume-test.log）。

42cb937 工作树于 00:40:43 完成 Android preview profile arm64 构建，Gradle 64.8 秒，包内 NovoRUDP ARM64 ELF 检查通过。APK build/app/outputs/flutter-apk/app-preview-profile.apk，157331528 字节，SHA256 19ABACBA493D9729B2119519CA0B948B18CEC3947C5F64750104CE9D9742D899。日志 build/integrated-download-resume-build.log。包含当前文件续传修复和此前已读恢复；API https://test.wuyexin.cn/kingclub-v2，仍未配置主网中继地址，保留原有三处 onboarding 工作树修改。尚未安装 A/B，手机安全存储与进程重启续传未验收。

## 2026-09-16 真实 TCP 断线续传及缓存隔离验证

本机 HttpServer + Dio 原生 IOHttpClientAdapter 实际传输 1 MiB + 3 字节文件：第一块完整，第二块连续三次仅发送 1 字节后断开 TCP；下载器销毁后重新创建，仅请求第二块，第一块由磁盘 AES-GCM 缓存恢复，完整输出字节一致。请求携带预期测试授权头、每次下载重新授权、最终再次授权。服务器授权 API 为注入，非 CCSOP/手机联调。

缓存安全/清理测试验证错误密钥拒绝、消息身份变化不命中、块序号替换认证失败、过期块失效、四个目录限制及当前传输保留。测试发现 Windows 正反斜杠导致当前目录字符串比较失配，改用 path.equals 后修复，避免清理时误删当前缓存。7 项续传/缓存测试及 3 文件静态检查通过（build/chat-download-socket-cache-test.log）。尚未打包安装。

## 2026-09-16 文件下载跨页面加密续传

ChatFileDownloader.open 接入账号独立密钥的 AES-256-GCM 完整分块缓存。每块 AAD 绑定账号、单/群类型、消息/资产 ID、文件名、大小、SHA256 和分块序号；不落盘下载授权令牌。恢复先重新取得授权，再读取缓存，缓存解密失败按未命中重下，最终仍校验整个文件 SHA256 并再次授权。缓存不可用时可正常重新下载。

网络重试耗尽、取消或离开页面后保留已完成密文块，明文工作文件仍清理；正常完成、完整性失败、权限拒绝清理对应缓存。有效期一天，每次开始清理过期目录并保留至多四个传输目录（并发下为尽力限制），系统临时目录被回收时正常重新下载。没有后台下载任务，需用户重新打开文件触发续传。

25 项下载测试通过，3 文件静态检查通过（build/chat-download-resume-test.log）。新增真实磁盘/加密缓存测试：网络中断后重建下载器只请求剩余块、篡改密文重新请求损坏块、授权撤销不请求分块且清缓存、退出下载页后恢复；最终字节完全一致，明文临时目录清空。网络授权/分块和密钥注入为测试替身，尚未完成手机进程重启与平台安全存储验收。本批尚未打包安装。

## 2026-09-16 已读恢复整合包

20a80b7 工作树整合单聊/群聊已读持久化补交、本地 relay 已读与补交后的列表刷新。11 个测试文件 70 项通过（build/integrated-read-recovery-test.log），覆盖单/群消息控制器、历史存储、队列、未读合并与分页。2026-09-16 00:27:29 Android preview profile arm64 构建成功，Gradle 76.8 秒；包内 NovoRUDP ARM64 ELF 检查通过。

APK：build/app/outputs/flutter-apk/app-preview-profile.apk，157265992 字节，SHA256 86014A094D7943AAA4AE786F95B37D1BA9B9144159D58ECD9260385DD689A5DA。构建日志 build/integrated-read-recovery-build.log。API https://test.wuyexin.cn/kingclub-v2；未配置主网中继地址。保留原有三处 onboarding 工作树修改。本包尚未安装 A/B，不代表双机验收或主网切换交付；前述本批未打包状态由此更新。

## 2026-09-16 已读补交后主动刷新列表

已读重试接口成功且恢复任务仍有效时，发出账号隔离的本机通知；会话列表订阅后重新读取服务端结果并更新父级未读数，不再只依赖 WebSocket 事件。退出页面、重绑仓库和登录变化时取消旧订阅。失败不发成功通知；本地队列清理失败仍允许刷新已获服务器确认的状态。

14 项相关测试通过，4 文件静态检查通过（build/read-recovery-list-refresh-test.log）。页面测试实际使用 SQLite 本地消息，模拟服务器已读失败后恢复，不注入 WebSocket/relay 事件，验证列表和父级未读归零及队列清空；额外验证失败不通知、账号通知隔离。安全存储和网络为测试替身，尚未打包安装或手机验收。

## 2026-09-16 群聊已读离线补交

群聊已读通过 GroupChatRepository 接入账号独立的群已读安全存储队列，直接会话与群会话使用不同存储键及恢复接口（605/622），相同标识不相互覆盖。重启后的前台恢复任务一并补交；旧响应不删除更新序号，空群序号 0 不创建持久化请求。群权限拒绝保留待处理记录、继续其他群，恢复时仍由服务端鉴权。

已核查当前后端 group-messages.ts：读取要求有效群和有效成员，readSequence 仅取最大值；group-admission.ts 重新入群把已读位置设为当前群序号。因此旧的合法已读序号不会把重新入群后的状态倒退。此结论来自代码审查，不代表本批退群/入群已完成双机验收。

20 项单聊/群聊持久化已读、恢复任务及控制器测试通过，4 文件静态检查通过。新增 4 项测试覆盖群与单聊同标识隔离、仓库重建后正确接口补交、较早确认晚到、群权限拒绝不阻塞其他群、队列类型校验及空群；接口和安全存储为测试替身。日志 build/group-durable-read-test.log。尚未打包安装；前述“群聊仍沿用既有已读实现”由本节更新。

## 2026-09-16 单聊服务端已读离线补交

真实单聊 MessagingRepository 在发送已读前写入账号独立的 FlutterSecureStorage 队列；同一好友仅保存最大序号，成功确认只移除不大于本次确认的记录，避免较早响应覆盖新请求。请求失败保持原错误语义，同时保留待补交记录。重建仓库后现有前台恢复任务在启动、恢复、网络 ready 与 15 秒轮询时补交；已读与消息恢复并行，已读接口慢不推迟本轮待发送消息读取。关闭任务后不继续请求其他好友，不清掉仍待确认的记录。

16 项队列/前台恢复/控制器已读重试测试通过，4 文件静态检查通过（build/durable-read-retry-test.log）。新增 6 项覆盖仓库重建、晚到确认、账号隔离、网络失败保留、关闭时未完成请求、慢已读不阻塞消息恢复；安全存储与接口使用测试替身，不能替代系统安全存储重启及双机验收。此批仅接单聊，群聊仍沿用既有已读实现；服务端未读列表在离线补交成功前保持原计数，未实现本地服务器未读数的乐观覆盖。尚未打包安装，OS 杀进程期间不运行任务。

## 2026-09-16 会话菜单离线标记本地已读

单聊菜单“标为已读”先落库本地直连/中继消息的已读状态并更新列表及父级数量，再提交有效的服务端序号。纯本地会话 lastSequence=0 不发送服务端已读请求。服务器请求失败不回滚本地已读，服务器未确认的未读数量仍保留，错误仍可见，不把服务端同步伪装为成功。待回执继续由现有持久化 read/read_ack 队列重试。

在线、纯本地、服务端已读网络失败三种页面测试通过，实际 SQLite 验证本地未读归零、两条待发送回执保留、纯本地无服务端已读调用；网络响应为注入。另 3 项会话未读合并和列表缓存回归通过，2 文件静态检查通过。日志 build/offline-local-mark-read-test.log / build/offline-local-mark-read-regression.log。本批尚未打包安装；服务端已读意图断网持久化及自动重试尚未由此实现。

## 2026-09-16 媒体草稿恢复整合包

以 3ed7bcc 工作树构建，包含录音稳定消息编号、文件/图片/视频/录音残留草稿防重复上传。12 个测试文件共 40 项通过（build/integrated-media-draft-recovery-test.log），涵盖草稿存储、恢复页面、视频生命周期及单聊/群聊媒体队列。

00:07:29 Android preview profile arm64 构建成功，Gradle 66.2 秒，APK 157265992 字节。路径 build/app/outputs/flutter-apk/app-preview-profile.apk，SHA256 F8A65BBE89D6080883B9CA16B65A863D1BD7CAD1AA6B12C7B0C21BF2BD3B1B93。构建脚本检查包内 NovoRUDP 为真实 ARM64 ELF，通过。日志 build/integrated-media-draft-recovery-build.log。

API 为 https://test.wuyexin.cn/kingclub-v2，未配置主网中继地址，不代表手机端主网路由已接通。保留构建工作树原有三处 onboarding 修改，未将它们纳入本次聊天提交。尚未安装 A/B 或进行本批双机验收；前述“尚未打包”记录在此整合包中更新为已打包，真机状态不变。

## 2026-09-16 图片、视频残留草稿恢复

图片和视频发送页与文件页一致，在上传前按本人、草稿 ID、消息类型检查当前已加载消息。已经入队或已确认的草稿仅尝试清理并返回，不重复上传，视频也不重复压缩和准备资产；上传处理后再次检查，避免覆盖已被恢复流程接管的消息。

8 项页面恢复及视频生命周期测试通过，3 文件静态检查通过。覆盖文件、图片、视频各自的待发送/已确认状态，发送调用数和队列数量保持不变。使用真实控制器和内存队列，网络、草稿删除和视频播放器为测试替身；尚未加载或已清除历史的恢复不在此检查范围内。本批尚未打包、安装或双机验收。

## 文件发送页识别已入队残留草稿

文件发送页在上传前按本人、草稿 ID、file 类型检查当前会话消息；已存在时仅尝试清理草稿并返回，不读源文件、不重新打开上传器。上传结束后同样复查，已被其他恢复流程接管时不再次入队覆盖原资产。保留已确认页面布局和草稿入口。

8 项文件发送页及单聊/群聊队列测试、2 文件静态检查通过。页面测试使用不存在的本地源文件，分别预置真实控制器中的待发送/已确认文件，再点击发送；均正常返回、草稿清理被调用、没有新增发送调用、队列/消息数量不变。网络响应及草稿删除器为注入，未进行手机崩溃恢复验收；只识别当前会话已加载记录，未覆盖已清除的历史。尚未打包安装。

## 已入队录音的残留草稿不再上传

VoiceDraftSender 在上传前检查当前会话是否已经包含本人、同一草稿消息编号的语音；存在时仅清理残留文件，保留原队列/历史和资产。上传完成后再检查一次，避免期间其他恢复操作已接管同一消息却被重新入队覆盖。会话及登录失效检查仍先执行。

13 项发送器及单聊/群聊语音队列测试、2 文件静态检查通过。新增待发送与已确认两种残留草稿场景，均不打开上传器、不追加 API 发送调用，原队列/历史各保持原数量，文件已清理。测试使用当前控制器中可见的队列/历史；尚未加载或已被清除的历史仍依赖稳定 ID 和服务端幂等，不宣称全部崩溃时点均已真机验证。尚未打包安装。

## 录音草稿使用稳定的消息编号

VoiceDraftStore 按账号及草稿文件名生成固定 UUID v5，重新创建草稿存储后不变；不以音频内容生成编号，独立录制不会仅因内容相同合并。VoiceDraftSender 校验存储账号与会话账号，并把该编号传给单聊/群聊发送队列。两个控制器新增可选 clientMessageId，未提供仍生成 UUID，非法编号在入队前拒绝。

12 项录音草稿、发送器和单聊/群聊队列测试通过（更新后的群聊 3 项另行重跑通过），9 文件静态检查通过。覆盖存储重建后编号不变、不同账号隔离、越权路径拒绝、发送器传入草稿编号、单聊及群聊恢复队列后沿用指定编号。未实测手机进程被杀；这提供重试去重身份，不代表遗留草稿与已上传资产的全部冲突清理已完成。尚未打包安装。

## 23:46 近期修复整合包与原生回归

基于 `3cf4573` 构建 Profile/preview/ARM64 APK，2026-09-15 23:46:27 完成，Gradle 65.0 秒，157265992 字节；SHA256 `0DD13767CA4D18EE4E515D13F8C6ADDFA4AA9AFD9B7A7FBF43D85A700BBC0E2D`。产物仍为 `build/app/outputs/flutter-apk/app-preview-profile.apk`，原生 NovoRUDP ARM64 ELF 检查通过。包含已读加密恢复、语音等待取消、文件分块重试及清理、头像主动刷新；本机原有三处 onboarding 修改随工作区打包但未纳入聊天提交。尚未安装 A/B。

原生 UDP 丢包回归通过。首次中继测试超时后核实 PID 4128 已退出、45172 无监听，且电脑 WLAN 已变为 192.168.3.63，原 192.168.1.5 不再属于本机。启动脚本拒绝失效地址后改用回环 127.0.0.1，实际中继 PID 19752、`wss://127.0.0.1:45172/novovm`，原生中继/历史关联/控制器重建已读恢复测试通过。日志为 `build/integrated-media-receipts-native.log`（含首次失败和 UDP 通过）、`build/integrated-media-receipts-relay.log`（恢复后通过）及 `build/integrated-media-receipts-build.log`。

API 仍为 test.wuyexin.cn/kingclub-v2；APK 未注入中继地址，不代表手机去中心化通道验收。当前中继只监听回环，不供手机连接，新局域网及跨公网尚待验证；正式历史和成员目录在本机测试中仍为注入数据。

## 聊天列表主动刷新重新获取头像资料

切回聊天栏目、App 回前台、手动刷新时清除会话列表的资料 Future 缓存，重新获取授权头像信息。修复成功资料一直复用、好友换头像后列表仍使用旧 fileId 的问题。媒体文件缓存继续按 fileId 复用，普通父组件重建不额外刷新资料；通讯录原有主动刷新规则保持不变。

13 项头像缓存、会话列表及通讯录测试通过，2 文件静态检查通过。页面测试覆盖首次资料失败重试、后续成功资料重新激活刷新、完整前后台生命周期恢复刷新、普通重建共享资料。资料为注入响应，未实测另一台手机更换头像或线上资料更新推送；尚未打包安装。

## 服务端已读进度随加密历史恢复

单聊历史提交在同一 SQLite 事务中将 conversationId 与 peerReadSequence 写入现有 AES-GCM presentation 快照，复用账户及会话 AAD，不新增明文读进度列或迁移。相同会话的本地进度取较大值，未提供回执的普通消息提交不擦除快照；epoch/historyVersion 校验仍拒绝过期提交。重开控制器从与消息会话身份一致的快照恢复已读，不把缓存用于发送权限判断。

53 项真实 SQLite 历史、单聊控制器及群历史回归与 3 文件静态检查通过。新增服务响应已读到 2、迟到快照为 0、数据库关闭并用原密钥重开、接口离线后两条消息仍已读；另验证不同好友不共享快照、清空移除快照、旧 epoch 不能恢复。服务响应为注入，磁盘读写/加密/重开真实；尚未打包安装。

## 下载器关闭等待活动任务退出

ChatFileDownloader.dispose 同步失效和取消请求，关闭底层客户端后等待活动下载 finally 完成，再清理已完成文件；重复 dispose 复用同一个 Future。关闭后拒绝启动新下载，避免调用方以为已完成清理时旧下载仍在初始化或写入。

25 项下载/详情/转发测试及 2 文件静态检查通过。新增延迟临时目录回调时关闭，验证 dispose 等待旧操作退出、重复关闭返回同一 Future、释放迟到回调后无文件落地、关闭后不发网络请求；原有真实 TCP 中断重试及会话变化清理仍通过。未安装手机，本节点不代表跨页面持久化续传已完成。

## 文件下载中断仅重试当前分块

下载端将每个 1 MiB 分块完整接收后再写磁盘及加入 SHA-256，避免部分字节在重试时重复写入。连接/超时/流中断、提前 EOF 及 502/503/504 对当前块最多尝试 3 次，间隔 250/500 毫秒；保留此前完整分块，重试前检查登录和取消状态。授权过期沿用重新授权，权限拒绝、元数据不匹配、超长块和最终哈希错误不按网络故障重试。重试失败仍清理临时文件。

24 项下载、详情及转发测试和 2 文件静态检查通过。新增连接故障、部分流断开、提前 EOF、重试耗尽；额外真实本机 HttpServer/TCP 发送第二块第一个字节后断开，实际请求序列为 [0,1,1]，最终文件逐字节和哈希一致。授权元数据为测试注入，不算真实会员/手机验收。仅同一次下载内短暂断线恢复，未实现退出页面/重启后的持久化续传；尚未打包安装。

## 原生播放器停止等待期间取消旧播放请求

ChatVoicePlayback 在等待原生 stop 完成前记录操作代次。用户停止、切后台或点击另一条语音后，旧 toggle 的等待即使随后完成，也不能重新授权、下载并播放已经取消的语音。最新语音仍按原有串行播放器队列启动，界面样式不变。

15 项语音播放测试及 2 文件静态检查通过。新增用延迟 stop 的输出适配器覆盖主动停止、切后台、连续点击另一条语音：前两种不再发授权请求或播放，后一种只请求并播放最新语音。属于时序故障注入测试，未验证手机播放器延迟；本修改晚于 21:15 APK，尚未打包安装。

## 21:18 原生 UDP / SUPERVM 中继回归

在 `3dcacbe` 工作区执行 `nearby_text_channel_test.dart` 和 `member_relay_runtime_test.dart`，使用真实 Rust DLL、UDP 丢包代理、SQLite 和仍在运行的本机 SUPERVM WSS 中继（PID 4128，45172 端口），2 项全部通过、无跳过，约 9 秒。日志：`build/read-retry-native-regression.log`。

覆盖握手/送达/已读/已读确认丢失后的重试、4000 字中文分片、重复发送不重复入库、16 个未知旧回执不阻塞后续回执、中继入站进入会话列表及未读数、读后归零、正式历史关联及控制器重建后仍显示已读，以及前后台连接释放/恢复。成员目录、正式历史与服务故障由测试注入；真实部分是传输、认证链路及持久化，不代表线上 API 全链路、Android 双机、跨公网或手机 NovoRUDP 自适应已验收。

## 21:15 Profile 测试包已构建，待安装

`build/app/outputs/flutter-apk/app-preview-profile.apk` 于 2026-09-15 21:15:31 构建完成，Gradle 68.0 秒，157265992 字节；SHA256 `16B1CE49455770372B89429EBF83DBEED5683A6C548AA0F8C5707C1E6A4C80D4`。包含聊天代码截至 `2a11c4d` 的近期修复，构建脚本已核验原生 NovoRUDP ARM64 ELF。工作区原有三处 onboarding 修改保留并参与本机打包，未纳入聊天提交。

API 为 `https://test.wuyexin.cn/kingclub-v2`；未注入 SUPERVM 中继地址，因此不能将此包算作手机中继自适应验收。A/B 手机安装操作仍暂停，本包尚未覆盖安装。前文各节点的“尚未打包”是当时状态，本节更新构建状态，不改变尚未双机验收的结论。

## 单聊与群聊并发已读失败可重试

标记已读分别跟踪已成功进度与仍在请求的序号，只有请求成功才推进确认值。修复两个并发请求先后失败时，回滚到另一条失败请求的序号、导致后续可见消息不再重试的问题。历史代次变化会清空跟踪值，旧请求完成不能覆盖新代次状态。

34 项单聊、群聊及已读重试测试通过，3 文件静态检查通过。新增覆盖单聊/群聊各自的正序失败与逆序失败、较低序号重新提交、成功后不重复提交及较高序号继续同步。故障由注入请求模拟，不算双机断网验收。

## 翻页响应乱序不回退单聊已读进度

同一 conversationId 的 peerReadSequence 仅向前推进，避免 loadOlder 的旧响应晚于 synchronize 返回时把已读降回未读。不同 conversationId 采用自身进度，不继承旧会话读标记。保留现有消息布局。

20 项单聊控制器/页面测试与 2 文件静态检查通过。新增测试用延迟响应重现历史翻页和同步并发：先收到已读到 2 的新响应，再释放已读到 0 的旧历史页，两条发件仍均为已读。这是控制器注入响应验证，尚未打包安装或双机验收。

## 已读回执队列避免被已清理消息阻塞

read_ack 表示认证端已经处理该通知：即使本地已清理对应发件，也确认接收到的合法 ID，而不创建消息、不写入虚假的已读记录。仅匹配现有发件的 ID 更新状态并发出变更事件。这样排在前面的未知 ID 不会占住 16 条重试批次，阻塞后续真实消息。接收方已读消息再次被同设备重传时，重新置为待报告，让发送方重建状态后仍能重新获知已读。

真实 Rust/UDP 丢包测试增加 16 条发送方不存在的记录加一条有效后续回执，约 9 秒内完成原有丢包重试及队列收敛，发送方仍只有原来两条消息；3 项 SQLite 已读/副本回归覆盖重启后再次重传会重报、未读仍为 0。4 文件静态检查通过。尚未打包安装。

## 跨设备相同消息 ID 入库前校验正文

persistNearbyText 在同一事务中检查相同会员、ID、收发方向的全部设备副本，逐个解密验证正文一致性。新设备副本冲突或未知设备日志尝试绑定到冲突会员历史时直接回滚；不会先保存再在读取历史时报错。认证文本通道原有 ACK 顺序位于持久化成功之后，因此该拒绝不会发出成功送达确认。

6 项真实 SQLite 历史/关联/已读回执回归与 2 文件静态检查通过，新增断言包括未知日志绑定失败仍保持未绑定、新设备冲突不留记录、原历史保持可读、同内容副本正常接受。协议 ACK 顺序为源码核对，本节点未额外执行故障注入传输测试，尚未打包安装。

## 单聊气泡显示送达/已读状态

真实单聊将控制器 peerRead/peerDelivered 接入原有消息显示模型，自己的成功发件在气泡下方显示“已读”或“已送达”，已读优先。复用发送中提示的 11 号字、颜色及右侧 52 间距；失败与发送中仍使用原提示。只有服务器发送确认、没有远端送达证据时不显示“已送达”；收件、群聊和演示消息不生成这一回执标记。

19 项页面/控制器测试通过，新增页面覆盖仅服务器接受不显示回执、送达标记显示、已读覆盖送达、收件无标记；2 文件静态检查通过。测试数据为注入状态，前序真实原生回执证据独立记录。尚未打包安装，手机端视觉与实际双机状态切换仍待验收。

## 正式历史替换后保留中继已读状态

nearbyPeerReadIds 按会员与本机发件 ID 查询已经认证且持久化的已读标记，正式消息关联后仍可查询。单聊控制器分批补齐已加载正式发件的已读信息，以服务器 readSequence 或有效中继回执判断 peerRead；去重替换、重新创建控制器后不因中继正文退出临时历史而丢失已读。历史清空和无效读取会丢弃内存投影，不借用其他会员相同 ID 的状态。

19 项控制器/数据库测试及 4 文件静态检查通过。真实 Rust/WSS 回执联调后注入正式服务端历史，连续创建两次控制器，均只有一条正式消息且 peerRead=true；服务端历史/目录仍是测试注入。尚未打包安装，当前验证为控制器消息状态，不能代替手机气泡已读样式验收。

## 中继已读回执与丢包重试

SQLite 14 增加 readReported。接收方本地已读保持持久待报告状态；认证文本通道发送最多 16 个 ID 的 read 数据帧，发送方在匹配设备、会员及发件后持久化已读/送达，再返回 read_ack。接收方仅确认同一设备/会员的已读收件。失去确认后仍保留待报告，每 3 秒按批重试，通道关闭停止定时器；重连重新读取待报告。重复设备副本继承同一会员同 ID 的本地已读状态，向对应设备独立确认。旧端忽略新增操作，不冒充旧端支持已读回执。

MemberRelayText 传播发送方历史变化，正式会话的本地已读回调触发即时发送，列表标读也由周期任务补发。真实 Rust/WSS 测试验证远端 outgoing.read=true 且本地待报告清空；真实 UDP 代理分别丢弃 read/read_ack，约 6 秒自动重试后收敛。SQLite 范围隔离/重启/待确认持久化、已有历史回归通过（初始 5 项，补充副本继承后 4 项），8 文件静态检查通过。会员目录仍为测试注入；尚未打包安装、未验收手机已读标记，服务端正式消息的多端已读与主网跨公网能力不能据此算完成。

## 已读中继消息仍参与服务端收录核对

会话核对改用 nearbyUnconfirmedIncomingIds，包含可见且尚未确认服务端收录的已读收件。nearbyUnreadIds 仍只用于未读操作，不改变已读语义。避免本地已读后永久跳过核对，在设备时间较新或服务端撤回时用旧中继正文覆盖最新预览。分页、会员隔离、隐藏与正式关联过滤仍保持。

7 项真实 SQLite/组件回归通过，新增覆盖本地已读→服务端匹配撤回预览→第二设备重传相同 ID→离线预览保持撤回、未读仍为 0；服务端响应在该测试中注入。3 文件静态检查通过。本节点在 20:42 APK 之后，尚未重新打包或安装。

## 20:42 原生中继列表联调与安装包

本机 SUPERVM WSS 中继当前重启并验证监听 192.168.1.5:45172（PID 4128，测试配置最长运行 2 小时）。member_relay_runtime_test 使用真实 Rust DLL、WSS、握手/加密、收发回执与 SQLite：服务端发送失败后中继消息进入控制器，同时能够生成本地会话、保存/读取加密列表缓存，重复送达后仍为 1 未读，标记已读后归零。1 项集成测试通过、静态检查通过；会员目录与服务器失败仍注入，未据此宣称手机或公网主网联调完成。

当前工作区构建 profile/preview/arm64 APK 成功，Gradle 66.4 秒，157265992 字节，包含已验证 ARM64 ELF NovoRUDP JNI。文件 build/app/outputs/flutter-apk/app-preview-profile.apk，SHA256 `2C8F6FE6810A0827C40491F52E9F01C82A3EFB701511F03B9EC6D63777135F3D`。构建包含工作区已有的三处 onboarding 编辑，未将其纳入本节点提交。API 为 https://test.wuyexin.cn/kingclub-v2，未注入中继端点，默认仍不开启中继传输/列表核对，普通真实列表缓存可用。尚未安装到 A/B，后端 113/114 仍未上线。

## 在线首屏保留中继会话及分页排序

每次真实列表页返回后再次合并本地中继目录，因此服务端首屏刷新不会抹掉已经出现的中继会话。缺少当前页服务端信息时复用已有昵称/备注等显示缓存，未获得过资料仍使用会员账号；后续正式行替换临时行。翻页先按群/好友独立身份合并，随后按置顶、最新时间稳定排序已加载行；临时行不占服务端偏移，也不会冒充已加载的正式行阻止末页资料补全。缓存写入期间点击加载更多会合并保留一次请求，避免点击被忽略。

新增真实 SQLite 页面测试覆盖首屏中继预览不消失、排在旧消息前、翻页请求偏移为 0/1、正式昵称替换且只出现一行；纯函数验证群与好友同名 ID 不冲突、置顶和同时间顺序保持。专项及既有回归共 24 项通过，3 文件静态检查通过。本节点未打包安装；排序只覆盖已加载服务端页与本地目录，未加载的普通服务端会话仍需翻页。临时行中的缓存服务端未读/设置在正式页到达前可能过时。

## 断网中继消息即时更新列表

offlineRelayConversations 在本地完成会话目录、最新预览及未读投影，不调用服务器。冷启动缓存恢复和中继变更事件均接入；新会话没有昵称缓存时先显示真实会员账号，不冒充已获取资料。未读以缓存服务端部分加当前本地部分计算，重复事件不累加；本地独有且已隐藏的行消失。局部更新按置顶与时间稳定排序，账号切换、较新的本地读取及已完成的服务端刷新会使旧读取失效。服务端翻页返回已有好友时替换原行而不重复插入。

22 项测试通过，包含真实 SQLite 和页面：注入持续网络失败后，新中继消息仍出现、未读 2→3、重复通知维持 3、隐藏回到 2；既有在线列表、长按已读和控制器回归通过。尚未打包安装。服务端未读仍是上次缓存值，离线昵称/头像/权限不会伪造刷新；在线分页首屏与全局排序仍需继续统一，不能将此视为公网主网去中心化实测完成。

## 聊天列表断网冷启动缓存

SQLite 13 增加 conversation_list_cache。真实持久化仓库进入列表时先恢复上次成功加载的会话及未读回调，再请求服务器；失败保留列表并显示已有断网提示。成功刷新后替换加密快照，空列表也覆盖旧值。缓存采用账号绑定 AES-GCM，仅投影显示字段，剔除头像临时链接、令牌和发送权限。损坏缓存不阻止网络刷新，缓存写入失败不把成功请求误报为网络失败。

验证：真实 SQLite 加密/重启/账号绑定/字段过滤测试和断网冷启动页面测试通过，页面显示缓存好友及预览、未读回调为 2、无演示会话；之前联同会话控制器等 21 项通过，新增页面测试后专项 2 项通过，5 文件静态检查通过。接口断网在组件测试中注入，尚未安装到手机。离线显示的是上次快照，未读可能过时；首次无缓存仍需网络，本地中继独有新消息的纯离线建行及实时刷新尚未完成。

## 本地独有会话接入列表

服务端分页读尽后，枚举加密会员目录，排除已加载和本页已有好友，补入仍有可见中继消息的会话。昵称来自真实资料接口，备注、免打扰、置顶及最后序号来自真实历史接口；已不可访问的资料不生成行。新增行仍走现有头像、点击聊天和菜单布局，不伪造服务端消息序号。

列表单独记录 nextServerOffset，新增本地行不推进服务端偏移。真实 SQLite 加注入接口测试覆盖跨页排重、名称/设置/预览/未读、隐藏过滤和偏移不跳项；连同页面及既有控制器共 21 项通过。本节点未打包安装；本地独有会话仍需读到服务端末页才出现，断网展示、首屏优先合并和跨页排序尚未完成。

## 本地独有会话的加密会员目录

SQLite 12 新增 nearby_member，主键仍为账号作用域的会话哈希，会员账号使用 AES-GCM 加密并将当前账号与主键绑定为 AAD。在持久化已经过身份绑定的中继消息的同一个事务内维护映射，冲突消息整体回滚。nearbyConversationMembers 提供稳定的哈希游标分页，只枚举尚有可见收件或已送达发件的会员；未知设备、已隐藏、已关联服务端以及仅有未送达发件的目录不出现。

真实 SQLite 6 项测试通过，覆盖新建、重启、11→12 升级、目录不含明文账号、游标分页、回执/隐藏/服务端核对过滤以及身份冲突回滚。旧库的单向哈希不可反解，升级保留消息，后续经过身份绑定的重传可恢复映射。本节点是本地会话合并所需的数据读取能力，尚未创建列表行或安装到手机。

## 中继消息列表预览与时间

已存在的真实会话在核对未读后，读取该会员最新的本地中继消息；时间新于服务端行时更新预览和时间。SQL 在取最新一条之前排除服务端已收录、已隐藏、已关联正式历史和未送达发件，避免这些内容遮蔽有效收件。该读取模式不改变聊天区历史读取行为，也不伪造服务端序号。

真实 SQLite 合并测试及页面测试通过：新收件预览可见、长按已读归零、未送达发件不抢占预览、较新服务端预览保持、隐藏后不再返回。本节点未打包安装，仍依赖中继配置及后端 114；仅更新已返回会话的内容，跨分页排序及本地独有会话仍待完成。

## 正式列表接入中继未读

配置中继端点且使用持久化真实仓库时，会话列表调用核对合并函数，并订阅账号作用域内的中继变更。列表未读回调传递合并计数；长按标为已读在服务端成功后同时标记本地收件，不显示同时隐藏本地历史。切换登录账号及页面销毁取消订阅，异步打开历史检查仓库身份。

验证：真实 SQLite 与页面组件测试覆盖初始计数 2、新收件变 3、长按已读归零；连同既有会话控制器、合并测试共 20 项通过，3 文件静态检查通过。接口在该组件测试中注入。默认无中继配置的安装包仍走原接口；本节点未打包、未安装。启用需先部署后端 114 契约，服务端列表中不存在的本地会话仍待补齐，不代表全局离线未读已完成。

## 核对结果持久化与会话未读合并

SQLite 11 增加 nearby_server_presence，只保存账号作用域内的会员哈希与消息 ID；它表示服务端已收录，不伪造已读或正式 messageId。来自会话核对的匹配 ID 仅为本地已有的该好友收件写入标记。新设备重传同 ID 不重复计入本地未读，消息正文仍保留供历史合并。

conversationsWithRelayUnread 按 200 个 ID 分批查询、保存归属匹配结果，最后只把尚未收录的本地未读加入返回会话计数，群聊计数保持原值。检查历史所属账号及整个操作的登录代次。该函数需要后端 114 契约；最新列表接入范围见上节，尚不能新增服务端列表中不存在的会话。

五项 SQLite/核对合并测试通过，覆盖升级、跨设备与重开保留、送达/已读独立，以及 202 个待核对 ID 分为 200+2 请求；四文件静态检查通过。接口回复为注入数据，尚未双机、未打包安装。

## Flutter 会话本地 ID 核对契约与未读分页

MessagingRepository.conversations 增加可选 knownLocalMessageIds，校验最多 200 个 UUID、统一大小写并去重；省略时仍发送原请求。启用核对时要求各会话包含匹配列表，拒绝未知 ID、重复匹配和群聊错误匹配，缺少能力不默认为核对成功。

nearbyUnreadIds 用消息 ID 游标分页，跨设备/会员重复 ID 仅提交一次；已读、隐藏及已关联正式历史的本地记录不进入核对。使用游标而非偏移量，前一批标已读后不会跳过后面的待核对项。

二十二项仓库/SQLite/控制器测试通过，四文件静态检查通过。仓库测试为注入接口回复；真实 MySQL/加密 HTTP 证据在后端 c635fa5。页面尚未开始携带新参数，待核对结果与列表红点合并完成后接入；线上 114 尚未部署，未打包安装。

## 中继文字未读持久化与可见消息已读

SQLite 10 新增 wasRead。按会员和消息 ID 聚合未读，跨设备副本只计一次；任一副本已读、被隐藏或已有正式历史关联时，不再算额外本地未读。已读操作只提交当次显示的 ID 快照，后到的新消息保持未读。正式单聊沿用前台、当前页面、滚动到底部的判断后触发本地已读，写入成功通知对应好友更新；送达与已读仍是不同状态。

二十二项存储/控制器测试通过，覆盖升级、重开数据库、跨设备已读副本、新消息保留未读及仅可见快照标记；七文件静态检查通过。聊天列表和底部 bar 尚未消费本地未读统计，因此不能宣称红点已接通。此已读只保存在当前账号本机，不是远端已读回执或跨设备同步。未打包安装。

## 非当前会话的前台恢复队列接入中继回退

ChatOutboxRecovery 的正式单聊恢复控制器现与页面共用 textSender，读取账号历史以关联后续正式确认；当前可见会话仍由页面负责，避免双重恢复。已记录 peerDelivered 的队列项只继续服务端原 ID 确认，恢复任务重建不再次中继发送。群聊路径保持原有发送与权限规则。

二十四项恢复队列/单聊控制器测试通过，两文件静态检查通过。新增测试使用内存队列和注入网络/中继回执，验证恢复任务重建、单次中继、服务恢复确认与队列清理，不作为真实杀进程或操作系统后台投递证据。此功能仍限 App 前台运行/恢复前台，未打包安装或进行双机验收。

## 普通文字网络失败自动尝试认证中继（第一阶段回退）

正式单聊在服务端返回 NETWORK_ERROR 时，以原 clientMessageId 尝试已配置且在线的会员中继；只处理无引用的普通文字，权限拒绝、拉黑及其他消息类型不改道。实时目录授权后最多尝试两个设备，每次仍执行认证握手；不使用历史公钥缓存作为授权。收到持久化回执才标记 peerDelivered，保留服务端 queued 项等待恢复后的同 ID 确认，不伪造服务端记录；后续服务重试不反复中继投递。

十八项控制器测试通过，覆盖同 ID 回退、保留队列、恢复确认去重及权限拒绝不改道。一项真实 WSS/原生库测试将注入的服务网络失败通过控制器回退到实际中继，接收方 SQLite 落盘并刷新会话；五文件静态检查通过。真实网络故障、Android 自动选设备及跨网验收未完成；当前为服务优先的中继回退，不是直连/主网优先的完整自适应。构建默认未配置中继地址，尚未打包安装此节点。

## 中继与服务端记录显示顺序及加载解耦

单聊按本地接收时间升序插入中继记录，保持正式记录原有服务器 sequence 顺序；不再把本地倒序查询结果直接追加到会话尾部。时间相同的本地记录按消息 ID、发送人稳定排序，服务端缺少时间时保留其序号顺序。此处本地时间只用于显示，不作为全网共识或服务器序号。

中继历史读取改为独立刷新，不阻塞正常服务端历史加载和发件恢复。十六项控制器测试通过，新增混合时间顺序、正式序号保持和本地读取未完成时正式历史仍可用；两文件静态检查通过。未重新打包或安装，本节点仍待实机验收。

## 正式单聊页面接入中继历史与通知（已构建，未手机验收）

正式持久化会话现在传入中继历史读取函数和按账号过滤的好友更新通知，使用原有气泡转换与显示路径。读取函数固定创建时登录代次，换账号后拒绝读取。配置了可信中继地址及公钥时，NovoRudpBindingRuntime 先打开账号历史并订阅 MemberRelayText，再启动中继；退出账号关闭文字通道及订阅。普通构建未设置中继地址，仍不会自行启用中继连接或改变发件路由。

真实 WSS/原生库测试已把收到的文字通过 MemberRelayText 事件传给 DirectChatController，验证其会话消息更新且没有伪造服务器序号；连同原控制器测试十五项通过。三文件静态检查通过。Profile preview 构建成功（69 秒，149.9 MB），APK SHA256：EEDC1BC291E29A905BB14744AF6945D3BF68EF8791B90ADB62AB5DCFEBAC67DD。未安装手机；测试会员目录为注入，页面实机显示、会话列表联动、完整自动选路仍待验收/开发。

## 清空聊天隐藏本地中继记录（已接清空返回路径）

SQLite 9 增加本地 hidden 标记。用户确认清空并收到详情页成功返回后，单聊控制器在清除正式缓存的同一事务内隐藏该好友现有中继记录；普通缓存失效、撤回后的刷新不会顺带隐藏全部中继消息。保留加密日志及去重标记，同 ID 在原设备重传或另一设备首次重传都不会重新展示；新 ID 正常展示，其他好友不受影响。中继刷新等待清空事务，旧异步读取不能恢复清空前快照。

十八项 SQLite/控制器测试通过，覆盖旧结构升级、清空后重传、新设备副本、重新打开数据库、好友隔离及既有发送流程；六文件静态检查通过。此处是仅对本人隐藏，不是安全擦除。中继数据源仍未在生产页面启用，未安装手机，本节点不代表自动选路已交付。

## 单聊控制器合并中继记录（控制器接入完成，页面尚未启用）

DirectChatController 接受中继历史读取函数与按会员通知的事件流，初始化和服务端历史合并后读取本地补充记录；好友事件触发刷新。按发送人和 clientMessageId 去重，已送达的本地发件副本不与发送队列重复显示；未送达发件继续由原队列展示。中继记录使用既有文字气泡数据结构，不伪造 messageId 或服务端 sequence，因此不误走服务端撤回/引用接口。重置和销毁拒绝迟到读取，读取失败清除中继快照但保留服务端历史。

单聊控制器十四项测试通过，包含中继实时刷新、会员事件隔离、服务端确认去重及重置/销毁迟到回调；两文件静态检查通过。当前生产页面还没有传入该数据源：本地中继记录的清空/隐藏规则及会话列表联动仍需完成后接线。不能视为手机已交付。

## 按好友读取中继文字并跨设备去重（读取路径完成）

nearbyMemberMessages 按认证会员作用域读取本地消息，按原消息 ID 与收发方向合并多设备副本；任一副本已关联服务端记录，则整个本地消息不再作为额外记录返回，撤回/隐藏关联也不会重新出现。保留独立的送达状态，不伪造服务端序号。未知归属记录不展示，跨设备同 ID 内容冲突明确失败。MemberRelayText.messages 支持离线读取，读取前后核对登录身份。

两项真实 SQLite 测试及一项真实 WSS/原生库收发读取测试通过，四文件静态检查通过。中继测试目录仍为注入数据。聊天 UI 尚未消费这个读取接口，自动选路与手机验收仍未完成。

## 已认证中继接入持久化文字通道（主机验证通过）

MemberRelayText 连接 MemberRelayRuntime 与加密 ChatHistoryStore：按已认证会员归属保存收发文字，接收落盘后回执，按原消息 ID 去重，并向订阅者通知对应好友账号；连接断开、退到后台或登录身份变化后停止交换。桥接层不接管服务端发送队列。

实际 SUPERVM WSS、原生加密库和双端 SQLite 测试通过：文字到达、重复发送仅保留一条、发送端收到持久化回执、正式历史提交关联原消息。两文件静态检查通过。此测试的会员目录与服务端确认记录为注入数据；真实 HTTP 授权证据见下文独立联调。正式聊天界面合并显示及自动路由尚未接入，手机未安装本节点。

## 正式历史缓存提交自动关联对等记录（已接存储写入路径）

SQLite 8 增加会员作用域哈希及 member/id/outgoing 索引。对等通道可携带已经认证的 peerAccount，在发送/接收日志上保存归属；同一记录禁止改绑其他会员。ChatHistoryStore.commit 提交 direct 会话的已认证历史时，在同一事务中关联该会员各设备的对等记录，旧 epoch 响应不会关联；内容冲突连同历史写入一起回滚。未知会员归属记录保留，不推测归属。

实际 SQLite 覆盖旧结构升级、同会员多设备关联、未知身份隔离、错误 epoch、事务回滚、不可改绑及回执状态独立；连同历史与单聊控制器测试共二十六项通过，静态检查通过。正式历史缓存提交已调用该逻辑，但运行时尚未把认证通道交给正式聊天并填写 peerAccount，界面合并与自动选路仍未完成，手机未安装此更新。

## 对等日志关联服务端历史（存储层完成）

本地 SQLite 升级到 7，为对等消息保存服务端消息 ID。reconcileNearbyText 仅供已认证服务端历史调用，按 clientMessageId、收发方向及两方账号关联，并核对正常文字内容；冲突则整批事务回滚，其他会话/群聊/非文字消息不误关联。已由服务端保存的记录退出对等自动补发队列，但不伪造接收方已送达回执。服务端隐藏/撤回占位也会关联，避免后台重发同一消息。

真实 SQLite 验证旧 6 升级、批量冲突回滚、方向隔离、重放幂等、撤回占位、重开后关联保留以及送达状态独立。连同现有加密历史测试共十三项通过，静态检查通过。当前只是存储能力，尚未在正式历史刷新中调用，也尚未把对等记录显示到会话界面；不宣称统一历史已交付。

## 真实会员 HTTP 与双端中继运行时联合验证（主机通过）

新增 opt-in member_relay_http_test，关闭 Flutter 测试 HTTP 替身，使用 KingclubSecureClient、隔离 API 的实际加密 HTTP、MySQL 设备绑定目录及实际 SUPERVM WSS。两名临时会员真实注册设备，按公钥反查归属；两端 MemberRelayRuntime 自动认证、主动连接、反查接收方请求并完成签名握手，实际加密帧被接收。通过真实撤销接口撤销接收设备后，发送端权限复核失败且后续发送拒绝。测试三秒通过，静态检查通过，无注入目录响应。

临时 API 仅发布服务器回环端口，经本机 SSH 隧道访问；测试停止后输出清理完成，远端与本机凭据文件已删除，测试容器停止，隧道进程终止。未替换在线 API，未触控或安装手机。此证据证明会员授权与中继运行时能联合工作；正式聊天历史、自动路由切换和两手机验收仍未完成。

## 双端运行时主动连接与复用（主机真实中继验证）

MemberRelayRuntime.connectPeer 按会员和设备绑定发起通道，同一目标并发调用共用任务；已有通道重新验证权限后复用，限制同时查询/连接数量。前后台断线清理全部通道与待建任务，旧查询结果不能污染新连接。接收端沿用自动反查及应答入口。

真实 WSS 测试现由两端 MemberRelayRuntime 完成连接，不再手工拼装握手：重复主动请求是同一 Future，再次连接返回同一有效通道，加密帧送达，关闭主动端后发送拒绝；被动端后台关闭、前台重连和迟到查询清理仍通过。一个实际网络集成测试和两文件静态检查通过。目录数据为注入响应，真实 HTTP 联合测试、正式聊天选路、统一历史和手机安装仍未完成。

## 中继请求自动查询会员并应答（运行时接通，主机验证）

MemberRelayRuntime 在存在 incomingChannels 消费者时自动处理提议：按来源公钥查询会员、重新验证设备目录并完成原生签名握手，然后交出带会员账号的通道。最多八个待建/已建对端，每分钟最多十六次新建尝试，重复待建请求合并；查询超过五秒拒绝。后台/退出/断线清理待查询、握手及已建通道，迟到结果不能恢复。已存在通道只有在新握手成功后才替换，防止重连被旧连接永久挡住。

实际 SUPERVM WSS 与原生密码库测试完成运行时自动查询、应答、接收加密帧及后台关闭，前台重连和关闭期间迟到查询仍通过；一个集成测试和两文件静态检查通过。目录响应仍为注入数据，真实目录接口已独立通过后端验证，但两者联合验证尚未完成。正式聊天尚未订阅该通道并合并消息历史，手机配置仍未启用，不算完整去中心化聊天交付。

## 来自中继的设备身份反查（代码完成，未上线）

后续后端隔离验证已完成：113 在合成测试库执行，真实 MySQL/Redis/加密 HTTP 的公钥归属、陌生和不存在公钥拒绝、设备撤销、旧目录兼容通过。脚本退出 0，本轮临时会话活动数重新查询为 0，测试容器退出移除。在线 API 未替换，客户端 resolvePeer 与真实加密 HTTP 的联合验证、正式消息路由仍待完成。

后端扩展既有设备目录接口，允许以 peerId 查询，只有本人或互关未拉黑好友可得到归属账号。客户端 resolvePeer 校验账号格式、非本人、cacheSeconds=0、设备列表唯一性及查询公钥确实存在于返回列表；后续握手仍重新核验权限和签名。未知来源不能自行声明会员身份。

后端 npm run verify 通过（94 文件、395 测试），客户端设备绑定相关六项测试及静态检查通过。服务层数据库结果与客户端目录响应为注入测试数据，未算真实联调。接口登记迁移 113 未执行、线上接口未更新，完整中继会员映射和手机聊天路由仍未交付。

## 中继单方发起与接收入口（主机真实验证）

MemberRelayHandshake 支持任意一方显式 initiate，以及接收端使用 initialOffer 在收到请求后才开始会员绑定握手。双方同时主动连接时通过身份排序保留一个提议，取消另一个原生握手，仍使用签名会话编号匹配应答。MemberRelayRuntime 公开 incomingHandshakes 事件；这不是自动授权，调用方仍需把公钥来源映射到会员并通过设备目录核验。

真实 SUPERVM 测试特意由排序较大身份单独发起，接收端收到后才创建握手，成功建立并关闭通道；随后两端同时主动发起完成原有补发和撤销测试。运行时集成另确认实际中继请求能从接收入口送出，源/目标匹配，前后台重连仍通过。两个真实网络测试通过，四文件静态检查通过。会员映射与正式聊天消息路由尚未接入，未宣称手机已能自动接收去中心化聊天。

## 中继运行时接入应用生命周期（主机验证，未开启手机选路）

设备注册成功后，只有同时显式配置 KINGCLUB_NOVORUDP_RELAY_URL 和 KINGCLUB_NOVORUDP_RELAY_PEER 才启动 MemberRelayRuntime。使用系统可信 TLS 和指定的中继签名身份；没有默认种子或忽略证书校验。前台重新确认本机绑定再连接，退后台关闭，断线后前台间隔 30 秒重试，退出会话清理；迟到注册查询不能恢复已关闭连接。提供已认证连接事件供后续对端握手/选路接入。

实际 SUPERVM WSS 与原生密钥测试确认前台认证及心跳、后台关闭、返回前台新连接、待查询期间关闭后迟到结果被丢弃。一个集成测试通过，三文件静态检查通过。测试绑定关闭 Flutter 的模拟 HTTP，使用真实网络，仅测试客户端信任本机测试证书。未在手机启用上述配置，重试计时和手机系统后台行为尚未实测，正式消息仍走既有 CCSOP 路径。

## 已连接中继通道的设备绑定复核（主机验证）

会员绑定握手建立的中继通道每 15 秒重新查询本机和对端绑定，并核对原握手公钥；提供 revalidate 供后续设备/关系事件主动触发。并发复核共用请求，复核期间暂停收发。查询拒绝、失败或超过 5 秒时关闭该对端通道，保留共享中继连接。没有把目录的 cacheSeconds=0 改为可用于新连接的缓存授权；已有通道仍存在周期检测窗口，不宣称即时撤销。

实际 WSS 测试在成功握手和断线补发之后撤销注入目录中的绑定，确认主动复核关闭消息流、并发只查一次本机和一次对端；之后发送被拒绝，发送方加密日志保留待发，接收方无消息。六项相关测试通过，四文件静态检查通过。自动定时触发、线上撤销事件及手机生命周期尚未完成真实验收；这一策略当前依赖 CCSOP 权威目录，不代表离线去中心化授权已完成。

## 会员设备绑定接入中继握手（主机集成通过）

补充拒绝路径：设备目录没有有效绑定时立即返回 NETWORK_KEY_DENIED，握手二次查询出现公钥变化时拒绝继续，收到的信令同时校验本机目标地址。实际 WSS 测试确认拒绝在两秒内返回，同一中继连接随后仍能完成有效握手及补发；六项相关测试通过。这不代表已建立通道能实时接收线上撤销事件，后续仍需接入生命周期管理。

新增 MemberRelayHandshake，连接前读取会员设备目录，握手开始、应答和完成通过现有绑定层重新验证本机及对端设备。使用协议已签名的 session_id 匹配应答，不依赖中继不会保留的扩展编号。会话变化、断线和八秒超时会清理握手；成功后由加密帧通道接管。

实际 SUPERVM WSS、原生密码库、断线重连补发及设备绑定测试共六项通过，静态检查通过。会员目录使用注入测试数据；尚未验证线上会员目录与两台手机的完整中继链路，也未启用正式聊天自动选路。本次没有安装手机更新。

## 对等通道中文长度与普通聊天统一（主机真实验证）

修复对等日志原先按 4000 UTF-8 字节限制、导致中文约 1333 字就失败的问题。现在与 Flutter 输入及 CCSOP 字符串规则一致：最多 4000 UTF-16 单元；接收端先限制总字节最多 12000，再解码检查文字长度。512 字节分片上限从 8 增至 24，保持加密帧 976 字节载荷界限及接收队列/同时重组数量限制。

实际 UDP 丢回执恢复与实际 SUPERVM WSS 测试分别传输完整 4000 汉字；接收文本、加密 SQLite 持久化和同 ID 去重通过。日志重开保留 4000 汉字，4001 ASCII 字符及超过同一 UTF-16 上限的 emoji 被拒绝，未写入无效消息。三个相关集成/持久化测试通过，静态检查通过。此改动扩展尚未启用的对等文本协议接收范围，后续设备协商必须匹配该能力；未宣称旧接收器支持长文本，未接手机正式路由。

## 对等文字中继重连补发（真实主机集成通过）

PeerTextFailover 新增 resumePending，从同一账号/可信对端的加密日志分批读取未确认消息，经既有选路策略发送，保持原 ID。并发恢复调用共用一次执行；途中断网或权限变化会停止，尚未确认记录留待之后恢复。

真实 SUPERVM 集成新增关闭双端 WSS→发送返回失败→发送方 SQLite 仍有待发、接收方无该消息→双端重新完成中继和对端签名握手→两次并发恢复→只打开一次新中继通道→接收方仅一条原 ID 记录、发送方待发清空。包含之前直连成功、丢回执转中继与重放验证的完整测试用时 21 秒通过，静态检查通过。未使用模拟网络回执。此项未验证手机进程冷启动，也未接正式聊天生命周期与 CCSOP 历史，仍非手机自适应功能交付。

## 对等文字直连转中继策略（主机验证，尚未用于手机会话）

新增 PeerTextFailover：对已经鉴权的直连通道先尝试收件回执，默认 3 秒超时或失败关闭直连，再建立/复用中继通道。两条通道必须属于同一可信对端、同一加密本地日志；发送前落盘，切换保持原消息 ID，最多排队 8 条且串行切换。中继连接默认 8 秒截止，迟到创建的通道会关闭，登录或业务权限失效停止发送。双通道均失败时保留本地待发送记录，不伪造成功。

验证使用实际 UDP、真实 Rust 加密、真实 SUPERVM WSS 中继与 SQLite。先放行 UDP 回执确认策略返回 direct 且不打开中继，再由 UDP 代理丢弃回执，确认策略自动返回 relay、实际发生回执丢弃、接收端该 ID 仅一条、发送端待发送记录清空。通道发现、在线会员设备鉴权接线、手机运行入口、CCSOP 历史合并/回退仍未完成；此策略当前不改变 App 正式消息路由。

## SUPERVM 中继可靠文字通道（主机真实互通，未接正式自动选路）

NovoRudpRelayFrameLink 已把真实 WSS 中继接入既有分片、去重、加密 SQLite 落盘及收件回执通道。实际运行上游 SUPERVM 中继和 Rust 动态库，合成双端通过 3850 字符分片传输、同 ID 重复发送只有一条接收记录、反向文字以及会话失效关闭验证；原 UDP 丢握手/回执恢复测试也通过，共两项真实集成测试、无跳过。五个相关 Dart 文件静态检查通过，测试结束中继 active_connection_count=0。

未操作 A/B、未安装、未部署 CCSOP。正式 App 仍仅登记 NovoRUDP 身份，尚未自动选择直连/中继；成员设备信任与中心历史合并、自动回退和双手机验收仍未完成。详见 2026-09-15-novorudp-mobile.md。

## 媒体转发准备可取消（代码完成，真机验收待做）

真实转发页在确认目标后、写入发送队列前提供“取消转发”，该阶段也允许返回页面。取消会关闭本次图片/语音/视频/文件转发器并解除选择页忙碌状态；每次操作的序号阻止旧下载/上传结果继续入队，也避免旧任务结束时解除新任务的忙碌状态。发送队列写入开始后不再提供准备取消，避免将已入队消息误报为已取消。已经传到服务端的孤立媒体仍遵循既有未引用资产清理策略，本次不改变服务端媒体生命周期。

9 项转发页面测试和相关静态检查通过，新增延迟文件准备→取消→重新发起→旧成功结果到达→新任务取消失败回调的覆盖，断言队列始终无写入；原有多种媒体/位置的真实发送字段、幂等重试和账号失效测试保留通过。尚未重新打包、安装或进行 A/B 大文件取消验收。

## 入群审核申请人资料（代码完成，真机验收待做）

审核列表通过现有鉴权访客资料接口读取申请人昵称和头像，不再把内部账号作为显示名称。头像沿用 ChatMemberAvatar 的私有媒体缓存与路径校验；昵称/头像共用同一查询。点击申请人进入现有 PublicMemberPage，返回后重新确认审核列表和管理权限。读取失败显示默认头像及“申请人”，刷新可重试。权限、会话或前后台变化时清空页面的资料引用，迟到响应不能恢复已失权列表。

5 项审核页测试通过，涵盖真实页面资料路由、账号绑定、昵称显示、迟到资料隔离和原有同意/拒绝/失权确认流程；静态检查通过。这是接口注入的页面验证，不是线上图片下载或 A/B 新申请验收，未改审核结果与审批接口。

Android preview/profile ARM64 真实 API 构建通过，Gradle 64.5 秒、149.8MB，包含前一节点的审批通过实时入口；APK SHA256 `b50333bf6ea647e2ae62e8a5f089986e3249246540a6da2c38f38763cddec165`。保留工作区 onboarding 三文件修改，未将其纳入本次提交。A/B 尚未安装。

## 入群申请通过后的实时入口（代码完成，双机验收待做）

申请页原先提交后只保留一次申请回执，审批完成仍显示等待。现在监听群变更与连接恢复事件、应用回到前台，并提供手动刷新；通过现有群资料接口重新查询当前账号是否为成员，确认后显示“进入群聊”，进入真实群会话。查询不会再次提交入群申请。审批通知早于提交回执时会在收到回执后补查；重复事件合并为一次后续查询。会话失效或后台期间的迟到查询结果不能恢复入群入口。

13 项申请、二维码预览、审批页面测试通过，新增覆盖实时确认、无自身成员记录不放行、通知先于回执、手动刷新不重复提交、登录失效拒绝迟到成功；相关静态检查通过。沿用服务端 K260913000619 仅允许活跃群成员读取的接口契约，未新增数据库或部署。尚未安装/双机验收。拒绝、过期的实时精确状态仍需申请人查询接口支持，目前不根据成员查询失败推断被拒绝。

## 文字草稿恢复（代码完成，手机验收待做）

补充页面级验证：4 项 Flutter widget 测试通过，覆盖文字与引用恢复、取消引用后退出保存、迟到读取不覆盖新输入、发送队列写入期间输入下一条不被清理，以及队列写入失败保留草稿/重试成功清理。测试通过可注入的草稿存储运行真实 DirectChatPage，未替换页面输入或发送逻辑。相关静态检查通过。此证据仍不代替 A/B 真机验收；本次不重复打包或安装。

单聊与群聊输入按账号、会话分别写入安全存储，保存文本和引用信息；输入停止 300ms、离开页面和应用切至后台时触发保存。恢复结果不会覆盖读取期间用户新输入的内容。消息进入持久发送队列后才清理对应草稿；清理使用草稿 ID 比对，保留期间新写的其他草稿。发送沿用草稿 UUID，离线重试和重建控制器使用同一消息标识。突然杀进程仍可能丢失最后尚未落盘的输入，不保证操作系统终止前执行异步保存。

26 项草稿存储、单聊及群聊控制器测试通过；涵盖账号/会话隔离、迟到清理、会话失效、非法草稿以及使用固定草稿 ID 的丢失回执恢复。六个相关 Dart 文件静态检查通过。本节点尚未安装 A/B；输入后退出重进、引用恢复和发送过程中继续输入的真机验收待做。

## Recoverable photo drafts (implemented, native acceptance pending)

Photo selection now uses the existing verified private draft store with separate image-peer/image-group target namespaces. It validates the 20MB source bound before saving, restores a hashed private copy on re-entry, and offers explicit discard from the send preview. Missing/damaged copies can be replaced through the picker. Draft cleanup occurs only after durable message queueing, before upload-journal acknowledgement. No message is sent simply by restoring a draft.

Direct and group image queues accept a validated optional clientMessageId. The persisted photo draft UUID is reused on retry, including reopening the send flow after acknowledgement/cleanup interruption; existing callers still receive fresh IDs. Nine image queue and draft-store tests passed, including direct/group controller recreation, repeated submission with the same ID and single visible receipt, failed queue writes, membership rejection, scope isolation and session loss during draft copy. Changed Dart files pass static analysis and diff checks. Android profile/preview ARM64 build succeeded: SHA256 5bbded244ff516498afb0f86b161331182cc5a164c0bec121bb4ab7d54873f81, 157003848 bytes.

Not installed on A/B in this node. Actual kill/restart during photo upload, resuming the native picker path and two-device duplicate-count acceptance remain pending. The draft store retains one current photo per conversation; this is not multi-photo batch composition or an automatic background upload queue.


## Android location map handoff (native acceptance pending)

Location details now has an explicit View in Amap action. The Android bridge accepts validated microdegree coordinates, coordinate-system name and place name, builds only a fixed HTTPS Amap marker URI, and dispatches ACTION_VIEW. GCJ-02 uses coordinate=gaode; WGS84 uses coordinate=wgs84. Longitude precedes latitude and formatting is locale-independent. No chat text, account identifiers, authorization headers or address field are sent to the external map. Nothing opens until the user taps. The web URI may hand off to Amap when installed; otherwise the browser handles it. Map/browser launch failure retains the existing copy-location fallback.

Five location model/widget tests passed, covering unchanged coordinates, single pending launch, session invalidation and late-response suppression. Changed Dart files pass analysis, Android profile/preview ARM64 build passed. APK SHA256 bbe9f4b90f55031107fb2b49c25a97513e4d52901dc938395e005f26c468d142, 157003848 bytes. No phone installation or map launch performed: actual app/browser handling, coordinate placement and route navigation remain unverified. iOS map integration is not implemented and is not claimed delivered. This adds an external map handoff, not an embedded map or live-location sharing.

Official coordinate and URI reference: https://lbs.amap.com/api/uri-api/gettingstarted


## Open exported chat files (implemented, native acceptance pending)

Android file details now offers an explicit Open saved file action after a successful verified export. The native controller retains only its most recently completed document URI, addressed by the same random export operation ID; callers cannot pass an arbitrary URI or path to open. ACTION_VIEW uses the system chooser and temporary read permission, never write permission. Export MIME type is selected from the filename extension with binary fallback. Cancellation/disposal clears the saved-document handle without deleting an already completed export. Repeated open taps are suppressed. No files open automatically after download or save.

Four exporter tests passed for successful save, cancellation, authorization revocation and disposal, including ensuring only a successful live export can dispatch openSaved and that disposed exporters cannot launch it. Changed Dart files pass static analysis. Final real-API Android profile/preview ARM64 build succeeded, APK SHA256 a8e5e38a63106ba0df711fd39853b17318c8b56004ef87b25145bea1229ee1ee, 157003848 bytes. Not installed on A/B in this node; external viewer availability, document-provider behavior and actual PDF/text rendering remain pending native acceptance. A availability clarification remains pending; no touch actions performed.

Android URI permission reference: https://developer.android.com/training/secure-file-sharing/share-file


## Group cached-history revalidation and update package

Group history pagination now displays its disk page immediately and revalidates the original page boundary with the server. A transient network failure preserves cached viewing; explicit CHAT_GROUP_ACCESS_DENIED follows the existing membership-revocation path to clear memory/disk and disable send access. Added delayed-response tests verify both outcomes after a local cache hit. Twenty-two group controller/history tests passed, changed-file static analysis and diff checks passed.

Built the real-API profile/preview ARM64 package successfully, including the earlier direct-history and image replacement fixes. APK SHA256 447c3722f18334ae56d2c6a8ac7dcbe561470be8d702fb275527660b8926a3c7; size 157003848 bytes. Build artifact: build/app/outputs/flutter-apk/app-preview-profile.apk. The package has not been installed on either phone in this node; touch operations remain paused. No group settings, membership or messages were changed by this verification.


## Cached image replacement isolation

CachedMediaImage now creates a fresh FutureBuilder state for each replacement request so the previous successful image is not retained while a new URL, content identity, headers or private/public mode is loading. The normal cache remains shared; an optional cache dependency enables controlled pending-response verification without external downloads. A widget test confirms the first image disappears while its replacement is pending and late completion after disposal does not restore an image. Together with existing media-cache coverage, nine tests passed; changed files pass static analysis.

This fixes stale-image display during replacement, including reused avatar/content widgets. It does not revoke files already downloaded to a device, establish end-to-end encryption, or prove open-media permission revocation while offline. The existing profile media route listens for parent visibility invalidation; full two-phone acceptance remains pending. This source update is not yet installed on A/B.


## Profile privacy authorization acceptance

The deployed content permission gate passed a rollback-only database check using the authorized test pair: onlyChat denied the peer viewing owner content, reverse direction and self access stayed allowed, either block side denied visitor content, and rollback restored the original follow/block/onlyChat flags. No persistent test relationship changes or events. Backend regression coverage and verification passed (93 files, 386 tests); the existing six public-profile widget tests passed, including late-response invalidation and retaining chat access when content is restricted. Full two-phone settings/open-media acceptance and offline cached media revocation remain unverified. No phone installation or touch actions in this node.


## Older history refresh and live call metadata check

Direct-history pagination now renders the local older page immediately, then requests the same page boundary from the server to update call metadata and authoritative content. It no longer returns permanently after a disk hit. Offline failures retain displayed cached records. No cursor advance is made by metadata refresh. Nine direct-history tests passed, covering older cached rows appearing before a pending network response, persistence of refreshed video-call metadata, unchanged cursor, offline cache access, remote clear and generation protection; changed-file static analysis passed. This pagination update is committed but not installed on devices.

Read-only verification against the deployed metadata helper checked six actual ended-call message rows: all six projected metadata (four audio, two video). Only aggregate counts were returned. This establishes runtime data projection, not native redial acceptance. A/B touch operations remain paused; neither native redial nor B's updated call icons are claimed verified.


## Call metadata release and cached-history compatibility

Backend metadata deployed as kingclub-v2-api:call-history-metadata; TURN configuration, no-active-call guard and runtime health/readiness/status checks passed. Initial native A check showed existing audio 00:15 and video 00:29 records, but old disk rows lacked new metadata/icons. Added one background latest-page refresh after incremental catch-up when restoring cached direct history; rows remain visible and the sync cursor is retained. Eleven targeted history/call-widget tests passed, including metadata persistence and refresh-once behavior; changed-file static analysis passed.

The updated profile/preview ARM64 package built successfully and was installed on A. Final visual verification was stopped when A appeared in the system phone screen; native icons and record-to-call redial remain pending. B was not operated or updated. User confirmed audio can be heard clearly; no broader noise or cross-network acceptance is inferred.


## Native call-record actions (code verified, not installed)

Client support for the backend ca666ff optional call metadata is implemented. Valid direct-message records show an audio/video icon in the existing bubble and text style. Tapping uses the existing exclusive call launcher with the same media kind and the current conversation peer; the old call ID is not reused as a new request ID. Repeated taps and late completion after leaving retain existing protections. Plain text, malformed metadata and group records never become redial entries. Authoritative call records are excluded from client recall eligibility; server enforcement remains required.

Encrypted history caching retains only validated callId/mediaKind/endReason/durationMs and drops extra nested data; hidden/recalled messages do not retain the call payload. Sixteen targeted parsing, native widget call-entry and SQLite history tests passed, including reopening the database and cancelling a pending launch after leaving. Changed production files and new call tests pass static analysis. These are code-level results: the metadata backend is not deployed, this client is not yet built/installed, and actual record-to-call redial remains pending.

## 2026-09-15 16:57 Native video compression and group playback

Generated an 8-second synthetic 1920x1080/30fps testsrc2 video with 440Hz audio, without private media. Original size 20,071,696 bytes, SHA256 2dc044e30f183d43f59ca75824d9a9a3835544f5c45e7a432b8970ea9ec2a474. An interrupted download was completed and the local hash verified against the remote source before selecting/sending it on A. File name kingclub-ab-compression-20260915.mp4, sent only to the authorized A/B test group.

On installed c7f71f9 worktree package, native PLAN reported skip=false/cached=false/hdr=false, duration 8021ms. Encoder selected HEVC CBR mode 2, 1,500,000bps, 1280x720. Native output validation accepted 1,962,230 bytes (90.22% smaller). Send-stage elapsed times: optimizing 17ms, uploading 3382ms, processing 4869ms, queueing 9123ms, queued 9256ms. Actual UI displayed compression progress (captured at 53%), followed by a video card with the synthetic thumbnail and 8-second label. Tapping the sent group card opened playback; captured frame time advanced to approximately 1.23 seconds with the pause control active.

This is one synthetic high-bitrate sample, not a universal compression ratio or camera-video quality acceptance. Native validation checks duration and audio-track preservation; audible playback confirmation and B reception remain pending. No app code/package change was needed for this check. Current policy still preserves small/low-bitrate, HDR or unsupported exports as original rather than claiming compression occurred.

## 2026-09-15 App-level outbox recovery

Native A evidence: RESTART-GROUP-A-1643 was queued while Android had no default network, then the process was force-stopped before connectivity was restored. Cold-starting the App and opening the conversation list still showed the preceding message. Entering the group restored and sent the durable queued message, exactly once in the visible history. This exposed the missing App-level drain; page re-entry recovery alone is not sufficient.

The new foreground recovery worker reads the account-specific secure outbox on startup/resume, connection-ready and every 15 seconds while foreground. It reuses existing direct/group controller receipt validation, stable client IDs and group membership checks; it does not mark messages read. Visible conversation pages take over delivery and cancel that worker's controller before opening theirs. Session changes, backgrounding and disposal stop workers; failed items are left for explicit handling. No operating-system background execution or killed-App delivery is claimed.

Five recovery tests cover direct/group delivery without pages, repeated drain deduplication, visible-page ownership, closing during history and revoked group access/failed-message exclusion. Two existing acknowledgment/failure race tests and the session rebind test passed; modified source/test static analysis passed. The app smoke suite reported 14 failures; temporarily restoring app.dart and direct_chat_page.dart to HEAD (preserving all unrelated onboarding edits) reproduced 14 baseline failures, and task sources were restored afterward. This suite is not reported green.

Native acceptance at 16:51: built profile/preview ARM64 in 65.6 seconds, 149.7MB, APK SHA256 6c517ced59add404f9598b35e563134227b95e77a92547b74ffa9fe6e1583b5d. Installed only A (Success). A queued APP-RECOVERY-A-1651 with no default network, then was force-stopped while still offline. Both previous enabled network settings were restored. Cold start and opening only the conversation list, without entering the group, showed APP-RECOVERY-A-1651 as the new server-backed list preview. This directly verifies recovery no longer depends on reopening a conversation. B remains on the prior package; recipient delivery and broader media recovery are not claimed by this check.

## 2026-09-15 16:41 Sender offline group retry on A

A composed OFFLINE-GROUP-A-1640 in the authorized A/B test group. Before sending, both Wi-Fi and mobile data were temporarily disabled; Android dumpsys connectivity confirmed Active default network: none. A single send displayed the queued message with the waiting-for-network label. A finally block restored both previously enabled settings; subsequent reads confirmed wifi_on=1, mobile_data=1 and an active default network. Without another send or retry tap, the queued label disappeared after reconnection and only one copy of the test message was visible.

This is actual sender-side offline enqueue and automatic recovery evidence, not recipient delivery or server-level duplicate-count verification. The first screenshot immediately after restoration still showed queued; the later 16:41 screenshot showed completion, so no subsecond retry latency is claimed. B receipt, offline process-death recovery and acknowledgment-loss races remain separate acceptance items. No app code or installed package changed.

## 2026-09-15 16:38 Group transfer and file sender acceptance

Using the existing installed preview and real API, A transferred ownership of the authorized A/B test group KINGCLUB-AB-0915-OK to B. A then displayed itself as an ordinary member and B as owner; owner-only transfer/name-edit controls disappeared and the departure action became leave-group. Current owner is B. Recipient-side owner controls, transfer-back and removal/rejoin remain pending; B was not operated further while awaiting the concurrent-device-use clarification.

A selected the existing synthetic kingclub-ab-file-20260915.txt through the native Download picker and explicitly sent it to this group. The 69,675-byte file appeared as a 68.0 KB file message. After force-stop and cold start, the conversation list showed the file preview and reopening the group restored the file message together with prior text history. This verifies sender-side submission and restart retention only; B group download and content-hash verification remain pending (the earlier direct-chat file hash verification does not substitute for this).

The user confirmed voice clarity. No application code or APK changed in this acceptance node.

## 2026-09-15 16:30 群资料保留滚动位置双机验收

`8e2918f` 工作区 Profile/preview ARM64 包构建 68.3 秒、149.7MB，真实测试 API 与 NovoRUDP ARM64 ELF 检查通过；A、B 覆盖安装 Success、冷启动 Status ok。APK SHA256：`fe4f2e5212d930a1edcd67b9974b6ed266d738dfe31d081b329f8cd1cdde4405`。保留未提交 onboarding 三文件。

B 停留在群资料下方成员列表，A 将测试群名称从 `KINGCLUB-AB-0915-OK-R` 改回 `KINGCLUB-AB-0915-OK`。保存后 B 的群公告、开关、搜索框及两个成员行仍在原位置，未回到顶部；随后手动滚回顶部，名称已自动更新，证明保持位置的同时仍接收资料更新。角色保持 A 群主、B 普通成员。本次验收范围为群资料元数据更新，网络断开、踢人重入及角色改变导致行数变化等仍分别验收。

## 群资料滚动位置修复（代码验证完成）

群资料页收到群变更或重连通知时保留现有列表，待真实资料返回后更新，避免列表高度归零导致滚动位置被重置。权限拒绝、会话变化仍沿原逻辑清除资料。增加可注入事件流，与默认线上事件处理共用代码。

新增延迟事件回归验证：滚动到成员列表后触发变更，等待期间及返回更新资料后滚动偏移保持不变；随后权限拒绝会移除资料和开关。设置、成员管理、群主转让三套共 6 项测试通过，业务文件静态分析通过。本节为代码测试证据，手机结果另行记录。

## 2026-09-15 16:23 群刷新修复安装与管理员双机验收

`ceb7ff6` 工作区构建 Profile/preview ARM64，真实测试 API，NovoRUDP 设备绑定与 ARM64 ELF 检查通过。Gradle 68.2 秒，149.7MB；仍保留未提交的 onboarding 三文件。APK SHA256：`d0b2d21de55d96c3f02b3dd2ed0518902b1f63d7a5d49f2461eaeb30b2b7bce3`。A、B 覆盖安装均 Success，冷启动均 Status ok。

A 将测试群改名为 `KINGCLUB-AB-0915-OK-R`，B 当前会话自动更新标题。覆盖保存前后约 5 秒的 12 次原生截图采样（单次约 0.4 秒）中，两条消息气泡区域像素完全一致，未再观察到之前的消息清空；这是本次采样证据，不代表逐帧性能分析。

A 在成员菜单将 B 设为管理员并确认，两端成员行显示管理员，B 自动出现入群申请入口；A 再取消管理员并确认，B 管理员标识与入群申请入口自动消失。最终恢复 A 群主、B 普通成员，测试群保留。此次证明真实管理员授予/取消与两端 UI 同步，不替代完整服务端越权或群主转让验收。

新发现：群资料页收到变更通知时清空 `_details`，随后重建列表会回到顶部；后续修复应保留滚动位置，同时继续处理权限失效。踢人/重新入群、转让、群禁言及群媒体等仍待验收。

## 群资料刷新闪空修复（代码验证完成，待更新双机包）

群变更通知不再无条件清空当前历史；保留消息并重新同步群资料、成员权限和历史版本。服务端拒绝访问时仍清空历史，撤回导致的历史版本变化仍按原流程失效旧消息。新增延迟刷新测试，确认改名等待期间消息保持显示，随后名称更新；移出群的拒绝响应仍清空消息并关闭访问。

群控制器、历史缓存、退群流程共 22 项测试通过；修改的两个业务文件静态分析无问题。测试文件存在原有的非空断言与 if 花括号提示。本节点尚未重新打包安装，不将代码验证写成手机验收通过。

## 2026-09-15 16:09 双机建群、群消息与基础管理实测

沿用 15:59 安装包与当前在线服务。A 从聊天加号进入发起群聊，仅勾选获授权的 B，创建 `KINGCLUB-AB-0915`。A 发送 `GROUP-A-1602`，B 未手动刷新即在会话列表看到新群、消息预览和未读 1，底栏同时显示 1。B 打开群收到该消息并回复 `GROUP-B-1603`，A 当前群会话收到回复；两端显示真实成员头像及对方昵称。

A 在群资料将群名改为 `KINGCLUB-AB-0915-OK`，B 当前打开的会话标题自动更新；A 发布公告 `AB-test-only-20260915`，B 在群公告页读取到相同内容，普通成员页面无编辑入口。B 的资料页无群主转让入口、群名无编辑箭头；本次仅验证实际角色对应的界面，不以隐藏按钮代替服务端越权测试。修改群名时 B 消息区曾短暂清空后恢复，应继续优化非权限变更时的刷新体验。

停止并重新启动 B 后，群名、列表预览、双方历史消息均保留，已读角标保持清零。测试群保留用于后续验证。本次通过的是双人测试群的建群、双向文字、未读、改名同步、公告发布/读取和重启历史；扫码入群审核、邀请确认、转让/踢人/退群、禁言、群媒体和群通话仍不能标为全部交付。

## 2026-09-15 15:59 好友资料栏目修正版已安装

A、B 均覆盖安装 Profile/preview ARM64 最终包并成功启动，登录与历史保留。APK SHA256 为 `16404c011102aa72bf0cc598f39dbfb19da977f67f6651f7d56539c6217d0800`，最终构建 147.5 秒，149.7 MB，NovoRUDP ARM64 ELF 检查通过；包含保留的三处 onboarding 未提交修改，未将这些修改纳入本节点提交。

B 再次从聊天消息头像打开好友资料，作品/动态/相册已单行、左对齐，保持原白色资料面板与真实信息。首次修正包曾使整行居中，已追加左对齐并重新安装；最终截图复验通过。8 项资料/内容测试通过，追加单行宽度与左侧位置回归通过，静态检查无问题。本段安装信息取代下方较早的当前包记录。

## 2026-09-15 15:46 离线补收与好友资料实测

通过 ADB 停止 B 的 App，发送前后确认 B 无 App 进程。A 在真实会话发送 `OFFLINE-A-TO-B-1545` 后重新启动 B：无需手动刷新，首页底栏显示未读 1，会话列表显示同一消息预览及未读 1；打开会话看到消息，返回后列表与底栏角标均清零。本次证明接收端 App 退出后的冷启动补收，不代表发送端断网续发、后台系统推送或多端并发验收。

B 点击该消息的 A 头像，打开真实好友主页，显示真实头像、会员号、互相关注与私信入口。发现窄屏下作品/动态/相册按钮固定宽度扣除内边距后文字换行；已改为最小宽度并按文字自然扩展，保留单行，极大字体下允许横向滚动。320dp、1.35 倍字体的回归及已有资料/内容权限测试共 8 项通过。安装和真机修正结果另记。

## 2026-09-15 15:43 语音消息与文件双机复验

A、B 会话中均可见双向约 3 秒语音消息，用户明确反馈“能听清楚”。此前语音无声反馈在本次双机前台录制、发送、播放场景复验通过；不据此认定后台、蓝牙或群语音全部验收。

A 从原生文件选择器选择本次生成的无个人资料测试文件 `kingclub-ab-file-20260915.txt`，在发送页确认后 B 实时收到文件卡片。B 点击下载显示“下载完成，文件校验通过”，再通过 Android 原生保存选择器保存到 Download。仅从 B 读取该新文件，与 A 原文件比较：均为 69,675 字节，逐字节一致，SHA256 为 `94c7eaf31df54be5149faac4def828d8728e34f66381da23e0b39f104549ad27`。测试文件未通过 USB 预先放入 B。本次证明 A→B 在线文件发送、下载及系统保存；断点重试、大文件、转发及反向发送仍待验收。

## 2026-09-15 15:20 双机通话历史已补齐并实测

后端新增 112 通话历史迁移，结束事务同时生成持久聊天记录与双方通知。复用现有文本展示，兼容当前已安装 APK，无需重打包。完成接听后显示服务端计算的时长，未接听、取消、拒绝等显示对应结果；重复动作不重复生成，不改变好友许可或扣经验值。专用通话卡片与点击回拨尚未实现。

已核对并补回本日两次旧测试通话，旧连接时间没有保存，因此只显示通话已结束。修复发布后双机再次真实接通、挂断：双方聊天均显示“语音通话 · 00:15”和“视频通话 · 00:29”；B 会话预览和未读气泡更新，重新进入会话读取正常。后端 92 个文件共 378 项测试、隔离 MySQL 事务验证和在线运行检查通过。

发布回归曾发现基础 env 缺少旧容器的 TURN 覆盖参数；已恢复旧容器并修正发布脚本，最终完整继承有效运行配置，预检查中继配置后重新发布，并通过上述双机接通测试。

此前用户确认视频双向实时画面与声音正常；啸叫仅在两台手机靠近且同时外放时出现，属于本次测试的声学反馈条件，未据此声称所有降噪/回声场景验收完成。当前实测仍限同一 Wi-Fi、前台双机，不代表跨网络、后台、主网路由或群通话已验收。

## 2026-09-15 14:55 双机语音首次实测通过

A 从与 B 的会话发起语音，B 接听，两端显示通话中。用户明确确认“双方都能听到”。原生 WebRTC 日志显示远端轨道、DTLS 握手完成与 CONNECTED。B 挂断后返回通讯录，A 同步显示“通话已结束 · 01:03”。本次为两台已登录安卓、同一 Wi-Fi、前台语音；尚不代表跨运营商/弱网/后台/长通话完成。

同时观察到公网 TURN 候选连接超时，当前连接成功不能证明公网中继可用；也不能将 WebRTC 通话认定为 NovoRUDP 主网承载。视频通话随后进入实测，结果另记。

## 2026-09-15 14:52 角标修正版安装与重启复验

- 修正提交 `8d1cc6b` 已推送 XujueKing/main 并核对远端。Profile/preview ARM64 构建 96.1 秒，原生 ELF 检查通过；包含已有三处 onboarding 工作区修改，未将它们纳入本次提交。
- APK SHA256：`633c7fbf6c0a3ff1e5ac4adb905a007b2a3fd1a81a020ef01f0e6aebb6a93e72`。A 14:51:53、B 14:52:00 覆盖安装 Success，两端冷启动成功，当前进程 fatal/unhandled/native-link 错误匹配均为 0。
- 安装前 B 在真实会话发送 `TEST-B-TO-A-1451`，A 停留首页时底栏显示 1，会话列表同时显示未读 1。安装冷启动后 A 仍能读取三条历史，打开后未读清零；B 冷启动进入通讯录仍显示 A 的真实头像与资料。安装前两端通讯录都已确认列出对方。
- 待处理申请已经由用户接受，未为重新展示角标而删除好友或改写申请。新申请的顶部/底栏联动已通过定向自动化，但安装后的真实新申请角标仍待下一次申请复验。通话、群聊、去中心化路由验收不因本次文字通过而完成。

## 2026-09-15 14:47 双机扫码与首次消息

- 用户明确授权仅 A、B 测试会员之间执行好友申请、测试消息和通话邀请。B 扫描 A 个人二维码并发出申请，A 的“新的朋友”显示 1；用户在 A 点击接受后显示“已添加”。
- 双端截图确认 A 的“你好”到达 B，B 的表情回复到达 A，双方显示各自真实头像。仅证明这次在线双向消息，不等同于语音、视频、离线恢复或去中心化通道验收。
- 用户发现申请仅在“新的朋友”显示角标。修正为 ContactsController 同一个待处理入站申请数量同时更新通讯录标签与底栏消息总角标，处理/会话切换后清零；网络暂时失败保留最后确认数量，已处理和本人发出的申请不计入。
- 定向验证：联系人与 Shell 共 21 项测试通过，覆盖数量向上层传播及清零；本次修改尚待安装，不把自动化结果当成手机角标复验。
- 当前双机安装为 14:37 的字体适配版本，详见 app_layout/2026-09-15-text-scale.md；下文 14:25/12:29 是历史安装记录。

## 2026-09-15 14:25 A/B installation update

This entry supersedes earlier installation records. User designated the original PCLM50 as A and new PKL110 as B. Built the 55c57f4 worktree (including the three preserved, uncommitted onboarding edits) in profile/preview ARM64 mode with the real test API. Gradle took 69.1 seconds; APK size 157003848 bytes; SHA256 f4f5cdca82e75480ef365bcfd32e94633369e4954307afd8f20a171f0aea7d47. Native ARM64 packaging verification passed.

Explicit-serial ADB install -r returned Success on both devices. Package timestamps: A 14:24:54; B 14:25:24. Both start commands returned ok and both package processes were present. Startup error-pattern counts were zero for each current process. A data was preserved; B was a new preview installation. This is installation/startup evidence, not chat acceptance. B must log in with a separate test member before actual dual-account chat verification.

Both phones independently reached the actual local SUPERVM signed NAT observer and punch responder over Wi-Fi (responses verified on the computer). The App relay carrier also passed the actual WSS daemon test on the computer. Automatic App route selection, two-phone encrypted peer chat and media acceptance are still pending; new standalone transport classes are not claimed as active App routes.


## 2026-09-15 12:29 当前手机安装版本

本记录优先于下文历史安装状态。ec26f6c 工作区已构建并覆盖安装到测试安卓，保留 onboarding 三文件未提交修改。Profile/preview ARM64、真实测试 API；Gradle 65.7 秒，APK 157003848 字节，SHA256 3cbc23a7ac59c2cd1d4765b7dfe3a06636e64b3d2489241e3b81d16e9c9ce9d6，NovoRUDP ARM64 ELF 检查通过。

ADB install -r Success，lastUpdateTime 2026-09-15 12:29:59。首次 am start 返回 ok 后未查到进程，未据此判定启动验收；再次启动返回 ok，随后两次查到进程，当前进程日志 FATAL EXCEPTION / Unhandled Exception / UnsatisfiedLinkError 匹配数为 0。尚未验证实际页面交互与手机流畅度。

本包包含群通话摄像头切换、扬声器控制、邀请过期、TURN 续签、ICE 重连与媒体列表网络错误宽限等客户端修改。语音播放、视频发送生命周期、单人通话媒体回归合计 33 项通过；群媒体与会话相关 28 项通过。均不能替代真实双机验收。在线群通话服务仍未发布，视频本次新发送结果仍待确认。

不能把有入口、实现了控制器、受控HTTP测试通过或APK安装成功等同于双机真实交付。下表保留全部目标范围；“待验收”不是完成。普通聊天免费，不扣经验值，已确认UI保持。

| 功能 | 当前实现证据 | 未完成或未验收项 |
| --- | --- | --- |
| 文字单聊 | 真实双机在线互发、未读角标、冷启动历史及接收端退出期间消息补收/读后清零已通过 | 发送端断网续发、后台系统推送和多端并发一致仍待专项验收 |
| 好友资料/头像 | 本次双机通讯录和会话均显示对方真实头像，冷启动后好友仍在 | 资料页作品/动态权限、旧问题会员的历史数据不能据本次新账号测试一并认定通过 |
| 扫码加好友/互关 | B 扫 A，A 接受后两端列出好友并能互发；重启仍保留 | 修复后的新申请角标尚待下一次真实申请验证，不为验证而删除已有好友 |
| 备注/分组/拉黑/仅聊天 | 联系人仓库与real_friend_remark、real_relationship_groups等测试 | 名称含real的Flutter测试仍可使用注入HTTP，不能作为真实接口证明；双端权限/作品可见性验收待补 |
| 建群/邀请/扫码申请/审核 | A 真实建群选择 B，两端群列表、双向文字、未读及重启历史通过 | 独立邀请、扫码申请审核、过期/重复申请/退出重入验收待补 |
| 群管理/公告/转让/退群 | 群主改名同步到 B，公告发布/读取及普通成员无编辑入口已实测 | 转让、踢人、退群、禁言及真实权限变化仍待验收；群名刷新短暂清空消息需优化 |
| 单聊/群聊历史、撤回、隐藏 | 单聊双机历史、重进读取及语音/视频通话结果已实测；撤回、隐藏和群历史有实现 | 断网重连与多端隐藏/撤回完整验收待补；专用通话卡片和点击回拨未实现 |
| 表情/图片 | 现有发送和自定义表情入口；GIF/WebP处理已有验证记录 | 自定义表情云同步代码及隔离真实 SQL/加密 HTTP/双 WebSocket 通知验证通过；在线接口尚未发布，双机恢复及手机视觉验收待补 |
| 文件 | A→B 原生选择、发送、实时接收、下载及系统保存通过，69,675 字节文件逐字节及 SHA256 一致 | 大文件、反向发送、转发和异常续传仍待验收 |
| 位置 | 双机聊天历史已观察到真实位置消息及地点文本 | 不据此推定定位权限、手选地点和点击打开地图的全流程已验收 |
| 语音录制/播放 | 双机互发约 3 秒语音消息，用户确认“能听清楚”，本次无声问题复验通过 | 群语音、后台及蓝牙场景仍待验收；已有代码和自动化不替代这些实机场景 |
| 视频消息/压缩 | 发送页调用optimizer与服务端prepare；原生编码已实现 | 用户18MB新发送结果未确认；实际压缩大小提示c3d34ab已装，尚未取得这次新视频结果 |
| 单人语音/视频通话 | 用户确认双向声音、视频画面正常；挂断同步和双方通话历史/时长再次实测通过 | 限同一 Wi-Fi、前台；公网 TURN 连通、跨运营商、后台、弱网重连及长通话仍待验收 |
| 人声处理 | WebRTC请求echoCancellation/noiseSuppression/autoGainControl | 设备是否实际生效及噪声/回声/蓝牙场景未验收 |
| 群语音/视频通话 | 已接群成员选择、前台邀请接听/拒绝、原生媒体会话、静音、摄像头/免提控制及授权头像；服务端mediasoup及678–682信令在隔离环境验证 | 未交付：在线媒体服务/接口未启用，TURN与真实多机声音/视频未验证；后台持续通话、系统来电仍缺失。SDK替代对象测试不算实际媒体验收 |
| 语音转文字/语音输入 | 674本人录音、675单/群消息识别及编辑/查看入口已实现；隔离MySQL+加密HTTP+真实中文模型通过，隐藏/撤回拒绝已验证 | 未交付：在线服务未发布；手机入口已更新；长录音、自然噪声、并发容量及双机验收待补 |
| 金币/红包/礼物/卡券 | 部分展示入口，卡券分享暂未开放 | 真实资产事务及完整业务未完成，不算可用聊天附件 |
| 系统/锁屏来电推送 | 用户此前同意后置 | 仍未完成，保留在总目标中 |
| 加密与NovoRUDP | Rust签名/握手/AEAD、分片补传、设备绑定API；后台绑定入口已安装 | 手机登记结果未确认；高频worker、可信信令、NAT/中继/服务端自适应、跨进程续传未接齐，完整聊天端到端加密未完成 |

## 当前代码与手机版本（15:20 核对）

两台手机为 14:52 安装的 Profile/preview ARM64 包，APK SHA256 为 `633c7fbf6c0a3ff1e5ac4adb905a007b2a3fd1a81a020ef01f0e6aebb6a93e72`，以本文件顶部安装与实测记录为准。在线双人通话历史补丁为后端 `25d4a63` / `call-history-112`，已保留全部原运行配置，未重新安装手机。App 验收记录 `9a199ad` 已推送；群媒体、表情云同步和语音识别仍不能算在线交付。

## 历史代码与手机版本（11:50，已被上述记录取代）

当前手机安装记录为2026-09-15 11:50:56，基于4f02f9b工作区的Profile/preview包，包含群通话前台入口、媒体收发与会话状态绑定。后续续租门控、摄像头/免提、邀请过期关闭、成员头像等修复已提交但尚未安装。APK SHA256为26a56ceb20733c9dd0940b22e4f361b2609c2a722096dbf50b253db687bec2e9；构建保留用户onboarding三文件未提交改动。安装和启动不代表实际聊天全流程验收。详细构建及逐项范围见[群通话接入记录](2026-09-15-group-call-client.md)。

该轮通过ADB核对上述更新时间；当时只读docker ps确认在线kingclub-v2-api使用kingclub-v2-api:network-keys-v1，未运行新群媒体服务。源代码截至ee41988。此段保留历史范围，不覆盖顶部后续发布和实测结论。

## 后续执行顺序

1. 语音消息与 A→B 文件完整性已复验；继续复验视频压缩的实际发送大小及双机播放。
2. 基于已完成的双机扫码、头像、在线消息与重进历史，继续离线补收、失败重试、撤回/隐藏、备注/分组/仅聊天权限验收。
3. 完成公网 TURN 连通和跨网络通话验收，推进群聊/管理、群通话与语音转文字上线及真机验证。
4. 完成剩余资产消息、表情同步和系统推送，推进NovoRUDP可信会话/worker/自适应集成，不以普通服务器聊天可用替代完整目标。

本矩阵依据当前源码入口、控制器与已记录验证范围，不宣称逐行端到端通过。后续每一项以真实验收证据更新，不给虚假完成百分比。

安装更新：59b0bf4代码对应profile/preview ARM64包已adb install -r Success并启动Status ok；构建63.2秒、149.0MB，SHA256 0169e74acaea6bc36f9d51e2ccc63f604054d2ce96a72b60d62ee4365ae7235f。包含群语音权限同步修复及实际视频上传大小提示；此前待安装状态以本记录为准。实机发送/播放验收仍未确认。

## 语音识别接入进展

客户端bc597c8包含录音识别草稿、加号语音输入、消息长按转文字；后端db1b35c包含本人及消息识别接口102及真实隔离测试。Linux CPU单核合成短句识别4451ms，仅是该短句基准。数字全零静音绕过模型已实现，完整VAD和识别准确率仍未验收。详细证据见2026-09-15-transcription-integration.md及服务端同名开发记录。

在线发布命令被自动审批审查拒绝，返回blocked by policy且无具体原因，命令未执行。不能将独立测试容器跑通视为在线发布；该段为历史发布记录，手机当前版本以本文件最新安装记录为准。


## 07:38 手机更新实证

- 2ba651b 工作区构建：Gradle 61.4 秒、149.0 MB，Profile/preview ARM64；NovoRUDP ARM64 ELF 检查通过。
- APK SHA256：c6b2e0af74e8a563c90ed36a5ac60a8fb2528ef8924d1c58bc8243ce75dddcd3。
- adb install -r 返回 Success；包 lastUpdateTime 为 2026-09-15 07:38:17；启动 Status ok。
- 新进程启动日志未匹配 FATAL EXCEPTION / Unhandled Exception；尚未观察到设备绑定 READY，不据此宣称登记成功。
- 本次未修改在线服务。语音识别仍未发布，视频本次发送成功与压缩结果仍未确认；手机号、消息内容和媒体不写入此记录。


## 08:29 手机更新实证

- a058292 工作区构建：Gradle 63.2 秒、149.2 MB，Profile/preview ARM64；真实测试 API 配置及 NovoRUDP ARM64 ELF 检查通过。
- SHA256：1944a45c7d433e4b922367a1c56e97c7316a1ecc88ab727c205e9f26e36944dc。
- adb install -r 返回 Success；包 lastUpdateTime 为 2026-09-15 08:29:58；启动 Status ok。近期启动错误日志未匹配 FATAL EXCEPTION / Unhandled Exception。
- 构建使用现有工作区，包含未提交 onboarding 三文件修改，未更改或提交这些修改。
- 分块校验/HMAC/AES-GCM 后台计算及失败通话资源释放已在包内。手机当时锁屏，尚未取得新的视频发送、帧流畅度或音视频通话验收。
- 表情云同步客户端已在包内；676/677 及迁移103–105仅隔离测试通过，在线服务仍未发布。语音识别同样未在线发布，不算可用功能。

## 扫码关系当前只读复核

线上数据库聚合查询：现有1条accepted扫码申请对应的关系lowFollows=1、highFollows=1，双方blocked=0。关联两侧会员均为active/approved，账号active，双方kingclubChatMemberState行均存在。当前记录满足contacts查询中的互关、未拉黑、会员可用及状态行条件。本次只读，不修改关注、好友或会员记录，不输出账号及个人资料。

源码复核：resolve在同一pair事务中同时置双方follow=true；事务结束没有用旧pair对象回写关注状态。通讯录筛选同一对follow字段；客户端监听friend-request.changed和重连事件，并在回到前台刷新。资料页unfollow需要确认后显式提交。以上不能还原此前异常发生时的状态，不能宣称历史根因已修复，也不能替代手机收到列表的验证；下一步以新包中的实际通讯录显示和请求结果判断客户端问题。


## 08:50 手机更新实证

- 8915937 工作区构建 Profile/preview ARM64 成功：63.4秒、149.2MB，真实测试API地址及ARM64 NovoRUDP库检查通过；保留未提交onboarding三文件修改，不将构建称为纯提交快照。
- SHA256：13fd7360a3145943b6525d2ef98295f60e043eb0d7286538969857e6aa6ef867。
- adb install -r Success；lastUpdateTime 2026-09-15 08:50:34；启动Status ok。近期启动日志未匹配FATAL EXCEPTION / Unhandled Exception。
- 包含无关会话事件不打断语音、播放失败清除对应缓存并重新鉴权重试、表情同步记录恢复，以及NovoRUDP后台封包/解包和首轮文件发送代码。设备绑定相关10项真实DLL回归通过；UDP仍未被选为实际聊天传输。
- 手机仍处于锁屏状态，未自动录音/拍摄/发消息。语音可听性、实际视频发送和新代码手机运行效果仍待用户操作及双机验收。在线ASR/表情云接口未发布状态不变。

## WebSocket timeout ownership

The production reconnect path now owns the original native handshake after its 10-second timeout. If that handshake later succeeds, its socket is closed instead of remaining an unobserved authenticated connection. A late native failure/close failure is consumed; early failures still reach the existing bounded reconnect policy. The signed URL, encrypted event codec and old-epoch checks are unchanged.

Validation: targeted analyze and five controlled connection lifecycle tests passed (success, delayed success, delayed success with close failure, delayed failure and early failure). These tests use a socket fake to control timing, not a phone/network outage simulation. The change is not yet installed and does not by itself prove multi-device message delivery or resolve the earlier missed-message report.

## Actual delayed WebSocket upgrade check

Added an actual localhost HttpServer/WebSocket upgrade test. The server receives the HTTP request but holds its upgrade until the production connector times out; after release, the server observes the client's late socket close. No fake WebSocket is used in this case. Six connection tests and analyze pass. This validates native socket cleanup, not TLS, CCSOP authentication, handset reconnect or message catch-up.

## Cache-clear generation at request entry

MediaCache.get now captures the cache generation before asynchronous key derivation and propagates it through lookup/download. Old work is rejected before directory creation/network work and again before returning a cached file or shared pending result. Previously a request could begin before clear but adopt the new generation afterward; the cache-hit branch also lacked the final check.

Validation: 20 cache/playback tests passed; analyze passed. Added cases pause an existing private-file lookup while clearing and clear immediately after get before any network call; both old requests reject and no old cache file is returned. Controlled filesystem/network fixtures are not handset logout acceptance. Not yet installed.

## Contacts loading regression check

Inspected the current ContactsPage/ContactsController/ContactGroupsRepository: first-entry UI does not use an artificial delay, relation groups and request counts load independently of the contact refresh, previous contact snapshots survive refresh failure, and group repository operation numbers reject stale responses after newer loads/saves. No additional loading or cache change was made without evidence of a defect.

Ran contacts_controller_test.dart, contacts_flow_test.dart and contact_groups_repository_test.dart: all 23 passed. Coverage includes account reset, incoming request count, private avatar lookup, alphabet drag, retained rows during partial failure and stale group-version rejection. Widget/demo and injected-API tests are not phone or live-interface acceptance. The earlier live SQL eligibility evidence remains separate; the user's actual missing-contact case is still awaiting handset observation.

## Unread badge display consistency

Bottom navigation now caps its visible unread number at 99, matching the conversation rows and the user requirement. Real-chat initialization already uses zero instead of demo counts; this inspection does not prove the earlier fixed badge or missing-message report resolved. Existing shell and mute widget regressions: 12 passed. No handset acceptance; not yet installed.

## Conversation refresh ordering

Conversation list requests now run sequentially. Repeated pagination shares the pending request; first-page refreshes arriving during a request are coalesced into a trailing refresh. This prevents overlapping offsets from invalidating one another and ensures updates observed while loading are fetched afterward. Existing account-generation guards remain in place.

Validation: new controlled widget test holds pagination pending, repeats load-more and tab activation, then verifies one pagination request followed by one fresh first page and replacement of old rows. Together with group routing and mute regressions, four tests passed; targeted analyze passed. This is injected API evidence, not real handset delivery. Not installed yet.

## 09:14 手机安装

- 51ea94f 工作区构建 Profile/preview ARM64，保留未提交 onboarding 三文件变更。真实测试 API；NovoRUDP ARM64 ELF 检查通过。Gradle 60.7 秒，149.2MB。
- APK SHA256: abca640b6cbfed659f1a6522d45d96b35b4401a930304d1be40950a4da6a050e。
- adb install -r Success；lastUpdateTime 2026-09-15 09:14:14；am start Status ok，WaitTime 3026ms；近期 AndroidRuntime/flutter error 过滤无输出。
- 包含顺序刷新/分页、底栏 99 上限、晚到 WebSocket 清理、缓存清除 generation 修复及 NovoRUDP 接收队列/同进程续传修复。仅设备身份绑定已接客户端，实际聊天尚未选择 UDP。
- 手机检查时处于休眠；安装启动不等于用户视频成功发送、语音可听或双机验收。在线表情云/ASR发布限制仍未解除。

## Interrupted offline catch-up regression

Added a controller test starting with sequence 1, catching up through 51, failing the next page, then resuming at 51 and completing through 131. All 131 sequences appear exactly once; an intermediate network error retains the known send permission and clears after successful catch-up. Existing implementation passed without a production code change. Direct/group controller suites: 21 passed; targeted analyze passed.

This is controlled repository-response evidence only. It does not prove actual mobile WebSocket delivery, offline server retention or the historical friend-removal report resolved.

## Invalidated native stream cleanup

NativeCallMedia now catches track enumeration failure during release and continues stream disposal, peer close/dispose and speaker reset. The cleanup error is still reported; inability to enumerate tracks is not counted as proven capture shutdown. A controlled invalidated-stream test verifies the remaining resources are released. Native media/state controller suites: 23 passed, targeted analyze passed. Not yet installed or tested on a live call.

## Retry transient history opening failure

Direct/group controllers now discard a failed history-opening Future, allowing the next synchronization to reopen storage. Successful initialization remains shared. Account checks, encryption, hidden-history revisions and existing files are retained; persistent corruption is still reported rather than silently deleting or bypassing history.

Validation: 17 history tests passed, including new first-open failure then successful synchronization and encrypted real SQLite persistence for both direct and group histories. Targeted analyze passed. Injected open failure/server replies are not Android storage failure acceptance. Not yet installed.

## Profile privacy late-response check

Added a widget regression holding an authorized content response pending, delivering a settings change with contentVisible=false, then completing the old content response. Old works remain absent, the restricted-content notice remains and private chat remains available. All five public profile tests passed. No production change was needed; this verifies client generation handling with injected replies/events, not server privacy enforcement or two-device acceptance.

## Video upload copy recovery

Video retry now checks the cached upload copy is present and nonempty before reuse. A missing or empty copy reruns preparation from the retained source; cancellation is rechecked after filesystem access. Original media is unchanged. Twelve optimizer/send-lifecycle tests passed, including actual filesystem deletion/truncation with an injected native encoder, and analyze passed. This does not establish the cause of the user's earlier interrupted upload or measure native codec quality. Not yet installed.

## 09:32 原生压缩回调与安装

ChatVideoUpload.finish 在 cancel 编码器之前递增 generation，作废已排队的旧 worker/native 回调，正常完成与失败也采用同一收尾。原生 acceptable 已验证缓存文件非空、较源文件小、时长差<=300ms及音轨存在性一致。此处为源码核查，不代表完整解码或真实音质验收。

当前工作区 Profile/preview ARM64 构建成功，Gradle87.9秒、149.2MB；真实API及ARM64库检查通过。SHA256 ee25ef156de3856cb325c28af6cb33d96ed1c9abec01a5d0f03a448d549f01e0。adb覆盖安装Success，lastUpdateTime2026-09-15 09:32:32，启动Status ok。包括群成员搜索、缓存打开重试、通话清理和视频副本恢复；仍包含未提交onboarding三文件。真实发送、播放和双机通话未验收，在线ASR/表情云未发布。

## Pending messages before history restoration

Direct/group initialization now restores the account outbox before opening history. A temporary history failure no longer prevents pending messages from appearing. Recovery tests include a pending message and prove subsequent matching history acknowledgement reconciles it once and removes its queue entry. Thirty-eight history/controller tests passed; targeted analyze passed. Existing membership checks remain. Not yet installed; controlled storage/API fixtures are not handset restart delivery acceptance.

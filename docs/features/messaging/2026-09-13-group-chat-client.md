# 群聊客户端真实数据接入

GroupChatRepository通过现有认证账号客户端调用617–622；不将groupId冒充好友账号调用单聊接口。建群在编辑器存活期间对同名和同成员集合复用requestId，成员顺序归一；确认返回群ID后清理请求标识。建群表单跨进程恢复尚未完成。

GroupChatController从共享账号安全Outbox读取groupId匹配的消息，不读取单聊recipient项；文本先写安全队列再发送，失败保留原clientMessageId，历史确认后才删待发项。初次最近页和after补收、before旧历史、按messageId去重、已读提交、同步期间通知补读已实现。群权限拒绝清除已确认历史并禁新发，自己未发出的草稿保留。清空/销毁后的迟到历史不能重现。

验证：5项测试覆盖建群重试ID、丢失发送确认后从历史恢复且不重复发送/不读取单聊草稿、群权限撤销、同步中通知、清理后迟到响应；两文件静态分析通过。

状态：此批数据层代码尚未接群页面入口；群主/管理员/成员管理、客户端建群页、群列表和聊天布局继续开发。未部署042/043、未作双机验收；不将此数据层标记为可用完整群聊。服务端已有真实加密接口与MySQL并发测试证据见后端群聊文档。

## 发起群聊与会话窗体接入

加号菜单发起群聊进入真实608选人，617确认后才进入群会话；失败保留名称/选择并允许相同请求重试，登录变化清空选择并禁止旧请求导航。群聊天复用已确认DirectChatPage布局和输入面板，通过ChatSessionController展示契约分别调用单聊/群控制器，不将群路由到601。队列保持groupId与recipient分开。

群右上角读取619显示群名/角色/实际成员昵称，点击成员打开真实资料；群消息头像定位实际sender账号。群变更清空旧群历史后重新鉴权，群资料接收群变更/重连时卸载旧成员数据再读取。

测试：选人实际账号、建群确认前不导航、群内620发送、无演示历史、确认后移除待发队列，以及现有单聊/群数据层共15项通过。相关7个文件静态分析通过。服务端group-chat-043已部署，待新APK和真机验收。

仍未完成：群列表整合回主聊天列表、成员昵称在消息行显示、群头像、邀请/入群/管理员操作、完整群设置、多端双机验收。既有真实群媒体入口继续拒绝未接通操作，不伪造发送。

## 群会话合并主聊天列表

607统一返回kind/groupId后，主聊天列表复用原短线、时间、预览、未读和侧滑样式。群点击传groupId进群窗体，长按/侧滑已读、置顶、不显示分别走622/623，不把群ID送到单聊接口；群菜单不提供个人拉黑。免打扰值由群设置返回，图标及小红点沿用现有样式。

621的hiddenThrough应用于已缓存群历史及迟到确认，避免远端隐藏后旧消息重新渲染。16项聊天/群控制器/建群回归通过，主列表真实群记录（peer为空）、群置顶623和群路由专项1项通过，三文件静态分析通过。

当前群列表/设置服务已部署group-list-044；App本批待打包。上一批群创建/会话包编译通过58.9秒，设备仍未真机验收。群免打扰设置开关和群管理仍待接页面，不能宣称完整群聊完成。

## 群资料中的会话设置

群资料增加消息免打扰、置顶聊天，读取621返回的本人设置，提交623后重新读取确认；失败不显示虚假成功。群read事件刷新当前群设置，会话失效立即清空资料、拒绝迟到响应。仅修改本人群成员设置，其他群成员不受影响。保持黑底、香槟文字、短分隔线和原生页面动画。

验证：群资料设置与群列表路由2项widget测试通过；设置页和测试静态分析无问题。覆盖仅发送变更字段、失败保留服务端旧值、等待确认禁用重复操作、会话失效拒绝迟到回包。尚未真机或两端验收。

## 群消息发送者

群会话以619的成员账号映射真实昵称，收到的消息上方显示12dp灰色单行昵称；自己的消息保持原布局。成员资料失败时用真实发送账号兜底，不阻塞已有消息显示。群关系变化清除名称缓存；迟到资料响应不得恢复已清空历史。离群成员暂用账号显示，头像图片接入仍未完成。

验证：群控制器、创建群流程、群列表9项测试通过；2个实现文件静态分析无问题。新增验证按发送账号匹配昵称、清除后重新取新昵称、旧资料回包不恢复历史。当前为代码与自动化验证，尚未更新真机安装包。

## 重连资料校正

断线重连会使群成员昵称缓存失效，重新读取当前资料，保留已加载的历史页；成员资料独立版本拒绝旧回包。返回群资料页主动同步设置，不单独依赖WebSocket通知到达。

验证：10项群控制器/设置/列表测试通过，含重连发生在成员资料请求途中时丢弃旧昵称、追加同步取新昵称且消息仍保留；2个实现文件静态分析通过。

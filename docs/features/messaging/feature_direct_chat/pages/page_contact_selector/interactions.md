# 联系人选择页交互

- 搜索只匹配好友备注/昵称，300ms 防抖；不匹配消息正文、账号或手机号。
- 点击好友只打开确认层，不立即发送；切换目标需先关闭确认层。
- 发送前重新校验 ShareIntentRef、目标 friendship 与 allowedAction。
- 发送使用 clientMessageId/幂等键；结果未知先查询，不重复生成消息。
- 取消返回来源且销毁 ShareIntentRef；成功返回一次性结果。
- 对方被删除/拉黑、业务卡片过期或消息被撤回时使用稳定失败说明。

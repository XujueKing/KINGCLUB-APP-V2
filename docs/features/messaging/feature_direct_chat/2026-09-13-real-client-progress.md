# 真实单聊客户端接入进度

状态：进行中，尚未完成双机验收，不代表聊天系统整体交付。

真实登录会话启用服务端会话列表及文字发送；本地待发队列使用安全存储，先持久化再清空输入框。网络中断保留同一 clientMessageId 重试，服务端确认或历史补收后去重。演示会话不进入真实列表；尚未接通的媒体发送不会伪造成功。

会话设置已接入置顶、免打扰、本人隐藏游标和方向性“仅聊天”。仅聊天不取消互关，不阻断聊天；公开资料内容、单条作品和媒体授权入口的完整接入仍待完成。

## 清空与同步竞态

清空成功后增加历史请求代次并解除旧同步合并；旧历史请求、旧分页响应及旧请求错误不得恢复已隐藏记录。新同步立即可发出，旧请求结束不能清除新请求的合并状态。待发送队列不因隐藏历史删除。

## 验证

- 现有聊天相关七个测试文件共 31 项通过（日志 build/real-chat-test.log）。
- 本次控制器与原生入口测试共 9 项通过，含旧请求晚到、新同步并发、旧分页晚到两个新增场景（build/chat-clear-race-test.log）。
- 本次修改控制器及测试静态分析通过（build/chat-clear-race-analyze.log）。

未验收：实际加密接口到测试服务的端到端联调、双机收发、好友资料与真实通讯录、媒体及群聊、语音视频通话、主网切换。以上不能用本地测试结果替代。

## 集成回归

2026-09-13：聊天、联系人、扫码申请、备注及公开资料相关14个测试文件共63项通过（build/chat-integrated-test.log）。messaging/contacts/scanner/shell/router静态分析通过（build/chat-integrated-analyze.log）。Android preview profile arm64构建已启动，结果以build/chat-integrated-build.log为准；不得提前声称打包成功。

当前ADB设备462606d8显示offline，未覆盖安装。真实聊天036/037仅已在隔离集成库执行，尚未发布手机使用的服务。因此本批为本地客户端实现与回归，不是完整真机端到端交付。

# 历史记录按媒体类型查找（进行中）

复用现有历史搜索页，在输入框下增加图片、语音、视频、文件、位置五个文字筛选入口，沿用现有淡金/灰色样式。选择类型会收起键盘、清空关键词和旧分页；输入关键词回到文字搜索。结果仍点击进入现有历史上下文，由已有私有媒体组件处理播放/下载，不把搜索结果当作下载权限。

单聊604和群聊621调用新增可选messageType参数。媒体模式不传query。分页按类型独立，切换类型后旧请求不能覆盖新结果；退出登录清空，关系/成员变化继续重验，已隐藏/撤回消息由服务端排除。

后端121尚未部署，本页接线未安装，不能视为手机可用。真实SQL和加密HTTP分页与权限验证已通过（后端4da7590）；限定发布补丁已提交（bd51408）。仍待完整镜像验证、最小部署、手机语音定位及转文字验收。

本地验证：5项历史搜索组件测试通过，相关实现及测试文件静态分析通过。覆盖媒体切换、迟到响应隔离、分页、进入上下文和返回关键词搜索；不替代实机验收。

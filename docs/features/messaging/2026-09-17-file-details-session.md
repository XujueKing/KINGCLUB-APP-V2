# 文件详情失效状态

文件详情原来只响应消息删除，且删除后仍显示文件名。现在监听登录变化，取消读取和导出、隐藏文件名和成功状态、移除读取/保存/打开入口，提示重新进入会话。迟到的读取器初始化会立即释放；保存/打开失败回调不覆盖失效状态。删除消息也同步隐藏文件卡片元数据。

验证覆盖读取器初始化中、读取中、完成后登录变化，以及本地/网络完成与未完成状态下删除；结合导出器用例合计 14 项通过。两个修改文件静态分析通过。此为组件和导出器测试，不代替手机实际切换账号/系统保存器中的并发测试；未安装手机。

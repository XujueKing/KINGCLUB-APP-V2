# 群历史语音被无关单聊通知打断

历史上下文页原来只过滤无关群事件，收到带有其他 conversationId 的单聊设置、好友关系通知时仍会清空列表、停止语音并重载。改为复用已有媒体通知范围判断，与图片、视频和搜索页保持一致。

明确属于单聊的通知不再打断群历史；当前群通知、未知范围通知和重连仍按原规则检查权限。当前群已读通知继续后台刷新，隐藏消息、权限失效及退出登录仍停止播放。

验证：历史上下文刷新、媒体展示和语音播放共18项测试通过，修改文件静态分析通过。回归用例在实际组件播放状态下注入无关单聊通知，确认不重读、不停止，并继续验证当前群隐藏及退出登录会停止。尚未安装手机，不以组件测试代替实机播放验收。

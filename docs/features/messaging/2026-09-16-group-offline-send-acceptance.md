# A 群聊离线发送与重启恢复

使用包含 6f6ba95 的 A Profile 包，在已授权双机测试群 KINGCLUB-AB-0915-OK 中验证；本次未操作 B。

1. A 关闭网络后发送 `OFFLINE-GROUP-RESTART-0916-0600`，消息显示“今天 06:00”及“等待网络恢复…”。
2. 保持断网强制结束进程、冷启动并重新进入同一群，原消息及等待状态仍在。私有证据为 `build/phone-a-group-offline-restarted.xml`。
3. 恢复网络后无需点击重试，消息变成普通历史行，时间为“今天 06:01”，等待状态消失。Android 默认网络为 122。
4. 群详情内完整搜索测试文字，返回一条“我 · 今天 06:01”记录；私有证据为 `build/phone-a-group-offline-search.xml`。
5. Wi-Fi 和移动数据设置恢复为 1。本次无源代码变更，不提交私有界面文件。

此项证明 A 群文本离线排队、跨进程恢复、联网自动补交及当前服务端搜索无重复。尚未确认 B 实际接收显示，也未覆盖服务端已提交而确认回执丢失的场景。

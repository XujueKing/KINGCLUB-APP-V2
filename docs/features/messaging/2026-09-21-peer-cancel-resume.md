# Peer 下载主动中断后续传

主动取消或关闭页面曾同时关闭接收许可，导致关闭时无法保存已收齐的分块。现在把接收许可与本地分块保留条件分开：网络立即停止，完整块保存进原账号、原消息的加密续传缓存；每块写入前后检查登录态和删除状态。退出账号或删除消息不保留这些分块。完整文件仍必须通过原 SHA-256 和权限检查，未增加任何绕过授权的路径。

回归用真实磁盘 receiver 预装两个块对应的片段，并在尚缺尾部时取消、销毁页面、退出账号或删除消息。取消/销毁后重建 downloader 仅通过 HTTP 请求块 1、2，不重复块 0；账号/消息撤销场景不复用旧块。片段跨 1 MiB 边界的未完整块不会保存。

验证：下载、媒体入口、准备顺序与新增中断回归 59 项通过；原本需要 native 库的 5 项另设 NOVORUDP_NATIVE_LIBRARY 执行，全部通过（真实加密、乱序丢包修复、错误对象/哈希、ACK预算、退出清理）。静态分析通过。尚未把本修改安装到双机，不作为公网断线实测结果。

双机更新：44958bab 的 profile APK 已在 A、B 覆盖安装成功并启动，SHA-256 为 `4dec3107b55d7e3178cf18a5a7000d72277d212f854c8b626350531c7a8f3d24`。上述“尚未安装”描述为自动测试时点；实际断线续传验收仍待进行。

## 8 MiB 实机文件路径（2026-09-21）

B 从聊天更多→文件发送合成 KC-RESUME-0921.bin，A 收到并读取，结果 8,388,608 字节，SHA-256 `7d212b9c884f5c77896de960ae17cc341cda43b14d6a971f34ca29ebd4badf7f` 与合成源一致。B 已持久化全部八个发送块，A 已保存八个接收块。

本次服务端出现文件块 0–7 的 HTTP GET，因此不能当作 peer 文件传输通过。取消按钮操作发生在读取完成之后，进入了系统保存选择页，已返回且未导出；不能把该操作记作中断续传验证。下一步定位大文件 peer 准备/授权/超时阶段，随后重新做中断实验。小图片/语音/视频 peer 通过的既有结果不能覆盖这个缺口。

大文件定位：为 profile/debug 构建补充不包含账号、消息、文件名、地址或异常正文的阶段计时（connect/prepare/receive/verify 与源恢复/发送）。release 不输出；只用于判断实际失败阶段，不改变超时或传输策略。静态检查通过。

## 连接与源文件准备分阶段（2026-09-21）

fe77f4f2 诊断 APK 已覆盖安装 A/B，SHA-256 `60ecbc8b3421c8020e553f1470cc130b17946ebaf69212080fe7a15afd137ca2`。B 通过文件入口发送新的 8 MiB 合成文件 KC-PHASE-0921.bin。A 在 connect 阶段 3005ms 超时；B 随后报告 source-ready，发送端授权/源恢复耗时 2157ms。证明原先外层 3 秒把寻址、接收授权与发送端缓存恢复叠加计算，提前终止尚在正常准备的传输。

修正：PeerFileChannel 在授权、建立 receiver、恢复本地块并发出 REQUEST 后返回接收对象；READY 等待与重试留在传输阶段，不再占用外层连接截止时间。接收器原有 8 秒无数据超时和 5 分钟总期限不变，拒绝立即关闭，权限续验与完整文件校验不变。新增发送端超过 3 秒才 READY 的回归，确认连接提前返回、REQUEST 重试继续、传输不被旧 3 秒边界误杀。负面授权测试同时等待接收结果，保证异步拒绝仍失败。

此修正的双机安装和实际大文件结果待后续记录，不代表跨网或中断续传已验收。

验证：静态检查通过，普通回归 44 项通过。启用原生库后的文件发送/取消续传回归 35 项通过、1 项失败：18 MiB 丢包和最终 ACK 丢失用例触发 `File transfer deadline`。该用例直接调用 sender/receiver，不经过本次修改的 PeerFileChannel，需独立复查；不能将整批原生回归标为通过。

## 大文件磁盘操作与重测

独立重测再次触发 90 秒上限。计时显示持续接收：50 秒约 10.7 MB，90 秒约 17.7 MB，未拿到最终 ACK；并非没有数据流动。将初始发送每片段读盘改为每 16 片段一个有界批次，保持原分片、密文、节流及补包协议；接收端连续片段写入省略冗余 setPosition，乱序片段仍按真实偏移定位。未放宽测试和产品超时。

改后真实加密 UDP 丢包/最终 ACK 丢失的 18 MiB 用例用时 87235ms，25484 个报文中注入/观测丢弃 3595 个，最终 SHA 校验及最终 ACK 重试通过。原生发送/取消续传/准备阶段 45 项通过，新增空文件及恰好 16 片段边界 2 项通过，静态检查通过。该调试环境耗时仍接近限制，不把它当作手机或公网性能承诺。

连接阶段修正版 f5a9b1d2 已覆盖安装 A/B，APK SHA-256 `c93fd6c8861ff338ee530615eb8ae5781266c1abdd6a703f077d6611f414ea1f`。本节磁盘批处理尚未打入此 APK。

双机实际结果：B 经聊天文件入口发送新的 KC-STARTUP-0921.bin（8 MiB），A 点击读取后显示“保存到文件”。A 私有输出 content.bin 的 SHA-256 为 `84513fa7f6e8e95f8fa841eaaeebd827c27e78e706d0932b32f0f483c8a43446`，与测试源一致。B 05:57:55 报 source-ready 2179ms；A 无连接超时日志。至 05:59 服务端 access.log 最近文件块 GET 仍为前一次 05:43 的失败回退，本次未下载 HTTP 文件块。证据确认实际聊天 peer 路径成功，不区分底层直连 UDP 与 relay，也不代表已完成中断续传。

## 实际聊天主动取消后 peer→HTTP 续传

115172f4 的 profile APK 已覆盖安装 A/B，SHA-256 `c97df6996b54201241ad2b48b7422e6d29afd3bb11c1c6cee648c92c6ba816c4`。B 从文件入口发送 KC-INTERRUPT-0921.bin，消息 `42b43942-1d6b-45bf-b0f4-fa73c8caf3f0`，大小 8,388,608 字节。A 点击读取约 12 秒后点击取消；截图当时仍在读取、进度约五分之一，诊断日志 receive 阶段 12549ms 结束。随后页面恢复“读取文件”，私有加密缓存只有 0.block（1048604 字节，含加密开销），没有完成标记。

暂时 force-stop B 的聊天预览版使对端不可用，A 再点读取。06:05:04–08 的服务端访问记录只出现该消息块 1、2、3、4、5、6、7 的 HTTP GET（均 200、1048576 字节），未请求块 0。缓存第 0 块保留 06:04 时间戳，其余块为 06:05，并产生 retained 标记。A 页面最终显示“保存到文件”，私有 content.bin SHA-256 `763dae415f7cfcbbda4b0b8dbbf0acb6327ccdd7af54cbe42a60359a01304adc` 与源文件一致。B 已重新启动，未卸载或清除登录数据。

该结果证明实际聊天文件在主动取消后复用 peer 完整块，由 HTTP 补齐余块；不将主动点击重试写成网络故障无感切换，也未区分此次 peer 的 UDP/relay 底层路径。公网跨网络、HTTP→peer 及各媒体实际中断组合仍需补足。

## 部分缓存不再锁定 HTTP

发现历史优化在存在第 0 块时直接跳过 peer。分块导入实现后，这会让已恢复的对端无法在新一次重试中优先使用。现移除该条件：完整持久化文件仍先离线恢复；未完成文件重新尝试已授权 peer，将缓存导入片段位图，只取缺失范围，失败才继续 HTTP。失败后的授权刷新和缓存重读也覆盖已有第 0 块的情形。

自动回归使用真实加密缓存和磁盘 receiver：取消后重新创建下载器，第二次连接仅注入第 0 块之后的缺失片段（包括跨块边界片段），确认 peer.completed 成功、HTTP 请求数为零、全文件内容一致。另保留 peer 不可用时仅请求缺块、缓存损坏、撤权、群文件和完整文件离线恢复检查。44 项回归及静态分析通过；此增量尚待双机安装和真实 HTTP→peer 场景。

## 实际聊天 HTTP→peer 接续

ada8c8e9 profile APK 已覆盖安装 A/B，SHA-256 `2366abfed14eff6c37326b557b91245bee8a93816c3c6e19bd9b2419f1003bd4`。B 发送新的 8 MiB KC-RETURN-0921.bin，消息 `061f3988-cec7-4f18-b134-c9f1e5e0bf94`；确认 A 收到后暂时 force-stop B。A 首次读取的 peer 连接超时，自动改 HTTP；约 5 秒后主动取消，缓存保留 0、1、2 三个完整加密块，无 retained。Nginx 06:15:13–15 记录块 0–3；第 3 块即使服务器已返回，也没有在取消前提交到本地缓存，不能当作已接收块。

重新启动 B，确认 relay 两个活动连接后，A 再点读取。B 06:16:13 报 source-ready（2189ms），A 的 receiver 文件持续增长，最终界面显示“保存到文件”，输出 SHA-256 `df6bc8ebf836078aeb78d1253830cc19c6f597c0b821b9a0fbfeb2cd3813304b` 与源一致。至完成，服务端该消息仍只有第一次的 4 条 HTTP 请求，没有新增请求。

该结果证明真实聊天文件可在 HTTP 部分缓存上恢复 peer 完成；与前节组成双向分块接续证据。此处含人工点击重试，不代表传输中断时自动无感切换；两手机仍同一 Wi-Fi，未独立证明本次 peer 底层是 UDP 或 relay。约 5 MiB 缺块耗时较长，后续还需优化恢复速度，不能仅凭内容正确宣称体验已完成。

## REQUEST 阶段路由恢复补齐

KC-AUTO-0921.bin 的实际下载在 06:21:29 已出现 receive TimeoutException（8447ms），早于 06:21:32 停止 B。随后 HTTP 块 0–7 完成，输出 SHA-256 `32efc86d12aee4a3f19efd7a83aec1bfaf3f0e9635d37b939f593d2a154a0871` 与源一致。因此只证明静默 peer 自动回退，不能计为传输途中断网接续验收。

代码核对发现 REQUEST 等待 READY 时没有调用已有路由恢复反馈。现连续两次 450ms 重试仍无 READY 时，仅调用一次 reportDeliveryStall，使已失效直连停止优先使用并重新探测；保留慢速源准备等待、原有超时和授权检查，不关闭共享连接。该修正不能证明上述真机超时的唯一原因，远端重启后的旧加密会话仍需验证。

10 项 peer_file_prepare 自动测试通过，包含请求在恢复反馈前丢弃、反馈后成功收到 READY 且停止重试，以及源准备超过 3 秒不提前失败；两文件静态分析通过。真机尚未安装此增量，不计入真机恢复已通过。

## 对端重启后旧会话恢复

复用 peer lane 原先仅 revalidate 权限。设备身份未变而对端进程已重启时，权限通过并不证明旧加密会话仍存在。现复用前通过加密 relay 帧交换随机探测标识，绕开可能失效的 UDP 映射；900ms 无响应后关闭该 lane，重新执行授权握手。保留其他好友和共享 relay，不改变 SuperVM 共识或服务端协议。并发打开仍合并，若此时收到新的合法握手则复用新 lane。

真实本地 SuperVM WSS 回归新增仅远端暂停/恢复场景：原身份不变，本端重新 connectPeer 获得新 lane，并验证新加密会话成功传输数据。原健康 lane 复用断言仍通过。运行 runtime、reconnect、live_relay、peer_file_prepare 共 18 项全部通过（没有跳过），三文件静态分析通过。该证据为桌面进程加真实 relay；尚未安装到 A/B，不能替代手机重启和公网跨网验收。当前手机静默超时与此问题是否同一原因仍未得到设备日志直接证明。

### 安装更新

随后从干净聊天 worktree 的 `3ecb00ab` 构建 profile 预览 APK，SHA-256 `9ac7f98ca29d9cfecaec8d254c657af8bfc5a8a243a2eb2df42a631cb9872c59`。A/B 均 `adb install -r` 返回 Success，已发起应用启动，未卸载或清除数据。初次构建因 D 盘空间不足失败；将旧动画 APK 和失败中间产物归档至 C 盘后重新构建成功。安装完成不等于真机恢复验收完成，下一步仍需实际下载、对端重启和途中断开检查。

## 真机传输中断后自动 peer→HTTP 接续

3ecb00ab 安装后，B 通过聊天文件选择器发送新的 8 MiB `KC-RECOVER-0921.bin`（消息 `e24b3755-f4ab-4bba-b3c0-37550148ea17`）。A 打开文件并点击读取。B 于 06:40:14 报 source-ready（2291ms）；A 的 `cache/novorudp-BGPRHO/receiving.part` 已增长至 2873344 字节。此长度并不代表这些范围全部连续接收，只用于确认开始写入；真正可复用完整块由后续 HTTP 范围佐证。

06:40:27 force-stop B，此后不点击 A。A 于 06:40:35 报 receive TimeoutException（整个接收阶段 25112ms），随后自动请求 HTTP **1–7**，全部 200；该消息没有块 0 的 HTTP 请求。06:40:40 最后一块返回，界面显示“保存到文件”。A 输出 `cache/kingclub-chat-download-WIODAJ/content.bin` 的 SHA-256 为 `790526f743ab5de0217eda3ec6b290d5bce0ca8ef055d5072af40d18d348f941`，与 B 发送源文件一致。完成后重新启动 B。

该证据验证实际聊天文件传输中的自动失败回退和完整块复用，不依赖人工重试。仍未单独区分此次 peer 数据走 UDP 还是加密 relay，不能冒充公网 UDP 验收；约 8 秒无数据等待也仍需评估体验。对端重新上线后的新传输恢复以及各媒体类型真实路径仍需继续验证。

## B 恢复上线后的实际新文件传输

B 重启恢复后通过系统文件选择器发送 `KC-ONLINE-0921.bin`（8 MiB）。A 未重启，在同一好友会话打开新文件读取，B 06:44:26 source-ready（2242ms）。最终 A 输出 `cache/kingclub-chat-download-NNSGOF/content.bin`，SHA-256 `2a7ed4446dbaca6d9162182728a96018d3bfdfae50b60a8c2d7906a7932dca3e` 与源文件一致，界面显示“保存到文件”。截至完成，服务器最后的 chat-file GET 仍是前一轮 06:40:40，没有本轮 HTTP 文件下载。这证明对端恢复上线后，新传输可重新使用 peer 完成，不会永久停留在 HTTP。

为区分中继与 UDP，06:44:43.060–47.344 临时挂起已核对路径的本机测试 relay 进程，finally 恢复返回 0。暂停期间接收临时文件长度依次为 3756624、4285616、4790208（约每 2 秒观察）。此现象支持 UDP 仍在传输，但短暂停顿不能完全排除先前缓冲数据，且文件长度不等于连续接收范围。因此不把此轮标记为全程纯 UDP 或公网直连通过；后续需要直接路径计数或更充分隔离证据。

## 认证入口路径计数

传输层新增仅含 stream/object、帧类型、载荷长度及 UDP/relay 标志的观察流。在各入口解密认证及权限检查之后记数，文件下载按自身 stream/object 过滤，排除 ACK、DONE 和控制帧。关闭下载时仅 debug/profile 输出四项计数，不输出身份、地址、路径、聊天内容或密钥；不持久化或上报。计数包含重复数据和修复载荷，是入口流量证据，不等于唯一下载进度。

真实本机 SuperVM 集成分别运行 relay-only 与启用 LAN 的两组 runtime 测试：前者文件为 relayFrames=5、relayBytes=4097、UDP=0；后者等待两端直连就绪后，文件为 udpFrames=5、udpBytes=4097、relay=0。两种文本路由策略各通过，共 4 项真实集成测试。额外取消续传、准备、接收回执 18 项回归通过，四文件静态分析通过。该增量尚未安装到手机；此前短暂停 relay 的手机记录不因此追溯升级为纯 UDP 通过。

## 手机路径计数实测

A/B 覆盖安装 a131c4fe profile，APK SHA-256 `969fde77bb89ceb11114051ce7ad58e91938b0415c5f80336b144993dd43a09d`，两端 install -r Success。B 从聊天文件入口发送新 2 MiB `KC-PATH-0921.bin`，A 打开读取。B 06:54:17 source-ready（742ms），A 06:54:34 报认证入口计数：udpFrames=2596、udpBytes=2533424、relayFrames=145、relayBytes=141520。计数包括修复和重复数据，不能将总字节当作唯一文件进度。

A 最终显示“保存到文件”，输出 `cache/kingclub-chat-download-AQWUKW/content.bin` 的 SHA-256 `7c05b57c3ea79036b2c023d5043e6dd173524412e0b6bfa803c00ac723f9588b` 与源一致。服务端 chat-file GET 的最后记录仍为前轮 06:40:40，无本轮 HTTP 文件下载。该证据明确证明实际手机聊天下载使用 UDP 数据路径，同时存在少量 relay 数据；记录为 LAN UDP/relay 混合完成，不标为全程纯 UDP、公网穿透或所有媒体通过。路径稳定性、传输耗时及修复开销仍需继续评估。

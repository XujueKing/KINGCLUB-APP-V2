# 关系权限页交互

- 普通权限以命名 patch + expected relationshipVersion 提交；失败回到权威旧值。
- 拉黑、删除、解除拉黑分别使用独立幂等命令和确认文案。
- 拉黑/删除成功后关闭设置页并返回更新后的用户主页或安全列表。
- 解除拉黑成功后状态为 stranger，不显示“好友已恢复”。
- 版本冲突或结果未知时禁用旧按钮，重新查询后渲染完整动作组。
- 不把 conversationId、账号、权限正文或服务端原始错误写入 URI/日志。

# 发送好友申请页交互

- 发送前校验 targetRef generation、字段和当前 allowedAction。
- 命令携带一次性幂等键；提交期间禁用返回外的重复动作。
- 超时进入 `resultUnknown` 并查询权威状态，不生成新幂等键盲目重发。
- 有草稿返回时确认放弃；成功后清除草稿且结果只消费一次。
- 对方已成为好友、已有申请、码失效或被限制时展示稳定状态，不显示服务端原文。

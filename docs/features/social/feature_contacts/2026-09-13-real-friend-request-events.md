# 好友申请实时更新

真实新的朋友页面监听 chat.friend-request.changed 和 connection.ready，回到前台也重新读取611。刷新整批读取成功后替换，迟到响应通过generation失效，显示仍来自真实申请记录。

登录变化清空列表、释放仓库、关闭当前申请处理面板，禁止再次读取旧账号或处理旧申请。接收/拒绝等待610确认，不提前标记成功，失效后不弹旧操作成功提示。

验证：新的通知/重连/退出登录迟到响应和底部处理面板清除测试、既有真实接收与入口回归合计8项通过，单文件静态分析无问题。

未完成：申请列表真实昵称和头像、双机端到端通知验收。此批不宣称完整好友申请体验交付。

## 通讯录申请数量与并发刷新

新的朋友红色数量改为611真实分页中的本人收到且pending的申请数，按requestId去重，排除本人发出、已接受和已拒绝。超过99由既有UI封顶显示99。进入通讯录、手动刷新、通知和重连更新；失败保留最近一次确认数量，登录变化清零，旧请求结果不能回填。

关系更新期间正在进行608刷新时，标记需要补读，完成后再读一个快照；多条通知合并，避免漏掉刷新发出后发生的接受好友或拉黑变化。

验证：20项通讯录/申请/入口回归通过，补充无好友时仍显示真实申请数量和登录变化丢弃迟到数量后，通讯录专项10项通过；两文件静态分析通过。数量当前读取完整611分页，后续大规模数据需改为服务端索引计数，不把此实现称为百万历史下的性能验收。

## 申请人资料

列表优先使用611的真实昵称，兼容旧服务回退账号。头像使用与好友资料相同的访客凭证、账号隔离缓存和42dp原布局；无授权图片保留文字占位。收到关系变更先清空旧授权列表再读取，登录变化继续卸载旧资料。8项页面回归与2项真实申请专项（含实际昵称显示和接受真实requestId）通过，静态分析通过。后端加密611和媒体权限集成通过；此批返回字段尚未部署、App尚未打新包，手机验收未完成。

上一批e97059b申请数量APK已编译通过（58.4秒），设备仍offline未安装。

发布更新：申请昵称及头像服务已部署requests-d158ace，App对应aac8119包编译通过58.0秒（build/request-profiles-release-build.log），设备offline未安装；真机验收仍未完成。

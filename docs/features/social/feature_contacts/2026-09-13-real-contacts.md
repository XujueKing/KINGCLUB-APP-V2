# 真实通讯录接入

状态：进行中，真实登录模式的通讯录页面已接入读取层。

依据用户批准的完整真实聊天与“互关即好友”规则，使用已登记 K260913000608 接口读取新版联系人，不迁移旧会员、不合并演示联系人。数据库与旧业务来源审查沿用 ../../messaging/feature_direct_chat/database_review.md；本批无新增服务接口或数据库对象。

ContactsController 绑定 MessagingRepository 的登录账号。完整分页成功后原子替换内存快照，按账号去重；中途失败保留旧快照并返回错误状态；空分页却声明继续时停止，避免无限请求。再次读取成功会移除不再是好友的账号。空备注回退昵称，性别未填不推断。会话失效清空快照并使迟到结果失效。

测试 contacts_controller_test.dart 四项通过：原子分页与去重、失败保留及关系移除、账号失效隔离、异常分页终止。

待完成：通讯录页面订阅真实数据与会话变化、头像公开资料页面、中文拼音索引、媒体头像授权、好友申请与关系分组真实页面。当前页面仍为演示模式，不将此读取层验收等同于通讯录交付。

## 好友主页当前切片

新增 PublicMemberPage，从真实单聊对方头像进入，读取612公开资料，头像/封面跨圆角资料区布局按用户9月13日截图；白色内容区、签名、个人标签与作品/动态/相册页签已建立。关注操作走602，私信以真实peer打开。测试验证仅聊天仍可私信，且不显示截图中的演示会员。

未完成：实际头像/封面文件、统计数字、内容列表及其媒体授权、主页设置入口、与截图同视口视觉比对。当前使用缺省头像区域，不能标为视觉复刻完成；未部署此页面。

## 页面接入验证

ContactsPage.realData 从 AppShellPage.realChat 传入；真实模式不拼接演示联系人、不显示固定2条新好友角标。分页快照用于现有短线列表与搜索，点击联系人使用服务端peer进入PublicMemberPage；保留演示模式用于原有UI回归。预读不显示全页人为加载，失败保留完整旧快照。chat.relationship.changed / chat.friend-request.changed / chat.settings.changed / connection.ready 通知重新读取，账号变化清空并废弃迟到响应。

本批25项测试通过（contacts_controller_test、contacts_flow_test、app_shell_flow_test），静态分析通过。包含真实联系人点击账号断言及无演示联系人；不是双机验收。中文拼音尚未生成，目前非拉丁首字符归#，后续必须补齐；头像媒体、新朋友/黑名单/分组页面真实接入仍未完成。不宣称通讯录全量交付。

## 新的朋友

真实登录从通讯录进入FriendRequestsPage.realData。读取611所有分页，区分申请者和接收者；自己发出的待审申请不能接受；接收者接受/拒绝调用610，服务端成功后重新读取，失败不伪造成功。已接受记录以真实peer进入单聊。账号变化清理列表，真实模式不展示演示申请与Mock状态面板。当前申请列表显示真实账号，头像/昵称丰富化待接。

7项相关测试通过（real_friend_requests_test + friendship_entry_flow_test），静态分析通过；测试包含服务端未确认前不得显示已添加。尚未完成扫码提交入口联调，不将后端已有申请服务等同于端到端好友流程完成。

## 扫码申请入口

MemberScannerPage 识别507会员码后将原始code与扫描时绑定的MessagingRepository传入MemberCardPreview。非本人可填写最多120字说明并调用609；首次提交固定requestId及说明，网络失败重试复用，避免同ID变更内容。pending仅显示等待确认；incoming引导新的朋友处理；friends/accepted/rejected各自显示服务端状态；本人不出现申请按钮。

qr_friend_request_test两项通过：首次失败不显示成功，重试ID/原码/说明不变且不伪造互关；本人不显示申请按钮。静态分析通过。相机识别、真实签名请求及双机通过申请仍待运行环境联调，不把widget测试当作真机端到端验收。二维码到期仍需重新扫码。

## 真实备注

PublicMemberPage右上角进入FriendRemarkPage，606按本人+peer保存备注；成功返回后重新读取公开资料。保存失败保留当前输入且不触发成功回调，可重试；请求处理中禁用重复保存与修改。真实模式隐藏未支持的个人说明编辑、虚构来源和添加日期。通讯录及会话列表原有真实读取优先使用本人备注，设置事件触发刷新。

real_friend_remark_test与public_member_page_test两项通过，包含首次网络失败草稿保留、二次成功及真实peer参数。后续仍需双机运行验证；右上角完整设置菜单与拉黑管理待补齐。

## 访客页权限失效

公开资料页订阅chat.settings.changed、chat.relationship.changed和connection.ready，收到通知先清除旧资料再读取612，应用恢复前台同样校验。登录状态变化后终止此页面读取，旧请求代次失效，重连通知不能重新填回旧账号资料。当前事件没有目标会员字段，因此保守地对相关关系事件全部刷新。

public_member_page_test两项通过，覆盖异步通知送达后、HTTP未完成前已清除旧资料，退出后迟到响应与重连事件不能恢复，以及仅聊天可继续私信。静态检查通过。此项仅证明资料状态失效；作品媒体内容接口与缓存仍未接完，不代表完整内容访问撤销已交付。

# 关系分组真实客户端（进行中）

615/616对应ContactGroupsRepository；账号由MessagingRepository会话绑定。真实通讯录建立repository后后台读取，分组编辑独立加载；不退回本地Fake写入。进入编辑使用完整通讯录，不受搜索过滤影响。保存确认后才更新图标；一位好友移组会从其他组移出。

保持已选图标、名称、成员输入，网络失败可原样重试并复用requestId。服务端版本冲突留在编辑器，可先刷新分组再重新保存；刷新失败仍显示错误，最终保存继续由CAS判断，不静默覆盖。repository废弃迟到读取，保存中不允许重叠读取/保存。账号变化清空分组及编辑内容、禁止保存。

验证：13项测试通过，包含失败留编辑并原id重试、保存确认后才发布、迟到load不回退保存版本和现有通讯录回归；3个相关源文件analyze无问题。

未完成：分组删除入口、多端实时outbox通知与后台重连刷新；已非好友但仍保留在分组中的成员清理流程（服务端全量好友校验会拒绝包含失效成员的保存，需完善避免阻塞其他编辑）；本轮040/615/616尚未发布，手机安装包也未包含本轮改动。不宣称真机与多端分组完成。

## 删除与失效成员清理

编辑已有分组可删除，确认后使用616保存余下分组，失败留编辑页与原列表；同样请求重试复用请求号。仅更改分组不调用好友或会话变更接口。

真实分组列表增加清理失效成员：先完整分页读取608当前通讯录，展示人数并确认，将全部分组成员与当前好友取交集，保留组名与图标后CAS保存；失败不应用本地变更。先前失效成员阻塞保存的问题现在可通过明确清理流程恢复。操作期间禁用进入编辑与新建，账号变化取消结果。

5项分组仓储/编辑/删除/清理测试通过，相关源文件静态检查通过。仍待多端实时通知、测试服务发布、手机更新和真机验收。

## 分组通知与重连

收到chat.groups.changed或connection.ready时刷新分组；编辑器使用独立仓库版本，编辑期间保留草稿并延后刷新，返回时再读取。没有通知时保存成功直接采用服务端确认结果，避免多余读取。新增事件回归验证空闲重连刷新、编辑期间保留草稿和版本、返回后补读；4项分组页面测试通过，相关3页静态分析通过。

发布记录：服务端已运行kingclub-v2-api:groups-041，040/041迁移及运行检查通过，旧容器保留。App 5dd56c2关系分组包编译通过（build/groups-release-build.log，61.6秒），设备仍offline，未安装；后续好友申请通知变更尚需新包。

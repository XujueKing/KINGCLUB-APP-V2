# 关系分组真实客户端（进行中）

615/616对应ContactGroupsRepository；账号由MessagingRepository会话绑定。真实通讯录建立repository后后台读取，分组编辑独立加载；不退回本地Fake写入。进入编辑使用完整通讯录，不受搜索过滤影响。保存确认后才更新图标；一位好友移组会从其他组移出。

保持已选图标、名称、成员输入，网络失败可原样重试并复用requestId。服务端版本冲突留在编辑器，可先刷新分组再重新保存；刷新失败仍显示错误，最终保存继续由CAS判断，不静默覆盖。repository废弃迟到读取，保存中不允许重叠读取/保存。账号变化清空分组及编辑内容、禁止保存。

验证：13项测试通过，包含失败留编辑并原id重试、保存确认后才发布、迟到load不回退保存版本和现有通讯录回归；3个相关源文件analyze无问题。

未完成：分组删除入口、多端实时outbox通知与后台重连刷新；已非好友但仍保留在分组中的成员清理流程（服务端全量好友校验会拒绝包含失效成员的保存，需完善避免阻塞其他编辑）；本轮040/615/616尚未发布，手机安装包也未包含本轮改动。不宣称真机与多端分组完成。

# KC-P-013 视觉 QA

- 检查日期：2026-08-28
- 实现目标：旧版 `openVideo` 的全屏单作品浏览结构
- 当前结论：`blocked`

## 可用视觉依据

- 旧版源码：`C:\Users\Poplar\Desktop\KingClub-app\pages\openVideo\openVideo.wxml`
- 旧版样式：`C:\Users\Poplar\Desktop\KingClub-app\pages\openVideo\openVideo.wxss`
- V2 运行环境：Android API 37 模拟器，1080×2400

旧版仓库没有保存可与当前状态、同视口直接并排比较的运行截图。因此可以核对页面结构和运行边界，但不能把源码推断冒充成严格像素对照。

## 已完成检查

- 全屏纵向单作品结构、顶部“发现”、作者区、文案、公开位置、静音控制和底部导航均正常呈现。
- 1080×2400 设备语义树中所有主要元素均在 `[0,0][1080,2400]` 安全边界内。
- 当前内容分支被正确选中，其他四个底部入口可点击。
- V2 运行截图已人工检查；首次检查发现作者文案和静音键被浮动底栏遮挡，已将内容操作区上移并在复查截图中确认完整可见。
- 页面没有发布、点赞、评论、收藏、分享或关注写入口。
- 调试烧杯入口已从可见界面移除，离线场景仅通过长按标题进入。
- 合成 Fake 内容没有暴露真实账号、精确距离或媒体 URL。

## 阻塞项

严格视觉验收需要旧版相同页面、相同内容状态和可比较视口的运行截图。当前只有 WXML/WXSS 源码，无法完成“参考图 + V2 截图”的并排像素差异判断。

## 验收边界

KC-P-013 的交互、异常状态、安全边界和设备布局已经通过离线 UI/Mock 功能验收；本文件的 `blocked` 只针对旧版 1:1 像素视觉结论，不阻塞继续完成其他页面的 UI Mock，也不授权真实服务接入。

# 真实商品目录读取适配

## 独立服务已部署

后续节点已完成：新服务器商业只读实例和独立网关入口已上线，原聊天容器 ID 保持不变。APP 桌台/目录地址默认使用现有 API 根路径加 /commerce（可通过 KINGCLUB_COMMERCE_API_BASE_URL 覆盖），登录和聊天不变。5 项地址及目录页面测试、变更文件静态分析通过。真实加密握手及未登录拒绝已验证；尚未安装 B 手机、未用真实会员查询商品，不能称为扫码全流程验收。后台部署详见独立后端 docs/commerce/READONLY_RUNTIME.md。下方“独立服务待部署”为较早节点状态。

## 最新页面接线状态

正式 type=9 桌卡路由已注入 OrderingTableRepository 和 OrderingCatalogRepository，使用已有 KINGCLUB_API_BASE_URL；不再依据 commerce flavor 自动展示预览。先查询桌台，再查询目录；缺少 API 地址、目录错误或换桌时不回退假商品。shopId 仅作为归属提示，0 仍由桌台解析器处理。

目录已接到原商品布局；分类按 ID 滚动，名称支持四语言，图片缺失使用中性占位，库存上限和售罄来自响应，购物车金额使用整数分。真实目录结算暂提示未开放，不进入 Fake 报价和 Fake 订单中心；商品/价格版本已在数据模型中保留，后续真正提交仍需服务端校验。

验证：6 个相关测试文件共 47 项通过，包含新增加载失败、迟到目录、真实分币价格和零库存测试；变更文件静态分析无问题。未安装手机，未声称真实 HTTP 点单成功。

远端只读确认：运行中的 kingclub-v2-api 尚无 dist/kingclub/commerce 模块及商业开关；新数据库建表成功不代表适配器已部署。现有 main.ts 会启动聊天 outbox、通话过期、通知等后台任务，因此不能将本分支整包覆盖或直接启动第二套相同 worker。下一节点需建立商业独立启动/路由方案再部署，保留聊天实例；本轮未重启或覆盖服务。

以下为本日先前适配器节点记录，页面“尚未接线”的描述已由上述状态更新。

下一节点页面接线：正式扫码路由不再默认预览，依次请求真实桌台和目录；加载/失败不显示固定酒水。沿用现有布局，真实分类按 ID 定位（同名分类不混淆），商品数量限制为可用库存，价格以分计算。商品图片接口尚未提供时显示中性占位，不借用其他品牌酒图。目录成功前禁用购物；真实目录不进入 Fake 结算/订单中心，支付链路完成前明确提示结算暂未开放。原无上下文 UI 示例保留用于布局回归，不作为正式扫码结果。

用户已授权扫码点单真实接入及新库独立运行。本节点实现 K260919000809 客户端适配器，不新增接口或数据表。

请求仅携带服务器确认的 tableId、tableSessionRef，复用加密超级接口与安全会话。响应必须匹配会员、经营主体、品牌、门店、城市、营业日、场次和付款模式；请求期间退出或切换账号则丢弃响应。失败不得回退演示商品。

金额保留整数分，显示格式由整数除法生成，禁止四舍五入成整数元。分类和商品使用四语言名称；商品持有 productRef、categoryRef、revision、available、soldOut，供后续服务端重新核价。重复编号、悬空分类、非法金额/数量、缺货标记冲突均拒绝，不使用图片或演示名称补造商品。

当前实际运行服务未发现商业 dist 模块；扫码路由未注入查询器，页面仍是固定列表。此节点只交付经过测试的数据适配器，尚未替换页面、部署商业后端或安装手机。后续需把适配器接入现有布局、清理模拟结算入口，再结合新服务器当前聊天版本进行独立部署验证。不能用旧分支整包覆盖运行中的聊天版本。

验证：商品适配器与原桌台适配器合计 25 项测试通过；新增文件静态分析无问题。覆盖四语言、整数分、账户变化、八种归属变化、畸形商品、重复编号和空目录。测试为客户端契约测试，未声称真实 HTTP 联调成功。

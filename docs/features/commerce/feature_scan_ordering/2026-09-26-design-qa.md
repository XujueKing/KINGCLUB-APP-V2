# 2026-09-26 点单流程视觉检查

final result: blocked（旧小程序同状态运行截图对照尚未完成；不影响已验证代码安装试用）

## 来源与实现

- 来源：用户历史点单截图，以及旧 `pages/shoping`、`pages/shoping2` 的 WXML/WXSS/JS。旧仓库只读。
- 实现：真实 Flutter 点单页和真实支付页，不是浏览器原型。
- 渲染证据：本地 `build/ordering-qa/{320,393,430}-{menu,cart}.png`，截图比例 2，顶部/底部安全区各 24 logical px。字体为本机微软雅黑；测试运行器的图标字体和按钮默认字体渲染不等同 Android 真机，不能据此宣称 1:1。

## 修正

- 原固定 470dp 面板没有动画，商品数量增长可能溢出，并遮挡结算栏。现为 60% 视窗高度，300ms 滑动/淡入，独立滚动，结算栏位于上层。
- 按源文件恢复米褐面板、黑色径向商品背景、半透明白色总价卡、36rpx 选择圆圈、100×158rpx 瓶图、22rpx 规格、32rpx 价格。
- 复用旧加减、删除、搜索、微信 PNG；500ms 加购动画从加号飞到购物袋。沿用紧凑底栏与现有聊天返回按钮位置。
- 真实确认付款页恢复米色卡片、商品图和明细、微信支付标识及固定金额栏；未提供的订单日期不杜撰。支付发起、幂等恢复、轮询及服务器成功确认保持原逻辑。
- 选择状态进入报价；付款成功只扣除该次已支付购物袋数量，不清掉未选商品。

## 检查及限制

- 自动检查涵盖菜单 320/360/393/430 宽度、系统字号、分类定位、短屏购物袋开关动画、全选/单选金额和报价、结算按钮可达、真实目录分价格、跨桌隔离、微信调起不等于支付成功。
- 历史 audit 文档列出的截图文件当前不在工作区；本轮没有得到旧小程序购物袋与付款页同视窗同状态截图，因此尚不能完成并排视觉验收。B 手机安装与用户实扫验收另记。
- 本次覆盖菜单、购物袋与真实付款确认页面；没有把未接通的优惠券/金币入口伪装为可用。

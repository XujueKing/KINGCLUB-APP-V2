# 散客先选人数开台（进行中）

扫码只读取状态，不自动建场次。已有场次直接点单；无场次且当天 rule.mode=aa 或 reservationRequired=true 时交预订部门处理；其他普通/最低消费/最少人数桌台返回选人数状态。顾客选择 1–10 或更多并确认后，801 重新校验营业日、规则版本、预约标记、人数与桌台状态，在同一事务建立场次、人数/杯数与请求回执。最低消费允许一人；最少人数按当天配置验证。

沿用超级接口 K260919000801 与 MySQL Routine，新建递增153；不在 TypeScript 写数据库业务逻辑。APP 使用选人数弹层，禁止重复点击并保留同次请求号；规则变化返回刷新，开台成功后再读目录。不得把第三方套餐核销或付款伪造成成功。

reservationRequired 是每日规则中的预约占用标记，不代表已实现外部预约系统同步。预订部门已开的场次直接复用。本次也处理用户已确认的两张吧台卡，保留ID与原名称。真实扫码验收与安装分别记录。

## 本次验证
APP 人数页面、桌台解析和点单入口共24项测试通过；320×568与430×932布局无溢出，定向静态分析通过。已有场次在新扫码入口不会再次进入旧人数补录页面，相关入口11项回归通过。
服务端153已上线，业务通过801调用Routine v3；688项后端测试、构建、迁移预检和线上 commerce 运行检查通过。仍待B手机实扫验收。
`n安装记录：commerce profile APK 构建成功，B 手机通过 adb install -r 安装成功并启动，保留应用数据；设备报告更新时间为2026-09-25 17:27:43。实体扫码业务验收待用户操作。

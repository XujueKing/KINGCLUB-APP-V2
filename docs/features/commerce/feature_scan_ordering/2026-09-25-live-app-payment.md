# 扫码订单 APP 支付接入

真实目录报价保留商品编号、版本、整数分金额，进入真实确认页；预览继续使用原演示页。814提交、815归属查询均经加密超级接口。客户端不提交支付成功状态、不依赖SDK返回判断收款。

接入微信Android OpenSDK 6.8.40，应用包名对应WXPayEntryActivity入口；微信调起后返回APP自动查询。幂等编号在请求前保存在安全存储，按登录账号和场次隔离；同订单重试保留编号，跨会话响应拒绝。不同购物清单遇到未确认订单时先核对原单，避免覆盖。真实订单失败不进入Fake支付结果页。仅服务器确认paid后清空当前购物车。

验证：订单数据与购物车原7项测试通过，新增订单归属/换登录拒绝2项及支付页测试通过；微信调起成功时界面仍等待服务器确认，只有owned返回paid才显示支付成功。修改文件静态分析无问题。安卓commerce profile构建成功，B手机覆盖安装成功并启动，保留数据。

后端已启用新支付事务及回调恢复流程。现有私有映射仍为小程序JSAPI类型，移动APP支付AppID、登记包名和签名尚待确认。未发生真实扣款，未完成0.10元微信支付与真实异步通知验收；不能将安装成功当作支付可用。B手机使用独立commerce包，不能直接假定它与已登记的正式APP包名及签名相同。

补充断网恢复：815按本人requestId查找已提交订单；若确认不存在才释放本地未确认记录。服务器确认paid后清空原购物车，SDK调起和取消均不清空。

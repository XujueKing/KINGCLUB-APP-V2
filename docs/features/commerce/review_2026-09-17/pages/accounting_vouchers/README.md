# 凭证列表与详情

状态：In Review，仅文档；文字线框 v1。[公共页面规则](../README.md) 和 [营业规则](../../CONFIRMED_OPERATING_RULES.md) 适用。管理员经营操作先由超级管理员执行；会员限本人权益，不因后台全功能权限改变数据隔离。

## 入口与返回

记账 → 更多 → 凭证；ledgerId、period、voucherId 可选。列表进入详情，返回保留期间。

## 页面和字段

期间/状态筛选 → 凭证列表 → 日期/来源 → 借贷分录 → 平衡结果/过账与冲正历史。

voucherId、sourceEventRef、templateVersion、lines[accountRef,debitMinor,creditMinor]、currency、postingDate、state、reversalOf。

## 操作与数据效果

经营事件按模板生成凭证，员工日常只操作业务页。超级管理员查看来源、处理缺模板待办；借贷不平衡不允许过账。草稿可改，已过账以冲正及更正单处理，不覆盖原来源。

ListVouchers/GetVoucherSource/PreviewVoucherPosting/PostVoucher/ReverseVoucher；来源事件去重，期间锁定拒绝回写；失败不能出现只有部分分录入账。

## 状态、审计及四语言

加载骨架，空记录给下一步；无权限返回，会话失效重登后查询原对象；离线仅只读/草稿，不确认账务或交付。失败保留输入，字段错误定位，未知结果持原 commandId 查询，不重复新建。返回保留门店、期间及筛选；切店不能改变原单归属。

接口/字段为语义草案，正式编号/DTO 未冻结。服务端校验主体、门店、实际操作者和对象版本；金额最小单位整数及币种，数量十进制加单位。审计来源、状态变更、操作者和更正原因；日志不记录完整地址、二维码、财务密钥。四语言共用状态键，金额与日期明确单位/时区，大字可换行、操作有读屏标签；文案资源仍待定稿。

## 验收设计（未执行）

同一销售事件重复传入只生效一份；借贷差 1 分拒绝过账；冲正可追到原凭证，原订单快照保留。

还需合成 Mock 验证并发/重复请求、错店、未知状态、返回和四语言；真实服务验收在项目 UI 验收后按批准契约进行。本轮无付款、退款、导入、交割或生产账务操作。

## 未定范围

科目编号、会计制度、税务及具体凭证模板待账套初始化定稿；此页仅规定软件流程，不冒充法定账务政策。

# 订单中心页交互

| 触发 | 行为 |
|---|---|
| 切换筛选 | 重置 cursor，加载对应状态族 |
| 下拉刷新 | 权威替换首屏并去重 |
| 滚动到底 | 单次加载下一 cursor |
| 点订单卡 | 仅传 OrderRef 打开详情 |
| 收到状态事件 | 标记需刷新，不直接改列表状态 |
| 点空态入口 | 返回首页/扫码等已批准入口 |

筛选选择可保留；订单内容不跨账号缓存。

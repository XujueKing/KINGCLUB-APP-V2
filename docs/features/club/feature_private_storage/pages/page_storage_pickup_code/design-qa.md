# KC-P-048 Android 视觉检查

检查日期：2026-08-27
视口：Android Medium Phone 1080×2400
入口截图：`android_storage_entry.png`
实现截图：`android_storage_pickup_code.png`
复刻依据：旧版 `savecode.wxml/.wxss` 与已批准 `Private Storage Wireframe v1 / Pickup`

## 对照结果

- 私人储物柜默认空柜画面未改变，演示入口收纳在原中央信息说明弹层。
- 旧版黑色径向背景、英文标题、中央二维码、品名/英文名/数量/剩余量/日期和储存说明结构已保留。
- 永久 storedId、内部库位、保存人员和静态网页二维码均已移除。
- Fake 视觉码每 30 秒轮换，展示倒计时；后台立即隐私遮盖，回前台重新签发。
- 正常、部分交付、已取出、暂停、离线状态有独立 UI，消费者无法自行核销。
- Android 竖屏无横向溢出，详情和说明可纵向滚动。

final result: passed


## 2026-09-12 物品提取详情参考图调整

- 物品按“物品名、整行居中功能说明、数量、获取日期、过期日期、最大抵用金额”排列；金额保留两位小数并带￥。无金额的普通物品不渲染金额行。
- 内容宽74%，二维码宽58%，二维码下留46逻辑像素；说明不占用左右双列，不显示英文名及剩余量。长内容可滚动。
- 正常物品提取码自动更新，不额外显示提示/刷新按钮；不可提取或请求失败时保留原因及刷新入口。酒水布局与字段不变。
- 数据、数量、日期、金额均来自现有接口；没有更改库存、期限、数据库或短码核销规则。当前二维码保持真实动态码，未替换成参考图静态二维码。
- 验证：页面静态分析通过；储物7项测试通过；使用内置预览仓库与中文字体渲染390×844截图 build/storage-qa/coupon-pickup.png，说明为完整两行，日期、金额右对齐且无溢出。
- 当前工作区另有设置页/根design-qa.md未解决合并冲突，因此本次未打包安装，不混入或提交其它暂存改动。


## 2026-09-12 提取码无闪空刷新

- 服务端30秒有效期保持不变。前台提前5秒请求更新，保留当前码直到新码返回，在同一帧替换图案，不叠加/淡化二维码。独立到期计时器保证慢请求不会无限展示过期码。
- 固定白色底板涵盖首次加载、刷新、异常及不可提取状态；异常文本改为深色以保证可读。后台隐藏码并停止计时，返回重新请求。
- 服务端仍在新签码时撤销旧码，因此不承诺网络传输期间旧码仍可核销；此次为视觉连续性修复，未改变核销安全边界。
- 已核实服务器/etc/cron.d/kingclub-storage-cleanup每天04:23运行：删除过期超过7天的短期凭证，每次最多10000条；不删除库存或StorageEvent存取审计流水。该限额在高用量时可能积压，不能宣称无限规模无积压。
- 测试：延迟响应时二维码保持可见，无加载替换；新响应直接替换；慢请求跨到期隐藏码而保留白底；错误、后台恢复及已核销停码通过。另6项储物测试通过。
- 当前工作区其它页面仍有合并冲突，本次尚未打包安装。

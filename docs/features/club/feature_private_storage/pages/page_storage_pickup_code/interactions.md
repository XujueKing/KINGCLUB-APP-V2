# 取件码页交互

- 进入/前台重读并签发短时 token；到期立即遮盖。
- 员工核验事件只触发 reconcile。
- 后台、会话失效销毁 token；离线提示工作人员协助。
- 不复制、保存、分享或显示永久编号。
- Preview/UI 验收构建长按“取件凭证”打开本地场景选择器；切换场景时立即销毁当前视觉码并递增凭证世代。
- 明确过期或重放拒绝可重新签发新的本地 Fake 凭证；结果未知只能刷新权威状态投影，不得直接宣布已取出。

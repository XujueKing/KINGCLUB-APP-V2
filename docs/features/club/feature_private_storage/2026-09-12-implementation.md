# 储物柜、翻转与媒体缓存

用户依据旧小程序截图要求还原，进一步授权程序、真实接口、数据库整理，以及翻转和流动动画。

- 柜子使用三列九宫格、原酒瓶/券/翻转/二维码PNG、原瓶形SVG；素材在assets/legacy/storage随包提供，不请求旧站。酒/物分页、选中、数量、剩余、有效期均取真实接口。
- 瓶/卡440ms纵轴翻转，过半切面，不镜像文字。背面酒液按服务端剩余百分比裁切，以正弦波和移动高光连续流动，0%/100%不越界；离页、后台、减少动画模式暂停。
- 小屏保留滚动，顶部和底栏使用真实安全区。已确认底栏90%茶色磨砂、无白边、未读气泡规格不改变。
- 真实提取码30秒轮换；实时重读余额，不显示永久旧码/内部仓位/员工资料。后台隐藏、失效或不可取不显示QR。
- 所有当前远程图片入口改用统一缓存；私有图片以会员隔离，签名变化使用不可变文件版本键复用，退出清私有缓存。固定封面视频继续本地播放。
- MediaCache存手机系统缓存目录，图片预算200MiB、视频800MiB，单文件20/200MiB，LRU淘汰；HTTPS、并发去重、临时文件完整后改名。图片/视频再次访问优先读磁盘，系统空间紧张也可能清理缓存。版本键必须随内容变化，不存签名URL。
- CachedMediaVideo为MP4文件缓存播放器，当前内容流仍为原有Mock，没有真实远程视频数据，不能宣称整条视频业务已接通；后续远程播放器统一使用此组件，不另建网络入口。不自动预下载未打开视频，也不支持HLS分片。
- 设置页显示真实缓存大小并实际清理；私有媒体不跨账号复用。提取码不写入媒体缓存。

验证：Flutter analyze；缓存命中/去重/签名变化/私有隔离/淘汰/清理、柜子切换/翻转/三屏幕布局、真实QR轮换与后台失效、底栏和已冻结会话流程回归。截图见build/storage-qa，使用独立测试fixtures；不作为真实会员库存。

部署：031与三接口已部署并有备份；新Profile包已安装到连接手机。真实库存初始空；旧会员归属和余额迁移待用户确认，不注入展示数据。员工入库/核销只提供主机受信脚本，员工界面仍是后续工作。

## 后续真机反馈修正

酒瓶距标题过近：展示区内部顶部留48rpx，保持网格和底栏布局；满瓶可用液量100%与瓶口留空分开表达，最高液面90%瓶形高度，双正弦轻波和渐变保持连续，空瓶不生波。

二维码点击原来因过期酒canPickup=false直接禁用，现所有有物品的格子均可进入详情。过期页显示“存酒已过期，请联系工作人员核实”，隐藏签码并保留详情；有效券仍签短码。未更改过期日期或放开核销。

验证：7项储物回归、静态检查通过，涵盖过期图标点击完整路由且不调用签码，以及100%留白/随时间改变液面/暂停。390×844满瓶截图已检查，原九宫格和底栏位置保留。

## 物品详情字段区分

用户要求物品无英文名和剩余量，有功能描述，抵用券显示可抵用金额。详情现按category条件展示：wine保留英文名、剩余比例及存酒说明；item显示功能说明，有maximumValue时显示最高抵用金额，金额保留最多两位小数。不重复页尾说明，长文左对齐自动换行，整体仍可滚动。真实接口与数据库字段无需改动。静态检查和3项相关回归通过。

## 过期储物入口

顶部右侧11号香槟金半透明文字，无背景，右边与柜子内容对齐，标题居中位置不动。按钮文案为过期储物，加载/读取失败时不可点；成功后筛选接口status=expired的酒与物品，半屏可滚动列表展示名称、数量和过期时间，酒额外展示剩余量。点击项目沿用真实详情与不可核销规则；没有过期记录时显示暂无过期储物。七项储物测试通过，包括混合类别筛选及过期券详情无二维码。

## 2026-09-12 同款存酒按批次显示及刻度水印

- 同酒款不同存入记录必须按独立 itemRef 各占一格，不按商品名或 assetKey 合并数量；每条记录保留自身数量、存入时间、有效期和剩余百分比。
- 已核对现有 storage-service 列表、存入操作和旧库导入脚本：按 holding/sourceKey 独立保存返回，没有按酒款聚合，本次无需数据库迁移或修改真实库存。
- 酒格右下角新增淡色百分比水印（40% 不透明度），例如 50%、100%；物品/券不显示酒水刻度。左上角仍为该条记录本身的数量。
- 回归覆盖同款两批不同日期、数量及 50%/100% 各自显示、点击对应详情；10 项储物测试及定向 analyze 通过。中文字体截图检查水印位置，三种手机尺寸无溢出；截图字体未覆盖按钮主题时的方框不代表真机字体。

- 用户后续微调：仅酒格右下角 100% 等百分比水印改为 10 逻辑像素、常规字重，保持淡色及位置；左上角数量和酒瓶背面刻度不变。

# 首页三联入口视觉与交互 QA

- 日期：2026-08-28
- 最终结果：`blocked`
- 验收范围：`一起玩 / 组局玩 / SCAN QR` 三联入口
- 视觉参考：用户本轮提供的旧版裁片
- 结构与动态权威来源：旧版 `pages/index/index.wxml`、`pages/index/index.wxss` 和 `app.wxss`
- Android 实现证据：[05-quick-actions-final.png](../../qa/2026-08-28/05-quick-actions-final.png)

## 2026-08-28 用户复验结论

- 用户指出上一版流光生硬、三项衔接不自然，原 `passed` 结论撤销。
- P1：流光层错误使用卡片等尺寸旋转渐变，圆角裁切后形成明显三角亮块。
- P1：三项各自维护刷新计时器，不能保证共享时间原点，连续运行时相位衔接不稳定。
- 修正门禁：改为共享时钟与旧版 `200%` 伪元素几何后，重新捕获至少三个连续时刻的设备证据，再决定是否恢复 `passed`。

## 修正后证据

- 连续设备帧：`../../qa/2026-08-28/06-quick-flow-01.png`、`07-quick-flow-02.png`、`08-quick-flow-03.png`。
- 十秒设备录屏：`../../qa/2026-08-28/09-quick-flow-final.mp4`。
- 九帧连续状态表：`../../qa/2026-08-28/10-quick-flow-contact-sheet.png`。
- 已修复原等尺寸渐变造成的三角亮块；现为旧版 `200%` 流光层，仅由卡片圆角裁切。
- 三项改为单一 33ms 共享时钟，以同一 elapsed time 分别计算 3/6/9 秒进度，不再存在独立计时器漂移。
- 静态字体、图片、卡片比例、间隙和按压态均保持上一轮确认值。
- 自动化结果：`flutter analyze` 通过；全量 `flutter test` 247/247 通过。

`final result: blocked`

当前只等待用户在已打开的 Android 模拟器中确认实际动态观感；用户确认后再把本组件恢复为 `passed`。真实接口与 SDK 仍保持阻断。

## 2026-08-28 第二次用户复验

- P1：修正后的光带仍接近竖直白条，未达到原版明显斜向穿过按钮的视觉。
- 处理：保留共享时钟和 `200%` 柔光层，仅修正 Flutter 渐变坐标映射，使光带本体以 30° 斜向经过。
- 状态继续保持 `blocked`，待新设备录屏复验。

## 斜向修正证据

- 新设备录屏：`../../qa/2026-08-28/11-quick-flow-diagonal.mp4`。
- 六帧状态表：`../../qa/2026-08-28/12-quick-flow-diagonal-contact-sheet.png`。
- 六帧中已分别捕获光带斜向经过扫码、一起玩和组局玩三张卡，未再出现竖直白柱。
- 自动化结果：首页专项 11/11 通过，`flutter analyze` 无问题。
- `final result: blocked`：等待用户在模拟器复验斜向动态观感。

## 2026-08-28 第三次用户复验

- P1：虽然光带角度已斜向，但三张卡仍分别出现光带，速度和跨卡衔接不符合要求。
- 最新确认：整行改为单一全局光带，以固定速度从左到右连续穿过三张卡；各卡只裁切显示该全局光带在自身范围内的片段。
- 状态保持 `blocked`，待单光带录屏复验。

## 单光带修正证据

- 设备录屏：`../../qa/2026-08-28/13-quick-flow-single.mp4`。
- 八帧状态表：`../../qa/2026-08-28/14-quick-flow-single-contact-sheet.png`。
- 连续帧确认同一条斜光按“一起玩 → 组局玩 → 扫码”顺序移动，速度恒定；跨越卡间黑色间隙时没有第二条光带或重新起跑。
- 状态保持 `blocked`，等待用户在模拟器确认动态观感。

## 2026-08-28 动效尺寸与速度调整

- 用户要求光带移动更快并放大。
- 调整目标：完整穿越周期 `6s → 4s`，光带宽度 `28% → 42%`；保持单光带、斜向、线性匀速和跨卡连续。
- 状态保持 `blocked`，待新录屏复验。

## 加速与放大证据

- 新设备录屏：`../../qa/2026-08-28/15-quick-flow-fast-wide.mp4`。
- 六帧状态表：`../../qa/2026-08-28/16-quick-flow-fast-wide-contact-sheet.png`。
- 连续帧确认光带宽度已放大并在 4 秒内匀速穿越整行，仍只有一条且跨卡顺序连续。
- 首页专项与 Golden 共 12/12 通过，`flutter analyze` 无问题。
- `final result: blocked`：等待用户在模拟器确认速度和宽度。

## 2026-08-28 第二次放大

- 用户要求在 42% 基础上继续放大光带。
- 调整目标：保持 4 秒匀速穿越，宽度 `42% → 60%`；单光带和跨卡连续逻辑不变。
- 状态保持 `blocked`，待新设备效果复验。

## 60% 光带证据

- 新设备录屏：`../../qa/2026-08-28/17-quick-flow-xlarge.mp4`。
- 六帧状态表：`../../qa/2026-08-28/18-quick-flow-xlarge-contact-sheet.png`。
- 连续帧确认光带已扩大至整行约 60%，4 秒单光带匀速与跨卡连续保持不变。
- 首页专项与 Golden 共 12/12 通过，`flutter analyze` 无问题。
- `final result: blocked`：等待用户确认最新宽度。

## 2026-08-28 光带透明度调整

- 用户要求光带透明度调整到 70%。
- 调整目标：中心峰值透明度 `30% → 70%`，两侧继续衰减至完全透明；60% 宽度、4 秒匀速、30° 斜向和单光带跨卡连续逻辑不变。
- 设备录屏：`../../qa/2026-08-28/19-quick-flow-opacity70.mp4`。
- 六帧状态表：`../../qa/2026-08-28/21-quick-flow-opacity70-contact-sheet.png`。
- 首页专项与 Golden 共 12/12 通过，`flutter analyze` 无问题。
- 状态保持 `blocked`，待用户确认更新后的设备动态效果。

## 2026-08-28 光带透明度二次调整

- 用户要求光带透明度由 70% 调整到 20%。
- 调整目标：中心峰值透明度 `70% → 20%`，两侧继续衰减至完全透明；60% 宽度、4 秒匀速、30° 斜向和单光带跨卡连续逻辑不变。
- 设备录屏：`../../qa/2026-08-28/22-quick-flow-opacity20.mp4`。
- 六帧状态表：`../../qa/2026-08-28/23-quick-flow-opacity20-contact-sheet.png`。
- 首页专项与 Golden 共 12/12 通过，`flutter analyze` 无问题。
- 状态保持 `blocked`，待用户确认 20% 设备动态效果。

## 2026-08-28 光带透明度第三次调整

- 用户要求在 20% 基础上“再大一点”。
- 本轮按小幅调整落为中心峰值透明度 `20% → 30%`；两侧透明衰减、60% 宽度、4 秒匀速、30° 斜向和单光带跨卡连续逻辑不变。
- 设备录屏：`../../qa/2026-08-28/24-quick-flow-opacity30.mp4`。
- 六帧状态表：`../../qa/2026-08-28/25-quick-flow-opacity30-contact-sheet.png`。
- 首页专项与 Golden 共 12/12 通过，`flutter analyze` 无问题。
- 状态保持 `blocked`，待用户确认 30% 设备动态效果。

## 已保留的静态对照结果

- 卡片宽度按旧版 `246:246:160`，统一高度 `140rpx`，三张卡上下边缘齐平。
- 中文按 `38rpx / 600`，英文按 `20rpx / normal` 等比换算；左、中卡两行均按旧版 `30rpx` 右边距对齐。
- `H01`、`H02`、二维码底纹和前景扫码框均复用旧版 PNG，并恢复各自尺寸、负偏移和旋转，不使用近似图标。
- 背景恢复旧版径向黑金渐变和 `15rpx` 圆角。
- 白色斜向流光在完整的 3/6/9 秒周期内连续横穿对应卡片；设备截图已捕获第一张卡的流光中间态。
- 按下期间整卡透明度为 70%，松开或取消恢复；无 Material 水波纹。
- 开启“减少动态效果”后不创建可见流光，入口点击和返回仍可用。

## 自动化证据

- `home_flow_test.dart` 校验尺寸比例、上下对齐、字号、字重、流光位移、70% 按压态和减少动态效果。
- `home_legacy_visual_test.dart` Golden 已更新并通过。
- `flutter analyze`：无问题。

本结论只批准该组件的 UI/Mock 复刻，不批准真实接口、支付、扫码 SDK 或生产服务接入。

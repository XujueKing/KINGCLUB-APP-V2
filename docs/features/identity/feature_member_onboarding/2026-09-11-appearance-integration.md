# 会员形象资料真实接入

用户要求完成形象页并统一前两页返回箭头，确认沿用旧版门槛，随后明确三张平均：实名自拍imageType=0＋正面形象照＋半身/全身照。不是两槽位平均。后端028使用legacy-three-photo-v1及旧INT取整，男80女85，未达标待审可换照片。

页面支持相册/拍照、原生压缩JPEG、每槽位私有上传和短期预览；上传失败可重选、取消拍照保留旧槽位。两槽位齐全后提交，版本/幂等键由后端验证，刷新不重评；已上传照片跨会话由服务器恢复。待审可换图，超时结果未知提示刷新或人工确认，不能以Mock结果通过。自拍评分后台单独保存，原件仍24小时清理；缺失自拍评分不降为两张、不重复实名。

手机号、实名、形象及共享注册页返回使用KingBackButton：图形8×16，点击区48×48，安全区左18.5/上4，实名页固定不随表单滚动。保留渐变input、按钮配色/高度位置、外部点击收键盘和未实名从封面重验手机号。

验证：analyze、20项注册布局/续接回归、真实形象页两槽上传→待审→替换重传测试通过；后端166测试、独立MySQL迁移/事务测试、IDC健康检查通过。已覆盖安装OPPO新包。既有实名自拍真实腾讯评分SCORED已保存；用户两张形象照端到端验收待操作。人工审核后台不在本次实现内。

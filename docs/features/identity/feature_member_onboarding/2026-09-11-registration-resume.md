2026-09-11 形象页操作区调整：用户要求“提交并评分”与实名页主按钮处于相同高度。按钮固定在底部，高45，下方间距10及13字号城市文字，安全区底部间距27；“切换手机号”改为“其它账号登录”，放在主按钮上方。内容区独立滚动。原换号逻辑及真实提交禁用状态保留。静态分析与9个注册恢复测试通过。

2026-09-11 实名页配色试用：用户要求反向参照形象页卡片配色。姓名及身份证输入框改为KingColors.surface底色、border细边线，聚焦brand边线，文字textPrimary、提示textSecondary；保留46高度及居中。人脸核验按钮使用形象页主题的brand/onBrand及禁用色，保留45高度和原位置。仅试用实名页，不改手机号页的共享渐变。生成无身份资料的页面预览，不重置已实名账号。

# 注册进度恢复

2026-09-11 独立手机号会员：用户明确不同手机号使用独立KingClub会员，物业统一账号可相同。后端027在新手机号冲突时分配UM业务账号，旧会员及实名保留，新会员identity_required；App账号字段按不透明字符串处理，不共享实名进度。绑定异常兜底改为中文；不通过App绕过验证码或强制重置旧会员。

15分钟策略（用户最新明确要求）：最近一次短信登录成功写mobileVerifiedAt到安全会话，只有0≤当前时间−该时间<15分钟允许自动续接。冷启动、已登录欢迎继续、后台回前台均检查，过期/时间缺失/时钟倒退时清本地会话并直接回手机号页；后台K103轮换不改变该时间。保留服务器实名事实，重新验证同号仍到形象资料，换号按新号服务端状态路由。此为App续接策略，不改后台统一会话TTL。

形象资料页新增切换手机号入口；清内存与安全会话，避免自动恢复回旧号，不发真实短信、不删除账号/实名/资产。旧版本会话没有新字段，首次升级要求手机号验证一次。新增边界、刷新不续期、到期手机号路由、切换账号回归；9个恢复测试通过，已有18个登录实名检查通过，analyze通过。

加载体验更新：用户明确移除单独恢复过渡页及错误圆圈K标志。真实冷启动显示原欢迎视频和正确logo、后台恢复；已登录从欢迎继续直接分流，不重复请求。慢网络保留欢迎界面，失败可点继续重试。6个恢复测试含慢响应无过渡页、继续不增加接口请求，analyze通过。K104最近9次服务端耗时1–4ms，仅为服务端业务时间，不是端到端加载时间。

返回行为更新：用户不接受形象资料返回直接退出App，现改为返回欢迎页，保留会话及实名进度；点击欢迎页继续经bootstrap回形象资料页，不再次登录/实名。下文“形象资料页返回退出App”为已废弃初版行为。已补真实页面返回与继续的路由回归，5个恢复测试通过。

用户明确要求实名成功后不因关机、App重启、错误返回而再次实名，直接继续完善形象资料。已实测腾讯照片V2核验成功一次，数据库verified且photos_required；不能以新建会员或本地Mock状态覆盖服务器结果。

每次冷启动通过安全存储取会话，K104刷新进度；访问凭据过期则K103轮换后再读K104。有效refresh到期/撤销才回登录；网络错误保留安全会话，展示重试，不把网络失败当作匿名重开实名。并发恢复合并为单次Future。

统一分流：active+approved首页；identity_required实名页；photos_required/changes_required形象资料页；pending_review/rejected/unknown/受限账号状态页。短信登录后、实名成功后与bootstrap使用相同分流；已实名误访问手机号/实名/欢迎入口转bootstrap刷新，不依据本地缓存直接发起有费核验。形象资料页返回退出App，不返回实名。显式登出清安全会话和内存会员状态。

真人首次成功后出现旧页面finally解除loading、provider已变更而误触发onInvalidFlow的竞态；完成标记令路由交接期间旧页面不再触发无效流程回跳。测试保留旧页一帧验证onNext仅一次、onInvalidFlow零次。

测试：session_registration_restore_test覆盖本地旧进度与服务器冲突、令牌轮换、网络失败、撤销、两次冷启动与误入实名；real_identity_navigation_test覆盖成功交接。与17既有登录注册检查合计23通过，analyze通过。安装后须用ADB强制停止再启动确认真实安全会话恢复，不重新发送短信或照片。

范围：本批完成注册续接路由。实际形象资料页真实模式不调用Mock；两张形象照上传、评分及后续偏好/审核仍需接入，不能由路由改动冒充已提交或入会已通过。

# 实名与成年核验页

- Scope ID：`KC-P-005`
- 文档状态：`Approved for Development`
- 所属功能：[会员注册、资料初始化与准入](../../README.md)
- 旧版来源：`regist2`
- 路由语义：`RealNameAdultVerificationRoute`，onboarding 分区，禁止外部直达
- 设计版本：`Legacy Regist2 Replica v6 / Adult Consent Restored`
- 最后更新：2026-08-29

## 用户任务

了解实名用途，输入本人姓名与证件号码，通过已批准的实名/活体核验并确认已成年。客户端不自行根据证件号计算年龄。

## 入口、出口与返回

- 入口：authenticated 且 snapshot.stage=`identityRequired|identityProcessing`。
- 成功：服务端确认 `kycStatus=verified` 且成年，replace KC-P-006。
- 未成年：留在阻断状态，清理输入，不创建会员申请。
- 返回：未提交时确认退出准入并回登录安全根；processing 时不能通过返回制造第二个核验流程。
- 非法状态：按最新 snapshot replace 当前权威步骤。

## 已确认视觉方向（2026-08-28）

- 页面主体按旧版 `pages/regist2` 的截图、WXML 与 WXSS 复刻，不再沿用通用 V2 表单卡片样式。
- 顶部保留 V2 的 `步骤 1/4` 标题和四分之一进度条，这是相对旧版唯一保留的新增导航元素。
- 主体顺序固定为：禁酒图标、旧版两行未成年提示、居中 `NAME:` 标签与姓名输入、居中 `ID CARD:` 标签与证件号码输入、成年声明勾选行、人脸核验按钮、城市文案。
- 删除法规小字后，禁酒提示与表单作为一个整体偏上布局；禁酒图标起始高度与上一页金色 Logo 的视觉节奏对应，剩余弹性空间主要保留在表单与底部人脸核验按钮之间。
- 旧版粉色全部替换为 KingClub 黑金色：图标和按钮前景使用香槟金，输入框使用金棕色，按钮底色使用深棕色；不得残留粉色。
- `nonine.png`、`camera.png` 与 `back.png` 使用旧版原始素材，不重新绘制。
- 首屏不显示“合成数据演示”、Fake 或测试说明卡；恢复黑金色成年声明勾选行。

## 线框

```text
[返回]                    步骤 1/4
[========------------------------]

                [禁酒图标]
             未满18岁未成年人
             不得饮酒注册会员
                 NAME:
          [                      ]
                ID CARD:
          [                      ]

 [勾选] 我已阅读并同意：本人已满18周岁，未成年人禁止进入
        本娱乐场所，遵守店内相关管理规定。

          [相机图标  人脸核验]
             SHANGHAI . ZHUZHOU
```

## 组件与规则

- 姓名：去首尾空格、长度/字符只做友好校验，不在客户端判定真实性。
- 姓名和证件号码恢复旧版居中英文独立标签与金棕色输入框结构；标签固定为 `NAME:`、`ID CARD:`。两个输入框按旧图约占页面宽度 80%，高度约 52dp，使用半径 26dp 的完整胶囊圆角。
- 证件号：按本次回退截图移除右侧查看图标；不提供复制，不进入自动填充日志，离开页面时清理。
- 成年声明：使用黑金色圆形勾选控件，文案固定为“我已阅读并同意：本人已满18周岁，未成年人禁止进入本娱乐场所，遵守店内相关管理规定。”；控件与首行文字顶部对齐，未勾选不得开始核验。
- 按钮：点击后先校验字段；提交期间锁定；正式“人脸核验”主按钮使用完整胶囊圆角。
- 开始核验后使用 FakeIdentityVerification；真实 SDK/网页流程后置。
- UI Mock 阶段不得在正式首屏显示“合成数据演示”或 `Fake` 字样；主按钮使用旧版正式文案“人脸核验”。
- 禁酒图标下方恢复旧版两行提示“未满18岁未成年人 / 不得饮酒注册会员”。
- 页面状态见 [states.md](states.md)，交互见 [interactions.md](interactions.md)。

## 数据与隐私

- UI Mock 只使用合成姓名和掩码占位，不保存真实输入。
- 真实方案优先让敏感材料直达批准核验方/服务端受控通道；不得 Base64 塞入通用超级接口日志链。
- 埋点只记录 `view/start/resultCategory`，不含字段长度、生日、证件尾号或供应商原文。

## 验收

见 [acceptance.md](acceptance.md)。当前只实现合成数据与 Fake 核验 UI；不得接入真实核验。

# 着装与音乐偏好页交互

- Chip 点击切换 selected，选中态同时使用图标/描边/语义，不只变色。
- 选择数量超过目录策略上限时阻止新选并说明上限；已选不丢失。
- “暂时跳过”需要明确保存 skipped，不弹惩罚性确认。
- 下一步保存结果未知时查询 snapshot/draftVersion，不重复覆盖。
- 200% 字体缩放时 Chip 自动换行，标签不截断；焦点顺序先着装后音乐再动作。

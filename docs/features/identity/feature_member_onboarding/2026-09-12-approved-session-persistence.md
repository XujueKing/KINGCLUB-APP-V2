# 已通过会员保持登录

用户明确要求：登录成功后不要每次要求短信验证。此为冻结后的指定增量需求。

已完成注册且账号、会员状态正常、审核通过的会员，不再受注册阶段15分钟窗口限制。短信登录和查询会员进度后将服务端账号/会员快照保存到安全存储；冷启动及回前台恢复时仍向服务端验证当前状态，访问凭据过期时使用既有刷新凭据续期。快照只决定是否尝试恢复，不代替服务端准入决定。

未完成注册保持此前15分钟规则，未实名从封面进入仍重验。主动退出、会话撤销、刷新凭据失效仍要求登录；网络失败保留凭据以便重试。旧安装若尚未保存通过状态，升级后可能需要再短信登录一次。未修改服务端会话有效期，不能称永久免登录。

验证：31项会话、真实登录及形象页测试通过；analyze无问题。新增跨天已通过会员恢复、访问凭据轮换、明确退出、服务端改为blocked及撤销会话不能靠本地快照放行的检查。

本机打包环境曾在JDK Unix domain socket连接处报Invalid argument。为本次构建进程指定项目build/jtmp作为jdk.net.unixdomain.tmpdir后已进入正常Gradle构建，不修改系统网络设置。

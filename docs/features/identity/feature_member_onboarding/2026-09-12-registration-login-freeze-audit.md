# 注册登录稳定基线审计

日期：2026-09-12。用户完成手机验收后要求再次审计，随后保持当前流程。此次仅审计和记录，不更改业务代码、会员状态、照片或评分，不重新发送短信或调用付费腾讯核验。

## 结论与版本

正常注册、登录及审核结果展示流程未发现本次验证范围内的阻断问题，固定当前用户已验收的交互。App 基线为 `2450984`，对应 preview/profile APK 已成功覆盖安装；后端为 `business/kingclub-v2 / f020d50`，运行镜像 `kingclub-v2-api:admission-20260912`。今后仅在用户提出新需求或发现可复现缺陷时调整本流程，不主动再改配色、布局、文案、评分规则或路由。

该结论不是全平台发布认证，也不代表人工管理后台和所有供应商异常恢复已经完成。

## 本次检查

| 范围 | 检查结果 |
| --- | --- |
| 手机号与验证码 | NEXT 由合法手机号、六位数字及请求状态决定；后端验证验证码。错误和过期提示、加载状态复位、请求限流保留。临时测试码只按既有指定号码摘要、用途及有效期匹配，未扩大范围。普通真实验证码仍需要有效 challenge。 |
| 登录分流 | 按服务端注册状态分流，不以新用户标记或 active 单独放行；已通过会员直接进入首页。 |
| 会话续接 | 15 分钟按最近短信验证时间计算；刷新不延长。冷启动、回前台及封面继续检查；未实名会员从封面继续仍要求手机号重验；换号清本机会话，保留服务端资料。该窗口不是连续前台运行中的定时强制登出。 |
| 实名 | 已验证状态不重复调用付费实名；拍照取消、异常复位、键盘失焦及路由切换竞态有处理；服务端未知结果不自动当成通过。 |
| 图片与评分 | 客户端压缩，服务端验证上传票据、会话、摘要、大小并规范化图片；按会员私有存储。三张有效照片平均分取整，男 80 / 女 85；未更换照片使用已保存结果，不因刷新再次评分。 |
| 注册顺序 | 实名 → 形象照片 → 风格音乐 → 兴趣偏好最终提交 → 审核状态。最后提交前不提前放行；照片版本校验、重复提交保护、事务保持。 |
| 审核交互 | 待审刷新发现通过，先显示成功画面，点击进入按钮才进首页；按钮位置保持一致。拒绝/限制显示本人公开原因，不能通过换照或偏好提交覆盖决定。 |
| 布局与返回 | 注册箭头、标题、输入框和主按钮沿用已验收版本；短屏/大字正文滚动，审核说明自适应折叠。成功页文明交友说明保持当前内容。 |

## 本次验证证据

- App 定向测试 42 项全部通过：`real_login_registration_test`、`real_identity_navigation_test`、`real_appearance_page_test`、`session_registration_restore_test`、`registration_admission_flow_test`、`welcome_video_test`。包含真实模式的仓库替身测试与 Mock 流程测试，不能等同于本次重新做了真实短信和真人核验。
- `flutter analyze` 无问题。
- 后端 `npm run verify` 通过：43 个文件、167 项测试；类型检查、构建、001—030 migration dry-run、静态部署检查通过。本机缺少 Docker，相关静态检查明确跳过，未在本次重新创建独立 MySQL 测试库；此前真实 MySQL 事务测试保留为历史证据。
- 线上读取并对比 8 个核心产物的 SHA-256：登录执行器、登录仓库、短信路由、临时码匹配、实名、形象评分、评分规则、准入决定均与本地构建一致。
- 公网 HTTPS `/health` 和 `/ready` 返回 200；两个照片上传入口无凭据请求均返回 401。容器内 runtime status 检查通过。
- 数据库只读一致性检查全部为 0：已通过但未实名、最终状态缺少兴趣提交时间、审核主状态与形象申请状态不一致、有效身份限制未反映到会员状态、处理中或未知的实名记录、处理中或未知的评分记录。只记录汇总，未导出个人资料。
- 通用 `check-deployment.mjs` 在精简运行镜像内整体不能通过：镜像未包含源码、Compose、文档和 `.env.example`，并且通用生产检查要求的 WebSocket/根数据库配置不适用于当前业务容器。此次未为通过检查而修改配置；不能将该命令报告为整体通过。健康、就绪、运行状态检查和产物比对独立记录如上。

## 明确保留的边界

1. 腾讯返回 `unknown` 或进程在请求后中断时，当前策略阻止重复付费和误放行，但尚无自动对账恢复闭环。实名页面刷新不能保证解除真实 unknown；评分页面提示联系客服。需要人工核对供应商结果后处理，不应把未知直接改为 verified/approved。当前线上此类记录为 0。这是后续运维能力待办，不能因为主流程冻结而标为完成。
2. 人工审核管理端 UI 尚未开发；拒绝、限制及解除有可信服务器脚本，人工通过此前按用户明确指令执行带审计的事务。不能称管理后台已完成。
3. 本轮未重新验证所有 Android 厂商、iOS、短信系统自动填充或真实供应商故障。手机验收以当前 OPPO 与用户反馈为证据；不得推广为全机型无缺陷。
4. 本次仅冻结注册登录已验收流程，不扩展到首页后的所有业务接口权限、旧黑名单导入、备案切换或其他模块上线验收。临时测试码仍按原授权有效期运行，未续期、扩大或删除。

## 后续变更约束

保留本审计为版本依据；发现缺陷应先记录触发条件和影响，定向修复、验证，避免顺手重做已确认页面。无新代码变化，本次无需重新打包安装。

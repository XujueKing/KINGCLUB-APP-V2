# 旧版欢迎封面页

- Scope ID：`KC-P-001A`
- 文档状态：`Approved for Development`
- UI 阶段：只允许 Flutter UI/Mock，不接真实协议目录或登录接口
- 旧版来源：`D:\WEB3_AI\KingClub-git\pages\login`
- 视觉基准：用户于 2026-08-28 提供的旧版封面截图

## 页面目标

完整复刻旧版进入页：用户在粉黑色 KingClub 封面上阅读并同意协议，点击 `NEXT` 进入旧版手机号登录页。

## 视觉契约

- 2026-09-10 用户批准视频封面，明确“就是要有声音，这个像游戏一样”：使用用户提供的视频，默认开启声音、自动循环播放；系统媒体音量由用户控制，不强行调整系统音量。
- 视频作为本地运行素材，以 H.264 MP4 保留音轨，按屏幕全幅覆盖；旧静态封面用于加载或播放失败兜底。Logo、协议、NEXT 与营业时间保留当前布局。
- 右上角提供声音开关；离开欢迎页（含打开协议）或 App 非前台时暂停音画，返回可见前台欢迎页后继续；销毁释放播放器。失败不能阻断协议或登录。
- 2026-09-11 声音开关改为 18dp 白色图标、70% 不透明度、无背景，保留 48dp 点击区。
- 左上角使用旧版粉色透明 `logo_1.png`，宽度为视口的 `21.33%`，左边距约 `6.7%`，上边距按旧版 `130rpx` 比例复刻。
- 页面底部依次为协议勾选行、粉色 `NEXT` 胶囊按钮、`BUSINESS HOURS`、`20:30-04:00`。
- 协议行沿用香槟金 `#C9B69E`，根据 2026-09-10 实机反馈提升到 `15dp`；链接下划线与字底保留 `2dp` 间距。营业时间弱化为 `13dp`。
- 勾选控件视觉直径调整为 `18dp`，未选中为黑底粉色圆框，选中为 `#FBAFDA` 圆底与居中的黑色小勾；外层点击区仍保持 `32dp`。
- `NEXT` 宽 `600rpx`（视口 `80%`）、高 `90rpx`（约 `45dp`）、圆角 `45rpx`，文字使用旧版 `#FBAFDA`。
- 协议勾选、说明、隐私政策、连接词和用户协议必须在同一行完成；窄屏仅允许整体等比缩小，不允许拆成两行。
- 背景、粉色 Logo 和大型 `King club` 装饰文字都来自原图/原素材，不用 Flutter 字体重绘。
- 微信小程序右上角宿主胶囊不属于 App UI，Flutter App 不复刻。

## 交互

- 默认未勾选，`NEXT` 使用旧版降低透明度的禁用态。
- 勾选后 `NEXT` 可点，进入 `/auth/mobile`。
- `《隐私政策》` 和 `《用户协议》` 分别打开现有本地只读协议页，返回后勾选状态保留。
- 不在本页请求短信、创建会话或展示账户状态。

## 线框

```text
[粉黑人物原图全屏背景]
[粉色 King club Logo]



[x] 我已阅读并同意《隐私政策》和《用户协议》（单行）
[                         NEXT                         ]
                    BUSINESS HOURS
                       20:30-04:00
```

## 配套文档

- [交互](interactions.md)
- [状态](states.md)
- [验收](acceptance.md)

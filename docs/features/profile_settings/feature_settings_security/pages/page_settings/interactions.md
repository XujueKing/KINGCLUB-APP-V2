# 设置页交互

| 触发 | 行为 |
|---|---|
| 点支付安全/注销/关于 | 固定 RouteIntent |
| 点通知权限 | Fake 系统设置；未来只打开 OS 设置 |
| 点清缓存 | 确认后只清媒体，不清会话/业务草稿 |
| 点退出登录 | 二次确认，远端撤销后本地原子清理 |
| 退出超时 | 本地仍安全退出并记录稳定提示 |

不得执行服务端返回的字符串路由、URL 或 interfaceId。

# 我的栏目：当前旧版还原与原生滚动

用户于 2026-09-12 明确要求按当前旧小程序还原布局，并以 Flutter 滚动替代小程序测量位置、touchend 判断横滑和 setData 吸顶。当前批准实施本页 UI；不恢复已撤销的远端合入。

依据：D:/WEB3_AI/KingClub-git/pages/index/index.wxml 389–580、index.wxss 1582 起及 index.js e_measureProfileGeometry/e_updateProfileHeader。旧仓库含用户改动，仅只读。

- 封面占位 400rpx，头像 180rpx、起点 260rpx，横跨资料面板；统计数字与名称横排且允许换行，编辑按钮按内容宽度。
- 资料面板左右 40rpx，恢复旧茶黑渐变、标签密度和页签字体；默认作品，三个页签有真实 PageView 横向滑动和点击动画。
- 同一 CustomScrollView，顶部工具条和页签分别用 SliverPersistentHeader 固定。封面与资料随滚动移出，固定菜单位于状态栏及工具条下，不使用定时器测位置或每帧整页 setState。
- 保留用户已批准的订单胶囊以及 QR、编辑、资产、设置流程和本地封面持久化；已验收的主底栏不修改。
- 本轮仍使用既有个人中心演示数据。未接个人资料/作品/动态/相册真实接口，空内容显示相应空态，不虚构上传内容或真实余额。
- 验证：封面/头像坐标、统计横排、菜单吸顶与回滚、横滑/点击一致、短屏与大字不溢出、原有路由/封面保存回归，中文截图与 Android 覆盖安装。结果完成后追加。

## 已执行验证

- flutter analyze 无问题。
- 首次全量测试391通过、3个可选截图测试跳过，2项旧个人中心测试失败：默认动态已变作品，以及旧测试选择了新增PageView内部横向Scroll而不是纵向Scroll。更新对应当前规格/真实手机视口后，47项App冒烟及原生滚动/字号测试全部通过。
- 首轮24项个人中心回归通过，涵盖封面持久化、编辑返回、设置路由、箭头对齐、吸顶与200%字号；独立中文截图完成。
- 保留既有空头像和演示资料，不硬接旧账号、不虚构真实作品、余额或好友关系。后续真实个人中心接口与内容列表独立实施。

- 最终圆角/封面遮罩与吸顶标题补齐后，5项中文截图、头图坐标及原生滚动测试通过，analyze再次通过；preview/profile arm64 APK构建成功（118.6MB）。手机462606d8从offline自动重连为unauthorized，已请用户在手机允许USB调试；本包尚未覆盖安装，不能把组件验证写成真机验收。

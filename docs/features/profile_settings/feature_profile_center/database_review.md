# 新版个人中心与注册资料补全（2026-09-12）

用户已验收 UI 并授权接真实资料；不迁移旧会员资料。旧 K_getNewUserInfo、getWX、k_getZodiac、k_level_config 与 K_Register_SetStyles 仅作为规则来源。

033：kingclubProfile（独立昵称/签名/生日/性别/私有头像封面/位置省市/编辑详情/版本）、kingclubProfileAssets（新版资产和统计投影初值）、kingclubProfileContent（新版本人发布内容）、目录及 K260912000501～505。034：kingclubRegistrationReward 唯一账号凭据，最终偏好提交同事务赠送50金币50经验；已完成注册会员读资料时幂等补发。余额不从旧服务器导入，现金零开户；社交/消费/发布操作尚须各业务继续接通，不能把投影表存在等同完整钱包/社交引擎。

实名成功：在原结果事务中保存姓名与证件号 AES-256-GCM 密文；域隔离派生密钥、每次随机 IV、账号绑定 AAD；私密证件不进入普通接口、Git、日志或客户端持久化。生日/性别同事务提取。历史补存匹配原 identityDigest，不再次调用腾讯、不改变审核结果。昵称为空返回加密实名中的姓名（用户明确沿用旧版）。婚姻状态由本人选择，不填不显示。定位由手机授权，持久化省市/时间，不保存坐标、不用证件籍贯替代。

接口全部 encrypted/session + 当前KingClub会员授权，禁止以请求userAccount授权；502字段白名单+版本检查+最近请求幂等；503补存需原核验摘要一致；505限定本人用途、服务端重编码去EXIF、限大小与频率，提交只接受本人上传文件。私有图片短期下载URL，App按会员和fileId缓存。

注册奖励有唯一凭据与事务，重复读取不重复发放。资料尚未关联发布功能时作品/动态/相册返回真正空列表，而非旧作品。照片本人展示使用已提交形象正面照作为初始头像，不公开实名核身照片。

部署：隔离kingclub数据库033/034完成，公版物业未改；测试会员实名已按摘要匹配补存，未记录其资料。npm run verify 181项通过，MySQL目录登记完成；App滚动/大字/封面/工具栏11项通过。首包真机读取资料成功。

历史迁移027/028只有CRLF差异，已与当前运行镜像逐字节核对恢复原件；未修改原SQL含义。033首次目录schema检查失败，未标记完成；补齐失败重试幂等后完整执行并验证。回滚保留原容器镜像与新增数据，不回退已核验身份。

限制：系统定位依赖手机定位/地理编码服务；拒绝或失败只显示提示。会员主页以外订单、聊天关系操作、钱包流水业务及内容发布不在本批完成范围。头像/封面取消后的未引用附件清理策略还需纳入平台保留期任务。

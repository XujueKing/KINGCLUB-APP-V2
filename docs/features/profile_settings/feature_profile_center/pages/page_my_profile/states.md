# 我的主页状态

| 状态 | UI | 可执行动作 |
|---|---|---|
| `legacyReady` | 截图中的完整旧版首屏；头像为空、动态默认选中 | 全部已批准 Fake 入口 |
| `worksSelected` | 作品选中并带下拉小三角，旧版三列区域保持空内容 | 切换页签 |
| `momentsSelected` | 动态选中并带下拉小三角，内容空白 | 切换页签 |
| `albumSelected` | 相册选中并带下拉小三角，旧版三列区域保持空内容 | 切换页签 |
| `fakeSheetOpen` | 二维码、设置、等级、统计或资产底部面板 | 查看并返回 |
| `editingProfile` | 昵称、签名编辑表单；头像不可添加 | 本地保存或返回 |
| `localCoverReady` | 使用应用私有目录中的已保存封面 | 查看、再次编辑 |

## 不变量

- 本阶段没有加载、联网失败或会话失效业务态，因为页面不连接服务端。
- 头像始终为空，不生成、不选择、不读取真人头像。
- 切换内容页签不得伪造用户作品、动态或照片。
- 真实接入前必须另行设计服务态、隐私态和失败态并重新批准。
- 本地封面缺失或损坏时安全回退默认上海夜景，不显示破图。

# 旧版“我的”完整 UI 复刻规范

- Scope ID：`KC-P-040`
- 文档状态：`Approved for Development`
- 设计版本：`Legacy My Profile Replica v1`
- 视觉真值：用户于 2026-08-26 提供的旧版“我的”完整首屏截图
- 数据阶段：全部离线 Fake，不连接服务器

## 首屏视觉

- 顶部使用上海城市夜景封面，约占首屏上方三分之一。
- 左上为个人二维码与设置图标；右上只保留 `EXP` 信息，忽略微信宿主胶囊。
- 头像为左下压住面板的大圆形，但用户明确要求内部留空。
- 昵称使用 `杨嘉琪`，右侧为钻石样式等级胶囊 `青铜 L-0`。
- 第二行显示 Fake 账号 `K45600000199` 和复制图标。
- 主面板为深棕黑径向层次、顶部圆角，并覆盖封面下缘。
- 四项统计依次为 `获赞 / 关注 / 互关 / 粉丝`，数值均为 `0`；右侧为 `编辑主页`。
- 三项资产依次为 `余额：￥0.00 / 金币 50 / 钻石 0`。
- 标签按截图显示：`男 24岁 / 颜值：148 / 河南省 · 安阳市 / 巨蟹座 / 单身 / 木系灵根`。
- 内容栏为 `作品 / 动态 / 相册`，默认 `动态` 选中并带下拉小三角。
- 底部保持旧版五 Tab 胶囊，“我的”选中。

## 可操作内容

| 入口 | Fake 行为 |
|---|---|
| 二维码 | 打开个人二维码离线预览，明确不包含真实账号/token |
| 设置 | 打开固定设置 Fake 面板，含支付安全、注销、通知、缓存、关于和退出 |
| EXP/等级 | 打开等级说明，展示青铜 L-0、EXP 50 |
| 账号复制 | 只把 Fake 账号写入页面内提示，不访问系统剪贴板 |
| 四项统计 | 打开对应旧版统计空列表 |
| 编辑主页 | 打开可编辑资料 Fake 表单并可本地保存返回 |
| 余额/金币/钻石 | 打开对应 Fake 资产流水页 |
| 作品 | 旧版三列作品区域；当前使用空内容说明，不伪造用户作品 |
| 动态 | 旧版时间线区域；截图基准为空 |
| 相册 | 旧版三列相册区域；当前使用空内容说明，不伪造真人照片 |

## 资源规则

- 夜景封面根据截图生成独立无文字背景图，用作 UI 资产。
- 二维码、设置、金币、钻石、性别、复制、等级和 Tab 图标优先复用旧版源资源。
- 不生成或使用真人头像；头像内部必须保持纯空白/浅灰。
- 不把整张参考截图裁成页面背景。

## 验收

- [x] 首屏结构、层级、颜色、间距与截图一致。
- [x] 头像区域为空，其他截图内容完整。
- [x] 所有可见入口均有可返回的 Fake UI，不能出现离线 Toast 占位。
- [x] 三个内容 Tab 可切换并保持旧版区域结构。
- [x] 底部“我的”保持选中。
- [x] 无真实资料、资产、社交、内容、二维码或设置调用。

实机证据：[android_my_profile_latest.png](android_my_profile_latest.png)。

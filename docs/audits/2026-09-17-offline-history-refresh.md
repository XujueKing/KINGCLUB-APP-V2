# 已有本地记录时安静刷新

- 单聊/群聊历史同步遇到明确 NETWORK_ERROR 且已有非hidden已确认消息时，不再通过error触发初始化弹出提示。本地记录继续显示，后续连接恢复仍从原游标补收。
- 无可读历史时仍报错；登录过期、权限拒绝、群访问撤销、主动发送和翻页错误处理不变。群访问撤销仍清除可见记录。
- 68项单聊/群聊历史与控制器测试通过。新增真实临时SQLite有/无缓存启动及网络/登录/群拒绝分支；原中段补收失败测试更新为不弹错，仍验证游标续收及131条消息无重复。4文件analyze通过。
- 尚未安装，不以自动化控制器测试替代手机提示验收。

## A 离线进入单聊与群聊

- 预览6716000 Profile构建65.4秒，159.4 MB，覆盖安装Success，前台稳定检查第二次通过。B未操作。
- 飞行模式开启且Wi-Fi关闭，设置读取1/0后强停重启，先进入A/B单聊再返回进入既有测试群。两页分别连续采集12张屏幕，单聊覆盖进入后约0.47—7.78秒，群聊0.47—7.52秒。
- 检查通知所在屏幕中央区域的全部采样帧，未见之前的网络刷新失败提示；本地图片、视频封面和消息保持可见。这是本次两页离线入口的采样证据，不代表逐帧或所有错误分支验收。
- 网络最终恢复飞行模式0、Wi-Fi1，移动数据开启。未发送或删除消息。截图、采样时间和局部拼图保存在忽略的build/offline-*，不提交聊天数据。

# A 文件消息删除真机验收

设备 A，预览 `727e329`。仅操作上轮在 A/B 会话发送的项目测试文件 `KINGCLUB-offline-file-test.txt`，未删除其他消息。

1. 删除前 app 私有发送源库存在一份 retained 目录。通过文件卡片长按“为我删除”并确认，文件卡片消失。
2. 同一发送源目录随后不存在，下载库出现消息删除标记。验证了私有文件源实际删除，不仅是列表隐藏。
3. 关闭 Wi-Fi/移动数据，确认无默认网络后终止并重启应用。重新进入 A/B 会话，测试文件卡片为 0，相邻原文字仍可见。
4. 无默认网络时核对 Download 原文件及此前导出的 `(1)` 副本，两者仍存在且 SHA-256 均为 `5701a74e050b80c61e4a6f7cca24f7d5781f498aecff29609f4f33c4e0dcb1a1`。
5. 已恢复 Wi-Fi/移动数据，确认默认网络恢复。

仅证明 A 单条发送文件删除、私有源清理、离线重进不恢复、用户导出副本保留。整段清空、B 接收副本、群聊及大文件不在本次验收范围。

# 历史搜索响应本地删除

搜索页此前只监听WebSocket及会话变化，本地删除通知未直接接入。现在单聊/群聊入口传入当前账号，搜索页订阅本地ChatMediaDeletion，按账号和会话种类隔离，记录已删除ID。当前结果命中或请求进行中则立即清除旧结果并刷新，原generation机制拒绝迟到响应；后续响应亦过滤这些ID及hidden/recalled行。未命中当前列表的删除不会额外闪空搜索结果，离页解除订阅。

验证：7项搜索页测试通过；新增单聊、群聊场景在完全没有WebSocket事件时本地删除立即消失，服务端再次返回旧内容也不会恢复，其他账号/种类通知无效，保留其余结果，离页后无残留回调异常。4个相关文件静态检查通过。尚未安装，真实搜索页删除联动待手机验收。未将此行为推广为所有搜索入口或B端均完成。

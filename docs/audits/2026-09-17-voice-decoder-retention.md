# 语音解码失败后的持久化副本清理

下载语音在播放前会写入离线 asset 缓存。之前原生播放器拒绝文件时只清理传输副本，离线副本仍在，下一次点击优先命中同一份失败数据。现在同时驱逐当前账号、当前语音 asset 的离线副本；传输清理失败不阻止该清理。成功播放和其他附件不受影响。

新增真实临时磁盘缓存测试：下载后模拟解码失败，重开缓存确认副本不存在，下一次点击重新申请授权、重新下载并保存新字节。使用模拟播放器和网络边界，不证明真机音频格式解码通过。语音播放测试 21 项通过，两个改动文件静态分析无问题。本节点尚未打包安装 A/B。

后续安装：预览工作树 26ad5be 已构建 Profile（65.1 秒，159.3 MB），覆盖安装 A 返回 Success，启动稳定检查第二次通过。进入聊天列表和 A/B 会话，原有两个 8 秒视频卡片与输入框正常存在。安装前输入框为空，未产生新消息，未操作 B，网络保持连接。此安装记录不把模拟解码失败测试升级为真机故障恢复结论。

# 删除图片时释放解码缓存

应用共享媒体存储删除图片文件时，同步驱逐对应 FileImage 的 Flutter 内存缓存。覆盖页面已关闭、图片仍在内存缓存中的情况；使用精确文件键，不清空其他消息或头像的缓存。

验证：真实 PNG 解码进入 Flutter ImageCache 后执行永久删除，目标图片的内存缓存与磁盘文件都消失，另一条图片两者都保留。连同媒体存储与消息媒体清理共 20 项测试通过，静态检查无问题。

此补丁尚未安装真机。定制 MediaCache 实例可注入相同回调；应用默认 shared 实例已配置。并不代表历史遗留无归属文件的批量迁移已完成。

# 阻止迟到本地读取恢复已撤销头像

复现：本地头像编号已读出但仍在等待登录状态复核时，另一资料请求确认头像已删除或拒绝访问并清空编号；旧本地读取随后继续，仍会返回旧编号并写回内存。

修复：本地读取开始等待持久化操作前捕获最近完成的资料决策版本，读取及会话复核后版本必须仍一致，否则丢弃旧描述。只启动新请求而没有完成新决策仍不影响当前读取。

两项新增测试在修复前均失败（返回 image-1），修复后通过；头像相关4个测试文件共18项通过，最终调整捕获顺序后10项快照测试再通过，两文件静态分析通过。本次只处理头像编号读取竞争，不声称已有图片文件被物理擦除；尚未安装真机。

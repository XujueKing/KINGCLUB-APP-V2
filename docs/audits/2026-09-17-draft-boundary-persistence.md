# 草稿清空边界持久化

- 将草稿引用清空边界从进程内映射改为账号/会话隔离的安全存储记录，和草稿操作共用串行锁。只有更大的边界可以覆盖旧值。
- 写入草稿前校验引用序号和已保存边界；读取也会修复“边界写完、引用清理前中断”的残留，保留正文并同步保存清理结果。非法草稿先验证，避免错误序号被静默当作应删除引用。
- 17 项草稿及迁移相关测试通过，2 文件 analyze 通过。测试注入中断后的持久化状态，核对恢复、迟到新草稿、防边界倒退、较新引用保留及其他账号隔离。
- 安全存储使用测试替身，未计作真实手机进程故障实测；补丁尚未安装。

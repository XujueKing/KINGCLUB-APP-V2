# 2026-09-12 持续开发接管

用户授权主动推进开发、验证、隔离环境部署与测试手机安装。保持已确认功能和本机UI基线，不重复历史成果，不将授权当作完成状态。

## 已核实基线

- App 33ff06f：真实注册登录/储物已接通，加密WSS、前台核销事件及记录路由已实现。390项测试通过、2跳过。
- 服务端 b5caa3d：核销事务、通知队列、KingClub独立实时通道已配置；公网合成核销事件测试32ms，临时数据已清理。
- 旧 pages/savecode/savecode.js listenCode 每2000ms查询 S231202504070676，状态取出或数量减少后跳 success。此为旧实现事实，新版不恢复全局2秒轮询。
- 员工核销目前仅受信主机脚本；SafeScannerPage为模拟页，未具备真实相机识别及员工核销权限。不能直接暴露现有接受operatorId的主机函数给会员。

## 下一开发切片：完整核销流程

1. 核对旧员工权限、门店范围和管理入口，定义服务器权威员工身份，不以隐藏按钮作为权限控制，不自动授予真实用户员工身份。
2. 登记扫码解析/核销预览及确认接口：扫码不扣库存；确认时重新核验短码、有效期、会员/员工状态，事务锁、幂等与审计完整。
3. 接真实相机，暂停重复识别与离页采集；预览显示酒/券正确字段；确认数量/余量后提交。
4. 会员侧在核销状态确认后展示旧版对应成功反馈，返回储物；实时事件只触发权威查询，不能直接改余额。
5. 验证无权限、他店、过期、重放、并发、撤权、断网重试、通知丢失补拉及双端显示。使用合成库存，不改真实券余额。

## 后续顺序

- 真实单聊与会话列表：持久消息、服务器权限、增量补拉、幂等、已读/未读、媒体缓存、应用内通知路由；先核对既有模块契约，保留已确认布局。
- 按功能清单逐项接入仍为模拟的页面，每项单独记录验证，不将已有页面视为真实接口完成。
- 系统/锁屏推送由用户明确后置，届时核对原有厂商上架账号及推送权限；当前不采购第三方服务。

本文件是执行顺序与验收清单，员工扫码工作台、真实聊天和系统推送均未据此标记为完成。

# A 真实发送帧采样

A、Profile 预览 2cab30c、60 Hz。在既有 A/B 测试会话空输入框输入唯一标记 KINGCLUB-PERF-20260917-01，待键盘稳定后清空 VM 时间线，点击真实发送按钮，采样 2.5 秒。B 未操作。

| 事件阶段 | 样本数 | 中位数 ms | P95 ms | 最大 ms |
|---|---:|---:|---:|---:|
| Flutter Frame | 92 | 4.14 | 5.56 | 13.18 |
| LAYOUT | 92 | 2.39 | 3.31 | 6.27 |
| GPURasterizer::Draw | 91 | 5.85 | 6.75 | 8.56 |

上述阶段未超过 16.67 ms。解析方式与键盘基线一致；不是屏幕端到端延迟，不能据此宣称用户顺滑度验收通过。未采同时收件、首次键盘、长列表或 B。

实际出现测试气泡；长按可见启用的为我删除和撤回，代码这两个入口要求服务端消息 ID（撤回还要求 canRecall），支持客户端已取得发送回执。未点击撤回/删除。强制停止再启动，进入同一会话，测试文字仍出现一次；本次网络在线，不把它当成独立离线持久化证据或 B 收取证据。

结束时空输入框、键盘收起、A 保留在会话；VM 时间线记录流已恢复，临时端口转发已移除。原始数据在忽略目录 build/chat-send-timeline.json、build/chat-send-frame-summary.json。

# 当前决定：本次远端更新已全部撤回

用户已撤销下文历史合并策略，包括“我的”例外。以e046dba本机基线恢复所有远端页面、素材与路由，保留之后本机储物及回归修正。备份分支backup/before-remove-remote-20260912保存撤回前状态，采用新提交恢复，不删除历史或强推。

# 2026-09-12 GitHub拉取及合并分析

## 来源与备份

- 本地main：e046dba，领先共同基线4个储物提交；远端origin/main：3e2c6e5，包含个人中心、资料头像、法律页面改动。
- 拉取前已有MERGE_HEAD=3e2c6e5的未完成合并。git fetch origin确认远端未增加新提交，因此直接完成当前合并，不重复pull或rebase。
- 合并现场（工作区文件、未跟踪文件、索引、暂存/未暂存二进制patch、MERGE_HEAD等）已备份到仓库外KINGCLUB-merge-backup-20260912-121019。

## 冲突处理

1. settings_page.dart：合并共享返回组件与MediaCache依赖；保留远端五项菜单及个人信息/法律入口，也保留真实缓存读数、确认清理及错误反馈。清理入口独立放在菜单下方。删除重复状态字段。
2. design-qa.md：两边历史记录均保留，在顶部说明当前最终行为及历史适用范围。
3. 隐性业务冲突：远端四按钮方案会删除当前会话已确认的中央爱心/加号。保留本地五按钮、90%茶色磨砂、选中滑动和安全区参数，只合入新的个人信息/关于回调；Shell测试及黄金图使用五按钮基线。
4. 保留本会话尚未提交的物品详情布局、动态码提前刷新、固定白底和失效停码改动。
5. 远端.gitignore、个人中心资源、头像持久化和其它已自动合并文件保留。

## 验证与剩余问题

- 完整flutter analyze通过，无问题。
- 本次涉及的设置、Shell、个人信息、个人二维码、法律阅读、储物及动态码定向测试45项全部通过。
- 全量测试首轮：377通过、2跳过、12失败。已修正其中法律页测试仍使用旧标题的问题，并在定向测试确认通过。未重跑全量，不将剩余问题宣称已解决或确定为历史问题。
- 待复核失败涉及：聊天附件截图、首页截图、两组注册偏好截图、3个登录/注册smoke、储物占位smoke、商务退出与迟到支付结果、偏好流程、实名照片上传流程。应分别复现并与两个父提交比较，不能为了通过而批量覆盖黄金图或改已冻结业务。
- 本次不推送GitHub、不部署后端、不修改数据库；Android构建结果将在下文补充。

- Android arm64 preview/profile构建成功，APK118.6MB；构建日志build/merge-build.log。未安装手机。

## 用户后续明确的合并优先级

- 底栏以本机为最终版本，远端底栏改动丢弃。
- 除我的栏目及直接关联子页，其余冲突以本机为主。远端个人中心必要的回调、路由及资源依赖兼容合入。


## 后续回归修复完成

- 全量flutter test：389通过，2个需要显式字体环境变量的可选截图测试跳过，无失败（build/regression-final.log）。flutter analyze通过。
- 旧smoke测试仍依赖身份资料/1/4标题、已移除的成年勾选框、旧输入宽度、旧物品演示入口和四按钮断言；现对齐本地已确认的五步骤、输入布局、空库存与五按钮。
- MockAuthRepository把rateLimited原样透出，与真实页面识别的AUTH_SMS_RATE_LIMITED不一致，已统一Mock错误码；真实认证流程未修改。
- 头像预览与退出测试补齐安全存储Mock；退出等待真实异步缓存清理，防止在路由尚未更新时过早断言。头像上传断言仍校验槽位、版本及评分调用。
- 聊天黄金图通过等待Image素材完成加载恢复通过，未改聊天基线或页面。
- 首页黄金图差异仅为本地已确认的底栏样式；偏好黄金图对应本地已完成的去进度条、统一箭头、紧凑布局与NEXT。逐图检查后仅更新这些基线；偏好视口设置改为真实390级手机逻辑宽度，避免setSurfaceSize与MediaQuery视口不一致。当前测试为393×852。
- 注册/偏好生产代码与本地父提交一致，未为测试回退业务。用户最终合并优先级已写入AGENTS.md；后续同步必须遵守。
- 本轮未重新安装手机或推送GitHub。


## 全部撤回的执行结果

- 已恢复远端引入的个人中心、设置、个人二维码、法律页面、路由、pubspec资源声明、.gitignore及对应文档/测试；删除远端新建的头像裁切、资料页及素材。上述生产路径与本地父提交e046dba无差异。
- 保留本机后续储物提取详情、提前换码、过期透明底板、芝华士/轩尼诗瓶型配对与本地测试修复。
- 静态检查通过。全量回归385通过、2项可选截图跳过、1个缓存smoke失败；失败原因为旧测试硬编码12.8MB却使用真实缓存，已注入可控缓存并定向复测通过（build/rollback-cache-test.log）。本次不再沿用远端“我的”UI的任何验收结论。
- Git使用新提交撤回变更，远端提交保留在历史中供追溯；未重写历史或推送。

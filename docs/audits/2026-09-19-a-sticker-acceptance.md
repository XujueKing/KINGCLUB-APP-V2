# A 手机自定义表情验收（2026-09-19）

本次验证已发布的 120 表情同步接口，未部署群通话候选版本。仅操作 A，B 未操作；未发送任何聊天消息。

- 进入既有 A/B 测试群，打开表情 → 添加的单个表情。初始没有自定义图片。
- 从项目内置 fail.png 制作临时测试文件，通过系统图片选择器添加；表情面板显示对应图形。
- 服务端只读检查显示同一操作时间的表情库更新，revision=1、一个分类。
- 关闭 App 进程并重新启动，重新进入该群及表情面板，测试图形仍正常显示。未清除 App 数据、登录或历史。
- 长按该测试图形并确认删除，服务端 revision=2；只读检查图片引用数量为 0。
- 清理本次推送的临时图片和本次手机截图/XML，退出表情面板。保留原有聊天记录和其他手机文件。

通过范围：手机选择并添加单个表情、图形展示、进程重启后保留、删除同步至服务端。服务端检查只输出版本及数量，不记录会员标识或图片授权。

未覆盖：清空本地表情库后的纯云下载恢复、第二设备同步、离线编辑冲突、表情发送/撤回、整包导入。进程重启能显示不等于已证明纯云恢复。

发现待修正文案：删除确认仍写“从本机收藏中删除，已发送的消息不受影响。”现在删除会同步至云端，需调整说明；本轮未修改已安装客户端。

系统图片选择器最初在“下载”搜索提示暂时无法加载内容，切换“最近”、更新测试文件时间并扫描后可以选中。不能把这次系统选择器错误当作应用上传失败。

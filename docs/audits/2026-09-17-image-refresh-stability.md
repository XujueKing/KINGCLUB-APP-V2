# 本地图片刷新保持显示

- 图片视图恢复前台时保留当前图片，网络重连校验期间也保留已显示内容；本地图片重新获准后保留同一个Image组件及尺寸，避免切换为加载占位。
- 网络重连校验遇到NETWORK_ERROR时保留可用本地图片；明确权限拒绝仍清除，删除通知/账号切换/关系变化原有失效处理不变。
- 15项图片视图测试通过，2文件analyze通过。新增真实PNG与临时文件测试：恢复前台、重连授权等待及成功/网络失败后，Image Element与尺寸不变；拒绝后图片消失。Windows清理前等待图片解码及生命周期磁盘读取完成。
- 本节点尚未安装手机，不能将组件稳定性测试扩大为全部聊天滚动/键盘/发送动画已验收。

## A 安装与画面采样

- 989f9a8 Profile构建64.7秒、159.4 MB，A覆盖安装Success，前台稳定检查第二次通过。B未操作。
- 在原A/B测试会话的项目双箭头图片上，按Home后恢复应用；连续12张截图覆盖恢复后约0.83—8.31秒，图片区域各帧保持可见及相同尺寸，无占位图。
- 开启飞行模式、关闭Wi-Fi，随后恢复飞行模式关闭、Wi-Fi及数据开启；再次采样12张覆盖恢复后约0.47—7.64秒，图片区域仍保持显示。该画面证据不直接证明WebSocket connection.ready事件到达时刻；该事件行为由前述组件测试覆盖。
- 最终读取网络设置为飞行模式0、Wi-Fi1。没有发送/删除消息。私有截图及局部拼图在build/offline-image-*及build/image-*-grid.png，未提交。采样不是逐帧录屏，不扩大为全部动画流畅度通过。

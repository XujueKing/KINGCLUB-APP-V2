# A 关系分组持久化实机验证

干净预览合入主线 228a88c，最终 preview 6c0cf4c。Profile ARM64 构建 65.6 秒、159.3 MB；A 覆盖安装 Success，前台启动通过，B 未操作。

实际经消息→通讯录→我的关系打开，原状态为空分组。按账号 SQLite 已升级 v19，integrity_check=ok，message=89，分组快照 version=2、30 字节加密 blob。

通过页面新建空测试分组，实际名称因输入长度限制为 KC-OFFLINE-T。执行 A 的 svc wifi disable、svc data disable 后强制停止并重启，重新进入关系页：出现网络读取失败提示，同时测试分组仍显示 0 人。这证明该次网络读取失败下重启后的本地恢复；wifi_on=0，但 mobile_data 设置值仍为 1，不据此宣称已证明所有网络传输均被禁用。

finally 恢复 wifi/data enable，两项设置读取均 1，与测试前相同。重连后编辑本次测试分组→删除分组→确认删除，重新启动进入关系页显示空分组提示。再次读取本地数据库：version=19、integrity=ok、消息仍89；分组快照版本4、30字节，与加密空数组一致。创建后版本3的119字节快照已被替换。仅删除本次空测试分组，没有调整真实好友所属关系。检查后重新启动 A。

仍待：带成员图标的离线真机展示、双端分组并发修改。不能把本次空成员分组验证推广为这些场景均通过。

# 群设置页通知刷新合并

群设置此前每条实时通知都并行请求详情和历史，迟到响应虽有版本保护，却仍产生重复请求。现在同一时刻只执行一组请求；期间有新通知则作废当前快照，并在结束后补拉一次。已有内容和滚动位置保持，真正失去权限仍按原逻辑清除详情。

新增组件回归：初次加载后阻塞一轮请求，连续20次通知不新增并行请求，旧结果不显示，随后仅一轮补拉展示最新群名。群设置、成员管理、退群、转让共12项测试通过，两文件 analyze 无问题。

不改变服务端事件契约：当前部分群变更事件没有 groupId，仍需刷新；没有错误地忽略无范围通知。本补丁尚未安装或双机实测，不将群管理总项标为完成。

# 没有历史消息行时的删除预览清理

补上上一节点的旧缓存缺口：首次收到 hidden/recalled 标记，即使该序号的历史消息还未下载，也会使对应列表预览失效。已落盘的同一标记不再次通知。低于清空边界的标记不作为新的删除重复处理，避免不会落盘的旧标记造成刷新循环。

原有存储回归扩展为 3×3：hidden/recalled/floor，分别配合无消息行、只有其他已删除标记、目标完整消息。验证初次清理、过期 epoch 拒绝、重放不通知、旧版本列表写入拒绝及重开数据库结果。历史与列表测试 41 项通过，静态分析通过。未新增手机验收，未安装新 APK；待发送队列未改动。

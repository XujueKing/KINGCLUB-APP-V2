# A 发送文件离线读取与保存真机验收

- 安装：预览提交 `727e329`，Profile，包含文件持久化、已下载文件离线读取及发送源库读取；覆盖安装 A 成功，启动稳定检查通过。B 未更新。
- 在 A/B 已授权测试会话，A 通过“更多→第二页→文件”发送项目生成的 `KINGCLUB-offline-file-test.txt`，50 字节。
- 禁用 A 的 Wi-Fi 和移动数据，确认 `Active default network: none` 后结束并重启应用进程。重新进入 A/B 会话，测试文件消息存在。
- 打开文件卡片并点击“下载文件”，在仍无默认网络的状态下显示“下载完成，文件校验通过”。这是本地读取结果，按钮文案尚未区分本地与网络。
- 点击“保存到文件”，系统保存界面生成 `Download/KINGCLUB-offline-file-test (1).txt`，App 显示“已保存到所选位置”。随后再次确认无默认网络。
- 原素材、手机源文件、系统保存后的文件 SHA-256 相同：`5701a74e050b80c61e4a6f7cca24f7d5781f498aecff29609f4f33c4e0dcb1a1`。
- 已重新开启 Wi-Fi 和移动数据。未删除用户聊天、原文件或主动导出的副本。

验收范围限 A 发送方、50 字节文本文件、离线冷启动读取及系统保存。B 接收方、群聊、大文件、删除后离线拒绝打开及外部文件查看器仍待实测。

# A 文件缓存更新安装

预览工作树从 e166242 合入 main 的 4893eae、f89b3d5，生成 b564104。内容为发送文件保留上限统一到 256 MiB，以及大分块 AES-GCM 加解密移出 UI isolate。

- Android ARM64 Profile 构建成功，Gradle 67.0 秒，APK 159.3 MB，保留既有 API、NovoRUDP、LAN 配置。
- 安装前 A（462606d8）的会话输入框为空且未聚焦。ADB 覆盖安装返回 Success，启动脚本第二次尝试验证前台稳定。
- 安装后实际打开消息栏目，聊天、通讯录及 A/B 会话入口存在；进入该会话后标题和输入框正常，先前删除的项目测试文件没有出现在可见页面。
- B 未操作；没有删除其他记录、发送新消息或改变网络配置。

本轮仅为安装与界面冒烟检查，不代表手机大文件离线读取、256 MiB 边界、帧率、整段清空或群聊清理已通过真机验收。

# A 历史 v17 真机升级

预览 e934ef2 包含到 1c99e33/33057df 的删除预览、旧标记清理和异常回滚测试代码。Profile 构建 64.5 秒，159.3 MB，A 覆盖安装 Success，启动稳定检查第二次通过。

安装前读取当前页面确认输入框为空，停止应用后复制其加密数据库及存在的 WAL/SHM 到忽略目录 build/v17-before。SQLite 版本 16，88 条 message，integrity_check=ok。安装后实际打开聊天列表及 A/B 会话，8 秒合成视频仍为一条、输入框正常。

停止后复制数据库到 build/v17-after：版本 17，88 条 message，旧 conversation/sequence 键缺失 0，integrity_check=ok。51 条密文字节变化，但期间发生了在线同步，不能据此推断 51 条全部是删除标记，也不能以本次比较证明全部正常消息密文未重写。字段过滤与普通行保留由隔离迁移回归覆盖。再次启动成功，未操作 B、未发送消息、未清空真实会话。私人数据库副本不提交 Git。

此结果证明该 A 数据库升级成功、键集合与记录数量保留、页面可打开；不证明大容量升级性能、断电恢复或待发送队列清理完成。

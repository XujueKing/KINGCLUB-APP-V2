# A 本次补发测试文字的为我删除

运行已安装 fb5a89f Profile。仅操作上轮A/B会话中本次生成的 KC-RECOVERY-0917-0809，未删除其他文字或对方记录。

实际长按本条→为我删除→删除确认。会话UI中该测试文字节点为0；返回列表，该文字预览节点为0，上一条本项目测试文字 KINGCLUB-PERF-20260917-01 预览为1。强制停止、重新启动、进入A/B会话后，新删除的文字节点仍为0、上一条测试文字节点为1。

停止状态读取数据库：v19、integrity_check=ok，message仍90行；相较发送前89行快照，原有记录键缺失0。唯一新增测试消息对应的加密payload从352字节变为354字节，说明同步更新仍保留记录键。未提取密钥或打印私信正文，不能仅由密文长度证明其解密字段。hidden标记仅保留身份字段且text为空由既有数据库回归测试证明，本次手机证据证明真实删除后的UI、列表预览及重启不恢复。

仅“为我删除”，不是双方撤回；B未操作。上轮补发记录所说保留此测试消息已由本次显式测试删除取代。删除整个会话、清空待发队列和跨端附件清理仍有独立未验收项。

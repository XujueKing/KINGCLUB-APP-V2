# A 历史数据库版本 16 升级验收

预览 `e166242`（Profile）覆盖安装 A，包含图片解码缓存释放、单条删除保留其他历史、stale 归属保留、服务端历史删除结果清理。启动稳定检查通过。

升级前 A 无未发送文字；停止应用后读取本地加密数据库副本，user_version=15，message 行数 82，integrity_check=ok。升级后进入消息列表及 A/B 会话，再读取数据库副本：user_version=16，message 行数 82，integrity_check=ok；原 (conversation, sequence) 键缺失数为 0。

UI 中原文字仍可见，之前删除的项目测试文件卡片不存在。仅核查数据库结构、数量及会话展示，没有解密或导出用户消息内容；数据库副本仅存于本地忽略的 build 目录，不提交仓库。

此验收不替代双机撤回、群聊清空或后台通知删除的真实行为验收。B 未更新。

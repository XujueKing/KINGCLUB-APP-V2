# Flutter V2 本期范围评审包

- 状态：`In Review`
- 审计基线：旧版 `KingClub-app / master / 505d222 / 1.1.37`
- 审计日期：2026-08-24
- 用途：确认本期 Flutter App 的完整功能、页面和明确不做项

## 已确认事实

- 旧版 `app.json` 注册 68 个主包页面和 1 个分包页面，共 69 个物理路由。
- `pages/index/index` 单个物理路由内嵌首页、通讯录/会话、短视频、私人储物柜和我的等多个逻辑页面，不能按“一个旧文件等于一个 V2 页面”迁移。
- 设置、管理员面板等入口由超级接口返回动态菜单，因此静态引用次数不能单独证明页面是否仍在生产使用。
- 旧版同时混有普通会员、群主/局长、员工、代理、财务和超级管理员能力。
- 通用成功页、APK 下载页、日志页和部分实验页不应原样迁移为 V2 产品页面。
- 本次只审计和写 V2 规划文档，没有修改旧版仓库，也没有创建 Flutter 工程。

## 本轮产物

1. [旧版 69 路由完整审计](LEGACY_PAGE_AUDIT.md)
2. [本期功能与页面范围提案](RELEASE_SCOPE_PROPOSAL.md)
3. [项目级 UI 交付门禁](../APP_SCOPE_AND_UI_DELIVERY_GATE.md)

## 当前结论

- **当前建议**：首发采用“普通会员完整消费闭环”，包含 46 个逻辑页面。
- **待用户决策**：4 个可选功能包，共新增 11 个逻辑页面；必须逐包确认是否进入本期。
- **当前建议**：员工/代理/财务/运营后台、调试页、APK 下载页不进入消费者 Flutter 首发，共排除 17 个旧物理路由。
- **当前建议**：旧版服务端动态账号绑定页先放入后续版本，待明确它是统一身份绑定还是代理关系绑定。

当前范围尚未冻结，任何页面目录都不能因为出现在提案里而直接进入 UI 开发。

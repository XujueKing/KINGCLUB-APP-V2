# KINGCLUB APP V2 分阶段迁移计划

## 总原则

- 旧版持续可运行
- 新旧系统可并行
- 每阶段有验收门槛和回滚路径
- API 和数据边界先于 Flutter 大规模页面开发
- 不以“代码已经写完”作为迁移完成标准

## Phase 0：资产盘点

目标：把隐含在源码和数据库中的业务规则显式化。

交付物：

- 数据库表、视图、存储过程、定时任务清单
- 103 个左右 `interfaceId` 的业务映射
- 页面到接口到数据表的链路表
- 支付、订单、会员、聊天状态机
- 外部服务和回调清单
- 现有生产指标和故障记录

退出条件：关键交易和聊天流程都能画出完整状态流。

## Phase 1：身份和 API v2 基础

交付物：

- person/account/app_membership 模型
- 旧 userAccount 映射表
- 登录、会话、刷新和退出接口
- API v2 错误格式、分页、幂等规范
- OpenAPI 仓库
- 小程序和 Flutter 的 API 客户端骨架

退出条件：旧用户能无损映射为新身份，并能同时访问旧 API 和 API v2。

## Phase 2：KingClub 核心业务拆分

推荐顺序：

1. 用户/会员只读信息
2. 门店、座位、商品
3. 预约和订单查询
4. 订单创建
5. 支付、回调和退款
6. 余额、金币和提现
7. 代理、客户、存酒和门票

退出条件：新旧系统金额、订单状态和库存持续对账一致。

## Phase 3：Flutter MVP

第一版范围：

- 登录和会员身份
- 首页核心内容
- 好友列表和单聊
- 消息推送
- 订单列表和详情
- 支付
- 个人中心

暂缓：

- 低频后台管理页面
- 复杂富文本编辑器
- 全量群管理能力
- 所有历史活动和边缘入口

退出条件：核心闭环有自动化测试，并完成 Android/iOS 真机灰度。

## Phase 4：微信小程序迁移 API v2

推荐顺序：

1. 用户和会话
2. 首页基础数据
3. 好友和聊天
4. 订单和支付
5. 会员及个人中心
6. 门店和代理功能

每个模块迁移后保留旧接口开关，至少观察一个完整业务周期。

## Phase 5：同城社交域拆分

- 确认跨 App 共享范围
- 迁移公开资料、关系、作品和可见性
- 客户端只能通过社交服务访问
- 对不同 App 返回不同视图，不直接暴露全量个人资料

## Phase 6：下线旧多端容器

条件：

- Flutter 覆盖目标用户和核心功能
- 推送、支付、聊天稳定
- 数据对账无持续差异
- App Store/Android 渠道版本稳定
- 旧 App 有明确升级提示和截止时间

## 粗略投入参考

在需求相对稳定、有 2 名 Flutter、1 名后端、1 名测试的前提下：

- 资产盘点和 API 基础：约 1～2 个月
- Flutter 核心 MVP：约 4～6 个月
- 大部分现有功能覆盖：约 8～12 个月

这是规划级估算，不是排期承诺。数据库规模、团队熟练度、设计稿和接口现状都会显著影响时间。

